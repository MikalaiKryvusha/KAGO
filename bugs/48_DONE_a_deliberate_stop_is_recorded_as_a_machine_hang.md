# Bug 48 — НАМЕРЕННАЯ остановка записывается как ЗАВИСАНИЕ МАШИНЫ, и частота теряется навсегда

**Статус:** 🔴 ОТКРЫТ — сфабриковано агентом 2026-08-24 00:2x при остановке прогона по слову
владельца «СТОП». **В журнале владельца сейчас лежит ложное зависание, которого не было.**
**Версия:** `main` @ `0fd2027` · **Класс:** `bugs/20` («своя смерть приписана карте»), НОВОЕ ЛИЦО

## Что произошло — по шагам, с уликами

1. Владелец сказал остановить прогон. Агент убил процесс (`TaskStop`).
2. В журнале упреждающей записи осталось намерение **без вердикта** — процесс не дожил до записи
   исхода. `runs/sweep/journal.jsonl`, `seq 695`:

```json
{"at":"2026-08-24T00:21:42+03:00","frequencyMhz":2355,"voltageMv":845,"seq":695,"state":"intent",...}
{"at":null,"outcome":"hung","seq":695,"verdict":"ЗАВИС","state":"verdict",
 "why":"НАМЕРЕНИЕ БЕЗ ВЕРДИКТА: ступень 2355 МГц / 845 мВ была в полёте, когда прогон перестал существовать"}
```

3. Следующий прогон прочитал это как пол зависания и **ПРОПУСТИЛ 2355 МГц целиком**:

```
ПОЛ ЗАВИСАНИЯ: 2355 МГц уже вешала машину на 845 мВ — спуск туда больше не идёт
2355 МГц ПРОПУЩЕНА: край уже зажат журналом — прошло 850 мВ, повесило 845 мВ
```

**Машина не висла ни секунды.** Карта после убийства была чисто откачена сторожем (128 точек в ноль).
Тем не менее 845 мВ теперь объявлено КРАЕМ кремния на этой частоте — навсегда, пока запись не
поправят.

## Почему существующая починка `bugs/20` это не ловит

`bugs/20` научился отличать **падение писателя** (`writer-crash` — свой класс исхода, в пол не идёт).
Но здесь писатель не падал: его **убили снаружи**, штатно и по слову владельца. Для журнала это
неотличимо от смерти машины — а между ними разница ровно та, ради которой журнал и ведётся.

⚠️ **И это не редкий случай.** Владелец останавливает прогон командой в чате; агент останавливает
его по кнопке; `Ctrl-C` делает то же самое. Каждая такая остановка сегодня **портит улики о кремнии**
и молча вычёркивает частоту из будущих прогонов.

## Цена, уже уплаченная

- 2355 МГц выбыла из полосы 2355…2310 в следующем же прогоне — покрытие 0 из 7 вместо возможного.
- Число 845 мВ будет использоваться как край этой частоты всеми будущими сессиями и всеми
  инструментами, читающими журнал.

## 🔴 ИСПРАВЛЕНИЕ ДОКУМЕНТА 2026-08-24 09:1x — ШАГИ 1 И 2 УЖЕ ПОСТРОЕНЫ, И ЭТО МЕНЯЕТ ДИАГНОЗ

Первая редакция этого документа предлагала построить класс исхода и обработчики сигналов. **Оба стоят
в коде с 2026-08-16, заведены `bugs/14`:**

| Что предлагалось построить | Где оно уже есть |
|---|---|
| класс исхода `stopped-by-operator` | `sweep-journal.mjs:86` — `RUNG_OUTCOME.STOPPED = 'operator-stop'`; в пол зависания не идёт по построению (`hangFloors`), в «два подряд» не считается |
| обработчик дописывает вердикт ДО `exit` | `engine.mjs:7180` — `SIGINT` · `SIGTERM` · `SIGBREAK` зовут `closeAsOperatorStop(journal, …)` ПЕРЕД `process.exit(130)`, запись `fsync`-нута (`appendLine`) |
| поправка уже испорченной записи | `sweep-journal.writeCorrection` + готовый инструмент `tools/close-operator-stop.mjs` |

**Значит дефект НЕ в коде развёртки, а в способе, которым её остановили.** Прогон убили **жёстко, без
сигнала** (`TaskStop` — убийство дерева процессов), а такое убийство до обработчика не доходит НИКОГДА
и не может дойти: это ровно то свойство, которое сторож `T5` держит нарочно («процесс, убитый
наотмашь, — по-прежнему ЗАВИС»). Ослабить его нельзя, иначе настоящее зависание перестанет
опознаваться.

⚠️ **И проект это уже знал.** `EXPERIENCE.md` несёт запись ровно об этом случае: *«жёсткая остановка
не закончена, пока журнал не закрыт руками (`tools/close-operator-stop.mjs`)»* — и второй половиной
там же: *«механизм, выводящий смысл из ОТСУТСТВИЯ, прочитает вашу собственную аварийную остановку как
событие, ради обнаружения которого он построен»*. Урок был, инструмент был, шага не сделали.

**По правилу канона это ВТОРОЙ страйк одного класса, а два страйка = механизм, а не третье
напоминание.** Настоящий открытый пункт этого бага — единственный, и это шаг 3 ниже: жёсткое
убийство неотличимо от смерти машины, поэтому решать за оператора молча нельзя. Формулировка шагов 1
и 2 сохранена ниже зачёркнутой, а не стёрта: документ о том, как заявка разошлась с кодом, не прячет
собственного расхождения.

## План починки

1. ~~**Остановка по сигналу — СВОЙ класс исхода**, наравне с `writer-crash`: `stopped-by-operator`.
   В пол зависания не идёт, краем не становится, в документ кривой не пишется.~~ **УЖЕ ЕСТЬ** —
   `RUNG_OUTCOME.STOPPED`, `bugs/14`, 2026-08-16.
2. ~~**Обработчик сигналов дописывает вердикт ДО выхода.** Сейчас `SIGINT`/`SIGTERM` гасят окно и зовут
   `process.exit(130)`, то есть `finally` не исполняются и вердикта не появляется.~~ **УЖЕ ЕСТЬ** —
   `engine.mjs:7180`, вердикт пишется и `fsync`-ится перед `exit`. Утверждение первой редакции о том,
   что вердикта не появляется, было НЕВЕРНО: оно описывало код до `bugs/14`.
3. **🔴 ЭТО И ЕСТЬ ЕДИНСТВЕННЫЙ ОТКРЫТЫЙ ПУНКТ. Убийство БЕЗ сигнала (kill -9, `TaskStop`) остаётся
   неотличимым — и это надо назвать честно.**
   Лечение только частичное: возобновление обязано спрашивать, а не решать молча. Ступень без
   вердикта, у которой сторож зафиксировал ЧИСТЫЙ откат, — кандидат в `stopped-by-operator`, а не
   готовое зависание.
4. **Поправка к уже испорченной записи** — `sweep-journal.writeCorrection` уже существует (`bugs/20`,
   `f7b7739`). Применить к `seq 695`. ⚠️ Это правка улик владельца, поэтому — по его слову.
5. **Блок + мутация:** прогон, убитый сигналом, не оставляет пола зависания; мутация «вернуть
   безусловный `hung`» краснит блок.

## Решения, принятые без владельца

- **Поправка НЕ применена.** Запись в журнале — улика владельца о его кремнии; править её молча
  нельзя даже когда точно знаешь, что она ложная. Вопрос задан в чат 2026-08-24 09:1x, ответа нет.
- **Диагноз документа исправлен на месте, а не переписан начисто** (2026-08-24 09:1x). Отменённые
  шаги 1–2 оставлены зачёркнутыми: документ про то, как заявка разошлась с кодом, не имеет права
  прятать собственное расхождение — та же форма, что в `bugs/40`.

## Ссылки

`bugs/20` (родительский класс, `writer-crash`) · `sweep-journal.writeCorrection` (лекарство) ·
`runs/sweep/journal.jsonl` seq 695 (сама ложная запись) · `engine.mjs` (обработчики `SIGINT`/`SIGTERM`,
`process.exit(130)`) · R15 (долговечность записи)

## STATUS: DONE (2026-08-29 10:0x, session 61 backlog revision, EXP-0170 class)

Verified against artifacts, not memory: (1) the false hang of seq 695 carries a CORRECTION in the
live journal («2355 МГц / 845 мВ: ЭТО БЫЛА ОСТАНОВКА ПО СЛОВУ ВЛАДЕЛЬЦА, А НЕ ЗАВИСАНИЕ МАШИНЫ»)
— applied 2026-08-24 by the owner's word «1 - снимай» (recorded in STATUS then); (2) the CLASS is
closed by the deliberate-stop machinery: `closeAsOperatorStop` + `RUNG_OUTCOME.STOPPED`
(sweep-journal), which the two-in-a-row brake ignores by construction. Ticket lagged the code.
