# Bug 53 — 28 % времени прогона карта простаивает, и это же окно, в котором уезжает таблица

**Статус:** 🟡 **ЗАМЕР ИСПОЛНЕН 2026-08-26 19:4x, И ОН ПОКАЗАЛ, ЧТО ПОЛОВИНУ ПРОСТОЯ УЖЕ УБРАЛ
`bugs/59`.** Найден при разборе вопроса ВЛАДЕЛЬЦА 2026-08-24 22:0x.
**Версия:** `main` @ `5b3456e` · **Где:** ступень развёртки, промежутки между формами прожига

## Симптом — измерен, а не замечен

Разбор всех проб телеметрии живого прогона 21:5x (полоса 2355…2175 МГц, 828 с):

| величина | секунд |
|---|---|
| карта ПОД НАГРУЗКОЙ | **596** |
| карта в ПРОСТОЕ | **232** |
| **доля полезного времени** | **72 %** |

Простоев 71, медиана **4 с**, самый долгий 6 с. На каждую 36-секундную ступень приходится
**около 12 секунд простоя** при 24 секундах прожига.

## Почему это дорого ДВАЖДЫ

1. **Время карты владельца.** Четверть каждого прогона карта не делает ничего. Полоса из 25 частот
   стоила 828 с; без простоя — около 600.
2. 🔴 **ЭТО ТА ЖЕ ПАУЗА, В КОТОРОЙ УЕЗЖАЕТ ТАБЛИЦА.** Замерено в тот же вечер: на горячей карте
   соседние пробы таблицы V/F расходятся на **15, 23 и 30 МГц**, на остывшей стоят неподвижно
   тринадцать секунд подряд. Живые пропуски прогона — потолок пробит ровно на **+15 и +23 МГц**.
   То есть простой между формами не просто тратит время, а **производит тот самый дрейф**, из-за
   которого 7 частот из 25 остались без строки.

## Что именно занимает эти 12 секунд — НЕ УСТАНОВЛЕНО

Кандидаты, ни один не проверен замером:

1. запуск процесса нагрузки на каждую форму (замерено раньше: старт процесса — 132 мс, но форм три);
2. запись кривой — 127 отдельных вызовов NVAPI на ступень;
3. перечитывание через договор устаивания (введено 2026-08-24, до 6 проб по 250 мс = 1,5 с худший
   случай) — **добавлено мной в этот же день, и его вклад надо мерить отдельно**;
4. откат: обнуление кривой плюс своё перечитывание;
5. взведение и разоружение сторожа;
6. пауза остывания между формами, если она есть в конфигурации.

**Первый шаг починки — не оптимизация, а ЗАМЕР:** разложить 12 секунд по этим шести адресам. Чинить
неизмеренное здесь особенно опасно, потому что пункт 3 — свежий сторож, и соблазн снять его ради
скорости велик, а он лечит другой дефект.

## Ограничение владельца, названное в тот же вечер

> *«не нужно греть карту, инструмент наш должен уметь тюнить карту в любом состоянии»*

То есть лечение **«ждать, пока карта остынет»** запрещено, и лечение «греть, чтобы держать
стабильной» — тоже. Сокращать надо сам простой, а не подгонять под него тепловое состояние.

## Решения, принятые без владельца

- Не начато в этой сессии: чат закрывается, реализация переносится в новый по его слову.
- Разложение 12 секунд по адресам поставлено ПЕРЕД любой оптимизацией — иначе первым под нож пойдёт
  договор устаивания, введённый в тот же день против другого дефекта.

## Ссылки

`bugs/52` (окно наблюдения прячет два прожига из трёх — из-за него простой и выглядел как 26 с) ·
`bugs/50` (пропуски по пробитому потолку) · `plans/36` класс C7 (дрейф между чтением и записью) ·
`runs/dashboard/telemetry.jsonl` прогона 21:5x

---

## ✅ ЗАМЕР ИСПОЛНЕН 2026-08-26 19:4x — 12 СЕКУНД РАЗЛОЖЕНЫ ПО АДРЕСАМ

Собственное требование этого документа — «первый шаг починки не оптимизация, а ЗАМЕР» — исполнено
**без единой записи в карту и без единого прожига**: разобраны уже снятые данные, журнал упреждающей
записи (`runs/sweep/journal.jsonl`) и телеметрия прогона 2026-08-25 22:4x (223 пробы, 6 ступеней).

### Находка 1 — МЕЖДУ ступенями простоя НЕТ ВОВСЕ

| величина | замер |
|---|---|
| разрыв «вердикт → следующее намерение», медиана | **0,0 с** |
| то же, сумма по прогону | **0,0 с** |
| доля времени ВНУТРИ ступеней | **100 %** |

Проверено на четырёх прогонах подряд (11 · 11 · 10 · 6 ступеней). **Значит искать нечего в
переходах между частотами — весь простой живёт ВНУТРИ ступени**, и это сузило поиск вдвое до того,
как что-либо трогалось.

### Находка 2 — профиль ступени, посекундно, и он ОДИНАКОВ у всех шести

Пробы разложены по секунде от намерения; порог нагрузки 50 % `utilization.gpu` — та же мера, что у
прибора мощности, а не выдуманная здесь. **Разброс между ступенями — ноль.**

| секунды | состояние | что это |
|---|---|---|
| **0–2** | 🔴 простой **3 с** | ГОЛОВА: запись кривой (127 вызовов NVAPI), взведение сторожа, сверка штампов эталонов |
| 3–7 | 🟢 нагрузка 5 с | форма 1 — `furnace/transient@0`, и это **ровно её включённая фаза** |
| **8–13** | 🔴 простой **6 с** | ВЫКЛЮЧЕННАЯ фаза переходной формы (5 с) + смена процесса |
| 14–22 | 🟢 нагрузка 9 с | форма 2 — `furnace/sustained@0` |
| **23** | 🔴 простой **1 с** | смена процесса между формами 2 и 3 |
| 24–34 | 🟢 нагрузка 11 с | форма 3 — `branchy/sustained` |
| **35–37** | 🔴 простой **3 с** | ХВОСТ: откат (обнуление кривой) и разоружение сторожа |

Три блока нагрузки видны поимённо и совпадают с порядком набора в коде — это независимое
подтверждение, что разметка верна, а не подогнана.

### Находка 3 — ГЛАВНАЯ: `bugs/59` уже съел больше половины простоя

Простой раскладывается на **середину** (принадлежит набору форм) и **края** (принадлежат записи и
откату):

| адрес | секунд | судьба после `bugs/59` |
|---|---|---|
| голова | 3 | остаётся |
| **середина: выключенная фаза переходной формы** | **5** | ✅ **исчезла — формы больше нет** |
| **середина: две смены процесса между формами** | **2** | ✅ **исчезли — форма одна** |
| хвост | 3 | остаётся |
| **итого** | **13** | **остаётся 6** |

**Предсказание ступени после правки: 3 + 10 + 3 = ~16 с** против сегодняшних 37.
Это согласуется с независимой оценкой `plans/50` (~17 с), полученной другим путём — из арифметики
форм, а не из телеметрии.

⚠️ **Это ПРЕДСКАЗАНИЕ по разбору, а не замер на кремнии.** Подтвердить может только живой прогон.

### Что это меняет в самом баге

Из шести кандидатов, перечисленных выше, замер закрыл четыре:

1. запуск процесса на каждую форму — **да, но цена уже уплачена правкой `bugs/59`** (форм одна);
2. запись кривой — **в голове, 3 с вместе со сторожем и эталонами**; отдельно не разделено;
3. договор устаивания — **внутри той же головы**, отдельного вклада не видно на секундной сетке;
4. откат — **хвост, 3 с**;
5. взведение и разоружение сторожа — **распределено между головой и хвостом**;
6. пауза остывания между формами — **ЕЁ НЕ СУЩЕСТВУЕТ**: то, что выглядело паузой остывания, было
   выключенной фазой переходной формы. Кандидат снят замером.

## 🔴 ЧТО ОСТАЁТСЯ И ПОЧЕМУ ЭТО НЕ НАДО СРЕЗАТЬ

Оставшиеся 6 секунд — это **голова и хвост, то есть ЗАПИСЬ В КАРТУ И ЕЁ ОТКАТ.** Не накладные
расходы стенда, а машинерия безопасности: правило R5 («у каждой записи откат в том же модуле»),
R9 (сторож взведён на каждой записи), R10a (откат — список, а не цепочка), договор устаивания
(введён 2026-08-24 против отдельного дефекта).

**Срезание здесь — ровно тот соблазн, о котором предупреждает шапка этого же документа**, и цена
ошибки называется по имени: `bugs/03`, зависание машины владельца на 5 ч 40 мин. Шесть секунд на
ступень — это 2,5 минуты на полосе из 25 частот. Против этого — четыре слоя отката, три из которых
требуют живой операционной системы.

**Вывод замера: оптимизировать голову и хвост НЕ рекомендуется**, и рекомендация эта основана на
разложении, а не на осторожности вообще. Если владелец захочет их всё же сократить — это его
развилка, и предъявлять её надо с этой ценой.

## Остаток работы по этому багу

- [ ] **Живое подтверждение** предсказания 37 → ~16 с. Едет бесплатно на ближайшей развёртке,
      которую владелец авторизует; отдельного прогона ради этого не нужно.
- [x] ~~Разложить 12 секунд по шести адресам~~ — **исполнено 2026-08-26, оффлайн.**

## Ссылки на артефакты замера

`runs/sweep/journal.jsonl` (границы ступеней) · `runs/dashboard/telemetry.jsonl` прогона
2026-08-25 22:4x (223 пробы) · `bugs/59` (правка, съевшая середину) · `plans/50`
