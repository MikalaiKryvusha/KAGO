# Баг 12 — шаг 7 алгоритма владельца не исполняется наверху диапазона: частота НЕ закрепляется

> **Заведён:** 2026-08-16, по замечанию владельца в чате во время подготовки первого боевого прогона
> **Родитель:** `ideas/03_edge_finder_algorithm.md` шаг 7 · `bugs/02` · `bugs/11`
> **Статус:** ✅ **ЗАКРЫТ 2026-08-30 13:5x (сессия 67) — РЕПРО ПРОГНАНО, разбор в конце документа.**
> Наверху рабочего диапазона частоту держат ДВОЕ, и прогон говорит это по каждой ступени:
> «потолок держит **кривая + замок**». Оба хвоста шапки закрылись раньше, миграцией облака тегов.
> Прежняя строка (для истории): 🟡 ОТКРЫТ, СОСТОЯНИЕ ТРЕБУЕТ ПРОВЕРКИ РЕПРО (шапка поправлена 2026-08-30, сессия 65).
> Прежняя строка — «🔴 ОТКРЫТ — чинится в этой же сессии» — стояла с 2026-08-16: метка без даты не
> стареет и потому лжёт тем дольше, чем дольше живёт (`bugs/25`). **Что найдено сверкой с кодом:**
> закрепление частоты в `vf-step.mjs` ЕСТЬ и обвязано отказами (`heldBy: 'закрепление частоты'`,
> ветки «прожигать нечего без закрепления», `demandPin`). **Чего НЕ сделано:** не прогнано репро
> этого тикета — «наверху диапазона», — а без него закрывать нельзя: наличие механизма не есть
> доказательство, что он срабатывает в той самой точке. Следующей сессии: прогнать репро, потом решать.
> **Вовне:** правило формы записи → `PROJECT_ARCHITECTURE_INTERNAL_MAP.md` (R18) · урок → `EXPERIENCE.md`

---

## Что владелец заказывал

`ideas/03` шаг 7, его собственными прописными буквами:

> **«Ставится частота. ОНА ФИКСИРУЕТСЯ В ВИДЕОКАРТЕ. ВИДЕОКАРТА БЛОКИРУЕТСЯ РАБОТАТЬ ИМЕННО НА ЭТОЙ
> ЧАСТОТЕ И НИ НА КАКОЙ ДРУГОЙ НИ ПРИ КАКИХ УСЛОВИЯХ.»**

И в чате 2026-08-16, трижды: *«но в прогонах мы ЗАКРЕПЛЯЕМ ЧАСТОТЫ. ИСПОЛЬЗУЕМ МЕТОД ФИКСАЦИИ
КАРТОЧКИ НА ЧАСТОТЕ, ЧТОБЫ ИМЕННО ЕЕ ПРОЖИГАТЬ»*, *«я уже сто раз объяснял алгоритм!!!»*,
*«читай мой алгоритм выше, и ты поймешь, что я ДАВНО ПРОШУ ЗАКРЕПЛЕНИЕ!!!»*.

## Что делал код

`chooseWriteShape` (`lib/vf-step.mjs`) наверху диапазона отвечал «потолок держит САМА КРИВАЯ,
ЗАКРЕПЛЕНИЕ ЗДЕСЬ ЛИШНЕЕ И ВРЕДНОЕ» и возвращал `pinRequired: false`. Закрепление ставилось ТОЛЬКО
ниже пола кривой (2157 МГц), где потолка иначе не удержать. То есть на всём рабочем верхе диапазона —
там, где и идёт первый прогон, — шаг 7 не исполнялся вовсе.

## Чем это плохо по существу (не по букве)

Потолок и закрепление держат частоту по-разному, и разница измерена на этой карте:

| механизм | что обещает | что карта делает на самом деле |
|---|---|---|
| потолок частоты | «не выше N» | садится на **20–30 МГц НИЖЕ** N (потолок 2887 → 2857 · 2917 → 2887 · 2842 → 2812) |
| закрепление `-lgc N,N` | «ровно N» | ровно N, и это проверяется чтением под нагрузкой |

Развёртка пишет в документ кривой факт «частота F → напряжение V». Под потолком прожигается
**F − 20…30 МГц**, а записывается против **F**. Это утверждение, которого никто не мерил, —
ровно класс ложного `[TESTED]`, ради которого заведён `TESTING_FRAMEWORK.md`.

## Как код до этого дошёл — и почему лечили не то

2026-08-14 первый прогон в отгружаемой форме поставил на одну частоту ОБА механизма: потолок кривой
на 2842 **и** закрепление на 2842. Карта под потолком села на 2812, замок потребовал ровно 2842,
доказательство замка честно отказало — и ступень, где все три формы нагрузки дали PASS, доложилась
как НЕИЗВЕСТНО.

**Вывод, сделанный тогда, был верен наполовину: «один держатель, никогда два». Убрали при этом не
тот держатель.** Мешал ПОТОЛОК, а сняли ЗАКРЕПЛЕНИЕ — то самое, которое владелец требовал прописными
буквами. Форма «замок без потолка на испытуемой частоте» не пробовалась ни разу.

## Вторые грабли, найденные при починке: сторож R13

Наивная починка — равномерный подъём кривой плюс замок — упирается в сторож R13, поставленный после
BSOD на машине владельца 2026-08-15 (`bugs/11`). Равномерный подъём тянет вверх ВЕСЬ хвост кривой,
включая точки выше максимума карты: набор ловушек показал `подняли точку до 3185 МГц при максимуме
карты 3090 — превышение 95 МГц`, и запись была отвергнута.

**Сторож прав, и ослаблять его нельзя.** Значит форма должна нести потолок — но потолок КОНВЕРТА,
а не потолок испытуемой частоты:

```
кривая поднята на Δ, верх подрезан на МАКСИМУМЕ КАРТЫ (3090)   → R13 удовлетворён
частота заперта замком на испытуемой (например, 3045)          → шаг 7 исполнен
```

Держатели не дерутся: потолок стоит на 3090, замок на 3045 — связывает замок, а потолок только не
пускает хвост за конверт карты.

⚠️ **Вырожденный случай — сама частота 3090:** потолок конверта и замок совпадают, то есть
воспроизводится ровно 2026-08-14. Первая полоса берётся **3082…3015**.

## Починка

1. `chooseWriteShape` — режим `demandPin` (алгоритм владельца, шаг 7): закрепление обязательно,
   независимо от того, способна ли кривая удержать потолок. **Сделано.**
2. `runRung` — при закреплении потолок испытуемой частоты в атом НЕ передаётся (`capForAtom`).
   **Сделано.**
3. `runStep` — замер андервольта ключуется частотой ПОД ИСПЫТАНИЕМ (`capMhz ?? pinMhz`), а не
   потолком: иначе у каждой закреплённой ступени не было бы записи вовсе. **Сделано.**
4. Потолок КОНВЕРТА вместо потолка частоты, чтобы пройти R13. **ОТКРЫТО.**
5. Блоки и мутации на всё вышеперечисленное. Частично сделано (адресаты F–I в `engine --selftest`).

## Урок

**Когда два механизма дерутся, вопрос не «какой убрать», а «какой из них требовал заказчик».**
Сняли тот, который стоял в спецификации прописными буквами, и полтора месяца этого никто не заметил,
потому что оставшийся механизм давал числа — просто не о той частоте.

---

## ✅ РЕПРО ПРОГНАНО 2026-08-30 13:3x (сессия 67) — ЗАКРЫТО

Репро — та самая полоса наверху диапазона, на двойнике (`--card virtual`, записей в живую карту
ноль): `node automation-engine/lib/twin-assembly.mjs --smoke --from 2887 --to 2842`, код 0.

**Прогон отвечает на вопрос тикета по КАЖДОЙ ступени, дословно из лога:**

> `1045 мВ обслуживает 2887 МГц и ПРОШЛО (сдвиг +45 МГц, потолок держит **кривая + замок**, решала
> форма furnace/sustained@0)`

Так все 16 ступеней на 2887 МГц — верх рабочего диапазона, ровно то место, про которое заведён
тикет. Раздел «Что делал код» описывает состояние, которого больше нет.

**Шапка прогона называет обоих держателей и причину, по которой их двое:**

> `КОНВЕРТ КАРТЫ: 3090 МГц — выше него ни одна поднятая точка кривой ничего не предлагает (R13,
> bugs/11). Частоту в прогоне держат ДВОЕ: ВЫПРЯМЛЕННАЯ КРИВАЯ снизу — всё, что торчало бы выше
> испытуемой частоты, придавлено на неё, — и ГРАНИЦА ЧАСТОТЫ сверху. ⚠️ ОДНОЙ КРИВОЙ НЕ ХВАТАЕТ:
> подъём делает её верх ПЛОСКИМ, и арбитраж буста сходит с плато на 2–3 ступени сетки — замерено
> 9 из 9 при безупречной записи (bugs/50, researches/11 §8).`

**Пункт 4 «Починки» («потолок КОНВЕРТА вместо потолка частоты») ИСПОЛНЕН, вопреки метке ОТКРЫТО:**
в `engine.mjs` стоит `capForVector = demandPin ? envelopeMhz : clockMhz` с отказом, когда конверт не
передан, и с отдельным отказом на вырожденный случай `clockMhz >= capForVector` — тот самый 3090,
который тикет назвал заранее.

**И одно уточнение, которое важнее закрытия.** Замок ставится НЕ пином `-lgc N,N`, а ДИАПАЗОНОМ
«пол лестницы … потолок»: карта остаётся свободна вниз. Прежний вывод «замка не ставить» был про
ДРУГОЕ направление — поднять карту К потолку замок не умеет, и это по-прежнему верно
(`researches/11` §1, §5). То есть буква шага 7 («работать ИМЕННО на этой частоте и ни на какой
другой») на верхе диапазона недостижима не по недосмотру, а по свойству карты, замеренному
`researches/11`: `-lgc 3082,3082` выдал 2887 и не сдвинулся. Исполняется СМЫСЛ шага 7 — прожиг идёт
на испытуемой частоте, и запись ведётся против ВЫДАННОЙ частоты, а не заказанной.

### Оба хвоста из блока сессии 33 закрылись раньше, миграцией облака тегов

| хвост | состояние | чем закрыт |
|---|---|---|
| «слово статуса `lever-limited` врёт, когда остановил наш потолок глубины» | ✅ закрыт | в закрытом словаре есть `depth-capped`; сторож регресса стоит блоком `engine --selftest`: «регресс: наш потолок глубины по-прежнему depth-capped (починка 17 августа цела)» |
| «закрытая нашим потолком частота не даёт затравку соседке» | ✅ закрыт | `seedFor` больше не носит переписанный от руки список статусов — он спрашивает `claimsBurnProof`, а тот смотрит `PROVEN_TAGS` (`burn:short` · `burn:long` · `stop:edge-found`). Класс `burn:` НАКОПИТЕЛЬНЫЙ и от `stop:depth-capped` не зависит, поэтому строка с пройденным прожигом затравку даёт |

## Решения, принятые без владельца

- **Тикет закрыт по репро на ДВОЙНИКЕ, а не на живой карте.** Вопрос тикета — о ФОРМЕ ЗАПИСИ и о
  том, кто держит частоту; это утверждение о логике движка (I3), и двойник его судит полноправно
  (слово владельца о равноправном стенде, `GOAL.md`). Что двойник НЕ доказывает и здесь не
  заявляется: как поведёт себя кремний под этой формой — это вечер при владельце (E43-AC5).
- **Пункт 4 отмечен исполненным по чтению кода, а не по отдельному прогону.** Его исполнение видно
  в шапке того же репро («КОНВЕРТ КАРТЫ: 3090 МГц»), то есть прогон его и предъявил.
