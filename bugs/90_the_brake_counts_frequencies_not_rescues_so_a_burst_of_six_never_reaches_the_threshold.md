# Баг 90 — тормоз считает ЧАСТОТЫ, а не спасения: шесть срабатываний за 18 с не добрали до порога

**Статус:** 🔧 **ПОЧИНЕН В НОВОЙ РЕДАКЦИИ 2026-08-31 (сессия 74), ЖИВОГО СВИДЕТЕЛЯ ЖДЁТ.** Тикет был
переопределён владельцем 2026-08-31 17:33 (`interviews/024` = E): чинить надо было НЕ то, что
написано в первой редакции. Разбор первой редакции верен по фактам и оставлен целиком; отменён её
ВЫВОД. Исполнена вторая редакция — четверо новых ворот приёмки взяты, разбор в разделе «Починка».
**Заведён:** 2026-08-31 (сессия 73) · **Найден:** живым прогоном на карте владельца ·
**Починен:** 2026-08-31 (сессия 74), пункт №0 эстафеты

---

## ✅ ПОЧИНКА — ИСПОЛНЕНА 2026-08-31 (сессия 74)

**Ворота намерения, как они были записаны до первой правки поведения:** код кладёт
`STALL_KIND.rescue` в `report.stalls`, и три записи накопителя ОСТАНАВЛИВАЮТ полосу; слово
владельца требует, чтобы спасения полосу не останавливали никогда; спецификация (`plans/82` Ш3 и
ворота B90-AC1н…AC4н ниже) говорит: ПОКАЗЫВАТЬ, но не останавливать. Расхождение разрешено по
старшинству — слово владельца выше кода и выше блока батареи, который этот код утверждал.

### Что сделано, по местам

| Место | Было | Стало |
|---|---|---|
| `engine.mjs` → `STALL_KIND` | третий род `rescue` | рода нет; на его месте разбор, ПОЧЕМУ его нет |
| `engine.mjs` → `LEDGER_SOURCES` | ключ `[DECIDED_BY_FUSE]` | ключа нет; спасение уходит веткой ВЫШЕ словаря |
| `engine.mjs` → отчёт полосы | спасение в `report.stalls` | свой список `report.rescues` + счётчик `report.fuseTrips` |
| `engine.mjs` → сводка прогона | строки не было | `СРАБАТЫВАНИЙ АВАРИЙНОЙ ЗАЩИТЫ: …` печатается ВСЕГДА |

**Форма выбрана СВОИМ СПИСКОМ, а не фильтром над `stalls`, и это решение, а не вкус.** Фильтр на
месте вызова («класть всё, кроме спасений») отменённое поведение не убирает, а прячет: следующий
потребитель `stalls` про фильтр забудет и вернёт его через другую дверь — ровно так `bugs/67`
вернулся через конвейер `tee`. Ветка спасения к тому же стоит ДО словаря источников, поэтому даже
возвращённый в словарь ключ до накопителя не доедет.

### Два числа, которые этот тикет запретил путать

Первая редакция тикета измерила: **шесть срабатываний уместились в ДВЕ частоты**, потому что канал
накопителя почастотный. Поэтому в отчёте их теперь ДВА разных поля и в сводке — два разных слова:
`report.fuseTrips` (срабатывания, читает счётчик защиты) и `report.rescues` (частоты, считает
полоса). Смешать их больше нечем.

### Ворота приёмки новой редакции — сняты прогоном

| # | Критерий | Порог | Снято |
|---|---|---|---|
| **B90-AC1н** | Череда спасений НЕ останавливает полосу | остановок 0 | ✅ **18 срабатываний на 6 ступенях, `haltedBy` = null, полоса прошла все шесть** |
| **B90-AC2н** | Число спасений ВИДНО в сводке | 1 строка | ✅ строка есть и проверяется КАК СТРОКА (`sweepReportLines`), а не как поле отчёта |
| **B90-AC3н** | Отказ АПВ полосу ОСТАНАВЛИВАЕТ по-прежнему | полоса встаёт | ✅ блок «остановка названа СВОИМ именем» зелёный, `stoppedBy` = `fuse-rescue`, не тронут |
| **B90-AC4н** | Пульс и канал драйвера считаются как раньше | 0 правок в их блоках | ✅ оба блока накопителя не правились ни строкой и зелены |

**И ворота B90-AC2 первой редакции («фикстура МЕРЯЕТ раскладку, а не задаёт её») взяты заодно —
она их и нарушала.** Стоял счётчик `fuseTripsFn: () => trips++`: чтение с побочным действием, где
«сработала защита» было свойством числа ВЗГЛЯДОВ на счётчик, а не полосы. Теперь счётчик двигает
прожиг, а читается чисто.

### Мутации — пять, каждая красит своё

Судится тремя чтениями сразу (код выхода · красные строки · сводная строка на месте):

| # | Мутация | Красных | Что покраснело |
|---|---|---|---|
| **М1** | вернуть спасение в накопитель (отменённое поведение) | **4** | все четыре блока новой ветки; полоса встала на 2 частотах — форма живого вечера |
| **М2** | спасение никуда не записывается | **2** | ровно два блока о видимости; блок «не доезжают до накопителя» законно остался зелёным |
| **М3** | счётчик считает ЧАСТОТЫ вместо срабатываний | **3** | оба числа и различитель «не смотрел ≠ не нашёл» |
| **М4** | «не смотрел» подделано под ноль | **1** | ровно свой блок (R4b) |
| **М5** | строка сводки печатается только при спасениях | **1** | ровно свой блок (ноль тоже печатается) |

🔴 **ШЕСТАЯ МУТАЦИЯ СПЕРВА ПРОШЛА ЗЕЛЁНОЙ, И ЭТО НАХОДКА, А НЕ УСПЕХ ([[EXP-0216]]).** Первая
редакция починки снимала счётчик защиты ДО полосы и вычитала («защита от журнала, который живёт
дольше прогона»). Мутация «зашить начало нулём» не покраснила НИЧЕГО — и разбор показал почему:
журнал защиты создаётся ею же при старте прогона и своего пути ни с кем не делит, поэтому начало
ВСЕГДА ноль. Разность защищала от невозможного и покраснеть не могла ни на одной фикстуре — лишняя
сущность с видом сторожа. **Убрана, а не подпёрта фикстурой** (`PHILOSOPHY.md` → Оккам); условие
её возврата названо в коде.

### Проверка близнецов

`TWINS: искал по всему дереву (--include=*.mjs, без node_modules) `report.stalls` · `DECIDED_BY_FUSE` ·
`rescuedByFuse` · `STALL_KIND.rescue` — других носителей класса «спасение попадает в канал, чьё
накопление останавливает полосу» НЕТ.` Единственное совпадение вне движка —
`lib/twin-assembly.mjs:1006`, и это проверка записи СТУПЕНИ (`rec.decidedBy`), а не решения полосы:
ступень обязана называть спасение своим именем, и это поведение не менялось.

### Названо честно, но НЕ чинено этим тикетом

Прогон, где все частоты пропущены спасением, закрывает ноль строк, и правило «ни одна частота не
закрыта» (существующее, `bugs/67`) ставит `ok: false` со строкой `ОСТАНОВЛЕНО (halt): …`. **Полоса
при этом НЕ останавливалась — она дошла до конца**, и слово «ОСТАНОВЛЕНО» в этом случае вводит
читателя в заблуждение. Это ПРЕЖНИЙ дефект формулировки, ставший достижимым по новой дороге;
чинить его здесь значило бы чинить второй дефект в одном тикете. Записан, чтобы не потеряться.

### Решения, принятые без владельца

**Одно, и оно инженерное, а не о существе.** Форма починки (свой список против фильтра над
`stalls`) выбрана мной — владелец назвал ЧТО (спасения не останавливают), а не КАК. Разбор выбора
и его альтернатива — выше в этом разделе. Порог, единица счёта и само правило остановки — его
решение, и оно исполнено буквально: остановки по числу спасений нет ни с каким числом.

---

---

## 🔴 РЕШЕНИЕ ВЛАДЕЛЬЦА, ОТМЕНЯЮЩЕЕ ВЫВОД ЭТОГО ТИКЕТА — читать первым

Дословно: *«Сколько угодно спасений разрешаю. KAGO - это автоматизированный движок тюнинга
видеокарты. В идеале он должен работать по принципу "Запустил и забыл - оно само все сделало"»*.

**Тикет был заведён как «тормоз не тормозит». Владелец говорит: тормоза быть не должно.** Спасение
— это УСПЕХ защиты, а не повод прекращать работу. Порога по числу спасений не заводится НИ С КАКИМ
числом, и вопрос «какой единицей считать» вместе с ним отпадает.

**Что тикет становится:** 🔴 **`STALL_KIND.rescue` обязан ПОКАЗЫВАТЬ, но НЕ ОСТАНАВЛИВАТЬ.** Шаг
Ш6 `plans/81`, сделанный мной в тот же день, провёл спасения в накопитель, который ОСТАНАВЛИВАЕТ
полосу, — то есть построил ровно то, что владелец запретил. Починка: спасения считаются и видны в
сводке (это критерий P81-AC6, он остаётся), но в решение об остановке не входят.

**Что при этом НЕ трогается, иначе починка станет снятием защиты:** отказ АПВ (сток не подтверждён
перечитыванием) останавливает полосу по-прежнему — именно так вечер 31.08 и кончился; тормоз «два
зависания подряд на одной ступени» (R15) остаётся; почастотный счёт для ПУЛЬСА и КАНАЛА ДРАЙВЕРА
не трогается вовсе — про них владелец не говорил.

**И куда уехала настоящая работа:** раз череду спасений нельзя лечить тормозом, её лечат ПРИЧИНОЙ —
`bugs/86` (срыв замка, воспроизведён дважды) и `bugs/92` (порча записей драйвером). Ответ владельца
не убрал работу, а переставил её с симптома на причину.

### Новые ворота приёмки — по его решению

| # | Критерий | Шкала · Прибор · Порог |
|---|---|---|
| **B90-AC1н** | Череда спасений НЕ останавливает полосу | Шкала: остановок по числу спасений · Прибор: двойник, 9 срабатываний · **Порог: 0** |
| **B90-AC2н** | Число спасений за вечер ВИДНО в сводке | **Порог: 1 строка** (это же P81-AC6) |
| **B90-AC3н** | Отказ АПВ полосу ОСТАНАВЛИВАЕТ по-прежнему | Прибор: двойник с неподтверждённым стоком · **Порог: полоса встаёт** |
| **B90-AC4н** | Пульс и канал драйвера считаются как раньше | **Порог: 0 правок в их блоках** |

---

## ⬇️ РАЗБОР НИЖЕ ВЕРЕН ПО ФАКТАМ И ОСТАВЛЕН ЦЕЛИКОМ — отменён только его ВЫВОД

**Цена, как она виделась до решения владельца:** тормоз против череды спасений не тормозит. Риск (б)
плана 81 был объявлен закрытым в тот же день — **преждевременно, и это признание, а не уточнение.**
⚠️ Само измерение (шесть срабатываний → две записи) остаётся верным и ценным: оно показывает, что
единица счёта у накопителя ПОЧАСТОТНАЯ, и это надо знать, когда его будут трогать ради пульса или
драйвера.

---

## Симптом

Живой прогон 2026-08-31 17:04: **шесть срабатываний аварийной защиты за 18 секунд.** Полосу
остановил ОТКАЗ АПВ (нечем было подтвердить сток), а накопитель предупреждений — **не остановил**,
хотя заведён ровно против такого.

## Улика

Протокол защиты `runs/death-watch/2026-08-31T14-03-05-531Z-fuse.jsonl` — шесть строк `intent`
с 14:04:20 по 14:04:37. Сводка прогона при этом:

```
ПРОПУЩЕНО ЧАСТОТ: 2 — halt: 2 частот(ы) — 2145, 2115
ОСТАНОВЛЕНО (fuse-rescue): АПВ НЕУСПЕШНО — аварийная защита не смогла вернуться на пост
```

**Две записи в накопителе при пороге три.** Шесть спасений уместились в две частоты.

## Корневая причина — канал накопителя ПОЧАСТОТНЫЙ

`engine.sweepRange` кладёт запись в `report.stalls` **в ветке пропущенной ЧАСТОТЫ** — то есть
ровно один раз на частоту, каким бы ни было число срабатываний внутри неё. Шаг Ш6 плана 81 добавил
третий РОД (`STALL_KIND.rescue`) в существующий канал и тем унаследовал его единицу счёта.

**Ш6 сделал спасение видимым, но не сделал его считаемым.** Это разные вещи, и я их спутал.

> ⚠️ **И вот почему на двойнике этого было не видно.** Репетиция смерти дала 9 срабатываний на
> ТРЁХ частотах — по 3 на частоту, и накопитель получил 3 записи, то есть ровно порог. Блок
> `Ш6: череда срабатываний ОСТАНАВЛИВАЕТ полосу` зеленел честно. На живой карте шесть
> срабатываний легли в ДВЕ частоты — и порог не взялся. **Стенд не опроверг механизм, он не смог
> его выразить** (класс `bugs/26`, [[EXP-0199]]): в фикстуре число срабатываний на частоту было
> задано моим сценарием, а не измерено.

## Гипотезы починки (НЕ реализовывать без решения владельца по существу)

Вопрос «сколько спасений за вечер допустимо» — это про БЕЗОПАСНОСТЬ ЕГО МАШИНЫ, значит его.
Вопрос «какой единицей считать» — инженерный и мой ([[EXP-0214]]).

1. **Считать срабатывания, а не частоты** — накопитель получает запись на КАЖДОЕ срабатывание,
   источник счёта — `fuse.tripCount` (уже есть, уже читается ступенью). Наименьшее изменение:
   ступень уже знает `tripsNow - tripsBefore`.
2. **Два порога вместо одного** — «за прогон» (есть) и «внутри одной частоты» (нет). Второй ловит
   именно всплеск.
3. Ничего не менять и полагаться на отказ АПВ как на фактический тормоз — **отвергается**: он
   сработал ПОТОМУ ЧТО драйвер начал портить записи, то есть по счастливой случайности. Тормоз,
   работающий из-за другой поломки, — не тормоз.

## Ворота приёмки

| # | Критерий | Шкала · Прибор · Порог |
|---|---|---|
| B90-AC1 | Всплеск срабатываний внутри ОДНОЙ частоты останавливает полосу | Шкала: срабатываний до остановки · Прибор: двойник с подложенным всплеском на одной частоте · **Порог: ≤ порога владельца, сегодня — 6 и не остановило** |
| B90-AC2 | Фикстура МЕРЯЕТ число срабатываний на частоту, а не задаёт его | Прибор: блок двойника · **Порог: сценарий не диктует раскладку** |
| B90-AC3 | Прежний почастотный счёт (пульс, драйвер) не сломан | Прибор: существующие блоки накопителя · **Порог: 0 правок в них** |

## Решения, принятые без владельца

⚠️ Эта строка — ПЕРВОЙ РЕДАКЦИИ, и она верна для неё: тогда починка не начиналась, а единица счёта
была вынесена как вопрос к владельцу. Он на него ответил ОТМЕНОЙ самого вопроса. Решения второй
редакции — выше, в разделе «Починка».

## Связи

`plans/81` Ш6 (объявил риск (б) закрытым — преждевременно) · `bugs/80` · `bugs/84` (тот же
накопитель, тот же класс «читатель не получает записей») · `bugs/26` · `plans/82` · [[EXP-0199]] ·
[[EXP-0219]] (вырожденный контроль в моей же фикстуре и сторож, который не мог покраснеть).
