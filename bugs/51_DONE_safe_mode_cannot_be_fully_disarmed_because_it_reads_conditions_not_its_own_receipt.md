# Bug 51 — безопасный режим НЕЛЬЗЯ снять до конца: он читает УСЛОВИЯ, а не собственную расписку

**Статус:** ✅ ЗАКРЫТ 2026-08-30 (сессия 65) — все пять пунктов плана исполнены, сторож доказан красным.
**Версия:** `main` @ `9bed2c5` · **Где:** `tools/safe-mode.mjs` → `aspects()`, признак `torrent`
**Класс:** «наблюдаемое условие выдано за наше действие» — сосед `bugs/45` (там развёртка приняла
применённый профиль за заводское состояние карты)

## Симптом

Владелец сказал снять безопасный режим. `node tools/safe-mode.mjs --off` отработал **4 из 4 по
расписке** — FTP вернулся в `Running`, J:, F:, E: снова пишутся. И тут же напечатал:

```
БЕЗОПАСНЫЙ РЕЖИМ ДЛЯ ДИСКОВ — HALF-ARMED
  ВЗВЕДЁН НАПОЛОВИНУ: torrent — да, а ftpsvc, disk:J, disk:F, disk:E — нет
  ...
  ⚠️ ВЗВЕДЁН НАПОЛОВИНУ. Прогон KAGO писать в карту откажется (plans/30 AC4).
     Снять: node tools/safe-mode.mjs --off
```

**Совет в последней строке — это ровно та команда, которая только что отработала.** Петля.

## Цена, наступившая немедленно

**Развёртка сейчас не запустится.** Гейт `plans/30` AC4 пропускает полностью взведённый и полностью
снятый режим и ОСТАНАВЛИВАЕТ половину — а половина теперь недостижимо-постоянна. Единственный способ
выйти из неё сегодня: владелец сам запускает qBittorrent.

То есть механизм, поставленный защищать диски владельца, **блокирует работу проекта в состоянии,
которое сам же объявил дефектным, и выхода из него не предлагает.**

## Причина — найдена, одна строка

`tools/safe-mode.mjs:145`:

```js
armed: machine?.torrent == null,
```

Признак `torrent` считается ВЗВЕДЁННЫМ всякий раз, когда торрент-клиент не запущен. Но «не запущен»
и «мы его закрыли» — разные факты, и у второго есть свидетель: **расписка отката**. В ней
(`runs/shell/rollback/safe-mode.json`, от 2026-08-23T20:49:13Z) **четыре пункта, и торрента среди них
нет** — на момент взвода клиент уже был закрыт, значит тулза его не трогала и восстанавливать ей
нечего.

**Обобщение, и оно шире торрента:** детектор состояния читает УСЛОВИЯ мира, а расписка знает НАШИ
ДЕЙСТВИЯ. Пока эти два источника не разведены, любой признак, который случайно истинен и без нас,
навсегда оставляет режим «взведённым наполовину». Тот же класс у `ftpsvc`: если владелец однажды
остановит службу сам, повторится точь-в-точь.

## План починки

1. **Развести два вопроса, а не подкрутить один.** «Взведён ли режим» = «стоит ли то, что МЫ
   поставили», а это расписка. Наблюдаемое условие остаётся, но отвечает на другой вопрос — «защищён
   ли писатель прямо сейчас», — и печатается отдельной колонкой.
2. **Признак, взведённый НЕ нами, в оценку полноты не входит.** Он показывается («торрент-клиент не
   запущен — не нами»), но ни ARMED, ни HALF-ARMED из-за него не возникает.
3. **Гейт судит по той же величине**, иначе появится вторая пара «правда ↔ зеркало».
4. **Последняя строка вывода не может советовать команду, которая только что отработала.** Если
   выхода нет — сказать, что его нет, и почему.
5. **Блок + мутация:** режим, где один признак истинен без расписки, читается как ПОЛНОСТЬЮ СНЯТЫЙ;
   мутация «считать полноту по условиям» краснит блок. Фикстура уже почти есть — `safemode` набор
   несёт `torrent: null` в четырёх сценариях.

## Решения, принятые без владельца

- **Ничего не чинил в этой сессии** — владелец назвал следующей задачей `bugs/50`. Дефект заведён
  документом сразу, по правилу «заметка на ходу едет в беклог, а не в переключение задачи».
- **qBittorrent НЕ запускал**, хотя это вернуло бы режим в «полностью снят» одним движением: запуск
  чужого приложения на машине владельца — не то действие, которое агент делает молча ради зелёного
  индикатора.

## Ссылки

`plans/30` (безопасный режим и его гейт, AC4) · `bugs/45` (тот же класс на карте: наблюдаемое
состояние принято за заводское) · `runs/shell/rollback/safe-mode.json` (расписка, знающая правду) ·
`tools/safe-mode.mjs:110` (снятие условий) и `:144-147` (признак `torrent`)

---

## ✅ СТАТУС: ЗАКРЫТ (2026-08-30, сессия 65)

**Петля разомкнута там, где она рождалась: два вопроса разведены.**

| пункт плана | сделано |
|---|---|
| 1. развести «взведён ли режим» и «стоит ли условие» | `dutiesOf` получил расписку и проставляет каждому дежурству `byUs`; условие (`armed`) осталось на своём месте и по-прежнему печатается |
| 2. признак, взведённый НЕ нами, в полноту не входит | `safeModeState` исключает `byUs === false` — тем же фильтром, что `absent` и `refusedByGuard`, и по той же причине |
| 3. гейт судит по той же величине | гейт зовёт `safeModeState`; второй пары «правда ↔ зеркало» не появилось — величина одна |
| 4. последняя строка не советует только что отработавшее | при чужом условии вместо «Снять: --off» печатается: **«⛔ ВЫХОДА ЭТОЙ КОМАНДОЙ НЕТ»**, имя чужого дежурства, почему `--off` его не снимает, и единственный настоящий выход — `--on` |
| 5. блок и мутация | четыре новых `ok` в `safemode`; набор **15 → 19 блоков** |

### Правило намеренно УЗКОЕ, и это половина починки

**Нет расписки — атрибутировать нечем, и прежнее чтение сохраняется.** Первая редакция была шире:
«взведено, но не в расписке» = «не наше» без оговорки. Она **покраснила пять существующих блоков
набора** — на фикстурах без расписки любое взведённое условие переставало считаться. Это был
правильный красный: широкое правило объявляло чужим то, о чём просто нечего знать. Сузил до того
случая, который тикет и описывает — **расписка ЕСТЬ, в ней четыре пункта, торрента среди них нет**.
Ни одна существующая проверка при этом не правилась: 15 из 15 остались зелёными как были.

### Доказано красным, адресность объявлена ДО прогона

| мутант | объявлено | получилось |
|---|---|---|
| полнота снова считается по УСЛОВИЯМ (`byUs` не смотрим) | красный ровно новый блок | ✅ ровно он: «после --off режим читается ПОЛНОСТЬЮ СНЯТЫМ, хотя торрент не запущен» — получено `half-armed`, ждали `disarmed` |
| целый код (откат копией) | ноль красных | ✅ 19 из 19 |

Батарея **40 наборов / 1663 блока / 0 красных** (`safemode` 15 → 19).

### Что это меняет для владельца

**Обход из эстафеты больше не нужен.** Стояло: «безопасный режим снимается только ПОЛНЫМ
взведением (`--on`), `--off` оставляет `HALF-ARMED` и гейт отказывает прогону». Теперь `--off`
снимает режим до конца, и прогон не заперт — при этом закрытый торрент-клиент по-прежнему ВИДЕН
в выводе, просто с честной пометкой «НЕ НАМИ».

**Класс шире торрента, и решение тоже.** Тикет предупреждал: то же повторится с `ftpsvc`, если
владелец однажды остановит службу сам. Починка сделана не для торрента, а для правила — поэтому
и для `ftpsvc` тоже.

## Решения, принятые без владельца — второй набор (закрытие)

| # | решение | почему так, и чем отменить |
|---|---|---|
| 1 | **Отсутствие расписки НЕ означает «ничего не наше»** | Иначе правило объявляет чужим то, о чём нечего знать, и краснит пять честных блоков. Наблюдение поверх рассуждения: широкую редакцию отверг прогон |
| 2 | **Чужое дежурство ПОКАЗЫВАЕТСЯ, а не прячется** | Спрятать значило бы вылечить индикатор, а не машину: владелец должен видеть, что условие стоит, и знать, что оно не наше |
| 3 | **`--off` по-прежнему не трогает того, чего не ставил** | Запустить чужое приложение ради зелёного индикатора — то же решение, от которого сессия 54 отказалась намеренно (см. выше). Не пересматривалось |
