/**
 * ТОЧКИ ВХОДА ДВИЖКА, ПИШУЩИЕ В КАРТУ — ОДИН СПИСОК НА ВСЕХ ЧИТАТЕЛЕЙ.
 *
 * ⚡ Заведён 2026-09-07 по `bugs/110`, и повод стоит назвать точно, потому что он и есть довод.
 *
 * Список существовал с самого начала — в `tools/tidy.mjs`, внутри `runInFlight`, с комментарием
 * «забытая здесь команда — это команда, которую уборка однажды убьёт посреди записи в GPU». Он был
 * ПОЛНЫМ: уборка знала все три режима. А пролог аварийной защиты (ворота окна · судья · проба ·
 * вход 2 · сейлок) оказался заведён только в ОДНОМ из них, и выяснилось это не чтением, а живым
 * прогоном при владельце у машины: `--band` пошёл писать в карту, не подняв ни одного сторожа.
 *
 * Читателей у списка теперь двое, и вопросы у них РАЗНЫЕ:
 *   · уборка спрашивает «кого нельзя убивать» (`tools/tidy.mjs`);
 *   · сторож движка спрашивает «у кого обязан быть пролог защиты» (`engine.mjs` → `selfTest`).
 * Источник обязан быть один. Две копии разошлись бы молча, и четвёртый вход появился бы
 * незамеченным ровно так же, как появился третий.
 *
 * ПОЧЕМУ ОТДЕЛЬНЫЙ ФАЙЛ, А НЕ ЭКСПОРТ ИЗ ОДНОГО ИЗ ДВУХ: `selfTest()` движка синхронна и
 * динамического импорта не допускает, а статический импорт `tools/tidy.mjs` в движок (или
 * двенадцатитысячестрочного движка в лёгкую уборку) утянул бы за собой чужое дерево ради одной
 * константы. Модуль без зависимостей и без побочных эффектов — самая дешёвая форма единственного
 * источника.
 *
 * ⚠️ ДОБАВЛЯЕШЬ СЮДА РЕЖИМ — он обязан оказаться либо в `GUARDED_ENTRY_POINTS`, либо в
 * `UNGUARDED_ENTRY_POINTS` (`engine.mjs`). Блок покрытия краснеет на любом, кто не попал никуда.
 */
export const ENGINE_CARD_WRITING_MODES = Object.freeze(['--sweep', '--band', '--search']);
