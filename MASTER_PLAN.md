# KAGO — МАСТЕР-ПЛАН

> Дорожная карта: как мы попадаем из **текущего состояния** в видение из `GOAL.md`. Верхний
> уровень — фазы и вехи, а не задачи на день (те живут в `plans/`). Выведен из `GOAL.md` и из
> `RTX_5070Ti_Undervolting_Master_Plan.pdf`; обновляется навыком `/revision`.
>
> Это **живой справочник**, а не задача — тегом `DONE` не помечается.

---

## Видение (одной строкой)

Оркестратор, который сам подбирает и держит два безопасных профиля андервольтинга для RTX 5070 Ti,
переключается ярлыком с рабочего стола и всегда умеет вернуть карту к заводскому состоянию.

## Ведущие принципы

- **Ноль сторонних GUI в зависимостях.** Слово владельца из `GOAL.md`. Управление идёт своим кодом
  на Node.js — сначала через `nvidia-smi`, потом через собственный мост к NVAPI.
- **Заводское состояние — состояние по умолчанию.** Любой профиль живёт в энергозависимой памяти
  GPU. Пропал процесс, упала ОС, перезагрузились — карта заводская. Откат никогда не требует
  действий владельца.
- **Стабильность доказывается наблюдением, а не отсутствием краха.** Половина сбоев от
  недовольтажа — тихие: картинка есть, числа неверные. Профиль проверяется сравнением с эталоном.
- **Каждая точка кривой держит запас над порогом сбоя.** Отказ у края обвальный, а не плавный —
  стоять на краю нельзя (`researches/02`).
- **Профиль привязан к драйверу и VBIOS.** Драйвер обновился — профиль недействителен до
  перепроверки.

---

## Фаза 0 — Развёртывание KAIF

- **Цель фазы:** проект живёт под фреймворком, канон заполнен пониманием проекта, репозиторий
  опубликован.
- **Статус:** ✅ закрыта 2026-08-09 22:05 +03:00

| # | Пункт | Статус |
|---|---|---|
| 1 | Изучить проект и зафиксировать находки (`study`) | ✅ |
| 2 | Исследовать: возможен ли андервольтинг без MSI Afterburner (CLI-only) | ✅ `researches/01` |
| 3 | Подтвердить каноническое имя проекта (`project-name`) | ✅ KAGO |
| 4 | Заполнить плейсхолдеры в `AGENT_GUIDE.md` и навыках (`placeholders`) | ✅ плюс `tools/check.mjs`, `tools/gpu-info.mjs` — обе команды прогнаны |
| 5 | Заполнить внешнюю и внутреннюю карты проекта (`maps`) | ✅ |
| 6 | Вывести `MASTER_PLAN.md` из `GOAL.md` и PDF (`goal-plan`) | ✅ этот файл |
| 7 | Назначить сферу проекта (`sphere`) | ✅ `programming` |
| 8 | Написать `KAIF_FRAMEWORK.md` (`kaif-framework`) | ✅ |
| 9 | Подключить голос владельца (`owner-voice`) | ✅ ссылкой на `d:\work\krinik_voice\`; `AUTHOR_STYLOMETRY.md` внесён в `.gitignore` |
| 10 | Двуязычный `README` (RU/EN) по образцу KAIF | ✅ плюс `LICENSE` MIT |
| 11 | Написать полевой отчёт об установке (`field-report`) | ✅ плюс тикет улучшения `bugs/KAIF/01` |
| 12 | Прогнать `verify-final` | ✅ зелёный, установщик самоочистился |
| 13 | `git init` + публичный репозиторий на GitHub через `gh` | ✅ https://github.com/MikalaiKryvusha/KAGO |

> Дополнительно к методике: вопрос владельца о поиске оптимума в каждой точке кривой закрыт
> документом `researches/02` и лёг в основание фазы 5.

> Пункт 2 в исходный список установки не входил — он добавлен, потому что вопрос из `GOAL.md`
> решает архитектуру всего проекта, и мастер-план нельзя было писать честно, не ответив на него.

## Фаза 1 — Стенд и базовая линия

- **Цель фазы:** система умеет наблюдать за картой, грузить её своими нагрузками и знает, как
  выглядит «стоково». Ни одной записи в GPU за всю фазу.
- **Шаги:** `hardware-mon.mjs` (**только `nvidia-smi`** — HWiNFO64 из архитектуры убран) ·
  `event-logger.mjs` (TDR, WHEA, Kernel-Power, BSOD из журнала Windows) · `toolchain.mjs` (поиск
  `nvcc` и среды MSVC через `vswhere`) · **свои нагрузки** `workloads/*.cu` в трёх формах ·
  `stress-tester.mjs` с вердиктом PASS/SDC/CRASH · **золотые эталоны выходов** с отметкой драйвера
  и VBIOS.
- **Что изменилось против первой редакции:** фаза расширена с «телеметрии» до «стенда» —
  `researches/03` показал, что автоматизацию нагрузки у всех зрелых бенчмарков продают отдельно, а
  своё CUDA-ядро на этой машине собирается и даёт бит в бит одинаковый результат.
- **Операционный план:** `plans/02_epic01_phase1_harness_and_baseline.md`
- **Статус:** 🔲 открыта, план написан
- ~~«подтвердить, что `nvidia-smi -pl` действительно пишет»~~ — **сделано 2026-08-09**, см. журнал
  решений ниже.

## Фаза 2 — Профиль Silent Cold на одном `nvidia-smi`

- **Цель фазы:** первый работающий профиль без единой зависимости — фиксация частоты ядра и
  потолка мощности загоняют карту вниз по штатной кривой.
- **Шаги:** `profile-manager.mjs` **как интерфейс со сменными бэкендами** (сегодня `nvidia-smi`,
  завтра `nvapi`) · подбор точки по температуре и шуму · проверка по матрице приёмки.
- **Ограничение, названное заранее:** цель «≥97 % производительности» этой фазой не берётся —
  для неё нужна правка кривой.
- **Статус:** 🔲

## Фаза 3 — Оболочка запуска: ярлыки, трей, автозапуск, гарантированный сброс

- **Цель фазы:** закрыты все требования `GOAL.md` по управлению — на том профиле, что уже есть.
- **Устройство задано владельцем** (2026-08-09): **три ярлыка** — 🚀 Max Optimal, ❄️ Silent Cold,
  ⏹ Сброс к заводским. Любой из трёх становится запомненным состоянием для автозагрузки. **Иконка
  в трее только показывает активный профиль** — ни кнопок, ни меню, ни действий по клику.
- **Шаги:** `desktop-shortcuts.mjs` (`.lnk` через `WScript.Shell`) · `setup-desktop.mjs` ·
  запись запомненного состояния и его восстановление задачей при старте ПК · пассивная иконка
  в трее.
- **Трудность снята:** ловить гибель процесса больше не нужно. Сброс — это третий ярлык, а не
  сторож. Заводское состояние всё равно возвращается само при перезагрузке, потому что профиль
  живёт в энергозависимой памяти GPU.
- **Статус:** 🔲

## Фаза 4 — Собственный мост к NVAPI

- **Цель фазы:** KAGO читает и пишет V/F-кривую своим кодом, без сторонних бинарников.
- **Шаги:** FFI из Node.js (`koffi`) к `nvapi64.dll` · `nvapi_QueryInterface` → Pstates20 /
  clock-boost-table · **проверка чтением после записи** — не совпало, профиль отвергается ·
  замер шага сетки напряжения на живой кривой.
- **Запасной путь:** если Blackwell не поддастся — `green-curve` (MIT) как бэкенд, интерфейс из
  фазы 2 это допускает.
- **Статус:** 🔲

## Фаза 5 — Движок поиска Vmin и запаса надёжности

- **Цель фазы:** `engine.mjs` сам находит для каждой точки кривой напряжение чуть выше порога
  сбоя — то, что просил владелец.
- **Шаги:** трёхзначный вердикт `PASS / SDC / CRASH` · спуск до первого сбоя, бинарный поиск края
  по сетке · запас **≥4 шага сетки и не менее 25 мВ** над порогом · **разнородный набор нагрузок**
  (ветвистая, счётная, переходная) · порог точки = худший по всему набору · длинный прогон и
  выдержка.
- **Основание:** `researches/02` — Vmin зависит от программы на 100 мВ, до краха ОС от Vmin всего
  4–5 %, вероятность ошибки растёт обвально (3 % → 90 % на двух процентах напряжения).
- **Статус:** 🔲

## Фаза 6 — Два профиля и приёмка

- **Цель фазы:** `Max Optimal` и `Silent Cold` собраны, проверены по матрице приёмки из PDF,
  разложены по ярлыкам.
- **Шаги:** сборка профилей из результатов фазы 5 · 20 циклов SpeedWay · транзиентный тест ·
  ноль BSOD/TDR/WHEA · приёмка владельцем.
- **Статус:** 🔲

## Журнал решений

| Отметка | Решение | Почему |
|---|---|---|
| 2026-08-09 21:24 +03:00 | Потолок `nvidia-smi -pl` на этой карте — 250 Вт из 300, то есть −50 Вт максимум | Замер на живой карте. Цели PDF (−60…−120 Вт) одним power limit недостижимы — отсюда необходимость правки кривой |
| 2026-08-09 21:28 +03:00 | Лестница: фаза на `nvidia-smi`, затем свой мост к NVAPI. Владелец, чат | Держит ноль сторонних зависимостей из `GOAL.md` и даёт рабочий результат до дорогой части |
| 2026-08-09 21:28 +03:00 | Лицензия MIT. Владелец, чат | Единообразие с KAIF |
| 2026-08-09 21:30 +03:00 | `profile-manager.mjs` — интерфейс со сменными бэкендами, а не прямой вызов инструмента. Агент | PDF жёстко зашивает туда MSI Afterburner; `GOAL.md` это запрещает, а бэкендов будет минимум два |
| 2026-08-09 21:40 +03:00 | Оракул стабильности трёхзначный, с запасом над порогом. Агент, по вопросу владельца | Крах — не единственный отказ: больше половины программ сначала портят данные молча (`researches/02`) |
| 2026-08-09 21:40 +03:00 | Портрет голоса владельца подключается **ссылкой**, копия в публичный репозиторий не едет. Агент | `krinik-stylometry` приватен и содержит цитаты личных корпусов; правило владельца №39 — проекты ссылаются, копий не держат |
| 2026-08-09 22:35 +03:00 | Полное приватное ядро портрета затянуто в проект и поставлено под `.gitignore`. Владелец, чат: «можем сюда затянуть стилометрическое ядро но поставить его под гитигнор» | Агент работает с самой полной версией, публика не получает ничего. Отменяет предыдущую строку в части «только ссылка» |
| 2026-08-09 22:4x +03:00 | `nvidia-smi -pl` **пишет** на этой карте — проверено наблюдением с согласия владельца: 300 → 290 Вт, чтение 290, возврат 300 | Бэкенд пути A перестал быть предположением. Попутно: сообщение утилиты о прежнем значении врёт — в поле «from» печатается default, а не то, что стояло; состояние только перечитывать |
| 2026-08-09 22:50 +03:00 | **Три ярлыка, трей без кнопок.** Сброс к заводским — отдельный третий ярлык, тоже с записью в автозагрузку. Владелец, чат | Снимает необходимость ловить гибель процесса. У явного ярлыка нет режима отказа, у сторожа он есть — сброс GPU там, где ничего не сломалось. Отменяет строку `GOAL.md` про сброс при убийстве иконки |
| 2026-08-09 22:30 +03:00 | **KAGO пишет свои нагрузки, а не покупает чужие.** Агент, `researches/03` | Автоматизация у UNIGINE, 3DMark и OCCT продаётся только в платных редакциях, а бесплатный FurMark даёт ровную полку — не ту форму нагрузки, которая валит андервольт. Проверено обратное: своё CUDA-ядро собирается на этой машине и трижды подряд даёт `fd7d452ce569c9d7` — оракул SDC строится своими руками |
| 2026-08-09 22:30 +03:00 | **HWiNFO64 убран из архитектуры.** Агент, `researches/03` §3.5 | Это стороннее GUI-приложение (запрет из `GOAL.md`), а датчик, ради которого он там стоял, на RTX 50 отключён на уровне драйвера — эта карта возвращает `temperature.memory = N/A`. Телеметрия — один `nvidia-smi`, и он богаче, чем предполагалось: даёт запас до предела и причины троттлинга |
| 2026-08-09 22:30 +03:00 | **Фаза 5 формально зависит от фазы 4.** Агент, `researches/03` §2 | У `nvidia-smi` вообще нет поля напряжения — до собственного моста к NVAPI поиску Vmin нечем наблюдать свою же управляющую переменную. Подтверждает уже принятый порядок фаз, но теперь это доказательство, а не интуиция |
| 2026-08-09 22:41 +03:00 | **Фаза 3 (ярлыки и трей) идёт раньше фазы 4 (мост к NVAPI).** Агент, `plans/01_EPIC` §3 | После фазы 3 у владельца на руках законченный полезный продукт — профиль на ярлыке, переживающий перезагрузку. Это парето-срез эпика; всё, что дальше, покупает второй профиль |

---

> **Поддержание:** держать в согласии с реальностью. Когда `GOAL.md` или состояние проекта заметно
> меняются — `/revision` и перевывод фаз. Детальные пошаговые планы под каждую фазу живут в `plans/`.
