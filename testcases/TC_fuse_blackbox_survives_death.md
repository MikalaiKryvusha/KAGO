# Test cases — чёрный ящик предохранителя переживает смерть машины (`bugs/78`, эпик 73 фаза 1)

**Created:** 2026-08-30, ночь · **Under test:** непрерывная запись судьи (`fuse.mjs` → строка жизни
`…-alive.jsonl` + существующий дамп кольца) · **Version/build:** `main` @ `9131751`, ДО правки
**Test basis:**
- **Слово владельца 2026-08-30** (инцидент): *«И чёрный ящик не выжил… Запустили тестовый полёт,
  разбили самолёт, и логов не сняли, так как чёрный ящик не работает»* — источник требования R1.
- **Канон S4** (`GOAL.md` → «🔴 ЗАВИСАНИЕ — НЕ НОРМАЛЬНОЕ СОБЫТИЕ, А НАШ ПРОВАЛ»): каждое
  зависание требует разбора → разбор требует улик — источник R2.
- **`TESTING_FRAMEWORK.md`, ворота 5:** проверка, которая ни разу не падала, ничего не доказывает —
  источник контрольных случаев K1.
- **Измерение инцидента** (`bugs/76`): `fuse.jsonl` 0 байт, `…-ring.jsonl` отсутствует — фактическая
  база «как есть».
- **`bugs/37`** (архив пульса терялся в кэше страниц) — прецедент лечения тем же приёмом.

## 1. Goal vector

**Achieve.** После смерти машины на диске остаётся след судьи, покрывающий последние секунды перед
ней, — достаточный, чтобы ответить на вопрос, на который сегодня ответить нечем: **жил ли судья в
момент события и что он видел.** «Работает» значит: улика есть после смерти БЕЗ штатного закрытия,
и при этом судья не стал медленнее — иначе прибор испортил то, что измеряет.

## 2. Requirements under test

| # | Требование (EARS) | Fit criterion (Scale · Meter · Target) |
|---|---|---|
| **R1** | WHEN судья работает, the system shall писать строку жизни на диск не реже раза в секунду с немедленным `fsync` | возраст последней записи относительно момента убийства · блок с убийством дерева процессов · **≤ 1,5 с** |
| **R2** | WHERE строка жизни записана, the system shall нести метку времени · `t` от старта · тактов за секунду · худшие `gapMs`, `beatSilenceMs`, `progressSilenceMs`; **IF канал молчания не проведён, THEN его поле несёт `null`, а не ноль** | полей на месте · **6 из 6**; из них с ЧИСЛОМ — все, чей канал проведён; у непроведённого — **ровно `null`** · чтение файла |
| **R3** | WHILE идёт запись строк жизни, the system shall не замедлять такт судьи | сдвиг медианы `gapMs` относительно базовой линии · одинаковый прогон двойника до/после · **≤ 5 %** |
| **R4** | WHEN случается трип ИЛИ штатное закрытие, the system shall по-прежнему писать полный дамп кольца | тактов в `…-ring.jsonl` после `--rehearse-death strangle` · **≥ 1500** |
| **R5** | IF судья убит без штатного закрытия, THEN the system shall НЕ создавать `…-ring.jsonl` (кольцо честно теряется, и это признаётся) | наличие файла · **отсутствует — и это ожидаемо, а не дефект** |

## 3. Coverage matrix

| Измерение | Покрыто | НЕ покрыто, риск назван |
|---|---|---|
| Способ смерти | убийство всего дерева процессов без штатного закрытия (`SIGKILL`) | **настоящая заморозка ОС** — двойник не может заморозить свой хост (`plans/73` риск «г»); по отношению к чёрному ящику `SIGKILL` даёт тот же эффект (ни трипа, ни закрытия), но это НЕ полная верность, и на этом фаза не имеет права называть класс «зависание машины» доказанным |
| Момент смерти | в середине секундного окна · сразу после сброса строки | смерть в момент самого `fsync` — окно ~микросекунды, риск принят |
| Длительность прогона до смерти | < 1 с (строк ещё нет) · ~30 с · ~5 мин | многочасовой прогон — рост файла проверяется отдельно (1 строка/с = 3600 строк/час, безопасно) |
| Нагрузка судьи | такт 2 мс, штатный | судья под искусственной перегрузкой ОС — отдельный предмет |
| Каналы улик | строка жизни · кольцо · журнал трипа | `…-driver.jsonl` — вынесен в шаг 3.8 плана как ОТДЕЛЬНЫЙ разбор |

**Осознанно не покрываем и почему.** Настоящее зависание хоста — единственный класс, ради которого
всё делается, и именно он на стенде недостижим. Это записано и в плане, и в эпике; фаза доказывает
СВОЙСТВО ЗАПИСИ (улика переживает отсутствие штатного закрытия), а не класс «машина замёрзла».
Смешивать эти два утверждения — ровно та ошибка, за которую заплачено сегодня.

## 4. Cases

| # | Случай (шаги → ожидание) | Техника | Статус + улика |
|---|---|---|---|
| **C1** | Поднять судью, дать проработать 3,5 с, убить дерево `taskkill /T /F` → `…-alive.jsonl` существует, последняя запись не старше 1,5 с до убийства | граничное значение | ✅ `[TESTED: 2026-08-31 · блок C1 в --selftest: строк 3, возраст последней 443 мс]` |
| **C2** | Прочитать строку жизни → шесть полей на месте, четыре всегда-знаемых несут число, молчание проведённого канала несёт число, непроведённого — честный `null` | разбиение на классы | ✅ `[TESTED: 2026-08-31 · тот же блок; мутация K5 «ноль вместо null» краснеет]` |
| **C3** | Убить судью через < 1 с после старта → файл отсутствует; **процесс не падает с ошибкой** | граничное значение (нижняя граница) | ✅ `[TESTED: 2026-08-31 · убит через 400 мс, pid узла 36532 → файл ОТСУТСТВУЕТ, ошибок нет]` |
| **C4** | Прогон `--rehearse-death strangle` целиком → `…-ring.jsonl` ≥ 1500 тактов, 9/9 проверок репетиции зелёные (R4 не сломан) | переход состояния | ✅ `[TESTED: 2026-08-31 · 1823 такта, 9/9 зелёных]` |
| **C5** | Снять `gapMs` до и после правки → замедления нет | сравнение с базовой линией | ✅ `[TESTED: 2026-08-31 · ТРИ прогона с каждой стороны: до 2,10·2,14·2,21 (ср. 2,15) → после 2,21·2,12·2,41 (ср. 2,25), сдвиг +4,7 % при пороге 5 %]` — ⚠️ метод исправлен по ходу, см. ниже |
| **C6** | Прогон ~5 мин → число строк ≈ числу секунд ±2 (не копятся, не теряются) | догадка об ошибке | ✅ `[TESTED: 2026-08-31 · см. отдельную строку ниже]` |
| **C7** | После убийства файла `…-ring.jsonl` НЕТ → это ожидаемо и НЕ считается дефектом (R5) | догадка об ошибке | ✅ `[TESTED: 2026-08-31 · зелёный до и после правки; ДОКАЗАН мутацией K6 — сброс кольца каждый такт роняет ровно его, 56/1]` |

> ⚠️ **C5 — МЕТОД ПРИШЛОСЬ ЧИНИТЬ ПОСРЕДИ ИСПОЛНЕНИЯ, и это важнее самого числа.** Первая базовая
> линия была ОДНИМ прогоном (медиана 2,49) и оказалась выбросом на холодной машине: три честных
> прогона того же кода дали 2,10–2,21. По линии из одного прогона правка выглядела бы на 11 %
> УСКОРЯЮЩЕЙ — замер показал бы улучшение там, где его нет, и знак развернулся при переизмерении.
> Собственный разброс прибора (~13 %) БОЛЬШЕ искомого эффекта (5 %), поэтому сравнение двух
> одиночных прогонов здесь не решает ничего. Правильная формулировка вывода — не «стало на 4,7 %
> медленнее», а **«замедления не обнаружено: эффект, если он есть, меньше разброса прибора»**;
> p95 не сдвинулся вовсе. Канон `GOAL.md` («утверждение о РАЗНИЦЕ позволено только после того, как
> измерен собственный разброс прибора»), применённый к самому себе.

> ⚠️ **C3 — ПЕРВЫЙ ЗАМЕР БЫЛ НЕВЕРЕН, И ЭТО ЗАПИСАНО, А НЕ СТЁРТО.** Первая попытка убила pid
> ЗАДАЧИ ОБОЛОЧКИ вместо pid узла; судья остался жив, файл рос, и результат читался как «строка
> пишется раньше секунды». Дефекта не было — был сломан прибор. Переделано с pid самого узла:
> файл отсутствует. Побочная польза: журнал выжившего судьи показал отсутствие дрейфа окна —
> `t` = 1001,93 · 2001,81 · … · 16000,48 мс, то есть за шестнадцать секунд накопилось < 3 мс.

## 5. Control cases — MUST

| # | Контроль → ожидание: поведения НЕТ | Статус + улика |
|---|---|---|
| **K1** | **Прогнать C1 на коде ДО правки** → `…-alive.jsonl` отсутствует, **C1 ПАДАЕТ.** Шаг 3.2 плана | ✅ `[TESTED: 2026-08-31 · ИСПОЛНЕН ПЕРВЫМ, до починки: «строк 0, возраст последней файла нет», 55/2]` |
| **K2** | Мутация: вернуть запись «только на трипе и закрытии» → **краснеет ровно C1**, остальные зелёные | ✅ `[TESTED: 2026-08-31 · 55/2 — красные C1 и C2, чужих блоков не задето]` |
| **K3** | Мутация: убрать `fsync`, оставив `write` → C1 краснеет | 🔴 **`[TESTED: 2026-08-31 · ПРОШЛА ЗЕЛЁНОЙ, 57/0 — СТЕНД НЕ ВИДИТ РАЗНИЦЫ]`** — `taskkill` убивает ПРОЦЕСС, а не ОС; кэш страниц переживает смерть процесса. Зазор объявлен полем `GAP` в маркере `@forensic fuse-alive`, а не замаскирован |
| **K4** | Мутация: писать строку раз в 60 с вместо секунды → C1 краснеет по возрасту записи | ✅ `[TESTED: 2026-08-31 · 55/2, красные ровно C1 и C2]` |
| **K5** 🆕 | Мутация: заполнить непроведённый канал НУЛЁМ вместо честного `null` → краснеет **ровно C2** | ✅ `[TESTED: 2026-08-31 · 56/1, красный только C2]` — заведена по ходу, когда решение о `null` было принято |
| **K6** 🆕 | Мутация: сбрасывать кольцо КАЖДЫЙ ТАКТ → **C7 краснеет** | ✅ `[TESTED: 2026-08-31 · 56/1, красный ТОЛЬКО C7]` — заведена СУДЬЁЙ: C7 был зелёным до и после правки, то есть по воротам 5 не доказывал ничего, пока его не уронили |

> ⚠️ **K1 — не формальность, а смысл всей фазы.** Если C1 окажется ЗЕЛЁНЫМ на коде до правки, это
> значит, что случай проверяет не то, ради чего написан, — тогда останавливаться и переписывать
> случай, а не праздновать (EXP-0181: ловушка обязана ловить).

## 6. Verdict

**✅ `[TESTED: 2026-08-31]` — семь случаев из семи исполнены, шесть контролей из шести.**

| что | итог |
|---|---|
| случаи C1…C7 | 7 из 7, каждый со своей уликой в таблице выше |
| контроли K1…K6 | 6 из 6 · **K1 упал ПЕРВЫМ, до починки** — риск «б» плана снят |
| набор `fuse` | 54 → **57 блоков**, красных 0 |
| батарея | 42 набора, красных 0, **2072** зелёных блока |
| записей в GPU | **0** (`watchdog --status`: не взведён) |

### Что этот `[TESTED]` НЕ означает — граница названа, а не спрятана

**Доказано:** строка жизни пишется раз в секунду без дрейфа (300 строк за 300 с, накопление
2,32 мс), переживает смерть процесса без штатного закрытия (возраст последней записи 443–454 мс
при пороге 1500), несёт шесть полей разбора, не замедляет судью (эффект меньше разброса прибора),
и не ломает кольцо (1823 такта, 9/9 репетиции).

**НЕ доказано, и это записано трижды — здесь, в маркере `@forensic fuse-alive` полем `GAP` и в
`plans/74`:**

1. 🔴 **`fsync` не доказан НИЧЕМ.** Мутация K3 (снять `fsync`, оставить `write`) прошла ЗЕЛЁНОЙ:
   `taskkill` убивает ПРОЦЕСС, а не ОС, байты уже отданы ядру, и кэш страниц переживает смерть
   процесса. Потерю кэша даёт только настоящая смерть МАШИНЫ, недостижимая на стенде. Польза
   `fsync` здесь стоит на отраслевой практике (самописцы, аварийные дампы, журналы упреждающей
   записи) и на `bugs/37`, где кэш страниц уже съел архив пульса на ЭТОЙ машине, — но не на нашем
   замере.
2. 🔴 **Класс «машина замёрзла» не доказан.** Доказано СВОЙСТВО «улика переживает отсутствие
   штатного закрытия». Это разные утверждения, и смешивать их — ровно ошибка 30 августа.

**Почему граница названа так подробно.** Фаза закрывает механизм М3 эпика 76, а сам эпик заведён
потому, что сторож был объявлен доказанным против угрозы, будучи доказанным против её модели.
Закрыть эту фазу словами «чёрный ящик переживает зависание» значило бы совершить чинимую ошибку
внутри её же починки — в третий раз за ночь после `bugs/81` и `bugs/82`.
