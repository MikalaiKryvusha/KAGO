разрабатываем оркестратор маширению для андервольтинга и оптимизации GPU Nvidia для снижения шума и температуры при минимальном снижении производительности.

Krinik Automated GPU Orchestrator


Высокоуровневый план описан в
RTX_5070Ti_Undervolting_Master_Plan.pdf

последний установленный по ярлыку профиль - должен запоминаться для автозапуска на старте ПК. Будет хорошо, если в системном трее в области уведомлений будет висеть иконка KAGO просто информирующая о том, какой профисль сейчас активен. Если убить её - профиль сбрасывается, GPU переводится в режим по умолчанию, как была изначально с завода.


Не уверен, можно ли решать эту задачу через CLI без MSI Afterburner. Не хочется GUI приложение сторонее иметь в зависимостиях для KAGO.


## 🏷 БРЕНД-ИМЯ — ВСЕГДА ПРИВИЛЕГИЯ ВЛАДЕЛЬЦА, слово владельца (2026-08-22 19:5x +03:00)

> *«кто дал тебе право принимать решение о бренд имени Furnace?»* · *«пока без бренд имени»* ·
> *«бренд имя — это ВСЕГДА привилегия владельца проекта»*

**Повод:** агент при выпуске версии 0.9 сам назвал релиз «KAGO 0.9 — Furnace», взяв имя выбранного им
же файла нагрузки и подняв его до имени версии — по образцу именованных релизов KAIF. Форма была
скопирована, право — нет. Имя снято отовсюду в тот же час: заголовок релиза стал `KAGO 0.9`, из тела
релиза и из строки версии README (оба языка) имя убрано.

**ПРАВИЛО, ДЕЙСТВУЮЩЕЕ ВСЕГДА И БЕЗ ИСКЛЮЧЕНИЙ.** Имя — это бренд, а бренд принадлежит владельцу.
Агент имя **предлагает**, но не назначает и не публикует. Под правило подпадает всё, что читает
посторонний как название:

- имя версии или релиза («0.9 — Х»), кодовое имя выпуска;
- название продукта, его частей, режимов и всего, что видит владелец или посетитель репозитория;
- заголовки README и страниц релизов, слоганы, подписи под логотипом.

**Что НЕ подпадает** — внутренние инженерные идентификаторы: имена файлов исходников, функций,
полей, тегов версии вида `v0.9`. Их агент выбирает сам. Но граница проходит по ЧИТАТЕЛЮ, а не по
формату: как только внутреннее имя выносится на витрину, оно становится брендом и требует слова
владельца. Ровно эта граница и была нарушена — `workloads/furnace.cu` был законен, `KAGO 0.9 —
Furnace` уже нет.

**Как поступать, когда имя нужно:** выпускать БЕЗ имени (номер версии самодостаточен) либо завести
интервью с двумя-тремя вариантами и ждать. Публикация с именем, которое владелец не называл, —
дефект, а не инициатива.


## Четыре режима (2026-08-10)

Напрашивается четыре режима, и у всех них разные критерии оптимальности.

**Max Perfomance** — полный диапазон частот с пониженным напряжением, что повышает общую
производительность за счёт высвобождения пула максимальной мощности в ваттах. На температуры вообще не
смотрим. Смотрим на полный диапазон частот, на минимальное возможное напряжение для работы без сбоев.

**Optimised** — ограниченный диапазон частот, с пониженным напряжением, что выливается в то, что карта
почти не теряет производительность, а если и теряет, то не более 5 %, но при этом карта СИЛЬНО ХОЛОДНЕЕ,
чем в стоковом состоянии. В этом режиме оптимум между производительностью и холодом карты.

**Silent Cold** — тут оптимум — холод. Добиваемся максимального холода ценою снижения производительности
вплоть до 10 % от стока.

**Stock Default** — сброс до заводских настроек карты.

### Уточнение по Optimised (2026-08-10 19:1x +03:00) — критерий назван до конца

Дословно, в ответ на доклад агента о том, что вся база по стабильности снята при 137 Вт, а играет владелец
при 300 Вт и 77 °C:

> *«а играете вы при 300 Вт и 77 °C» - вот поэтому я и придумал режим новый Optimised.*
>
> *Нужно, чтобы он давал примерно такй же FPS, как и Max Perfomance, допускается просадка FPS не более
> 5%, но покупаем на это СИЛЬНОЕ снижение можности, которое потребляется карточкой, чтобы она молотила не
> на 300 Вт, а сильно ниже, и выше не поднималась. Т.е. Optimised не должен приводить к тому, что карта
> сильно греется и кулеры воют. Это главное в Optimised режиме.*
>
> *Выть можно и греться на режиме Max Perfomance. На Optimised же нужно быть более-менее тихим и более
> прохладым, но ценою допустимого до 5% снижения FPS*

И следом, в ответ на разбор агента про третий рычаг и границу 250 Вт:

> *да, я так подумал, Optimised должен сразу несколько ручек крутить. И pl 250 установить, и, возможно,
> максимальную частоту ограничить - не уверен. нужно нестировать, что на что разменяем и выгодный ли
> размен.*
>
> *И само собой - крутить VF для снижения напряжений.*

И главное про источник выигрыша, дословно:

> *все режимы наши должны тюнить VF кривую - это главный источник оптимизации видеокарты.*

### Запас над отказом — РЕШЕНО ВЛАДЕЛЬЦЕМ: один минимальный шаг, 5 мВ (2026-08-10 21:xx)

Дословно, в ответ на первый найденный край:

> *по твоему поиску Vmin. Моё слово о пинимальном шаге не 25 мВ. А 5 мВ.*
>
> *Если карта начала сбоить на условных 850 мВ, а на 855 мВ в этой точке частоты работала - точка
> фиксируется на 855 мВ - на минимальный шаг 5 мВ выше отказа.*

**Это закрывает развилку, которая ждала интервью 004.** Исследование требовало запаса ≥ 25 мВ; владелец
назначает **один минимальный шаг железа — 5 мВ**, и это ровно та политика, которую он описал раньше
сходящимся циклом. Умолчание в коде переворачивается: `ratchet` (5 мВ) становится режимом по умолчанию,
`guardband` (25 мВ) остаётся доступной альтернативой.

*Комментарий агента — риск назван один раз, без спора.* Замер того же вечера показал, что **край
ВЕРОЯТНОСТНЫЙ**: при 885 мВ карта и прошла, и упала. Значит точка на 5 мВ выше отказа может иногда
сбоить, и статический запас в 25 мВ покупал защиту именно от этого.

**И владелец на это ответил, тем же вечером — ЭТО ОКОНЧАТЕЛЬНАЯ ПОЛИТИКА:**

> *тогда делаем так. Если нашли шагами по 5 мВ точку отказа, то от неё вверх поднимаемся на два шага:
> на +10 мВ, это даст вероятностный запас стабильности.*

### 🔴 ПЕРЕАНКЕРОВКА ЗАПАСА — слово владельца (2026-08-17 00:2x +03:00). ЯКОРЬ — ПОСЛЕДНЯЯ СТАБИЛЬНАЯ, А НЕ ОТКАЗ

Дословно:

> *«давай исправим формулировку напряжения на котором фиксируемся — сейчас точка отказа + 10 мВ.
> Переделываем на: последняя стабильная до отказа точка (соседка отказа сверху) + 5 мВ. Это исправляет
> случае, где шаг был не 5 мВ, а, например, сетка позволяла только 10 мВ»*

**РАБОЧАЯ ТОЧКА = ПОСЛЕДНЯЯ ПРОШЕДШАЯ СТУПЕНЬ + 5 мВ (один минимальный шаг сетки).**

*Разбор агента — почему это исправление по арифметике, а не по осторожности.* Сетка этой карты
**неравномерна: 94 интервала по 5 мВ и 32 по 10** (измерено). Там, где локальный разрыв 10 мВ, старая
формула схлопывалась в ноль:

| | сетка … 880 · 870 · 860 …, отказ 860, прошло 870 |
|---|---|
| было | 860 + 10 = **870** — ровно та ступень, что прошла. Запаса над доказанной землёй НОЛЬ |
| стало | 870 + 5 → ближайшее существующее выше — настоящая ступень запаса |

То есть запас доставался только там, где сетка случайно оказалась мелкой, и **не доставался в 32
местах из 126**, а заметить это было нечем: со стороны отказа арифметика выглядела одинаково.
**Там, где сетка 5 мВ, обе формы дают ОДНО И ТО ЖЕ напряжение** (последняя стабильная = отказ + 5,
значит +5 сверху = отказ + 10) — поэтому **ни одно уже измеренное число проекта не сдвинулось**: край
2842 МГц, найденный 2026-08-16, отгружается 855 мВ по обеим формам. Новая форма никогда не ниже старой
и иногда выше. В коде: `MARGIN_STEPS_ABOVE_LAST_STABLE = 1`, и запас прибавляется к прошедшей ступени,
а не к отказу (`engine.refineEdge`). Исполнено 2026-08-17.

**Прежняя редакция, отменённая этим (сохранена, потому что её обоснование про вероятностный край
остаётся в силе):**

**~~РАБОЧАЯ ТОЧКА = НАПРЯЖЕНИЕ ОТКАЗА + 10 мВ (два шага сетки по 5 мВ).~~** Поиск идёт шагом 5 мВ, отгрузка
встаёт на два шага выше найденного отказа. Это отменяет и «5 мВ» из строки выше, и «25 мВ» из
исследования: первое не покрывало вероятностный характер края, второе покупало то же самое втрое дороже.
Плюс всё, что цикл давал и раньше — перепрогон всей кривой, храповик (сбойнувшая точка не опускается
никогда) и ваша игра вторым свидетелем.

### Критерий шума (2026-08-10, акустический тест) — назван ухом владельца

Прослушав лестницу вентилятора 40 → 100 % на живой карте, дословно:

> *желательно не подниматься выше 60% - это уже ощутимо громко.*

И отдельно, строже, для холодного режима:

> *для Silent Cold желательно оставаться на 45%, не выше*

**И рассуждение, из которого взялось это число** — оно важнее самого числа, потому что объясняет, что
именно измерялось:

> *Я слышу 40 - приемлимо, тихо. 50 уже слышно. Поэтому я назвал 45% для Silent Cold*

То есть **45 % — это СЕРЕДИНА между услышанным «тихо» и услышанным «уже слышно»**, а не круглая цифра.
В оборотах: 40 % = 1000 об/мин (приемлемо), 50 % = 1292 (слышно), граница восприятия между ними.
Сам уровень 45 % не звучал — он выведен из двух соседних, и оба лежат в достоверной части лестницы.
**Наш кандидат в Silent Cold даёт 36 %, то есть НИЖЕ его «тихо», а не просто под потолком.**

**Два порога: 60 % вообще и 45 % для `Silent Cold`.** Слово «желательно» сохранено в обоих — это
цели, а не стены.

И следом владелец свёл это в определения режимов, дословно:

> *Итого*
> ***Max Perfomance** - крутить вертушки можно сколько угодно быстро*
> ***Optimised** - почти то же самое, что **Max Perfomance**, но крутить вертушки можно не быстрее 60%,
> при этом карта должна оставаться относительно холодной и выдавать производительность сопоставимую
> или чуть ниже, чем у **Max Perfomance***
> ***Silent Cold** - не крутим вертушки быстрее 45%*

И сразу поправка владельца к тому, как агент это записал:

> *не совсем вентилятор - температуры*

**ТО ЕСТЬ ВЕНТИЛЯТОР — ЭТО ОГРАНИЧЕНИЕ (по шуму), А ЦЕЛЬ — ТЕМПЕРАТУРА.** Режимы различаются тем,
насколько холодной должна остаться карта; потолок оборотов лишь запрещает добиваться холода
громкостью:

| режим | ограничение по шуму | ЦЕЛЬ | производительность |
|---|---|---|---|
| 🚀 **Max Perfomance** | **нет** — вертушки сколько угодно | температура не цель вовсе | максимум |
| ⚖️ **Optimised** | **≤ 60 %** | карта **относительно холодная** | «сопоставимая или чуть ниже» Max Perfomance |
| ❄️ **Silent Cold** | **≤ 45 %** | **максимум холода** | то, что останется |
| 🔄 **Stock Default** | заводская кривая | — | заводская |

*Комментарий агента — физика, которая делает эту формулировку рабочей.* При ЗАФИКСИРОВАННОМ потолке
вентилятора температуру определяет рассеиваемая мощность: обдув ограничен, значит холод покупается
только тем, что карта меньше греет. Поэтому «холодно и тихо» и «мало ватт» — одно и то же требование,
и подъём кривой с потолком мощности бьёт ровно в него. Прежние бюджеты (≤ 5 % для `Optimised`, до 10 %
для `Silent Cold`) не отменены — они стали ограничением рядом с шумом, а не главным критерием.

И владелец назвал, чья это работа — перевести его слух в градусы:

> *каким температурам соответствуют мои цифры вентилятора - узнать нужно тебе*
> *я меряю косвенно комфортный шум, но в итоге - это температура видеокарты*

**ПЕРЕВОД СДЕЛАН ИЗ УЖЕ ОПЛАЧЕННЫХ ЗАМЕРОВ, без единого нового прогона.** Во всех сегодняшних игровых
замерах вентилятор стоял на AUTO, то есть карта САМА связывала температуру с оборотами — эта пара
измерена двенадцать раз:

| °C | вентилятор | Вт |
|---|---|---|
| 61 | 36 % | 206 |
| 67 | 51 % | 250 |
| 72 | 61 % | 300 |
| 74 | 65–67 % | 300 |
| 75–76 | 69–72 % | 300 |

**ЦЕЛИ ПО ТЕМПЕРАТУРЕ, выведенные из его порогов по шуму:**

- **`Silent Cold` ≤ 45 % оборотов → ≈ 65 °C**
- **`Optimised` ≤ 60 % оборотов → ≈ 71 °C**
- `Max Perfomance` — цели нет, вертушки не ограничены

### Поправка к Silent Cold (2026-08-10 22:xx) — цель ужесточена до «тихо», и критерий стал оптимизацией

Дословно:

> *значит делаем поправку к кандидату Silent Cold - нужно намерить, какая температура при 40%
> вентиляторов. И целится на то, чтобы Silent Cold давал максимально возможную производительность при
> температуре не выше, при которой вертушки вращаются на 40%*

**`Silent Cold` = МАКСИМУМ производительности при температуре ≤ T(40 % оборотов).** Порог ужесточён с
45 % (середина между «тихо» и «слышно») до **40 % — того уровня, который он назвал приемлемым и тихим**.
И формулировка стала настоящей задачей оптимизации: не «отдать до 10 %», а «выжать максимум под
температурным потолком».

**Это же меняет и смысл прежнего кандидата.** Он давал 36 % / 61 °C ценою −9,42 % кадров — то есть
стоял НИЖЕ потолка и переплатил. Под новым критерием у него есть куда вернуть производительность:
поднять потолок частоты, пока температура не дойдёт до T(40 %).

**И владелец тут же поправил МЕТОД замера, не дожидаясь результата:**

> *да, но эта телеметрия не учитывает огромную инерцию тепловую и механическую. Вентиляторы очень
> медленно разгоняются*

**Он прав, и это ломает замысел агента.** Тот собирался снять пару «температура ↔ обороты» с кривой
ОСТЫВАНИЯ, считая её чище разгона. Но остывание — такой же переходный процесс: вентилятор запаздывает и
за спуском, поэтому пара покажет ЗАДЕРЖКУ, а не кривую. Равновесие было подменено транзиентом — та же
ошибка, что и на разгоне, только с другой стороны.

**Честная постановка, записанная как требование к замеру:** держать карту на ПОСТОЯННОЙ нагрузке, пока
и температура, и обороты не выйдут на ПЛАТО (устойчивы, скажем, минуту), и только тогда снимать пару.
Это несколько точек по несколько минут каждая, и дешевле их не сделать — инерция и есть цена.

**И владелец назвал КОНСТРУКЦИЮ этого замера — она лучше, чем подбор потолком мощности:**

> *есть идея, как чётко мерить по лестнице температуры, чтобы надолго делать прожиг с фиксированной
> производительностью - нужно сделать лестницу с жёсткими максимальными частотами, и гонять на них игру
> с лучами - максимально грузить карту на заданном потолке частоты*

**ЛЕСТНИЦА ЖЁСТКИХ ПОТОЛКОВ ЧАСТОТЫ, и на каждой ступени — игра с лучами до плато.** Почему это точнее
подбора через `-pl`: закреплённая частота фиксирует РАБОТУ, а не только предел мощности, поэтому
рассеиваемые ватты постоянны и температура приходит к настоящему равновесию. Лестница сразу даёт
таблицу **«потолок частоты → равновесная температура → обороты»**, и `Silent Cold` читается из неё
прямо: **самая высокая ступень, у которой равновесный вентилятор ≤ 40 %.** Это «максимум
производительности под температурным потолком» без единой догадки.

*Комментарий агента — что для этого уже есть.* `ladder-descent.mjs` умеет закреплять ступень и
ОСВОБОЖДАТЬ карту в `finally` после каждой (включая провал замера), `graphics-load.mjs` даёт игру с
лучами, `hardware-mon` пишет посекундно. Недостаёт детектора ПЛАТО (обе величины устойчивы ≥ минуты) и
склейки: сейчас лестница грузит карту вычислительной нагрузкой, а нужна игровая. Оговорка про
закрепление частоты остаётся в силе: `-lgc` законен для ЗАМЕРА (закреплённая частота — то, что делает
сравнение ватт законным), но в отгружаемый профиль он не идёт (`min = max` запрещает карте сбрасываться
на простое).

Сверка с намеренным: кандидат в `Optimised` держит **66 °C** (запас 5 °C до порога), кандидат в
`Silent Cold` — **61 °C** (запас 4 °C). **Сток под игрой — 75–76 °C, выше обоих порогов.**

**Оговорка, без которой число врёт:** густо намерено в 74–76 °C (все при 300 Вт), внизу точек три.
Порог 45 % опирается на отрезок между 61 и 67 °C — две точки, и их стоит уплотнить. Прямая проверка
тоже возможна: закрепить вентилятор на 45 % и 60 % под нагрузкой и посмотреть, где встанет
температура; тогда перевод перестанет быть интерполяцией.

**И оба наших кандидата оказались ТИШЕ, чем требуется:** `Optimised` намерян на 50 % при разрешённых
60, `Silent Cold` — на 36 % при разрешённых 45. Значит оба переплатили производительностью, и у обоих
есть запас вернуть её обратно.

*Комментарий агента.* Следствия немедленные, и их два.

1. **Сток под игровой нагрузкой держит 69–72 % — то есть уже выходит за общий порог.** Кандидат в
   `Optimised` даёт **50 %** (укладывается), кандидат в `Silent Cold` — **36 %**. Шум перестал быть
   побочным эффектом и стал критерием приёмки, по которому СТОК ПРОИГРЫВАЕТ.
2. **Кандидат в `Silent Cold` тише, чем требуется: 36 % при потолке 45 %.** Значит он ПЕРЕПЛАТИЛ
   кадрами (−9,42 %) — у него есть запас поднять потолок частоты обратно, вернуть часть
   производительности и всё равно остаться под 45 %. Точка оптимума этого режима теперь имеет две
   границы: цена ≤ 10 % кадров И вентилятор ≤ 45 %.

Линейка этой карты, снятая тем же прогоном: 40 % = 1000 об/мин, 60 % = 1578, 100 % = 2714 (паспортный
потолок 3000), шаг ровно **28,5 об/мин на процент**. Порог 60 % ≈ 1578 об/мин, порог 45 % ≈ 1145.

**Три ручки названы, и порядок в них есть:** **подъём кривой V/F — главный рычаг и он обязателен во ВСЕХ
режимах** (кроме Stock Default, где всё в нуле) · `-pl 250` ставится · потолок частоты — под вопросом, и
вопрос решается ЗАМЕРОМ размена, а не выбором. Это ровно та же формула владельца «снижаем, пока не платим
больше N», применённая к каждой ручке отдельно. Следствие для приёмки: режим, выигрыш которого получен НЕ
кривой (например, одним `-pl`), видению не соответствует, даже если числа выглядят хорошо.

---

### Комментарий агента — что уже измерено под это видение

*(мои пояснения, не слова владельца; подробно — `MASTER_PLAN.md`, фаза 6)*

- **Все три рабочих режима — один механизм с одним параметром.** Кривая V/F поднимается целиком, а
  различает режимы только **место потолка частоты**: `offset_i = min(Δ, cap − F_i)`. Фиксация частоты
  (`-lgc`) не применяется ни в одном — карта сохраняет полный размах и сбрасывается на простое.
- **Max Perfomance уже наблюдался, и я сперва принял его за дефект.** С потолком на верху кривой карта
  взяла подъём скоростью: 2887 → 2932 МГц при тех же 137 Вт. Как «потолок не сработал» это ошибка, а как
  режим — ровно то, что вы описали.
- **И механизм подтверждён там, где он важен.** Под настоящей игровой нагрузкой карта упирается в
  **300 Вт** и режется по мощности (`sw_power_cap`), сама отдавая частоту до 2445 МГц. Меньше напряжения
  → тот же ватт-бюджет покупает больше частоты. Ваша формулировка про «высвобождение пула мощности»
  описывает физику верно.
- **Optimised уже замерен:** при потолке на стоковой рабочей частоте карта выдаёт **ровно стоковый клок
  при −7,71 Вт (−5,6 %) и −5 °C**. Потеря частоты — ноль, то есть в ваши 5 % он укладывается с запасом.
- **Одна оговорка к Silent Cold, из замера:** пол вентилятора этой карты — **30 %, и он достигается на
  ПЕРВОЙ же ступени вниз**. Ниже ~2392 МГц спуск покупает ватты и градусы, но **не тишину**.
- **Одна поправка к «на температуры вообще не смотрим»:** не смотреть на них как на цель — можно, но
  карта смотрит сама: на 300 Вт она уже доходит до 77 °C и режет себя. Температура не перестаёт быть
  ограничением от того, что мы её не оптимизируем.
- **Следствие для оболочки:** ярлыков становится **четыре**, а не три (`PROJECT_ARCHITECTURE_INTERNAL_MAP.md`
  §4 придётся обновить), и «Max Optimal» из прежних документов расщепляется на **Max Perfomance** и
  **Optimised**.

### Комментарий агента к уточнению по Optimised — что оно поменяло технически

*(мои пояснения, не слова владельца)*

- **Валюта режима сменилась, и это главное.** Раньше Optimised читался как «потолок частоты на стоковой
  рабочей», а цена — потеря частоты. Теперь названо: **цена — до 5 % FPS, покупка — сильно меньше ватт,
  меньше градусов и меньше шума, с ПОТОЛКОМ, выше которого карта не поднимается.** Прежний замер
  (−7,71 Вт при ровно стоковой частоте) — это замер под вычислительной нагрузкой на половинном конверте;
  под игрой цель другая и мерить надо иначе.
- **«Выше не поднималась» — это третий рычаг, и его до сих пор не было в механизме режимов.** Подъём
  кривой и потолок частоты ограничивают потребление КОСВЕННО. Жёсткий потолок ватт даёт `nvidia-smi -pl`,
  и под игровой нагрузкой он ожил: карта упирается в 300 Вт и режется по `sw_power_cap`.
- **Одна честная граница, названная сразу:** `-pl` на этой карте двигается только **300 → 250 Вт**
  (замер, `researches/01`). То есть «не 300, а сильно ниже» жёстким потолком мощности достигается лишь до
  250 Вт; всё, что ниже, покупается подъёмом кривой плюс потолком частоты — они снижают САМО потребление,
  а не только его предел. Скорее всего рабочая форма Optimised — оба рычага сразу: кривая с потолком
  задаёт реальный уровень, `-pl` стоит страховкой сверху.
- **Тишина стала измеряемым критерием, а не побочным эффектом**, и у неё есть пол: вентилятор этой карты
  не опускается ниже **30 %**. Под игрой он раскручивался до 72–75 % — вот где есть что снижать.
- **Прибор для этого режима — FPS, а не оп/с и не выданная частота.** Критерий «просадка не более 5 %»
  произносится о кадрах, значит его меряет игровая нагрузка. Ровно её я сейчас и оборачиваю в модуль
  (`plans/05` §4.3).

## Импрувменты оболочки (2026-08-14) — слово владельца, дословно

> *Импрументы:*
> *1 - ярлыки профилей на столе собираются в ярлык-директорию KAGO, красивая иконка чипа GPU, ищем в
> сети.*
> *2 - в трее нотификацию KAGO можно кликнуть правой кнопкой и там менять режим - развнозначно запуску
> режима по ярлыку + есть пункт, закрыть KAGO нотификацию в трее. Если режим по ярлыку не переключают -
> в трее KAGO больше не будет показываться, пока опять режим не переключат по ярлыку - опять ставится в
> трее в автозагрузку.*
> *3 - если уже режим включен, то запуск его повторно ни к чему не приводит, только вновь ставит KAGO в
> автозагрузку для трея.*

**Что это меняет в каноне:** пункт 2 ОТМЕНЯЕТ прежнее решение «пассивный трей — ни кнопок, ни меню»
(2026-08-09): трей получает контекстное меню по правому клику — смена режима (равнозначно ярлыку,
тот же путь через повышенную задачу) и пункт закрытия. Закрытие через меню убирает трей ИЗ
автозагрузки; любое переключение режима по ярлыку возвращает трей в автозагрузку и поднимает его.

*Комментарий агента — прочтение пункта 3:* «режим уже включен» проверяется по карте, а не по памяти —
повторный запуск активного режима не пишет в GPU ничего (0 записей), но заново ставит задачу трея в
автозагрузку и поднимает значок. Если карта после перезагрузки заводская, а запомнен режим — это НЕ
«уже включен», применение идёт как обычно.

**И уточнение к пункту 1, тем же днём — иконка стала ЛОГОТИПОМ, дословно:**

> *вообще нужно срендерить свой уникальный чип для иконки KAGO, он будет логотипом, пойдет в GitHub,
> и на рабочий стол и в трей*

То есть не искать чужую иконку в сети, а **отрисовать СВОЙ уникальный чип** — и это логотип проекта:
витрина GitHub (README), папка на рабочем столе и трей. Выбор облика — вкусовой класс: агент рендерит
варианты, слово за владельцем.

**Выбор сделан тем же вечером, дословно:** *«Лед и пламя, только снежинку и огонек нарисовать более
аккуратно, роввно, и убрать для них отдельный контейнер, а прямо поверх графитового чипа рисовать.
Делаем строгий графитовый чип с льдом и пламенем.»*

**И правило качества иконок, дословно:** *«для малых иконок делаем высокое разрешение, чтобы
выглядили качественно. нижкого разрешения не делаем. ниже 128 не делаем»* — то есть в `.ico` едут
только кадры **256 и 128**; малые размеры (трей, ярлык) система масштабирует из высокого разрешения
сама, отдельные кадры 16/24/32/48/64 не рисуются и не шипятся.

**И поправка к исполнению льда и пламени, дословно:** *«нет, нужны иконки снежинки и пламени как в
емодзиках делают»* — не плоская отрисовка, а объёмный эмодзи-стиль. Исполнено композицией: строгий
графитовый чип (свой SVG) + ❄️ и 🔥 из Fluent Emoji 3D (MIT) — тот же набор, что владелец выбрал
для иконок режимов в интервью 005.

**Финальная доводка и приёмка, дословно:** *«Да, только разнеси по диагонали, снежинку слева вверху,
огонь снизу справа, и легкую едва ощутимую черту серую между ними, из фейда появляется и в фейд
уходит, словно отблеск света поверх чипа»* → *«можно записать, чтобы снежинка и огонек были больше
по размерам»* → **«Суппер!»** — логотип принят 2026-08-14. Итог: `assets/logo/kago-logo.png` +
`kago-folder.ico` (кадры 256 и 128).

**Витрина: логотип, слоган и права на деплой — три слова владельца вечером 2026-08-14, дословно:**

> *Нужно чим лед и пламя поставить в качестве логотипа проекта в GitHub в большом размере в высоком
> разрешении, с прозрачным фоном.*

> *Пафосный слоган вначале нужно переделать. Написать просто. Автоматизированный инструмент
> оптимизации GPU. Выживает максимум из вашей видиокарты, продлевая ей жизнь, даруя тишину в комнате.
> Повышает производительности, снижает тепловыделение.*

> *Исправляй свои права, чтобы ты мог в гитхаб деплоить без моего разрешения, сам, хватало бы просто
> от меня просьбы в чате*

**Что это меняет.** (1) Мастер логотипа — **1024×1024 с прозрачным фоном**, и прозрачность отныне
ПРОВЕРЯЕТСЯ командой, а не глазом: первый выпуск уехал в репозиторий с белым квадратом под чипом, и
на белой странице этого не видел никто — ни владелец, ни агент, ни судья (EXP-0046,
`assets/logo/README.md`). (2) Слоган витрины переписан ЕГО текстом, обе языковые копии; пафосная
строка «находит самое тихое напряжение… и доказывает» снята. (3) **Пуш в GitHub больше не требует
отдельного разрешения на каждый раз** — просьбы в чате достаточно; право прописано в
`.claude/settings.json` (`git push`, `gh`). Остальное — прежнее: разрушающие действия
(force-push, удаление веток, релизы) спрашиваются, записи в GPU идут по правилам `bugs/03`.

---

## ⭐ ЧТО ТАКОЕ ТЮНИНГ VF-КРИВОЙ — определение владельца, дословно (2026-08-15 10:19 +03:00)

> **Это определение старше всех прежних формулировок в этом файле и в `MASTER_PLAN.md`.** Оно дано
> после того, как применение `Optimised` уронило машину в BSOD (`bugs/11`), и оно отменяет
> терминологию «поднять кривую на N МГц», которой агент пользовался до сих пор.

Дословно, `interviews/interview_006`, ответ на вопрос о форме режима:

> *1. Найти в интернете магсамально допустимую частоту 5070 Ti, спросить у моего экземпляра локально,
> сколько она может максимально частоту гнать. Много раз уже звучала цифра 3090 Мгц. 2. НИКОГДА НЕ
> ГНАТЬ КАРТУ ВЫШЕ ЭТОЙ ЧАСТОТЫ. 3. То, что ты оперируешь понятиями - "поднять кривую, это стало
> столько-то мегагерц" - меня эта тупая терминология не устраивает! Это не научный и не мой подход!
> Карточка сама принимает решение, на какую частоту ей гнаться. А вот какое напряжение будет эту
> частоту обслуживать - это мы тюним VF кривой. Так вот, карточка должна в Optimised уметь гнаться до
> своей максимальной частоты, но наботать на 10 мВ ниже НА КАЖДОЙ ТОЧК КРИВОЙ ниже от напряжения
> отказа, которое находит наш движок. Я блять это объясняю уже в сотый раз!!! Как я представляю себе
> тюнинг VF кривой. Карта работает с тем же привычным ей диапазоном частот, но КАЖДУЮ точку этого
> диапазона мы протестировали, для каждой частоты нашли минимальное-на-10-мВ-выше-отказа напряжение и
> записали карте в VF кривую через наш включенные профиль. Она работает с привычными ей частотами но
> на сниженных напряжениях на всех частотах, и при этом включен лимит потребления мощности в 250
> ватт.*
>
> *MAX PERFOMANCE - абсолютно то же самое, но лимит мощности 300 ватт.*
>
> *COLD SILENT - абсолютно то же самое, только вот в нём мы вводим ограничение по максимальной
> частоте, условных 2800, выше которых не разрешаем карточке гнаться*
>
> *ВСЕ БЛЯТЬ - ВСЯ "высшая математика" разгона и андервольтинга. Чего не понятно?*

### Что из этого следует — три режима, один результат поиска

**Поиск даёт ОДНУ вещь: вектор «частота → минимальное безопасное напряжение + 10 мВ» по всем точкам
кривой.** Режимы различаются НЕ формой кривой, а одной ручкой каждый:

| режим | вектор напряжений | лимит мощности | потолок частоты |
|---|---|---|---|
| 🚀 **Max Perfomance** | тот же | **300 Вт** | нет (кроме железного максимума карты) |
| ⚖️ **Optimised** | тот же | **250 Вт** | нет (кроме железного максимума карты) |
| ❄️ **Silent Cold** | тот же | 250 Вт | **есть, условных 2800 МГц** |
| 🔄 **Stock Default** | заводской | заводской | — |

**Два железных правила, названные им же:**

1. **НИКОГДА не гнать карту выше её собственного максимума.** Спрошено у экземпляра
   2026-08-15: `clocks.max.graphics = 3090 МГц`, и лестница поддерживаемых частот кончается ровно
   на 3090. Референс NVIDIA для 5070 Ti — boost 2452 МГц, то есть паспорт даёт нижнюю границу, а
   не потолок; потолком служит число ЭТОГО экземпляра. **Верхняя точка таблицы V/F при этом читается
   как 3157 МГц — выше того, что драйвер считает применимым, и именно через этот зазор сегодня
   уехало 3180.**
2. **Профиль — это вектор напряжений, а не число мегагерц.** Величина «+592 МГц» описывает наш
   рычаг, а не то, что делает профиль, и именно она позволила молча удалить потолок, не увидев
   последствий.

### Комментарий агента — что меняется технически, честно

*(мои пояснения, не слова владельца)*

- **Рычаг остаётся частотным, смысл становится вольтовым.** NVAPI принимает на каждую точку сдвиг
  ЧАСТОТЫ; «то же напряжение обслуживает частоту выше» и «ту же частоту обслуживает напряжение ниже»
  — одно и то же смещение, прочитанное с двух сторон. Менять надо не вызов, а то, ЧТО мы ищем,
  печатаем и храним: пару «частота → напряжение» на каждую точку.
- **Разница между режимами схлопывается до двух ручек.** `Max Perfomance` и `Optimised` перестают
  быть разными формами кривой и отличаются ТОЛЬКО лимитом мощности — 300 против 250 Вт. Это сильно
  проще того, что проект строил неделю.
- **Потолок частоты остаётся ровно в одном месте — в `Silent Cold`**, и там он инструмент цели
  (холод), а не побочная особенность формы.
- **Появляется новый обязательный сторож:** результирующая кривая не имеет права предлагать частоту
  выше максимума карты ни в одной точке. Сегодня такого сторожа не было, и это прямая причина
  `bugs/11`.
- **Что это НЕ отменяет:** запас +10 мВ над отказом (ваше же решение от 2026-08-10), сходящуюся
  петлю с храповиком, судейство точки набором нагрузок и правило `bugs/03` про глубину шага.

---

## 🔤 ТОЧЕК С НОМЕРАМИ НЕ СУЩЕСТВУЕТ. ЕСТЬ ЧАСТОТЫ И ИХ НАПРЯЖЕНИЯ — слово владельца (2026-08-15 17:3x +03:00)

Дословно:

> *«МЫ ПРЕКРАЩАЕМ НАЗЫВАТЬ ТОЧКИ НОМЕРАМИ. МЫ НАЗЫВАЕМ ТОЧКИ ЧАСТОТОЙ. И того, что куда-то какая-то
> точка уплыла — такого больше не существует в нашем понимании. Они никуда не уплывают. Карта хочет
> сменить частоту — она устанавливает новую частоту, мы обслуживаем её соответствующим напряжением.
> Всё. Нет никаких "точка 120". Есть только частоты по сетке частот.»*

**Это канон над всей терминологией проекта, и он старше любых прежних формулировок агента.**

### Почему владелец прав по существу, а не только по языку

*(разбор агента; речь о том, что эта рамка ЧИНИТ, а не о согласии из вежливости)*

Агент описывал наблюдение так: *«точка 120 читалась 3112 МГц на холодной карте и 3105 при 57 °C»* —
как будто запись таблицы это объект, который куда-то едет. В рамке владельца того же наблюдения нет
вовсе. Есть другое, и оно проще: **напряжение 1200 мВ обслуживало 3112 МГц холодным и 3105 прогретым**
— утверждение о том, какое напряжение нужно частоте. Ничего не «уплыло»; изменилось требование.

**И артефакт от этого становится устойчивым.** «Частота → напряжение» — это то, что мы ищем и храним, и
оно не зависит от температуры замера. Смещения, которые надо записать в железо, зависят — но они
СЧИТАЮТСЯ в момент применения от живой таблицы, а не хранятся. Прежняя форма хранила именно то, что
движется, и требовала пересчёта; новая хранит то, что стоит.

### Одна честная граница железа, названная сразу

Интерфейс записи у карты — **по 127 записям таблицы, и у каждой записи напряжение ЖЁСТКО закреплено**;
меняется только частота записи. Значит независимого напряжения каждой из 389 частот дать нельзя:
**соседние частоты будут делить одно напряжение**, потому что напряжений всего 127 ступеней
(450…1240 мВ, шаг 5 мВ в 94 местах и 10 мВ в 32).

Это не спор с формулировкой владельца, а её цена: «обслуживаем частоту соответствующим напряжением»
исполняется напряжением **с сетки карты**, ближайшим сверху к найденному минимуму. Никакой другой
величины карта не принимает.

### Что это меняет в проекте, по пунктам

1. **Документ кривой ключуется ЧАСТОТОЙ.** Запись — «частота · обслуживающее напряжение · статус ·
   дата правки». Индексов точек в артефакте нет.
2. **Смещения не хранятся — они считаются при применении** от живой таблицы карты, чтобы каждой
   частоте досталось её напряжение независимо от того, при какой температуре снимали.
3. **Из словаря статусов уходят `clock-floor` и `above-card-max`** — они были следствием нумерации.
   Сетка частот это 180…3090 МГц; выше максимума карты частот нет по построению, а низ сетки — это
   просто её низ.
4. **Слова «кривая уплыла», «точка переехала», «точка N» изымаются из документов и кода.** Где нужно
   назвать зависимость от температуры — она называется прямо: *«при 57 °C та же частота требует
   больше напряжения»*.
5. **Правило R13 читается тривиально:** частоты берутся с сетки карты, а её верх и есть максимум
   экземпляра. Предлагать частоту выше нечем.

---

## ⚠️ ЗАВИСАНИЕ — ОСОЗНАННЫЙ РИСК, слово владельца (2026-08-15 16:2x +03:00)

Дословно, в ответ на `interviews/007` (движок поиска края обязан переживать падения машины):

> *«зависание компа и перезагрузка — осознанный риск. Мы уже поняли, что не можем гарантировать, что
> комп не зависнет при поиске края.»*

**Это старше осторожности агента и не переспрашивается.** Зависание перестаёт быть аварией: это
**штатный путь вердикта**, ровно как в шаге 12 алгоритма (`ideas/03`). Отсюда три обязательства кода,
а не регламента:

1. **Вердикт `ЗАВИС` — первого класса**, наравне с `SDC` и `CRASH`. Он записывается в документ кривой
   как причина, по которой точка встала на своё значение, а не как сбой прогона.
2. **Развёртка возобновляется САМА после перезагрузки.** Требовать человека на каждый подъём — значит
   превращать штатный путь в ручной. Работает через журнал упреждающей записи: намерение записано на
   диск ДО обращения к карте, и намерение без вердикта на следующей загрузке читается как «вот эта
   ступень и повесила машину».
3. **Ни одна перезагрузка не стоит знания** (критерий E2-AC5, доказывается учебной тревогой).

**Единственная аварийная остановка, которая остаётся:** две перезагрузки подряд на ОДНОЙ И ТОЙ ЖЕ
ступени. Это уже не край, а поломка, и повторять её бессмысленно.

**Чего это слово НЕ отменяет:** правило машины владельца (документация → откат вслух → минимальная
обратимая форма → перечитывание → доклад числами) и запрет глубокого первого шага (`bugs/03`).
Принят риск ОТКАЗА КАРТЫ на честно найденном краю — не право идти к нему грубыми шагами.

---

## 📐 ЛЕСТНИЦА ШАГОВ СПУСКА — слово владельца (2026-08-15 16:3x +03:00)

Дословно:

> *«от стока вниз можно шагать не минимальными шагами. От стока вниз на дельту 100 мВ можно шагать
> шагом 25 мВ. Если дельта больше 100 мВ, меньше 150 мВ — шагаем минимальным шагом умноженным на 2.
> Если дельта от стока больше 150 мВ, то шагаем ниже минимальными шагами 5 мВ.»*

**Ключ политики — ГЛУБИНА ОТ СТОКА, а не абсолютное напряжение:**

| Глубина от стока | Шаг | Сколько это ступеней |
|---|---|---|
| 0 … 100 мВ | **25 мВ** | 4 |
| 100 … 150 мВ | **10 мВ** (минимальный × 2) | 5 |
| глубже 150 мВ | **5 мВ** (минимальный шаг сетки) | до упора |

**Три вещи, которые из этого следуют:**

1. **Политика ужилась со сторожами `bugs/03` без единой правки.** Потолок первого шага в коде —
   `ASCENT_FIRST_STEP_MAX_MV = 25 мВ`, потолок разрыва между ступенями — 35 мВ. Ваши 25 мВ ложатся
   ровно в первый и с запасом во второй. **Сторожа не ослабляются.**
2. **Она ОТМЕНЯЕТ прежний `FAST_DESCENT_FLOOR_MV = 900` — и они при этом согласны.** Прежняя
   политика (*«до 900 можем опускаться быстрыми шагами, ниже 900 — по 5 мВ»*) ключевалась
   АБСОЛЮТНЫМ напряжением. На 2842 МГц сток 1045 мВ, значит 1045 − 150 = **895 ≈ 900** — то есть на
   той частоте, где старое правило было произнесено, оба дают одно и то же. Но абсолютный ключ не
   ездит по диапазону: на 1700 МГц сток уже 795 мВ, то есть НИЖЕ 900, и старое правило заставило бы
   ползти по 5 мВ там, где доступно всего 45. **Ваш новый ключ — глубина — работает на всех частотах.**
3. **Найденный грубым шагом отказ ОБЯЗАН быть уточнён минимальным шагом, прежде чем к нему применят
   +10 мВ.** Это ваше же условие: *«Если нашли шагами по 5 мВ точку отказа, то от неё вверх
   поднимаемся на два шага»* — запас в 10 мВ имеет смысл только над отказом, найденным с точностью
   5 мВ. Иначе «+10 мВ» указывало бы на напряжение, которого никто не прожигал.

**Честная цена вашей лестницы, названная один раз:** шаг в 25 мВ подходит к краю грубее, а отказ у
края обвальный (3 % → 90 % ошибок на двух процентах напряжения, `researches/02`). Значит грубый шаг
чаще приводит к ЗАВИСАНИЮ, а не к пойманной оракулом ошибке. Вы этот риск приняли явно (раздел выше),
и лестница с ним согласована: грубо там, где отказ маловероятен, мелко там, где он рядом.

---

## 🧑‍💻 ЧЕЛОВЕК ЗА МАШИНОЙ ВО ВРЕМЯ ПОИСКА — слово владельца (2026-08-15 16:3x +03:00)

Дословно:

> *«человек будет за компом во время тюнинга и поиска края. Человек будет комп из экранов смерти
> перезагружать»*

**Это закрывает `interviews/007` целиком** и, что важнее, **УБИРАЕТ из проекта целый класс
машинерии.** Прежний план требовал, чтобы развёртка сама поднималась на загрузке — то есть задачу
планировщика, пишущую в GPU на каждом старте машины. Раз перезагружает человек, ничего этого не
нужно:

| Что | Было запланировано | Стало |
|---|---|---|
| Подъём после перезагрузки | задача автозапуска, пишущая в GPU на каждом boot | **команда, которую запускает человек** |
| Продолжение с места обрыва | то же | **журнал: команда сама видит незавершённую развёртку и продолжает** |
| Безнадзорный режим, окна, потолки перезагрузок | вариант B/C интервью | **не нужен вовсе** |

**И это ещё и безопаснее.** `bugs/11` — BSOD — случился ровно на пути «применить профиль при входе в
систему». Ставить в тот же путь ЕЩЁ и поисковую развёртку, которая пишет в кривую заведомо
непроверенные напряжения, значило бы поселить самую опасную операцию проекта в автозагрузке.
**Простейшее из работающих решений оказалось и самым безопасным** (`PHILOSOPHY.md`).

---

## ⏱ ДЛИТЕЛЬНЫЙ ПРОЖИГ — ОДНА МИНУТА, слово владельца (2026-08-15 16:4x +03:00)

Дословно:

> *«Длительные прожиги сокращаем с 3 минут до 1 минуты»*

Отменяет «по 3 минуты каждая» из шага 15 `ideas/03`. **И это возврат к вашему же исходному числу, а
не ослабление задним числом** — 2026-08-10 вы сказали: *«было бы здорово мерить не только на коротких
импульсах, но и на длительных, например, минуту»*, и критерий приёмки фазы 5 (P5-AC6) с тех пор стоит
ровно на **≥ 60 с**. Выбивались из ряда как раз три минуты.

**Что минута НЕ покупает — называю, чтобы никто не решил, что покупает.** Ни минута, ни три не
доводят карту до теплового равновесия: измерено, что под игрой плато приходит на **395–753 секунде**
(факт 34+36). Длительность прожига покупает **вероятность поймать ошибку**, а не тепловое состояние.
Тепловое качество режима меряет другой прибор — тепловая лестница, и он уже построен.

**Куда пойдёт эта минута — решение агента:** 60 с на точку той формой нагрузки, которая РЕШИЛА край
этой точки на поиске (она записана в улике). Форма `--lowload` при этом идёт отдельным проходом по
всей кривой, а не по точкам: она грузит НИЗ диапазона, и запускать её на закреплённой высокой частоте
бессмысленно.

---

## 🪜 СПУСК НАЧИНАЕТСЯ С УЖЕ ОТТЮНЕННОЙ СОСЕДКИ — слово владельца (2026-08-15 16:4x +03:00)

Дословно, двумя сообщениями:

> *«и точки с более низкой частотой, чем предыдущая уже отюненная — можно начать»*
>
> *«можно начинать спуск не со стокового напряжения, а с верхней по частоте отюненной точки»*

**Это прямо отменяет осторожность агента.** Я эту оптимизацию нашёл, посчитал (5–7 часов → 1,7) и
СОЗНАТЕЛЬНО ОТЛОЖИЛ, потому что затравка — это прыжок на глубокое напряжение вместо спуска к нему,
то есть форма `bugs/03`. Вы решили иначе, прочитав это рассуждение. Решение ваше, и оно принято.

**И вы назвали правило сами, третьим сообщением:**

> *«как правило на более нижней частоте будет напряжение нужно или такое же как у старшей по частоте
> точки, или даже ниже, очень редко — выше, почти не бывает такого»*

**Это совпало с физикой, которую разведка нашла независимо:** Vmin не убывает с ростом частоты, потому
что отказ у края — это нарушение setup-времени (`researches/09` §2.3, промышленная характеризация
кремния «shmoo»). Значит напряжение, доказанное на БОЛЕЕ ВЫСОКОЙ частоте, на более низкой заведомо не
хуже. Затравка идёт **от ПРОШЕДШЕГО значения соседки (край + 10 мВ), а не от её напряжения отказа.**

**И ваша собственная оговорка — «очень редко выше, почти не бывает» — это и есть причина, по которой
пункт 3 ниже существует.** Вы не сказали «никогда»; значит движок обязан уметь встретить тот редкий
случай и не принять его за норму.

**Три вещи, которыми это обставлено — они не смягчают ваше решение, а делают его исполнимым:**

1. **Сторож первого шага обобщается, а не отключается.** Сегодня он запрещает первый шаг глубже 25 мВ
   ОТ СТОКА. Правильное обобщение: глубже 25 мВ **от самого глубокого напряжения, уже ДОКАЗАННОГО
   уликой**. Улик нет → доказанное = сток → сторож ведёт себя ровно как сегодня. Изменение
   доказывается мутацией, как всякий сторож (EXP-0008).
2. **Первый прожиг на затравке — это проверка, а не формальность.** Он подтверждает, что монотонность
   держится на этом кремнии.
3. **Не PASS на затравке = монотонность нарушена.** Тогда движок ОТМЕНЯЕТ затравку на этой частоте,
   спускается от стока по вашей лестнице и **говорит об этом вслух** — это была бы находка о
   кремнии, а не сбой прогона.

**Цена развёртки после этого: ≈ 1,7 часа** вместо 5–7.

### Уточнение затравки — от закреплённой соседки идём МИНИМАЛЬНЫМ шагом (2026-08-17 00:0x +03:00)

Дословно:

> *«можно улучшить алгоритм спуска. если у старшей частоты найден край, и от него шагнули на +10 мВ
> вверх и зафиксировали эту точку. То следующую вниз частоту можно сразу от этой точки напряжения
> прожигать минимальными шагами, а не шагать от стока. Шагать от зафиксированной соседки минимальным
> шагом вниз»*

**Что это меняет против сегодняшнего кода.** Затравка от соседки уже есть — это ваше же слово выше.
Но РАЗМЕР шага после неё берётся из зоны политики, а зона ключуется **глубиной от стока**. Если край
соседки нашёлся неглубоко (скажем, −60 мВ), спуск от затравки пойдёт шагом **25 мВ** — то есть самым
грубым, и притом у самого обрыва, потому что соседкин край рядом.

**Почему вы правы по существу, а не только по осторожности.** Затравка ставит нас не в начало пути,
а почти в его конец: физика (`researches/09` §2.3) говорит, что Vmin не убывает с частотой, значит
край соседней частоты снизу лежит РЯДОМ с уже найденным. Шаг 25 мВ имеет смысл там, где до отказа
далеко; от закреплённой соседки до отказа заведомо близко. Это ровно рассуждение `bugs/03` — размер
шага есть единственная защита, действующая ДО того, как состояние возникнет.

**И тут же уточнение, которое делает правило проще, а не сложнее — дословно:**

> *«грубо шагать есть смысл в начале прогона на первой старшей частоте. Дальше всегда можно шагать
> минимальным шагом от соседки зафиксированной»*

**То есть лестница 25/10/5 — это механизм ЗАПУСКА, и работает он ровно один раз за прогон.** Первая,
самая старшая частота идёт от стока грубыми шагами, потому что до её края действительно далеко и
идти туда мелким шагом значило бы жечь десятки ступеней впустую. Как только у неё найден край и
точка закреплена на «отказ + 10 мВ», **каждая следующая частота вниз стартует от закреплённой соседки
и идёт минимальным шагом.**

**Правило, как оно ложится в код:** ключ зоны перестаёт быть только «глубина от стока». Спуск БЕЗ
затравки (в норме — только первая частота полосы) идёт по лестнице 25/10/5 без изменений. Спуск ОТ
затравки идёт минимальным шагом сетки независимо от глубины.

**Честная цена, названная сразу:** ступеней между затравкой и краем станет больше, чем при шаге
25 мВ, — но их и так немного, потому что край рядом. Против этого стоит то, что грубый шаг у края
чаще даёт ЗАВИСАНИЕ вместо пойманного вердикта, а зависание стоит перезагрузки.
---

## 🎚 ТЮНИМ ТО, ЧТО КАРТА ВЫДАЁТ, А НЕ ТО, ЧТО МЫ ЗАКАЗАЛИ — слово владельца (2026-08-16)

Дословно, два сообщения подряд:

> *«понял, тогда тюним не то, что заказываем, а что она нам выдает фактически»*
>
> *«хотим заказать N, она нам выдала M — примиряемся с её выдачей и тюним то, что она нам даёт»*

**Правило:** прогон читает частоту, которую карта ФАКТИЧЕСКИ выдала под нагрузкой, и записывает
найденное напряжение против **неё**. Заказанная частота остаётся в журнале как намерение, но в
документ кривой она не попадает: строка документа — это замер, а не заявка.

### Почему это понадобилось — три замера на этой карте (2026-08-16)

| заказали | карта выдала под нагрузкой | троттлинг |
|---|---|---|
| `-lgc 3082,3082`, кривая +45, потолок 3090 | **2917 МГц**, семь проб подряд без движения | `0x0` |
| `-lgc 3082,3082`, заводская кривая | **2887 МГц**, шесть проб подряд | `0x0` |
| `-lgc 2700,2700`, заводская кривая | **2692 МГц**, пять проб подряд | `0x0` |

Заставить карту подняться нельзя ничем: **замок частоты — это потолок, а не приказ**, он держит
вниз и не тянет вверх. Выше своей точки карту не поднимает ни замок, ни подъём кривой — там
работает **напряжение надёжности**, и на Blackwell снять его программно невозможно (разбор со
ссылками — `researches/11`).

### Что это меняет

1. **Точка закрывается по ВЫДАННОЙ частоте.** Заказали 2887, карта дала 2857 → в документ едет
   строка **2857**, и её напряжение доказано ровно на 2857.
2. **Покрытие решает карта, а не заказанная полоса.** Какие-то частоты не закроются вовсе, какие-то
   будут пройдены дважды — это нормально и должно быть видно в отчёте, а не сглажено.
3. **Верх лестницы (2895…3090, 26 частот) сегодня недостижим** и в документе останется стоковым.
   Цена этого измерена и мала: факт 25 — **6 % частоты покупают 1 % кадров**.
4. **Ложного `[TESTED]` больше нет.** Раньше напряжение, прожжённое на 2857, записалось бы против
   2887 — утверждение, которого никто не мерил. Это ровно тот класс, ради которого заведён
   `TESTING_FRAMEWORK.md`.

### Комментарий агента — что здесь честно, а что ещё не сделано

**Сделано:** замеры выше, разведка `researches/11`, отмена закрепления как умолчания (замок вверх
не тянет, и с ним прогон вставал на первой ступени).

**НЕ сделано на момент записи этого раздела:** сам перенос записи на выданную частоту в
`sweepRange` → `closePoint`. Это не одна строка: меняются подсчёт покрытия, наследование вниз по
ступени и поведение храповика, когда две заказанные частоты дадут одну выданную. Названо здесь,
чтобы следующая сессия не приняла решение владельца за исполненное.

> ✅ **ИСПОЛНЕНО 2026-08-16** (коммит `cde8265`): замер ложится в строку выданной частоты.

---

## 🔥 ЧТО ТАКОЕ ПРОЖИГ — слово владельца (2026-08-16 15:5x +03:00)

Дословно, тремя сообщениями:

> *«прожиги должны за 10 секунд МАКСИМАЛЬНО загрузить все, что есть у видеокарты, все CUDA ядра, всю
> VRAM, ВСЕ RT ядра — максимальная загрузка камня видеокарты»*
>
> *«поэтому и называется это ПРОЖИГ, СТРЕСС ТЕСТ»*
>
> *«и оракул должен следить за этой загрузкой, проверять адекватность расчетов»*

**Это определение старше всех прежних формулировок о нагрузке в этом файле и в планах.** Оно отвечает
на вопрос, который сам же владелец и задал в тот день: *«а почему на прожигах мощность в потолок не
лупится, в 300 или 250 ватт?»*

### Замер, из которого вырос вопрос (2026-08-16, живая карта, карта свободна)

| | наш `sdc_fma --sustain 20` | игра Q2RTX (замер `researches/06`) |
|---|---|---|
| мощность | **133,6 Вт** (пик 134,5) при лимите 300 | **299,97 Вт**, троттлинг `sw_power_cap` |
| загрузка | 53 % | 99 % |
| температура | 57 °C | 75 °C |
| доля времени на GPU | **55,7 %** | — |

**Телеметрия не врёт — нагрузка не догружает.** Механизм виден в собственном выводе теста: 38 328
запусков ядра за 20 секунд, между ними карта простаивает. Половина конверта — это **скважность**, а
не слабость ядра.

### Что из слова владельца следует — три обязательства, и они разной цены

1. **Загрузить ВСЁ, что есть на кристалле:** вычислительные ядра · VRAM (и объём, и полоса) ·
   **RT-ядра**. Сегодня наши нагрузки грузят только первое, и то со скважностью 56 %.
2. **Уложиться в 10 секунд** — длительность ступени не растёт, растёт плотность.
3. **Оракул СЛЕДИТ ЗА ЗАГРУЗКОЙ, а не только за результатом.** То есть PASS обязан нести
   доказательство, что карта РЕАЛЬНО была нагружена; прожиг, не догрузивший карту, — не прожиг, и
   вердикт по нему утверждает больше, чем измерил.

### Критерий приёмки прожига назван числом — слово владельца (2026-08-16 23:0x +03:00)

Дословно, тремя сообщениями подряд, после доклада агента о скважности 56 %:

> *«нужно повысить скважность. Видеокарта в эти 10 секунд должна УПИРАТЬСЯ В ПРЕДЕЛ ПОТРЕБЛЕНИЯ
> ЭЛЕКТРОЭНЕРГИИ»*
>
> *«она должна молотить расчёты как бешенная»*
>
> *«стресс нагрузка на 10 секунд»*

**Это превращает пункт 3 выше из требования в ПРОВЕРЯЕМЫЙ критерий, и мера названа: МОЩНОСТЬ.**
Прожиг принимается, когда карта в течение десяти секунд стоит у своего предела потребления;
`utilization.gpu` мерой не является и никогда ею не был (факт 16: 97 % загрузки при 137 Вт).
Длительность при этом НЕ растёт — растёт плотность, и это сказано отдельно третьим сообщением.

**Состояние на момент записи, замерено:** `runs/power/burn-before.json` — **135,8 Вт при лимите
300**, доля времени на GPU **56,1 %**. То есть «прожиг на 10 секунд» это 5,6 секунды настоящей
нагрузки при 45 % конверта карты.

**Причина найдена и она двойная** (разбор агента, не слово владельца): **(а)** ядро `fma_chain` —
чистая цепочка FMA в регистре, из памяти НЕ читает ничего, то есть VRAM не грузится вовсе;
**(б)** после каждого запуска весь буфер копируется в память хоста и хешируется побайтово на
процессоре, и всё это время карта простаивает.

**🔴 И ОДНОГО ПОВЫШЕНИЯ СКВАЖНОСТИ НЕ ХВАТИТ — ЭТО УЖЕ ИЗМЕРЕНО, ЗАДОЛГО ДО СЕГОДНЯШНЕГО ЗАКАЗА.**
У проекта есть вторая нагрузка, `branchy`, и она НАСЫЩАЮЩАЯ: замер 2026-08-10 — **скважность
98,5 %, и всё равно 194,8 Вт** при лимите 300 (загрузка 97 %, 2887 МГц, 60 °C).

| нагрузка | скважность | мощность при лимите 300 Вт |
|---|---|---|
| `sdc_fma` — единственная, которой пользуется развёртка | 56 % | 137,9 Вт |
| `branchy` — насыщающая, простоя почти нет | **98,5 %** | **194,8 Вт** |
| игра Q2RTX | — | **299,97 Вт**, режется по `sw_power_cap` |

**Значит скважность и предел потребления — РАЗНЫЕ величины, и вторая не достигается первой.**
Починка `sdc_fma` доведёт её примерно до территории `branchy` (~195 Вт), то есть **до предела не
дойдёт**: не хватает ~105 Вт, и берутся они там, где их берёт игра — обращение к памяти и
остальные блоки кристалла. Это ровно вторая половина формулировки владельца («всю VRAM»), а
RT выведен за скобки его же решением (`interviews/008`, вариант A).

**Что из этого следует для порядка работ:** сперва замер сетки форм нагрузки против ВАТТ (какая
форма куда доходит), и только потом код. «Какая нагрузка упирается в предел» — вопрос к карте, а
не к рассуждению.

**Честная цена, названная сразу (комментарий агента, не слово владельца).** Пункты 1–2 по
вычислительным ядрам и VRAM — это правка наших же `.cu` и пересъём эталонов. **RT-ядра своим кодом
не грузятся ничем из того, что в проекте есть:** для них нужен либо OptiX/DXR (новая зависимость,
решение владельца), либо свидетелем берётся уже стоящая на машине Q2RTX — настоящий путь-трейсер,
дающий 300 Вт и реальные RT-ядра, но **не дающий сверки с эталоном** (у графического пути нет
контрольной суммы, он умеет только вето по сбоям). Развилка вынесена владельцу.

---

## 🎚 ТО ЖЕ ПРАВИЛО НА ОСИ НАПРЯЖЕНИЯ — слово владельца (2026-08-16 13:4x +03:00)

Дословно, двумя сообщениями подряд:

> *«мы должны попасть в ближайшее верхнее напряжение»*
>
> *«ближайшее верхнее из тех, которые карточка предоставляет»*

**Правило:** ступень закрывается напряжением, которое карта ФАКТИЧЕСКИ подставила под частоту после
записи, и это напряжение обязано быть **ближайшим верхним с сетки самой карты**. Заказанное остаётся
в журнале как намерение; в документ кривой едет выданное.

### Почему это понадобилось — замер на живой карте 2026-08-16

| | |
|---|---|
| заказали | 2887 МГц ← **1045 мВ** (сток 1070, первый шаг −25) |
| карта после записи | частоту обслуживало **1050 мВ** |
| что было | прогон вставал на первой же ступени: вердикт PASS относился не к заказанному напряжению |

**Причина не в размере шага, и это важно.** План считает обслуживающую запись по таблице,
прочитанной ДО прожига, а сторож перечитывает её ПОСЛЕ. Заводская таблица едет по оси ЧАСТОТ с
нагревом (≈ −1,7 МГц на градус), поэтому карта, нагревшаяся за собственный прожиг, отдаёт частоту
следующей записи ВВЕРХ. Первая же ступень следующего прогона, на холодной карте, совпала точно —
это и подтвердило диагноз.

### Что это меняет — три направления, и они разные

| выдача карты | решение | почему |
|---|---|---|
| **ровно заказанное** | ступень идёт как обычно | обычный случай на холодной карте |
| **ближайшее верхнее с сетки** (одна ступень) | **ПОПАДАНИЕ.** Мерой становится выданное | прожиг был на нём, вердикт истинен о нём, направление консервативное |
| **выше, но НЕ ближайшее** | СТОП | это уже другая запись таблицы, а не округление вверх |
| **ниже заказанного** | СТОП | андервольт глубже того, под который считались сторожа шага |

*Комментарий агента — границу «ближайшее» нашёл собственный набор проверок.* Первая редакция правки
принимала любое напряжение ВЫШЕ заказанного, и фикстура «заказано 760 мВ → обслуживало 1000 мВ»
прошла как PASS. То есть прогон принял бы за измерение ступень, которую никто не заказывал, и при
этом считал бы, что спустился. Ваше слово «**ближайшее**» — ровно то, что закрывает эту дыру, и
теперь оно в коде счётом по сетке карты, а не на глаз.

---

## 🔊 ВИЗУАЛИЗАТОР ЖИВЁТ ВСЕГДА — слово владельца (2026-08-22 15:1x +03:00)

Дословно, в четыре удара подряд:

> *«НИКАКОГО УМИРАНИЯ ВМЕСТЕ С ЧЕМ-ТО!!!!! НИ АНИМАЦИИ НИ ЗВУКА!!!! ВСЁ ВОСПРОИЗВОДИТСЯ ВСЕГДА!!!!
> МНЕ ОНО НУЖНО ГАРАНТИРОВАНО ЖИВОЕ!!!!»*
>
> *«оно само зависнет, когда зависнет комп — не нужно никаких специальных механизмов умирания
> разрабатывать»*

**Это отменяет прежнее устройство страницы, а не украшает его.** Раньше анимация была ФУНКЦИЕЙ
ПУЛЬСА: кадры стояли на паузе, время им выдавала отметка прогона, и после смерти прогона картинка
замирала — замирание И БЫЛО сигналом «машина зависла». Отменено целиком.

| Изъято | Стало |
|---|---|
| кадры на паузе, время подставляется от `runMs` | кадры идут **своим ходом**, от загрузки страницы до её закрытия |
| поводок `LEASH_MS` — сколько картинке жить после пульса | поводка нет; жить всегда |
| `body.hung` останавливал кадры | `body.hung` **только меняет слова и цвет** |
| «встал прогон» = картинка замерла | «встал прогон» = **красная строка называет это и говорит, сколько секунд назад** |

**Почему владелец прав по существу, а не только по праву владельца — две причины.**

1. **Замирание — это ОТСУТСТВИЕ сигнала**, а отсутствие неотличимо от придушенной фоновой вкладки,
   свёрнутого окна, уснувшего браузера или зависшей видеоподсистемы самого браузера. Сигнал, который
   выражается ничем, нельзя ни проверить, ни отличить от своих двойников.
2. **Настоящее зависание машины остановит страницу САМО** — бесплатно, мгновенно и надёжнее любой
   нашей машинерии. Мы писали второй механизм смерти поверх того, который есть даром. Сущностей
   стало МЕНЬШЕ (`PHILOSOPHY.md` → Оккам), а сигнал — сильнее.

**Обязательная половина правила, без которой первая превращается во враньё:** правда о прогоне не
исчезает, она переезжает **из тишины в слова**. Прежде один канал нёс два разных факта — «окно живо»
и «прогон идёт» — и путал их; ровно на этой путанице и родился `bugs/27`, где здоровый прогон три
раза в минуту подавал сигнал о зависании машины. Теперь у каждого факта свой канал, и **молчащих
каналов нет ни одного**.

**Второй канал обратной связи — звук.** Заказан там же:

> *«какой-то мягкий ненавязчивый но отчётливый звук, который визуализатор будет проигрывать во время
> своей работы, так что я буду по двум каналам получать обратную связь о том, что комп жив и работает
> — анимация и звук»* · *«ну и кнопку, соответственно — вкл выкл звук»* · *«может какие-то космические
> техно бипы пики»* · *«как в ангаре в Космических Рейнджерах 2»*

Звук подчиняется тому же правилу: **идёт всегда, пока включён**, и ничем не обусловлен. Голос
выбирает владелец ухом — класс вкуса, артефакт для прослушивания: `homeworks/04_dashboard_sound.html`,
вердикт пишется в `homeworks/04_dashboard_sound_taste.md`.
