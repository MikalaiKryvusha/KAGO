# Идея 15 — у страницы визуализатора дефолтная иконка-глобус вместо нашей микросхемы

**Статус:** 🔧 **ИСПОЛНЕНО 2026-08-31 (сессия 71) — ЖДЁТ ГЛАЗА ВЛАДЕЛЬЦА.** Иконка вшита сборкой,
проверена grep-ом в `assets/dashboard/sweep.html`; вердикт «это наша микросхема, а не глобус» —
перцептивный, и его даёт владелец, а не агент (`AGENT_GUIDE.md` → the taste class).
Предложена владельцем 2026-08-30, вечер, во время живого прогона.

> 🔴 **ИНСТРУКЦИЯ ЭТОГО ТИКЕТА БЫЛА НЕВЕРНА, И ЭТО ВЫЯСНИЛ ПРОГОН, А НЕ ЧТЕНИЕ.** Пункт «править
> ИСТОЧНИК `assets/dashboard/_preview.html`» указывал не туда: **`_preview.html` — не источник, а
> ВЫВОД той же сборки**; источник — `homeworks/03_sweep_animation.html`, принятый владельцем макет
> (`build-dashboard-page.mjs`, `const SRC`). Правка в `_preview.html` до `sweep.html` не доезжает
> вовсе — я это сделал и увидел ноль совпадений в собранной странице.
>
> **Куда легло на самом деле: в СБОРКУ**, тем же приёмом, что проводка и звук. Принятый макет
> владельца не тронут — его вердикт вкуса это канон, а иконка вкладки к принятой ВЁРСТКЕ не
> относится. Вектор читается из `assets/logo/chip-base.svg` на каждой сборке, второй копии картинки
> в репозитории не заведено (DRY).
>
> Тот же класс, что [[EXP-0206]]: документ назвал механизм, которого не проверял.

**Класс:** улучшение того, что владелец ВИДИТ. Машины не касается, карты не касается.
**Где (ИСПРАВЛЕНО 31.08 по факту сборки):** `homeworks/03_sweep_animation.html` (принятый макет —
НЕ трогаем) + `assets/logo/chip-base.svg` (вектор иконки) → внедряет `tools/build-dashboard-page.mjs`
→ `assets/dashboard/sweep.html`. Файл `assets/dashboard/_preview.html` — ВЫВОД этой же сборки, а не
источник.

## Слово владельца, дословно

> *«заведи импрувмент - у сраницы визуализатора дефолтная иконка в виде глобуса. Нужно нашу иконку
> микросхемы в высоком разрешении поставить»*

## Цель и критерий приёмки

- **Вектор (Achieve):** вкладка окна наблюдения опознаётся среди десятка других по НАШЕЙ иконке.
  Сейчас во вкладке стоит глобус — то, что браузер рисует, когда иконки нет вообще.
- **Критерий (Scale · Meter · Target):** иконок в разметке страницы · grep по собранной
  `sweep.html` · **≥ 1 `<link rel="icon">`, разрешение ≥ 180×180 или векторное**, и владелец
  подтверждает глазом, что во вкладке микросхема, а не глобус.

## Симптом — проверен, а не предположен

```
grep -rn "favicon|icon\"" --include=*.html assets/dashboard/   →   НИ ОДНОГО СОВПАДЕНИЯ
```

Иконки в разметке **нет вообще** — ни `favicon.ico`, ни `<link rel="icon">`. Браузер в таком случае
рисует свою заглушку, и это ровно тот глобус, который видит владелец. То есть чинить нечего —
надо ПОСТАВИТЬ.

## Материал уже есть, рисовать заново не нужно

| файл | что это |
|---|---|
| `assets/logo/chip-base.svg` | **микросхема, вектор** — то самое, что просит владелец, и в векторе «высокое разрешение» бесплатно на любом экране |
| `assets/logo/kago-logo.png` · `kago-logo.webp` | растровый логотип |
| `assets/logo/preview128.png` | 128×128 |
| `assets/logo/kago-folder.ico` | иконка папки оболочки |

## Как делать — и одна ловушка, названная заранее

1. **Править ИСТОЧНИК, а не сборку.** `sweep.html` собирается скриптом
   `tools/build-dashboard-page.mjs` из `_preview.html` + `_wiring.js`; правка прямо в `sweep.html`
   уедет при следующей сборке. Сторож сборки (`npm run check`) печатает «страница окна наблюдения:
   собрана из текущих источников» — он же и поймает расхождение.
2. **Вшивать `data:`-URI, а не отдельный файл.** Страницу поднимает локальный сервер прогона со
   своими песочными путями; отдельный запрос за `/favicon.ico` — это лишний маршрут, который надо
   заводить и который в песочнице может не найтись. SVG-микросхема вшивается строкой
   `<link rel="icon" href="data:image/svg+xml,...">` и не зависит ни от какого маршрута.
3. **Проверять глазом владельца.** Это перцептивный критерий: «наша иконка» — про узнавание, а
   не про число. Агент готовит артефакт, вердикт даёт владелец (`AGENT_GUIDE.md` → the taste class).

## Почему это не мелочь

Прямо по границе молитвы: **Оккам и Парето действуют ВНУТРИ машинерии; на том, что владелец видит,
агент не экономит.** Вкладка — то, что владелец видит каждый вечер, и сейчас она подписана чужим
глобусом. Плюс практическое: во время прогона у владельца открыто несколько вкладок, и иконка —
самый быстрый способ найти нужную.

## Связи

`bugs/65` (окно наблюдения должно подниматься — владелец видит его каждый вечер) ·
`assets/logo/README.md` (набор иконок и их происхождение) · `tools/build-dashboard-page.mjs` ·
`GOAL.md` → «🏷 БРЕНД-ИМЯ — ВСЕГДА ПРИВИЛЕГИЯ ВЛАДЕЛЬЦА» (иконка — тоже витрина: форму выбирает
<!-- owner-review:allow because=проза о правиле бренд-имени (форму выбирает владелец, агент предлагает), а не вопрос. Идея исполнена 31.08 и ждёт глаза владельца, а не его слова. -->
владелец, агент предлагает)
