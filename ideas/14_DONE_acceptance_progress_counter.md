# Идея 14 — счётчик приёмки: `npm run curve -- --progress` и строка доставки сессии

> **Создано:** 2026-08-28 (агент, по аудиту `reports/KAIF_AUDIT/2026-08-28_audit_03_method.md` §6, правило Р1)
> **Родитель:** `ideas/13` (заказ аудита) · критерий приёмки владельца — `GOAL.md` → «🏁 КРИТЕРИЙ ПРИЁМКИ ТЮНИНГА»
> **Статус:** ✅ ИСПОЛНЕНА 2026-08-28 02:0x (сессия 56) — `npm run curve -- --progress [--json]`
> живёт в `curve-store.mjs`; 7 блоков самопроверки, обе мутации доказаны красным (подмена
> происхождения · черновик как отгруженный), каждая красила ровно свой блок. Живой прогон совпал
> с ручным разбором аудита: **краёв 11/389 (прожигом 9 · соседкой 1 · выведено 0) · не тронуто
> 302 · режимов 0/4**, плюс одна спорная строка напечатана поимённо (2835 МГц — край, чьё
> напряжение поставил храповик). Батарея: curve 72 → 79, итог 28 наборов / 1413 блоков / 0 красных
> **Наружу:** строка доставки — в открытие/закрытие каждой сессии (критерий 4 живёт в STATUS)

## Вектор цели

**Боль:** у проекта нет общего счёта — ни одна сессия не открывается и не закрывается расстоянием до
приёмки, поэтому вектор цели сессии сползает на последнюю боль (аудит, лицо Д1). **Куда хотим:** одно
число, одинаково читаемое владельцем и агентом, печатается одной командой и стоит первой и последней
строкой каждой сессии. **Тип цели:** Achieve (новый прибор) + Maintain (число обновляется только
командой — по образцу правила батареи).

## Что делает команда

`npm run curve -- --progress` — только чтение, карту не трогает:

```
ПРИЁМКА: краёв 11/389 (прожигом 11 · соседкой 22 · выведено 0) · не тронуто 302 · режимов отгружено 0/4
```

- «край» считается по закрытому словарю происхождения владельца (`GOAL.md`, четыре способа:
  прожигом · соседкой · интерполяцией · экстраполяцией); строка без происхождения края в счёт не идёт;
- «режимов отгружено» — из хранилища профилей: режим считается отгруженным, когда его черновик
  перестал отказывать и прошёл квалификацию;
- выход машиночитаем (`--json`) — для судьи и для строки доставки.

## Критерии приёмки

| # | Критерий | Прибор | Цель |
|---|---|---|---|
| 1 | Команда печатает счёт без записи в карту и в документ | журнал прожигов + diff документа | 0 записей |
| 2 | Счёт совпадает с ручным разбором `curves/measured.json` | сверка на текущем документе | вручную: краёв 11, не тронуто 302 — совпасть |
| 3 | Мутация «считать выведенное прожжённым» краснеет | самопроверка `curve` | ≥1 блок на подмену происхождения |
| 4 | Строка доставки входит в открытие/закрытие сессии | STATUS следующих сессий | строка присутствует |

## Границы

Ничего сверх счётчика: ни порогов, ни вердиктов, ни остановок — это прибор, не сторож (урок
`bugs/27`: тревога, врущая по расписанию, отменяет сама себя; здесь тревог нет вовсе).

## Шаги (для исполняющей сессии; план — этот документ, отдельного не нужно)

- [x] 1. Вход CLI — `automation-engine/lib/curve-store.mjs` (диспетчер аргументов ~строка 1837);
      добавить ветку `--progress` (+ `--json`). ✅ `cmdProgress`, обе формы прогнаны
- [x] 2. Определение «край известен» читать ИЗ СЛОВАРЯ (`CURVE_TAGS`), не изобретать: строка
      закрыта краем, если несёт `stop:edge-found` ЛИБО происхождение из четырёх законных
      (`origin:measured` при найденном крае · `origin:inherited` · будущие
      interpolated/extrapolated). `origin:ratcheted` и `stop:lever-limited` краем НЕ являются.
      Спорную комбинацию тегов печатать отдельной строкой «не классифицировано: N», а не глотать.
      ✅ `acceptanceProgress()`; будущие происхождения — `DERIVED_ORIGIN_TAGS`, считают 0 до
      механизма эпика 42; спорная строка сегодня одна и печатается: 2835 МГц
- [x] 3. «Режимов отгружено» — из хранилища профилей: режим отгружен, когда его черновик не
      отказывает и прошёл квалификацию (сегодня ожидаемо 0/4 — и это должно печататься честно).
      ✅ `qualified === true` по четырём режимам; нечитаемый файл профиля называется вслух и
      отгруженным не считается
- [x] 4. Самопроверка: блок(и) на фикстурном документе + мутация «выведенное посчитано
      прожжённым» краснеет (критерий 3 таблицы). ✅ 7 блоков; мутация красила ровно свой блок
      (78 зелёных / 1 красный), вторая мутация «черновик как отгруженный» — тоже
- [x] 5. Прогнать на боевом `curves/measured.json`, сверить с ручным разбором аудита
      (критерий 2), числа записать в STATUS строкой доставки. ✅ совпало: краёв 11, не тронуто 302
