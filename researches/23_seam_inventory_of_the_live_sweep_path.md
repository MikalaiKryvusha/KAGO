# Разведка 23 — ОПИСЬ ШВОВ живого пути развёртки (эпик 59 фаза 1)

> **Создан:** 2026-08-28 22:5x +03:00 · **Родитель:** `plans/60` (оперплан фазы) · `plans/59`
> (эпик цифрового двойника) · **Статус:** 🟢 опись снята грепами по коду 2026-08-28, строки — из
> вывода команд, не из памяти · **Вовне:** единственный источник объёма фаз 2–3; правится ТЕМ ЖЕ
> коммИТОМ, что двигает шов (пара правда↔зеркало схлопнута дисциплиной коммита)
>
> **Глаголы виртуализации (P60-AC3, других нет):** ПЕРЕКЛЮЧИТЬ — шов уже инъекционный, виртуальной
> сборке достаточно подать свою реализацию · ПРОРЕЗАТЬ — порт нужно создать · ПЕСОЧНИЦА — путь
> файла/каталога уводится в стенд · ПРОПУСК — ворота, отключаемые словом владельца.

## 1. `engine.mjs` — сборка `cmdSweep` (вход `--sweep`, ~8600–9260)

| # | строка | касание | когда | глагол → виртуальная сторона |
|---|---|---|---|---|
| E1 | 8666 | `loadCurveDoc({})` — документ живой карты | старт | ПЕСОЧНИЦА: `{name:'virtual-<профиль>', dir: benches/runs/}` (сигнатура уже параметрична: `curve-store.mjs:1121`) |
| E2 | 8672–8678 | `openNvapi` + Initialize + EnumPhysicalGPUs + `readVfCurve` | старт | ПЕРЕКЛЮЧИТЬ: applier двойника (`virtual-gpu.mjs`, поверхность `readState/apply/resetToFactory` есть) |
| E3 | 8688 | `ps.probeCard()` — лестница частот + конверт (спавнит nvidia-smi) | старт | ПЕРЕКЛЮЧИТЬ: лестница из профиль-файла двойника |
| E4 | 8826 | `openJournal({})` — БОЕВОЙ журнал зависаний `runs/sweep/` | старт | ПЕСОЧНИЦА: `openJournal({dir})` уже параметричен (`sweep-journal.mjs:125`); виртуальный ЗАВИС обязан лечь в журнал стенда (инвариант I1) |
| E5 | 9010–9022 | ворота safe-mode (читают состояние дисков машины) | старт | ПРОПУСК в виртуальном режиме — слово владельца: «ей взвод безопасности не нужен» |
| E6 | 9054+, 9066 | сэмплер `hardware-mon.mjs` (⚠️ говорит с картой через **nvidia-smi CLI**, не NVML — `hardware-mon.mjs:66 runSmi`) | всадник | ПЕРЕКЛЮЧИТЬ: виртуальный сэмплер из телеметрии двойника |
| E7 | 9070–9100 | всадники фьюза: судья `fuse --judge` (устройства НЕ касается) + проба `death-watch --probe` (NVML `GetPowerUsage`) | всадник | судья — БЕЗ ИЗМЕНЕНИЙ; проба — ПЕРЕКЛЮЧИТЬ источник ударов: виртуальная проба двойника, умеющая играть оба профиля смерти (фаза 4) |
| E8 | 9160 (callsite) | `readPointsFn: () => nvapi.readVfCurve(nv, handle).points` — свежая таблица пер-ступень | каждая ступень | следует за E2 (тот же applier) |
| E9 | callsite `saveFn` | `saveCurveDoc(d)` — запись документа | каждая частота | ПЕСОЧНИЦА (сигнатура параметрична: `curve-store.mjs:1117`) |
| E10 | callsite `recover` | `watchdog.recover()` — сторож записи живой карты | старт | ПЕРЕКЛЮЧИТЬ: чистый виртуальный сторож |
| E11 | 8930–8945 | окно наблюдения + дашборд (наши процессы; источник — телеметрия) | всадник | остаются; данные идут от E6 |
| E12 | 9146+ | `stopWhen` (выход судьи) | каждая ступень | БЕЗ ИЗМЕНЕНИЙ — устройства не касается |

## 2. `vf-step.mjs` — атом ступени `runStep` (единственный узел nvapi НА ПУТИ; 4 других узла — отдельные приборы, вне пути: `runAscent:1424` · `measureUndervolt:1543` · `probeCurveSnap:1734` · `runShapeExperiment:1855`)

| # | строка | касание | когда | глагол |
|---|---|---|---|---|
| V1 | 716–718 | `import('./nvapi.mjs')` + `./watchdog.mjs` + `./stress-tester.mjs` — ПРЯМЫЕ импорты, инъекции нет (шапка модуля признаёт: «untested offline: the write path itself») | вход атома | **ПРОРЕЗАТЬ — ЭТО И ЕСТЬ ПОРТ S9** (фаза 2) |
| V2 | 732 | `wd.recover({})` | вход | часть порта (виртуальный сторож) |
| V3 | 743 | `nvapi.openNvapi()` | вход | часть порта (applier двойника) |
| V4 | 769–770 | `readVfOffsets` + `readVfCurve` — «до» | преднастройка | часть порта |
| V5 | 877 | `wd.arm({...})` — взвод сторожа записи | перед записью | часть порта |
| V6 | 906 / 910 / 914 | `writeCurve` (кривая/плоско) / `writeVfOffset` (точка) — ЗАПИСЬ | ступень | часть порта; двойник уже держит планку теми же `buildRaiseAndCapVector`+`curveWriteRefusal` (I2) |
| V7 | 968 | `readVfCurveStable` — таблица «после», с усадкой | после записи | часть порта |
| V8 | 1004 | `classifyWriteFailure` — ЧИСТАЯ функция | — | НЕ шов (общая арифметика, якорь I2) |
| V9 | 1166 | спавн сэмплера `hardware-mon.mjs` на ступень | прожиг | ПЕРЕКЛЮЧИТЬ (та же замена, что E6) |
| V10 | 1175–1213 | `stress.judgeCandidate` / `stress.stressTest` — прожиг форм | прожиг | ПЕРЕКЛЮЧИТЬ через лаунчер stress-tester (см. §3) |
| V11 | 1362 | `nvapi.zeroCurve` — ОТКАТ в finally | всегда | часть порта |
| V14 *(найдены при резке 28.08)* | 1093 · 1132 | `pm.nvidiaSmiBackend()` — задник замка частоты | пин/граница | часть порта (`dev.clockBackend`) — В ОПИСИ НЕ БЫЛИ, найдены фазой 2; опись правится тем же коммитом по своему правилу |
| V15 | 1094 · 1135 | `pm.apply(...)` — применение закрепления/границы | пин/граница | часть порта (`dev.applyProfile`) |
| V16 | 1351 | `pm.resetToFactory(...)` — откат замка в finally | всегда при замке | часть порта (`dev.resetToFactory`) |
| V12 | 114–115 | `execFileSync('nvidia-smi', ['--query-gpu=temperature.gpu'...])` — температурный помощник (охлаждение между формами) | между формами | ПЕРЕКЛЮЧИТЬ: температура двойника |
| V13 | 1899–1900 | nvidia-smi `clocks.gr` | — | ВНЕ ПУТИ (внутри `runShapeExperiment:1855`) — строка в описи со сноской, порт не нужен |

## 3. `stress-tester.mjs` — горны

| # | строка | касание | глагол |
|---|---|---|---|
| T1 | 113 | путь бинаря `workloads/<имя>.exe` (параметр `exe \|\|` уже есть) | ПЕРЕКЛЮЧИТЬ |
| T2 | 115, 162 | лаунчеры `run \|\| spawnSync(...)` — ОБА инъекционны | ПЕРЕКЛЮЧИТЬ: виртуальный лаунчер = `runBurst`+`decideVerdict` двойника (в стенде уже доказано 96 блоками) |
| T3 | 875 | спавн `tools/gpu-info.mjs --json` (дамп карты) | ВНЕ ПУТИ развёртки (захват эталона `--verify-baseline`); сноска, порт не нужен |

## 4. Всадники и такт

| # | где | касание | глагол |
|---|---|---|---|
| R1 | `death-watch.mjs` → `openDriverProbe` (NVML `nvmlDeviceGetPowerUsage`) | живая проба | ПЕРЕКЛЮЧИТЬ: виртуальная проба (профили смертей двойника) — фаза 4 |
| R2 | `fuse.mjs` судья | устройств НЕ касается (вход — датаграммы) | НЕ шов |
| R3 | `timeBeginPeriod(1)` (winmm) в пробе/судье/отправителе | таймер СИСТЕМЫ, не карты | НЕ шов карты; в виртуальном режиме остаётся (безвреден и нужен такту) |

## 5. Grep-сторож (P60-AC2) — сигнатуры прогнаны свежими командами 2026-08-28

| сигнатура | попаданий на пути | все ли в описи |
|---|---|---|
| `openNvapi(` | engine cmdSweep 8672 · vf-step 743 (runStep) | ✅ E2, V3; узлы 1432/1550/1735/1874 — вне пути (приборы), названы в §2 шапке |
| `nvidia-smi` | vf-step 115 (V12), 1900 (V13, вне пути) · hardware-mon runSmi (E6) · profile-store probeCard (E3) | ✅ |
| `GetPowerUsage` (NVML) | death-watch openDriverProbe (R1) · fuse-rescue-hand → nvapi (рука 2 — на виртуалке руки бьют по двойнику, фаза 4) | ✅ |
| `workloads/*.exe` | stress-tester 113 (T1) | ✅ |
| `curves/measured` (документ) | curve-store `CURVES_DIR` — вход через E1/E9, оба параметричны | ✅ |
| `runs/sweep` (журнал) | sweep-journal `SWEEP_DIR:55`, `openJournal({dir}):125` — вход E4 | ✅ |
| `runs/death-watch` | death-watch `DEATH_WATCH_DIR` · fuse `FUSE_DIR` — всадники; виртуальной сборке — ПЕСОЧНИЦА теми же параметрами (`--out` у судьи уже есть; пробе добавить) | ✅ (пробе `--out` — задача фазы 3, строка названа) |
| `timeBeginPeriod` | death-watch · fuse | ✅ R3 (не шов карты) |

**Вызовов железа вне описи на пути `--sweep` — 0.** Исключения (вне пути) перечислены поимённо:
четыре прибора vf-step (§2 шапка), V13, T3.

## 5а. Статус порта (фаза 2, 2026-08-28 вечер)

**ПОРТ ПРОРЕЗАН** (`plans/61`, коммит с этой правкой): строки V1–V7, V9–V11, V14–V16 идут через
`device` (23 замены, живой адаптер бит-в-бит — батарея 370+75 без правки старых блоков); путь
записи впервые прогнан офлайн целиком; 3 мутанта красные у названных адресатов.

## 6. Что опись говорит фазам 2–3 (сводка объёма)

- **Фаза 2 (порт S9):** один порт в `runStep` закрывает V1–V7, V11 разом — это ОДИН объект
  устройства (`{recover, arm, open, readOffsets, readCurve, readCurveStable, writeCurve,
  writeVfOffset, zeroCurve}`), живой адаптер = сегодняшние вызовы бит-в-бит; сэмплер (V9) и
  лаунчер (V10/T2) — параметры рядом.
- **Фаза 3 (сборка):** E1–E10 — ВСЕ уже параметричны или тривиально переключаемы; новых портов
  в движке не требуется, нужна СБОРКА + два `--out`-класса пути (журнал E4, всадники R1).
- Самая дешёвая дорога к дыму: фаза 2 (порт) → фаза 3 (сборка) без единой правки `sweepRange`.
