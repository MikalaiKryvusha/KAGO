# KAGO — текущее состояние

<!-- KAIF:PRAYER:BEGIN — ОДИН ИСТОЧНИК: PHILOSOPHY.md. Правится там, раскладывается `node tools/prayer.mjs --apply` -->
## 🙏 МОЛИТВА ПЕРЕД РАБОТОЙ

> **Слово владельца, 2026-08-24:** *«давай поднимем принципы из ФИЛОСОФИИ на уровень молитвы перед
> началом любой работы… с просьбой тебя молитву озвучить перед каждой работой»*.
> **Каденция уточнена владельцем 2026-08-28 (интервью 017, Q2 = B).**
>
> 🔴 **АГЕНТ: ПРОИЗНЕСИ ЕЁ В ЧАТЕ ЦЕЛИКОМ ОДИН РАЗ НА СЕССИЮ — при входе (`/resume`, вход в
> цикл), дословно, пунктов не пропуская. ПЕРЕД КАЖДОЙ СЛЕДУЮЩЕЙ ЗАДАЧЕЙ — ОДНА СТРОКА: ТРИ
> принципа из шестнадцати, самых важных для этой задачи, своим выбором.** Это не украшение:
> каждый пункт оплачен провалом; полный текст раз в сессию переносит его в решения дня, а выбор
> трёх под задачу — применение, а не декламация. Разбор каждого принципа — `PHILOSOPHY.md`.

1. **ПРОСТОТА ВЫШЕ ВСЕГО.** Если долго — значит я усложнил, а не задача трудна. Застрял — заново
   понять задачу, а не громоздить.
2. **ОККАМ.** Не умножаю сущностей. Из двух решений беру то, где меньше движущихся частей.
3. **ПАРЕТО.** Ищу те 20 %, что дают 80 % пользы. «Сделано и работает» лучше «идеально и поздно».
4. **КОД ПРЕЖДЕ КОГНИЦИИ.** Что может сделать скрипт — делает скрипт. Модели остаётся суждение.
5. **НАБЛЮДЕНИЕ ВМЕСТО ДОГАДКИ.** Не помню — смотрю. Прогон, замер, источник вместо «должно работать».
6. **ТРИ ДВЕРИ.** Пробел закрываю источником или вопросом владельцу. Выдумать — запрещено.
7. **ЛОШАДИ, А НЕ ЗЕБРЫ.** Сперва проверяю самое простое и частое объяснение.
8. **МЁРФИ.** Называю риски вслух и раскладываю по ярусам. Названный риск наполовину управляем.
9. **ЛУЧШИЕ ПРАКТИКИ.** Почти всё решено до меня. Ищу проверенный путь, прежде чем изобретать.
10. **DRY.** Один факт живёт в одном месте. Пару лучше УБРАТЬ, чем за ней следить.
11. **УЧУСЬ ОДИН РАЗ.** Сверяюсь с опытом до работы, дописываю урок после. Дважды в один тупик не хожу.
12. **ЭЙЗЕНХАУЭР.** Важное и срочное — сейчас; важное и не срочное — в план; прочее — вниз.
13. **БРИТВА ХЭНЛОНА.** Не злой умысел, а недосмотр. Отлаживаю состояние мира, а не мотивы.
14. **КВАДРАТ ДЕКАРТА.** На трудной развилке отвечаю на четыре вопроса, а не на два.
15. **ВТОРОЙ ПОРЯДОК.** Думаю на три-пять ходов вперёд, а не о выигрыше прямо сейчас.
16. **КАРМА.** Оставляю репозиторий лучше, чем взял. Не срезаю углы за счёт владельца и следующей сессии.

> ⚖️ **И ОДНА ГРАНИЦА, ЧТОБЫ МОЛИТВА НЕ СТАЛА ОРУЖИЕМ ПРОТИВ ВЛАДЕЛЬЦА:** Оккам и Парето действуют
> ВНУТРИ машинерии. На том, что владелец видит и слышит, агент не экономит — там судит его глаз, а
> не мой счёт сущностей.
<!-- KAIF:PRAYER:END -->

> Этот файл ИИ-агент читает перед каждой задачей. Обновляй его при каждом значимом изменении
> состояния. Это **главная эстафета между сессиями**: новая сессия агента стартует с пустым
> контекстом и должна суметь стать продуктивной из одного этого файла. Пиши соответственно —
> конкретно, с путями и командами.
> 🧠 Ведущий принцип мышления — `PHILOSOPHY.md` (ПРОСТОТА: KISS + Оккам). Рабочий регламент —
> `AGENT_GUIDE.md`.
>
> ⚠️ **STATUS — это СВОДКА НАСТОЯЩЕГО, а не хроника.** Закрытая работа ПЕРЕЕЗЖАЕТ в
> `PROJECT_HISTORY.md`. Мягкий ориентир ~200 строк.
>
> ✂️ **Стрижка 2026-08-14 18:4x: 652 → 399 строк.** Унесено дословно в хронику (раздел «Стрижка
> STATUS 2026-08-14 18:4x»): полная редакция фактов, шаги 3–6 эстафеты, закрытые вопросы владельцу,
> сессии 8–15. Здесь осталось то, без чего следующая сессия ошибётся.
> ✂️ **Стрижка 2026-08-14 18:15 (сессия 17): сессия 16 унесена в хронику дословно** — вместе с
> исправлением её штампа по квитанциям коммитов (в STATUS стояло «17:3x → 18:4x», на деле
> 17:26 → 17:44; штамп был взят из головы, EXP-0019).
> ✂️ **Стрижка 2026-08-15 01:1x (сессия 18): сессия 17 унесена в хронику дословно.**
> ✂️ **Стрижка 2026-08-15 02:3x (сессия 19): сессия 18 унесена в хронику дословно** — 116 строк
> перенесены МЕХАНИЧЕСКИ (скриптом, не перепечаткой: сто строк измеренных чисел перепечатать —
> это и есть способ завести в хронике число, которого никто не мерил). Осталась выжимка её опоры.
> ✂️ **Стрижка 2026-08-15 11:4x (сессия 20): сессии 18–19 унесены в хронику дословно** — 228 строк
> перенесены МЕХАНИЧЕСКИ, тем же способом и по той же причине. 739 → 544 строки.
> ✂️ **Стрижка 2026-08-15 18:1x (закрытие сессии 21): сессия 20 унесена в хронику дословно** — 99 строк
> перенесены МЕХАНИЧЕСКИ, тем же способом и по той же причине. 693 → 594 строки.
> ✂️ **Стрижка 2026-08-15 23:5x (закрытие сессии 25): сессии 22–23 унесены в хронику дословно** —
> 91 строка перенесена МЕХАНИЧЕСКИ (скриптом со сторожем границ, не перепечаткой), эпик 03 закрыт
> целиком и его место в прошлом. 869 → 777 строк.
> ✂️ **Стрижка 2026-08-16 00:5x (закрытие сессии 26): сессии 21, 24, 25 унесены в хронику дословно** —
> 328 строк перенесены МЕХАНИЧЕСКИ (скриптом со сторожем границ, не перепечаткой), потому что фаза 2
> эпика 02 исполнена целиком и её постройка — прошлое. **897 → 569 строк.** Условие этого переноса
> было названо в STATUS заранее и наступило.
> ✂️ **Стрижка 2026-08-22 21:0x (закрытие сессии 37): сессии 28-32 унесены в хронику дословно** —
> 637 строк перенесены МЕХАНИЧЕСКИ, скриптом со сторожем границ. **1549 → 912 строк.** Условие
> переноса наступило и было названо заранее: край карты найден, фаза 2 эпика 02 закрыта, документ
> кривой мигрировал на облако тегов — постройка тех сессий стала прошлым.
> ✂️ **Стрижка 2026-08-23 17:3x (закрытие сессии 43): сессии 41 и 40 унесены в хронику дословно** —
> **315 строк** перенесены МЕХАНИЧЕСКИ, скриптом со сторожем границ (сторож проверял, что срез
> начинается ровно на 41-й и что в него не попал ни более новый блок, ни заголовок верхнего уровня).
> Условие названо и наступило: их постройка стала прошлым — пульс уехал в `plans/27`, часы прогона в
> дашборде исправлены и приняты владельцем, две частоты записаны в документ кривой.
> **1352 → 1039 строк.**
> ✂️ **Стрижка 2026-08-24 16:1x (закрытие сессии 47): сессии 44, 43, 42 унесены в хронику дословно** —
> **536 строк** перенесены МЕХАНИЧЕСКИ, скриптом со сторожем границ (сторож проверял, что срез
> начинается ровно на 44-й, кончается ровно перед разделом «Состояние машины», не содержит заголовка
> верхнего уровня и не захватил ни одной более новой сессии). Условие названо и наступило: их
> постройка стала прошлым — пятый вход читается, батарея закрыла свой класс, оба интервью закрыты
> владельцем, режим пережил перезагрузку. **1489 → 953 строки.**
> ✂️ **Стрижка 2026-08-24 23:2x (закрытие сессии 48): сессии 47, 46, 45 унесены в хронику дословно** —
> **289 строк** перенесены МЕХАНИЧЕСКИ, скриптом со сторожем границ (сторож проверял, что срез
> начинается ровно на 47-й, кончается ровно перед разделом «Состояние машины», не содержит заголовка
> верхнего уровня, не захватил текущую сессию и содержит каждую из трёх ровно по одному разу).
> **Условие было названо здесь заранее и наступило: живая развёртка закрыла частоту** (4 из 25).
> **1152 → 863 строки.**
> ✂️ **Стрижка 2026-08-30 12:3x (закрытие сессии 66): сессии 63 и 61 унесены в хронику дословно** —
> **175 строк** перенесены МЕХАНИЧЕСКИ, скриптом со сторожем границ (сторож требовал: срез начинается
> ровно на 63-й · ровно ДВЕ сессии в срезе · ни одной более новой · ни одного заголовка верхнего
> уровня · следующая за срезом строка — «## Состояние машины»). Условие названо и наступило: эпик 67
> закрыт целиком, `bugs/68` и долг по `lastPass` закрыты, защищённый цикл сессии 61 отработал.
> **1379 → 1203 строки.**
> ✂️ **Стрижка 2026-08-31 23:4x (закрытие сессии 75): ОДНА ПАРА УБРАНА, И ЭТО НЕ ВСЯ СТРИЖКА.**
> Раздел «ПЕРВОЕ СВИДЕТЕЛЬСТВО ПОД ИГРОВОЙ НАГРУЗКОЙ» — **42 строки** — унесён в хронику дословно,
> скриптом со сторожем границ. Взят именно он, потому что сессия 74 назвала его ПАРОЙ к разделу
> «ВЕХА», а пару в этом проекте предпочитают УБРАТЬ, а не сторожить: это DRY, а не перенос прошлого.
> **Сторож ОТКАЗАЛ с первой попытки** на смещении в одну строку — ровно за этим он и пишется.
> **1540 → 1499 строк.**
>
> ✂️ **Стрижка 2026-09-01 09:2x (сессия 76): БЛОК СЕССИИ 73 УНЕСЁН ЦЕЛИКОМ** — **148 строк**
> перенесены МЕХАНИЧЕСКИ, скриптом со сторожем границ. Условие было названо сессиями 74 и 75 и
> наступило: эстафета сессии 73 исполнена целиком, а ВНУТРИ её блока лежали и два остальных названных
> кандидата — предполётный лист 20:00 и сухой прогон полосы вечера. **Сторож ОТКАЗАЛ на обоих
> смещениях в одну строку** (вниз — «заголовков верхнего уровня 2, а не 1»; вверх — «0, а не 1»), и
> проверен он был ДО переноса, а не после. **1599 → 1451 строки; сумма сверена скриптом:
> 1451 + 148 = 1599.**
>
> ✂️ **Стрижка 2026-09-04 (закрытие сессии 78, `/end-chat-soft`): ВСЕ ТРИ НАЗВАННЫХ КАНДИДАТА
> УНЕСЕНЫ, ПОТОМУ ЧТО ИХ УСЛОВИЯ НАСТУПИЛИ** — блок сессии 74 (эстафета исполнена: `bugs/97`,
> половина `bugs/96`, замер Max Performance 01.09), блок сессии 75 (живой свидетель записи взят
> 01.09), раздел «ВЕХА» (приёмка Optimised закрыта замером 01.09) — плюс закрытые №0/№1 (сессия 74)
> и №3 (`bugs/95` DONE 04.09). **6 срезов, 344 строки** перенесены МЕХАНИЧЕСКИ скриптом со сторожем
> границ (каждый срез начинается ровно на своём заголовке, кончается ровно перед следующим
> названным, несёт ровно один заголовок верхнего уровня и ни одной чужой сессии; сумма сверена:
> 1419 + 344 = 1763). Перенос сделан НЕ в последние минуты, сторож написан до него.
> **1763 → 1418 строк.**
>
> ✂️ **Закрытие сессии 79 (2026-09-04 02:2x, `/end-chat-soft`): стрижки НЕ БЫЛО, и это названо, а не
> забыто.** Условия всех названных кандидатов (блоки сессий 76 и 77, раздел `bugs/91`) — события при
> владельце у машины, и ни одно не наступило: сессия была офлайн. Добавлен блок сессии 79 (~90 строк).
> **1418 → 1510 строк.** Следующая стрижка — по тем же кандидатам, когда наступят их условия.
>
> ✂️ **Закрытие сессии 81 (2026-09-04 12:5x, `/end-chat-soft`): стрижки НЕ БЫЛО, и это названо.** Условия
> всех прежних кандидатов — события при владельце у машины, а сессия была офлайн; добавлен блок сессии 81.
> **1510 → 1685 строк.** Два новых кандидата, чтобы не выбирать заново: **блок сессии 79** — когда виджет
> кривой получит глаз владельца на живом окне (E26-AC6) и живого свидетеля; **блок сессии 80** — когда
> владелец решит развилку про уборку окна показа (хук `Stop` → `tidy --apply`).
>
> ✂️ **Закрытие сессии 82 (2026-09-04, день): стрижки НЕ БЫЛО, и это названо.** Условия всех кандидатов —
> события при владельце у машины; сессия офлайн. Добавлен блок сессии 82. **1685 → 1809 строк.**
>
> ✂️ **Стрижка 2026-09-04 22:3x (закрытие сессии 84): БЛОК СЕССИИ 83 УНЕСЁН ЦЕЛИКОМ** — **99 строк**
> перенесены МЕХАНИЧЕСКИ, скриптом с шестью проверками границ (срез начинается ровно на своём
> заголовке · ровно ОДИН заголовок верхнего уровня · ни одной чужой сессии · срез несёт свою
> эстафету, значит взят целиком · следующая строка — заголовок · срез не пуст и не абсурден);
> сумма сверена скриптом: **2001 + 99 = 2100**. Условие названо и наступило: обновление KAIF 2.5
> закрыто, его запись живёт в `KAIF_FRAMEWORK.md` и в отчёте, отчёт и тикет уехали в исток
> (origin #41, #40), а эстафета блока — «офлайн первым `bugs/104`» — исполнена этой сессией, тикет
> закрыт тегом `DONE`. **2100 → 2001 строк.**
>

> ✂️ **Закрытие сессии 83 (2026-09-04, вечер): стрижки НЕ БЫЛО, и это названо.** Условия кандидатов
> прежние — события при владельце у машины, сессия офлайн. Добавлен блок сессии 83. **1809 → 1916 строк.**
> 🆕 **С KAIF 2.5 долг стал МЕХАНИЧЕСКИМ, а не только названным:** `node .kaif/kaif-core.mjs check`
> печатает бюджет строк для всех девяти перечитываемых документов и предупреждает выше него —
> сегодня `STATUS.md 1916/~200`, `GOAL.md 1958/~300`, `AGENT_GUIDE.md 1655/~1200`,
> `MASTER_PLAN.md 466/~300`, `REQUIREMENTS_FRAMEWORK.md 251/~250`. Это предупреждение, не отказ;
> лечение прежнее — уносить в хронику, а не поднимать число.
>
> 🧹 **ТРИ БЛОКА «ПРЕЖНЕГО ДОЛГА» (сессий 69, 74 и старый на ~660 строк) УБРАНЫ ЗДЕСЬ 2026-09-01,
> И ЭТО НЕ ПОТЕРЯ, А СНЯТИЕ ПАРЫ.** Они стояли «для сверки» и называли кандидатов, которые с тех
> пор УНЕСЕНЫ: раздел «ПЕРВОЕ СВИДЕТЕЛЬСТВО» — сессией 75, блок сессии 73 вместе с предполётным
> листом 20:00 и сухим прогоном полосы вечера — сегодня. То есть документ в трёх местах называл
> три разных числа долга и указывал на строки, которых в файле уже нет. Это ровно та пара
> «сводка ↔ документ», за которую проект платил трижды (`interviews/008`, `interviews/013`,
> `bugs/40`), а правило DRY велит пару УБРАТЬ, а не сторожить. Дословный текст цел в истории git;
> действующая цифра долга и действующие кандидаты — ровно один абзац выше.
>
> 🟡 **Кандидаты, ждущие не времени, а СОБЫТИЯ** (перенесены сюда из убранных блоков, потому что
> событие не наступило): **вся ветка про потолки как ручку режима** · **факт 28** · **таблица
> доступного андервольта** · **матрица размена факта 27** — все четыре ждут **закрытия эпика 41**,
> после которого «сколько андервольта доступно» перестанет быть арифметикой и станет измеренной
> таблицей.
>
> ✂️ **Стрижка 2026-09-08 (закрытие сессии 89, `/end-chat-soft`): УНЕСЕНЫ СЕССИИ 86 И 87 ЦЕЛИКОМ И
> РАЗДЕЛ «ПРИ ВЛАДЕЛЬЦЕ У МАШИНЫ» — 516 строк**, перенесены МЕХАНИЧЕСКИ, скриптом с шестью
> проверками границ. **2715 → 2199 строк; сумма сверена скриптом: 2199 + 516 = 2715.**
> Условия названы и наступили: живой прогон 07.09 состоялся и его эстафету исполнила сессия 88 ·
> раздел «ПРИ ВЛАДЕЛЬЦЕ У МАШИНЫ» стал ПАРОЙ к более новому порядку в эстафете сессии 88 и убран
> как пара, а не как прошлое · **тема микрофризов ЗАКРЫТА** подтверждением глаза владельца
> (`researches/34` §10).
> **Сторож ОТКАЗАЛ на обеих пробах ДО переноса:** конец среза смещён на сессию 87 — «заголовков
> верхнего уровня 2, а не 3»; начало смещено на сессию 85 — «4, а не 3».
> 🟢 **И перед резкой ТРИ РЕШЕНИЯ ВЛАДЕЛЬЦА подняты из среза в раздел «Решено владельцем — не
> пересматривать»** (граница его вопросов · шесть попыток спасения · гашение фона), иначе они уехали
> бы в прошлое вместе с блоком. Ровно за этим стрижка и делается руками, а не одним `sed`.
>
> 🟡 **ОСТАТОК ДОЛГА — ОДНО ЧИСЛО И ОДНО МЕСТО: 2199 строк против ориентира ~200** (после этой
> стрижки; актуальное число всегда в последней строке стрижки, потому что только она его меряет).
> Здесь стояли ДВА блока долга с РАЗНЫМИ числами — 2001 и 1685, оба устаревшие. Это та же пара
> «сводка ↔ документ», которую файл уже снимал 01.09, и она отросла заново. Снята повторно.
>
> **Кандидаты, ждущие не времени, а СОБЫТИЯ при владельце у машины:** блок сессии 76 (потолок
> `Silent Cold` по оборотам и живой шаг `plans/84`) · блок сессии 77 (`P83-AC3` учебной границей) ·
> раздел «№2 `bugs/91`» и список шагов `plans/82` (когда разбор `bugs/91` закроется) · блоки сессий
> 79 и 80 (глаз владельца на живом виджете кривой · развилка про уборку окна показа).
> **Новые кандидаты, названные заранее:** блоки сессий 81 и 82 — когда `bugs/91` и `bugs/101`
> закроются; блок сессии 85 — когда AC7 `plans/88` будет взят.
>
> ✂️ **Стрижка 2026-09-09 (закрытие сессии 96): БЛОК СЕССИИ 93 УНЕСЁН ЦЕЛИКОМ** — **139 строк**
> перенесены МЕХАНИЧЕСКИ, скриптом с шестью проверками границ (срез начинается ровно на своём
> заголовке · ровно ОДИН заголовок верхнего уровня · ни одной чужой сессии · срез несёт СВОЮ
> эстафету, значит взят целиком · следующая строка — заголовок сессии 94 · размер не абсурден);
> **сумма сверена скриптом: 2208 + 139 = 2347.** Условие названо и наступило: эстафета сессии 93
> исполнена целиком сессиями 94 и 95 (ворота `P94-AC2` взяты живым прогоном, сторож такта
> `bugs/128` AC3 в батарее), а её главная находка — потеря разрешения таймера под EcoQoS — живёт
> теперь в `bugs/128` и уроком **EXP-0269**, то есть в срезе осталась её ПАРА, а не она сама.
> **Сторож ОТКАЗАЛ на ОБЕИХ пробах ДО переноса** (конец сдвинут на сессию 95 — «заголовков 2, а не
> 1»; начало сдвинуто на сессию 92 — то же), и написан он был раньше резки, а не после.
> 🟢 **И перед резкой ИЗ СРЕЗА ПОДНЯТО РЕШЕНИЕ ВЛАДЕЛЬЦА** — «окно наблюдения не поднимается
> впустую» (*«ну так закрой визуализатор, если нет прогона !!!!!!!!!!!»*), иначе оно уехало бы в
> прошлое вместе с блоком; теперь оно в разделе «Решено владельцем». Ровно за этим стрижка делается
> руками, а не одним `sed`. **2347 → 2208 строк** против ориентира ~200 — долг назван, а не спрятан.
>
> ✂️ **Стрижка 2026-09-09 (закрытие сессии 97): БЛОК СЕССИИ 85 УНЕСЁН ЦЕЛИКОМ** — **150 строк**
> перенесены МЕХАНИЧЕСКИ, тем же скриптом с шестью проверками границ; **сумма сверена скриптом:
> 2239 + 150 = 2389.** Условие названо и наступило: **оба интервью эстафеты сессии 85 закрыты
> владельцем** (`interviews/028` снято им 07.09 как не его уровень; `interviews/027` отвечено), три
> её тикета закрыты, батарея с тех пор выросла. **Сторож ДОКАЗАН КРАСНЫМ на ЧЕТЫРЁХ пробах ДО
> переноса** — старт вниз на 1 («срез начинается не на своём заголовке · заголовков 0, а не 1») ·
> старт вверх на 1 · конец вниз на 1 («заголовков 2, а не 1 · чужих сессий 1») · конец вверх на 1
> («следующая строка не заголовок»), и пропустил ТОЛЬКО точные границы.
> 🟢 **И перед резкой ИЗ СРЕЗА ПОДНЯТ ЖИВОЙ ДОЛГ** — приёмка эпика 03 (`bugs/25` пункт 1, не
> начата), иначе она уехала бы в прошлое вместе с блоком; теперь она в эстафете сессии 97. Ровно за
> этим стрижка делается руками. **2389 → 2239 строк** против ориентира ~200 — долг назван, а не
> спрятан; следующие кандидаты — блоки сессий 88 и 92, когда их эстафеты будут исполнены.
>
> ✂️ **Стрижка 2026-09-18 (закрытие сессии 100): БЛОКИ СЕССИЙ 94, 95, 96 И 97 УНЕСЕНЫ ЦЕЛИКОМ** —
> **453 строки** (452 строки блоков + пустая строка после них), механически, скриптом со сторожем границ (ровно один заголовок начала и один
> заголовок конца, начало раньше конца, перенесённый срез найден в летописи побайтно). Условие
> наступило: эстафеты четырёх сессий исполнены или перекрыты эстафетами 98–100. 🟢 **Перед резкой
> ИЗ СРЕЗА ПОДНЯТЫ живые долги**, которых эстафета 98 не подхватила — `bugs/129`, `bugs/127`, `bugs/132`
> AC5, протокол записи 3, фазы 3–4 эпика 95, безопасный режим дисков без тикета, — они теперь в
> эстафете сессии 100, п. 1а. **2068 → 1615 строк** (1944 было на закрытии сессии 99, блок сессии 100
> и п. 1а добавили 124; ✏️ здесь сперва стояло «1944 → 1615» при «452 строках» — два разных момента,
> склеенные в одну стрелку; поймано судьёй закрытия) против ориентира ~200 —
> долг назван, а не спрятан; следующие кандидаты — блоки сессий 98 и 99, когда их эстафеты
> (метод поиска края, эксперимент №2, `plans/99` Ш5–Ш6) будут исполнены при владельце.

---

## 🔤 ЯЗЫК ПРОЕКТА — СЛОВО ВЛАДЕЛЬЦА 2026-08-15, ЧИТАТЬ ДО ВСЕГО ОСТАЛЬНОГО

> *«МЫ ПРЕКРАЩАЕМ НАЗЫВАТЬ ТОЧКИ НОМЕРАМИ. МЫ НАЗЫВАЕМ ТОЧКИ ЧАСТОТОЙ. И того, что куда-то какая-то
> точка уплыла — такого больше не существует в нашем понимании. Они никуда не уплывают. Карта хочет
> сменить частоту — она устанавливает новую частоту, мы обслуживаем её соответствующим напряжением.
> Всё. Нет никаких "точка 120". Есть только частоты по сетке частот.»*

Полностью с разбором — `GOAL.md` → «🔤 ТОЧЕК С НОМЕРАМИ НЕ СУЩЕСТВУЕТ». Три замены:

| Изъято | Говорим |
|---|---|
| «точка 95», «точка 120» | **частота** — «2842 МГц» — и «напряжение, обслуживающее 2842 МГц» |
| «кривая уплыла», «точка переехала» | «при 57 °C та же частота требует больше напряжения» |
| «сдвиг точки» как хранимая величина | хранится **частота → напряжение**; смещения СЧИТАЮТСЯ при применении от живого чтения |

**Это исправление по существу, а не по языку.** Старая рамка делала из записи таблицы объект, который
куда-то едет, и породила проход пересчёта, чтобы за ним гоняться. В новой рамке артефакт устойчив:
«частота → напряжение» от температуры замера не зависит, а зависят смещения — и они не хранятся.

⚠️ **Числа ниже, снятые ДО 2026-08-15, в силе — переформулирована только рамка.** Закрытое прошлое
(`PROJECT_HISTORY.md`, `bugs/*`, планы эпика 01) сохраняет свои формулировки: оригинал не
переписывают под сегодняшний словарь.

---

## Где мы сейчас

> 🔤 **РЕГИСТР ЯЗЫКА — СЛОВО ВЛАДЕЛЬЦА 2026-08-15 21:1x, ДЕЙСТВУЕТ ВЕЗДЕ:** *«мы тут не прозу пишем, а
> серьезный инструмент, и пользуемся академическим и научным языком»*. Заимствования и транслитерации
> не термины: есть русское название — оно и есть название. Разговорные синонимы отклоняются, даже
> когда их предлагает сам владелец. Полностью — `AGENT_GUIDE.md` → «THE REGISTER».
>
> 🆕 **НОВЫЙ СТОРОЖ В ВОРОТАХ СБОРКИ 2026-08-15 21:0x: `npm run check` ловит ПОРЧУ КОДИРОВКИ.**
> Сканирует все `.mjs/.json/.md/.ps1/.cu` на последовательности, которые даёт UTF-8, прочитанный как
> windows-1251 (одна буква становится двумя, текст нечитаем, а код при этом продолжает парситься).
> Родился из ТРЕТЬЕГО срабатывания одного класса за день (EXP-0067): агент прогнал файл через
> `Get-Content | Set-Content`, русский текст испортился, а `node --check` остался зелёным.
> Доказан красным: `node tools/check.mjs --selftest-encoding`.

| Фаза | Состояние | План |
|------|-----------|------|
| 0 — Развёртывание KAIF + исследования | ✅ закрыта | `PROJECT_HISTORY.md` |
| 1 — Стенд и базовая линия | ✅ закрыта 2026-08-10 | `plans/02_epic01_phase1_harness_and_baseline.md` |
| 2 — Silent Cold на `nvidia-smi` | 🟡 приостановлена · §4.1–§4.5 закрыты; §4.6–4.7 ждут владельца | `plans/03_epic01_phase2_silent_cold.md` |
| **ЭПИК 02 · фаза 1 — сетки и документ кривой** | ✅ **ЗАКРЫТА 2026-08-15 21:2x, судья: СВЕРЕНО С ОГОВОРКАМИ.** Ворота фазы 2 открыты | `plans/14_DONE_epic02_phase1_grids_and_curve_document.md` §9 |
| **ЭПИК 02 · фаза 2 — движок развёртки** | ✅ **ЗАКРЫТА: девять шагов 4.1–4.9 исполнены 2026-08-16 01:1x, судья прошёл 2026-09-05 — СВЕРЕНО С ОГОВОРКАМИ, расписка в `plans/15_DONE` §9.** ✏️ Здесь стояло «ЗАКРЫТА 00:5x, судья: СВЕРЕНО С ОГОВОРКАМИ» — **расписки тогда не существовало**, а названное время было РАНЬШЕ исполнения последнего шага; судья это заявление опроверг (`bugs/25`). Развёртка есть как команда, стенд гоняет её без единого «ждёт», записей в GPU за фазу ноль. Ворота фазы 3 открыты | `plans/15_epic02_phase2_sweep_engine.md` |
| **ЭПИК 02 · фаза 3 — ПЕРВЫЙ ЖИВОЙ ПРОГОН** | 🟡 **ГОТОВА К ЗАПУСКУ, НЕ ЗАПУЩЕНА.** Сухой прогон на живой карте прошёл ВПЕРВЫЕ (код 0), репетиция заказа владельца на виртуальной карте прошла целиком. Записей в GPU по-прежнему ноль | ворота — `plans/13` §4 · дашборд — `plans/20` · форма записи — `bugs/12` |
| **Дашборд прогона и репетиция на виртуальной карте** | 🟢 **ИСПОЛНЕНО 2026-08-16 04:0x, облик принят владельцем живьём («Суппер! Мне нравится дашборд!»).** Открыт один шаг — 4.8, учебная тревога зависания | `plans/20_epic02_phase3_dashboard_on_the_bench.md` |
| 3 — Четыре ярлыка + трей | ✅ **ЗАКРЫТА 2026-08-14, судья: СВЕРЕНО С ОГОВОРКАМИ · один открытый прибор — серия P3-AC2 (0 из 5 естественных входов, журнал копится сам)** | `plans/06_DONE_epic01_phase3_shell.md` |
| 4 — Свой мост к NVAPI | ✅ закрыта 2026-08-10, судья: ПОДТВЕРЖДЕНО С ОГОВОРКАМИ | `plans/04_epic01_phase4_nvapi_bridge.md` |
| 5 — Движок поиска Vmin | 🔴 **МЕТОД ОТМЕНЁН 2026-08-15, работа переехала в ЭПИК 02** | `plans/05` (что переиспользуется — там же в шапке) |
| 6 — ЧЕТЫРЕ режима + приёмка | 🔴 **ПЕРЕОПРЕДЕЛЕНА, переехала в фазу 5 эпика 02** | `plans/13` |
| **ЭПИК 02 — поиск края по ВСЕМУ диапазону** | 🟢 **РАСПЛАНИРОВАН 2026-08-15, фаза 1 исполнена, фаза 2 ждёт стенда** | **`plans/13_EPIC_edge_finder_full_range.md`** |
| **ЭПИК 67 · фаза 4 — ПОЛИГОН** | ✅ **ЗАКРЫТА 2026-08-29 23:0x, все восемь шагов.** Пакет 30/30 чисто за 24,3 мин, покрытие по шести осям, ворота E67-AC5 взяты (обычная карта бит-в-бит), три ловушки краснят 3 из 3. Открытым уходит P71-AC8 наполовину — сложение двух причин на одной карте недостижимо структурно, причина названа | `plans/71_DONE_epic67_phase4_polygon.md` §Шаг 8 |
| **ЭПИК 03 — виртуальная видеокарта как стенд** | ✅ **ЗАКРЫТ ЦЕЛИКОМ 2026-08-15 20:5x — все три фазы.** Карта, придуманный край, пять ловушек, контрактный набор с двумя колонками, измерение движка. Ноль записей в GPU за весь эпик | **`plans/16_EPIC_virtual_gpu_bench.md`** · фазы `17` · `18` · `19_DONE` · разведка `researches/10` |
| **ЭПИК 26 — визуализатор кривой** | 🟢 **фаза 1 закрыта словом владельца 31.08 (три его правки статической карты) · фаза 2 — MVP исполнен 2026-09-04 (сессия 79): виджет «КРИВАЯ» в окне наблюдения, ждёт его глаза · фаза 3 — первый срез, рендер 4K** | **`plans/26_EPIC_curve_visualiser.md`** · оперплан `plans/85` |

**Шесть вещей, которые определяют всё остальное:**

1. **Форма профиля найдена, измерена и она ровно та, которую задал владелец** — кривая поднята,
   выше потолка ничего не предлагается, **фиксации частоты нет вовсе** (факт 15).
2. **Первая измеренная выгода в этой форме: −7,71 Вт (−5,6 %) и −5 °C при ТОЧНО стоковой частоте**,
   16 блоков из 16 зелёных, откат чист (факт 15). Одна пара прогонов — двух серий ещё нет.
3. **🔴 Вся база по стабильности снята при ПОЛОВИННОМ конверте карты** (137 Вт против игровых 300 —
   факт 16). Прибор для настоящей области ЕСТЬ и опора по нему снята: сток под игрой **56,42 FPS ·
   299,85 Вт · 75,5 °C · вентилятор 70,5 %**, пол прибора FPS между запусками **0,90 %**. Осталось
   прогнать под этой нагрузкой сами режимы.
4. **🔴 «КРАЙ НАЙДЕН» ОТМЕНЁН: 885 мВ никогда не было на карте** (факт 28, `bugs/02`). **Края этой
   карты проект по-прежнему не знает.** И оба «нащупанных кандидата в режимы» — не кандидаты по
   оборотам: они сняты с транзиента (факт 35); ватты и кадры в силе, обороты — нет.
   ⚠️ **НЕ ПЕРЕПУТАЙ ДВА РАЗНЫХ ЧИСЛА 885:** здесь — ОПРОВЕРГНУТОЕ значение 2026-08-11, посчитанное
   при подъёме ОДНОЙ точки. Позже, 2026-08-15, третий живой заход **измерил** 885 мВ уже в
   отгружаемой форме (вся кривая + потолок 2842) — это другое число, полученное другим способом, и
   оно в силе. Совпадение цифр случайно.
5. **🌡 T(40 %) ИЗМЕРЕНА НА ПЛАТО И РАВНА 59 °C** (факт 36) — это разблокировало `Silent Cold`:
   на стоковой кривой он читается прямо с лестницы, **потолок 2100 МГц**. Это НИЖНЯЯ оценка.
6. **Оболочка стоит живьём у владельца:** 4 ярлыка (его имена и объёмные иконки) + 7 задач
   планировщика + пассивный трей + расписка с откатом каждого артефакта
   (`npm run setup -- --uninstall`). Черновики режимов ОТКАЗЫВАЮТ до фазы 6. **Единственный
   открытый прибор: серия P3-AC2** — 5 естественных входов в систему, копится в
   `runs/shell/boot-apply.jsonl` сама. Пятая запись закрывает AC2 эпика.

## Что работает на диске (проверено прогоном, не заявлено)

> **Полная таблица стенда — `AGENT_GUIDE.md` → Test harness.** Здесь только то, что понадобится
> в первый час.

| Команда | Что она ДОКАЗЫВАЕТ |
|---|---|
| **`npm run selftest:all`** | 🆕 **ВСЯ ОФЛАЙН-БАТАРЕЯ ОДНОЙ КОМАНДОЙ — 26 наборов, 1184 зелёных блока, ~14 с.** (2026-08-23 17:0x: вошли СРАЗУ ШЕСТЬ — `events` 11 · `tidy` 13 · `fanladder` 34 · `bench` 4 · `shortcuts` 5 · `thermal` 55 — и это ЗАКРЫТИЕ КЛАССА, а не добавка. `events` нашёлся случайно, при попытке положить в него сторожа; после этого была снята полная опись всех модулей с режимом `--selftest` против списка батареи, и расхождение оказалось не в один набор, а в ШЕСТЬ: **батарея не звала четверть собственных наборов проекта, 122 блока не считал никто.** Пятеро из шести стояли в строке «вне батареи и намеренно» ниже — их общая причина разобрана в коде рядом с записью, а не обойдена. Трудным случаем был `thermal`: он ЕДИНСТВЕННЫЙ держит писателей в области видимости, и его доказательство снято АДРЕСАМИ СТРОК — ветка `--selftest` возвращает на :878, а все три места вызова писателей стоят ниже, на :949 и :907/915. **Снаружи остался ровно один набор, `nvml --verify-decode`, и он ПИШЕТ В КАРТУ — то есть исключён правильно и навсегда.**) (2026-08-23: вошёл `pulse` — 28 блоков — вместе с самим кодом, а не «потом».) (2026-08-22: вошёл `dashboard` — 39 блоков — по правилу самого списка «по одному, с доказательством инертности рядом с записью»; до того его блоки не гонял никто, и в них пять дней жил `bugs/27`.) Ни одной записи в видеокарту; карта может быть занята. Каждый набор судится ТРЕМЯ чтениями сразу: код выхода · красные строки во всех четырёх словарях батареи (`ПЛОХО`/`ПРОВАЛ`/`FAIL`/`❌`) · сводная строка ОБЯЗАНА быть на месте (набор, умерший на импорте, тоже «не печатает провалов»). Расхождение двух чтений — само по себе КРАСНЫЙ. **Числа самопроверок в этом файле обновляются ТОЛЬКО её выводом** (находка №2 аудита, `bugs/24`). `-- --only engine,vgpu` — подмножество · `-- --selftest` — собственный сторож батареи, **6 блоков** (F — улика красного набора, `bugs/102`) · вывод КРАСНОГО набора с 04.09 сохраняется в `runs/selftest/<штамп>/<набор>.log` |
| `npm run check` · `phase1:accept` | все `.mjs` парсятся (**40 файлов**, 0 провалов) плюс сканирование **234 текстовых файлов** на порчу кодировки, испорченных 0 — перемерено 2026-08-15 23:2x · вся приёмка фазы 1 одной командой |
| **`npm run journal -- --selftest`** | **ЖУРНАЛ УПРЕЖДАЮЩЕЙ ЗАПИСИ, песочница, карту не трогает.** `runs/sweep/journal.jsonl`: намерение тронуть карту пишется и **`fsync`-ится ДО первого байта в GPU** — обычная запись возвращается из кэша страниц ОС, а зависание уносит кэш с собой. **Намерение без вердикта на следующем запуске И ЕСТЬ ответ:** ступень закрывается как `ЗАВИС` и приписывается своей частоте и напряжению. Ключ — ЧАСТОТА + НАПРЯЖЕНИЕ, не индекс. Единственная аварийная остановка — **два зависания ПОДРЯД** на одной ступени. **17 блоков + 7 блоков проводки в движке** |
| **`npm run curve -- --grids`** | **СЛОВАРИ КАРТЫ, только чтение.** 127 ступеней напряжения (450…1240 мВ, шаг 5 мВ ×94 и 10 мВ ×32 — сетка НЕРАВНОМЕРНА) · 389 частот (180…3090 МГц, шаги 7 ×194 и 8 ×194). Негодный словарь НЕ записывается |
| **`npm run curve -- --init` · `--show` · `--verify`** | **ТЮНИНГ-КРИВАЯ, только чтение.** 389 строк «частота → обслуживающее напряжение → статус → дата». `--verify` сверяет СЕТКУ напряжений (то, что не движется); стоковые напряжения частот не сверяются намеренно — прогретая карта требует больше |
| 🆕 **`npm run curve -- --snapshot [--from <документ>]`** | **БОЕВОЙ СНИМОК, карту не трогает** (`plans/99`, эпик 98 фаза 1, 2026-09-13). Замораживает рабочий документ в `curves/battle/<момент>.json`: тело документа + момент + SHA-256 байтов источника + покрытие «краёв X из 389» в момент снятия. **Существующий снимок не перезаписывается никогда** (жёсткая ссылка, отказ называет файл). Первая строка вывода — «⚠️ СНЯТО БЕЗ ВОРОТ ПЕРЕДАЧИ»: ворота — фаза 2. Профиль указывает на снимок полем `curveSnapshot` (обязательный ключ; `curveRef` для рабочих режимов устарел с 13.09). Снят первый: `2026-09-13T20-25-42` |
| **`npm run curve -- --selftest`** | **40 блоков** без карты (число перемерено прогоном 2026-08-15 21:2x). Заголовок набора называет **12 адресатов мутаций ДО прогона**; сверх них судья прогнал **4 независимые мутации** — потолок R13 · закрытый словарь статусов · атомарная запись · напряжение вне сетки карты, — и каждая покраснила ровно свой блок, а целый код не покраснил ни одного |
| **`npm run vgpu -- --show <карта>` · `--derive` · `--selftest`** | **ВИРТУАЛЬНАЯ ВИДЕОКАРТА, целиком офлайн — карту не трогает вовсе.** Геометрия выведена из `curves/*.json` командой; придуманный край на каждой из 389 частот с ШУМОМ (дрожь ±8 мВ + дрейф ±20, зерно 20260815); вероятностный отказ (одна ступень 5 мВ = скачок 0,20 → 0,80); три исхода, где `ЗАВИС` — настоящая смерть процесса. **63 блока, 16 мутаций + 4 независимые мутации судьи, все пойманы.** Отказы кривой — та же функция, что у живого бэкенда, а не копия |
| **`npm run traps`** | **НАБОР ЛОВУШЕК, офлайн.** Пять карт-ловушек (`benches/cards/traps/`), выводятся командой `vgpu -- --derive-traps`. Гоняет **настоящие `engine.searchEdge` И `engine.sweepRange`**. Главное число: на T1 правильный движок встаёт за **2 ступени, неправильный проходит 13**. **28 утверждений, ждущих НОЛЬ** — четыре утверждения класса B прогнаны шагом 4.5 |
| **`npm run contract`** | **КОНТРАКТНЫЙ НАБОР над тремя швами, две колонки.** Виртуальная прогнана, живая — «не прогонялась», и зелёной от виртуального прогона не становится. Плюс блок «ноль веток „мы на моке“» (22 файла движка) и блок «ЧАСЫ» |
| **`npm run measure`** | **СТЕНД МЕРЯЕТ ДВИЖОК против оценки `researches/09` §4.** Три рода строк, и они не смешиваются: ЗАМЕРЕНО · ПОСЧИТАНО · ЖДЁТ. Итог: **оценка 1,7 ч выдержала (+11 %)**, и **три частоты из шести не искались вовсе** — ниже пола 2157 МГц отгружаемой формы записи нет (факт 38, воспроизведён замером) |
| **`npm run bench -- --from 3090 --to 180 [--max-depth N] [--sustain N]`** | **РЕПЕТИЦИЯ ПЕРЕД ЖИВЫМ ПРОГОНОМ, офлайн.** Настоящий `sweepRange` по виртуальной карте, с настоящим журналом в своей песочнице и документом кривой В ПАМЯТИ. Прожиги **УСКОРЕНЫ В 10 РАЗ ПО УМОЛЧАНИЮ** (слово владельца, `ideas/04`: 1 с вместо 10) — и паузы между ступенями тоже, иначе меняется скважность. Несёт ТЕ ЖЕ ограничения, что живой прогон (`--max-depth`, закрепление, конверт), иначе репетировалась бы другая работа. Кормит дашборд |
| **`npm run dashboard`** | **ОКНО НАБЛЮДЕНИЯ, только чтение.** Отдельный процесс на `127.0.0.1`, отдаёт страницу и поток событий; **своё окно Edge в своём профиле — умеет его закрывать** (`--close`). Ни карты, ни журнала, ни документа не касается |
| **`npm run pulse`** · `-- --all` | **ПУЛЬС СЭМПЛЕРА — ЧЕТВЁРТОЕ НАБЛЮДЕНИЕ, только чтение.** Раскладывает интервалы между пробами телеметрии по ступеням журнала. На роковом прогоне 2797 МГц: 865 · 860 · 850 мВ дают 0,11-0,13 с сверх обещания, **845 мВ — 4,49 с**, а оракул на всех четырёх сказал PASS. **ПОРОГА НЕТ И ВЕРДИКТА НЕТ** — печатает распределение (`ideas/10` §5.1, n = 1). `--all` считает ворота фазы 2. **24 блока, 11 мутаций** |
| 🆕 **`npm run hold`** | **ДЕРЖАЛАСЬ ЛИ ЧАСТОТА — замер по 391 прожигу, только чтение, карта может быть занята.** Читает пробы сэмплера из `runs/vmin/` (имя файла несёт держателя, заказанную частоту и подъём) и считает **ПРЕВЫШЕНИЕ над заказом** — величину, которую разгон карты подделать не может, в отличие от размаха. Заведён под шаг 1 эстафеты `bugs/86`, **потому что журнал на этот вопрос ответить не может (`bugs/87`)**. Результат: выше заказа больше чем на шаг сетки ушли 9 прожигов из 391, из них восемь на 15–23 МГц и один — на 690. **12 блоков, 3 мутации; контроль привязан к ЧУЖОМУ числу** — строке, напечатанной сторожем движка в `runs/sweep-session70.log:51` |
| 🆕 **`npm run entryguard`** · `-- --selftest` · `-- --freeze` | **СТОРОЖ ВХОДА ПРИБОРОВ `tools/*.mjs` (`bugs/95`), только чтение, шестые ворота `npm run check`.** Каждый прибор обязан нести сравнение `process.argv[1]` ↔ `import.meta.url`; иначе его импорт исполняет чужую работу с argv вызывающего (так `tidy` снимал окна владельца, `grant-agent-*` писали его файл прав, цикл импорта пересобрал бинарники прожига). База долга `decisions/entry-guard-baseline.json` только убывает — починенный прибор, оставшийся в базе, тоже красный. **7 блоков**, мутация красит своё; 2026-09-04: 34 прибора, без сторожа 0 |
| 🆕 **`npm run backlogtruth`** · `-- --report` · `-- --selftest` · `-- --freeze` | **СОГЛАСИЕ ИМЕНИ ФАЙЛА И СТРОКИ СТАТУСА (`bugs/25` п.4), только чтение, ВОСЬМЫЕ ворота `npm run check`.** Сверяет ровно ДВА сигнала, по которым считается беклог: тег `_DONE_` в имени и строку `**Статус:**` документа. 🔴 **Он НЕ решает, закрыт предмет** — урок сессии 65 («маркер ✅ чаще относится к ОДНОМУ ШАГУ, а не к предмету»): разошлись — находка, кто прав, решает разбор. Словари ОБОЮДНЫЕ: строка «фазы 1 и 2 закрыты, открыта фаза 3» законна. База долга `decisions/backlog-truth-baseline.json` — **25 расхождений на 07.09**, только убывает. **11 блоков**, доказан красным с обеих сторон (новое расхождение · устаревшая строка базы). На дне: **12 файлов с тегом `DONE`, чья строка статуса говорит «ОТКРЫТ»** — исходная таблица `bugs/25` от 21.08, скопом не переписаны намеренно (тег не всегда прав) |
| 🆕 **`npm run curvemap`** · `-- --selftest` · **`node tools/build-curve-map.mjs [--png <файл>]`** · виджет «КРИВАЯ» в `npm run dashboard` | **КАРТА КРИВОЙ — ОДИН РЕНДЕРЕР НА ДВЕ ПОВЕРХНОСТИ (`plans/85`), только чтение.** Факты — из документа и журнала через его ЧИСТЫЕ читатели (`provenRungs` · `hangFloors` · `corrections`; никогда `resumeState`): сток · наша эффективная линия (весь документ, пустота при нуле замеров) · прошедшие прожиги · полы ДВИЖКА · снятые им зависания (полым кругом) · ступень под тестом со следом от стока. Без флага печатает счёт по боевым файлам; `--png` — PNG ровно 3840×2160 браузером владельца (заголовок PNG + `magick identify`). Сервер окна отдаёт ту же картинку по `GET /curve.svg?mhz&mv&stock`; пути документа и журнала — швы `--curve`/`--journal`, двойник и репетиция передают СВОИ (E26-AC3). Превью страницы: `node tools/build-dashboard-page.mjs --preview --pulse <файл> --widget curve`. **25 блоков**, 7 адресатов мутаций названы в шапке; окно — 87 блоков (было 76) |
| `npm run gpu:info` · `mon -- --once` | карта, её конверт и лестница частот · телеметрия одной пробой |
| `npm run stress -- --workload N --sustain N` | вердикт PASS/SDC/CRASH/НЕИЗВЕСТНО. Формы: `--transient`, `--lowload`, `--sustain` |
| `npm run stress -- --capture-baseline` · `--verify-baseline` | снять эталоны на стоке · проверить, что штампы ещё сходятся с картой (R6) |
| `npm run prove:gradient` | градуированный оракул МЕРЯЕТ: переворот одного бита опознан при совпавшей контрольной сумме |
| `npm run nvapi` · `-- --curve` · `-- --fans` | **только чтение.** 17 ids · 128 точек кривой · кулеры и пол |
| `npm run nvml` | **только чтение.** Драйвер, имя, сдвиг и РАЗРЕШЁННЫЙ ДИАПАЗОН по доменам |
| **`npm run vfstep -- --shape --mhz 45 [--cap-at N]`** | **ПИШЕТ под сторожем, БЕЗ фиксации частоты.** Форма профиля целиком. `--dry-run` — план без записи |
| `npm run nvapi -- --fan-write 80 [--cool-to 42]` | **ПИШЕТ (вентиляторы), под сторожем.** Откат в AUTO в `finally`. Только вверх |
| **`npm run gfx -- --prove-not-capped`** | **ВОРОТА игрового стенда, прогоняются ПЕРВЫМИ.** Пройдено 2026-08-15 на столе 3840×2160: **+42,05 %** (раньше +46,03 % на 1280×720 — геометрия входит в замер) |
| **`tools/bin/PresentMon-2.5.1-x64.exe --process_name <РИСУЮЩИЙ exe> --output_file <csv> --session_name kago-<метка> --stop_existing_session --terminate_on_proc_exit`** | **СЧЁТЧИК КАДРОВ, только чтение.** Цель — `*-Win64-Shipping.exe`, а НЕ лаунчер (EXP-0064). Метка обязательна: фиксированный путь затирает сырьё. Гасить сессию в `finally`: `logman stop <имя> -ets` |
| `npm run gfx -- --run` · `--capture --label <м>` · `--spread <м>` | таймдемо Q2RTX с разбором FPS · телеметрия и окно журнала · разброс прибора. **PASS не выдаёт никогда** |
| **`npm run engine -- --search --cap N`** | **ПОИСК КРАЯ через атом, в ОТГРУЖАЕМОЙ форме** (вся кривая + потолок, лестница по напряжению). Восхождение → вилка → бисекция. Точка судится НАБОРОМ из трёх форм. НЕИЗВЕСТНО = стоп. **Ниже пола потолка (2157 МГц) ОТКАЗЫВАЕТ: держать потолок там нечем** |
| **`npm run engine -- --sweep --from N --to N --max-depth 150 [--dry-run] [--dashboard]`** | **РАЗВЁРТКА ПО ДИАПАЗОНУ. ДЕРЖАТЕЛЬ ПОТОЛКА РЕШАЕТСЯ НА КАЖДОЙ СТУПЕНИ И НАЗЫВАЕТСЯ ВСЛУХ:** выше пола потолка (~2157 МГц) держит КРИВАЯ и замка нет; ниже пола — ЗАКРЕПЛЕНИЕ частоты, форма `uniform` (замерено 2026-08-21, `bugs/26`; `demandPin` не по умолчанию — `researches/11`: замок это потолок, а не приказ), верх кривой подрезан на конверте карты (R13). `--max-depth` — потолок глубины от стока, условие прогона, печатается в плане по каждой частоте. `--dry-run` ничего не пишет и журнала не открывает; **прогнан живьём 2026-08-16, код 0**. Отказывает на самом конверте (3090 МГц) |
| **`npm run engine -- --band N,N,… --dry-run`** | **План развёртки, только чтение.** По каждой ступени печатает форму записи и КТО держит потолок — кривая или закрепление (факт 38) |
| **`npm run vfstep -- --set --point N --mhz M --cap C`** | **ОДИН кандидат под НАБОРОМ форм. ПИШЕТ под сторожем.** Порог = ХУДШАЯ форма, и она называется |
| `npm run vmin -- --show` · `--selftest` | храповик фазы 5: что известно про каждую запись таблицы. **Только чтение.** Словарь старый (индексы) — заменяется документом кривой эпика 02 |
| `npm run fanladder -- --period 15` | **ПИШЕТ (вентиляторы), под сторожем.** Акустическая лестница для уха владельца |
| **`npm run thermal -- --points 2100,1800 --runs 40`** | **ТЕПЛОВАЯ ЛЕСТНИЦА. ПИШЕТ (фиксация частоты), под сторожем.** Даёт «потолок → равновесная T → обороты → Вт → FPS» |
| **`npm run thermal -- --analyze`** | **Только чтение, без карты.** Детектор плато по ВСЕЙ записанной телеметрии — пересчитывает таблицу без новых прогонов |
| **`tools/bin/PresentMon-2.5.1-x64.exe --process_name <exe> --output_file <csv> --session_name kago-<метка> --stop_existing_session`** | **ИЗМЕРИТЕЛЬ КАДРОВ, только чтение, игро-независимый** (MIT, 956 КБ, под gitignore; разведка — `researches/08`). FPS считать по **`MsBetweenDisplayChange`**, НЕ по `MsBetweenPresents`. **Гасить сессию в `finally`:** `logman query -ets` · `logman stop <имя> -ets` |
| `npm run watchdog -- --status` · `--recover` · `--drill` | что держит карту · подобрать забытую запись · учебная тревога |
| `npm run questions` · `npm run ask <док>` | сторож места вопросов · поднять контур согласований |
| `node tools/grant-agent-rights.mjs` | **запускает ВЛАДЕЛЕЦ:** выдать агенту консольные права без вопросов (`--show`, `--revoke`). Агент себе их поднять не может — классификатор блокирует |
| **Самопроверки без карты** — ПЕРЕМЕРЕНЫ КОМАНДОЙ `npm run selftest:all` **2026-09-09 18:0x (сессия 96), 53 набора, красных 0, зелёных блоков 2697**, 87,2 с, код возврата 0. Рост 2688 → 2697 объяснён поимённо и целиком: `fuse` 165 → **174** — девять блоков ВХОДА 5 (канарейка, `plans/96` Ш5), и каждый доказан КРАСНЫМ пятью мутациями. Прежний замер: **2026-09-09 01:52 (сессия 93, закрытие чата), 51 набор, зелёных блоков 2653**, 85,2 с. 🆕 Пятьдесят первый набор — `writewatch` **7** (чёрный ящик записи: незакрытый вызов в драйвер опознаётся поимённо, `researches/36`). Рост 2606 → 2653 объяснён поимённо: `fuse` 116 → **150** (эпик 51 фазы 5в–6: проигрыватель `replayRing`, полевые блоки входов, сторож регресса `bugs/130`) · 🆕 `writewatch` **7** · `twin` 30 → **30**, `curve` 122 → **132**, `traps` 68, `stress` 70, `vfstep` 88, `engine` 490 → **497**, `polyguard` 22 → **24**, `dashboard` 87 → **88**. Прежний замер: **2026-09-07 16:2x (сессия 86), 50 наборов, зелёных блоков 2606**, 71,9 с. 🆕 Пятидесятый набор — `backlogtruth` **18** (согласие имени файла и строки статуса, `bugs/25` п.4). Рост против 2565 (утро 07.09) объяснён поимённо: fuse 109 → **116** (bugs/111, эхо собственного спасения) · engine 484 → **490** (bugs/110 покрытие входов + interviews/027 правило череды) · twin 29 → **30** (уставка окна здоровья стенда) · vgpu 122 → **125** (E3-AC3, единица воспроизведения) · traps 65 → **69** утверждений (68 зелёных + одна ЖДЁТ по объявленной причине) (девятая ловушка T9, одна строка ЖДЁТ по объявленной причине). Прежний замер: Рост против 2442 (сессия 78) объяснён поимённо: 🆕 `curvemap` **25** (один рендерер карты кривой, `plans/85`; фикстуры в памяти, боевые файлы не читаются) · `dashboard` 76 → **87** (маршрут `/curve.svg` настоящим запросом над песочными файлами, структура страницы с виджетом, проводка-читатель, пути кривой до сервера). Прежний рост 2417 → 2442 (сессия 78): 🆕 `entryguard` **7** (сторож входа приборов, `bugs/95`; пишет только в свой `mkdtemp`) · 🆕 `probeoffer` **9** (арифметика пробы предложения, `plans/83` Ш6; ветка возвращает до `askCard` и nvapi) · `profile` 74 → **83** (девять блоков репетиции `plans/84` на двойнике). Прежний рост 2386 → 2417 (сессия 76): `engine` 458 → 469 и `drivervoice` 26 → 33 (`bugs/96`), `nvapi` 48 → 58, `profile` 72 → 74, `profiles` 52 → 53 (`bugs/99`) | `check` ✓ · `entryguard` **7** · `probeoffer` **9** · `curvemap` **25** · `engine` **469** · `journal` **73** · `curve` **122** · `contract` 8 · `traps` **65** · `stress` 68 · `vfstep` **85** · `watchdog` **25** · `profiles` **53** · `profile` **83** · `safemode` **19** · `power` 28 · `descend` **44** · `vgpu` **122** · `burn` **10** · `cardgen` **15** · `gfx` 48 · `nvapi` **58** · `vmin` 38 · `dashboard` **87** · `pulse` **33** · `hold` **19** (превышение над заказом против размера подъёма, `bugs/86`/`bugs/87`) · `questions` **11** (область сторожа по тегу DONE, доказана красным, `bugs/74`) · `prayer` **6** · `events` **11** · 🆕 `drivervoice` **33** (шов чтения журнала различает «не прочитали» и «чисто», `bugs/96`) · `tidy` **15** · `fanladder` **34** · `bench` **4** · `shortcuts` **5** · `thermal` **55** · `trayautostart` **8** · `deathwatch` **16** · `fuse` **77** · `teamboard` **7** · `teamplace` **5** · `twin` **23** · `loopguard` **10** · `guardlint` **481** · `contour` ✓ · `polyguard` **22** · `shrink` **8** · `polytraps` **18** · `polygon` **16**. ⚠️ Вне батареи остался ОДИН: `nvml --verify-decode` 5 — и он ПИШЕТ В КАРТУ, то есть исключён правильно и навсегда. 🟡 Долг с адресом: в прогоне 00:11 `dashboard` один раз умер до первой строки (КРАСНЫЙ(0)), отдельно и в двух соседних батареях — зелёный 75; не воспроизведён, подозрение — конкуренция процессов внутри батареи (twin-selftest теперь спаунит `--i1`); чинить только по воспроизведению. **Прежняя общая причина для четверых — «их прогон трогает что-то вне процесса» — разобрана и не выжила:** для `tidy` и `fanladder` она неверна (их набор чистый), а для `bench` и `shortcuts` верна по букве, но они пишут в СВОЮ песочницу и сами печатают её границу («Desktop не тронут», «документ владельца и продакшен-журнал не тронуты»), а по мерке «пишет ли файлы вообще» пришлось бы выкинуть и `curve`, и `journal`, и `vgpu`, и `dashboard`. |

## Живые факты, определяющие решения (нумерация сквозная)

> **Полная редакция каждого факта — `PROJECT_HISTORY.md` → «Стрижка STATUS 2026-08-14 18:4x».**
> Факты 1–14, 19–23, 26, 31, 33 переехали в хронику раньше. Ниже — сжато, только то, что ещё
> ВЛИЯЕТ НА РЕШЕНИЕ. Числа не трогай без нового замера.

15. **🎯 ФОРМА ПРОФИЛЯ: подъём всей кривой + потолок на РАБОЧЕЙ частоте, БЕЗ `-lgc`.** Слово
    владельца: *«карта сама могла и разгоняться и снижать частоты, но работала на пониженном
    напряжении согласно кривой VF профиля»*. Арифметика: `offset_i = min(Δ, cap − F_i)`. Замерено,
    обе стороны от 42 °C:

    | потолок | выданная частота | Вт | °C | напряжение для потолка |
    |---|---|---|---|---|
    | 3172 — ВЕРХ кривой | 2887 → 2932 | 137,3 → 137,1 | 57 → 54 | 1240 → 1200 мВ |
    | 2887 — медиана стока | 2887 → 2857 | 136,95 → **126,42** | 57 → 53 | 1065 → 1040 мВ |
    | **2917 — медиана + 30** | **2887 = ровно сток** | 137,15 → **129,44** | 58 → 53 | 1075 → 1050 мВ |

    Простой не тронут. **Потолок на ВЕРХУ кривой — не потолок** (EXP-0031). Место потолка — рычаг:
    **30 МГц ≈ 2,8 Вт**. Ватты ОДНОСОСТАВНЫЕ (P5-AC8 требует двух серий).
16. **🔴 НАСТОЯЩАЯ ИГРОВАЯ НАГРУЗКА НАСЫЩАЕТ КАРТУ.** Q2RTX: 99 % загрузки, **299,97 Вт**,
    троттлинг `sw_power_cap`, 75 °C. Наш `sdc_fma --sustain`: те же 97 % загрузки, но **137 Вт**.
    **`utilization.gpu` — плохой прибор нагрузки, правду говорит мощность** · ось `-pl` ожила ·
    все наши PASS сняты при половинном конверте. Полностью — `researches/06`.
17. **У графической нагрузки СПЕРВА доказывается, что она не упёрта в потолок** (EXP-0032).
    Обязательно: `vid_fullscreen 1` · `cl_maxfps 0` · `drs_enable 0`. Ворота — команда, пройдены.
18. **Прибор ЦЕНЫ — выданная ЧАСТОТА, а не оп/с** (EXP-0030). Оп/с остаётся детектором clock
    stretching. Процент производительности — ручной 3DMark владельца.
25. **🎯 ЧАСТОТА ЯДРА — ПЛОХОЙ РЫЧАГ ПОД ЭТОЙ ИГРОЙ, измерено на двух нагрузках** (отменяет
    ожидание факта 16, что выгода придёт кадрами):

    | кадр | частота | FPS | вердикт |
    |---|---|---|---|
    | 2 луча (RT-тяжёлый) | 2760 → 2940 (+6,5 %) | 56,947 → 56,826 = **−0,21 %** | шум |
    | 0 лучей (RT-лёгкий) | 2737 → 2895 (+5,8 %) | 89,976 → **90,836 = +0,96 %** | эффект, едва выше пола |

    **6 % частоты покупают 1 % кадров.** Карта во всех прогонах стоит на 300 Вт; наши сдвиги трогают
    только ГРАФИЧЕСКИЙ домен, память не двигается вовсе. **Следствия:** Max Perfomance от подъёма
    кривой получает почти ничего · **у Optimised огромный запас** — если лишняя частота не даёт
    кадров, снятая их и не отнимет. **Граница: это ОДНА игра и она путь-трейсер.**
27. **🏆 ПЕРВЫЙ ИЗМЕРЕННЫЙ КАНДИДАТ В `Optimised`: −1,24 % кадров за −50 Вт, −9 °C, вентилятор
    69 → 50 %.** Матрица размена под игрой, все пары от 41…42 °C, откат каждой чист:

    | конфигурация | FPS | Вт | °C | вентилятор | МГц |
    |---|---|---|---|---|---|
    | сток | 57,60 | 300,1 | 75 | 69 % | 2767 |
    | только `-pl 250` | 54,45 | 250,1 | 67 | 51 % | 2602 |
    | только кривая +180 | 56,83 | 300,0 | 74 | 67 % | 2940 |
    | **кривая +180 + `-pl 250`** ← кандидат в **Optimised** | **56,89** | **250,0** | **66** | **50 %** | 2722 |
    | кривая +180 + `-pl 250` + потолок 2400 ← кандидат в **Silent Cold** | 53,10 | 205,9 | 61 | 36 % | 2400 |

    **Синергия и есть ответ: потолок мощности В ОДИНОЧКУ стоит 5,6 % кадров, а вместе с кривой —
    1,24 %.** Где карта зажата по ваттам, меньшее напряжение покупает частоту. **Бюджет 5 %
    потрачен на 1,24 % — можно резать глубже.** Чего не хватает до профиля: запаса надёжности,
    длинного прожига, второй серии ватт. ⚠️ **Обороты в этой таблице сняты с транзиента (факт 35).**
28. **🔴 ЧИСЛО «885 мВ» ОПРОВЕРГНУТО ЗАМЕРОМ 2026-08-11.** Поиск двигал ОДНУ запись таблицы, а перевод
    в вольты считался так, будто поднята ВСЯ кривая: сколько ту запись ни поднимай, **частоту 2842 МГц
    по-прежнему обслуживало 1040 мВ** — ровно, всегда, по построению. **Уходит и «Vmin ≈ 885 мВ», и
    вывод о вероятностном крае.** Остаётся в силе: отказ наступал между +540 и +555 МГц, но это
    про ~3400 МГц. Полностью — `bugs/02` и хроника. **Как проект поверил числу — читать там же,
    это защищает лучше стёртой строки.**
29. **🔊 ШУМ ПОЛУЧИЛ ЧИСЛО ОТ УХА ВЛАДЕЛЬЦА — и по нему СТОК ПРОИГРЫВАЕТ.** Его слова: *«желательно
    не подниматься выше 60 % — это уже ощутимо громко»*, *«для Silent Cold желательно оставаться на
    45 %»*. Сток под игрой 69–72 % ❌ · кандидат в Optimised 50 % ✅ · кандидат в Silent Cold 36 % ✅.
    **Линейка:** 40 % = 1000 об/мин, 60 % = 1578, 100 % = 2714 — **28,5 об/мин на процент**.
    **30 % — его ЕЖЕДНЕВНЫЙ ФОН** (*«я его слышу каждый день»*), значит Silent Cold на 36 % почти
    неотличим от привычного.
30. **Дефект акустического прогона ПОЧИНЕН** (`rpmSettled()` — прибытие решается плато оборотов),
    **но НЕ ПЕРЕПРОГНАН на живой карте: владелец до сих пор не слышал пол 30 % честно.** Один
    прогон `npm run fanladder` закрывает.
32. **⚖️ ЗАПАС: V_отгрузки = ПОСЛЕДНЯЯ СТАБИЛЬНАЯ СТУПЕНЬ + ОДИН минимальный шаг сетки.**
    Слово владельца 2026-08-17, **переанкеровка** его же прежнего правила «отказ + 10 мВ (два шага)»:
    *«Переделываем на: последняя стабильная до отказа точка (соседка отказа сверху) + 5 мВ»*.
    Причина арифметическая, не осторожность: сетка карты НЕРАВНОМЕРНА (5 мВ ×94, 10 мВ ×32), и в
    32 местах из 126 старая форма схлопывалась в ноль — «отказ + 10» указывал ровно на ту ступень,
    которая и так прошла. **Ни одно уже измеренное число проекта не сдвинулось:** где сетка 5 мВ,
    обе формы дают одно и то же. Реализовано СЧЁТЧИКОМ ШАГОВ (`config.marginAboveLastStableMv()`),
    а не милливольтами — на другой карте получится своё число. `guardband` 25 мВ остаётся
    консервативной альтернативой. ✏️ Строка исправлена 2026-08-23: несла отменённую формулу.
34+36. **❄️ SILENT COLD = МАКСИМУМ производительности при T ≤ T(40 % оборотов), и T(40 %) = 59 °C —
    ИЗМЕРЕНА НА ПЛАТО.** Порог ужесточён до 40 % словом владельца (*«Я слышу 40 — приемлимо, тихо.
    50 уже слышно»*). Сток, без андервольта:

    | потолок | выдано | равновесная T | вентилятор | Вт | FPS | держалось |
    |---|---|---|---|---|---|---|
    | 2400 | 2392 | 65 °C | 54 % | 223 | 51,63 | 395 с |
    | **2100** | **2092** | **59 °C** | **40 %** | **171** | **45,67** | **483 с** |
    | 1800 | 1792 | 56 °C | 33 % | 144 | 40,24 | 630 с |
    | 1500 | 1492 | 55 °C | 30 % | 128 | 33,82 | 753 с |

    **`Silent Cold` на стоковой кривой = 2100 МГц.** Три следствия: **(а)** ниже 1800 спускаться
    бессмысленно — 30 % это пол карты; **(б)** цена режима −19 % кадров от стока, много больше
    снятого бюджета в 10 % — владелец захочет видеть это число; **(в)** **это НИЖНЯЯ оценка:**
    отгружаемый `Silent Cold` несёт ещё и подъём кривой, значит под 40 % влезет потолок ВЫШЕ 2100.
    **Перепрогнать лестницу поверх подъёма — обязательный шаг.**
35. **🔴 НИ ОДИН ЗАМЕР ДО 2026-08-10 23:xx НЕ ВЫХОДИЛ НА РАВНОВЕСИЕ.** Детектор плато по всем 13
    записям: **плато 0 из 13**. Под игрой равновесие приходит на третьей–пятой минуте, прогоны
    длились ~60 с. **Транзиент занижает обороты на 10–18 процентных пунктов.** Ватты, кадры и
    выданные частоты НЕ испортились. Карта подходит к равновесию С ДВУХ СТОРОН, обе сошлись на
    65 °C / 55 % при 2400 МГц. **Правило: пара «обороты ↔ температура» без плато не считается.**
37. **🧪 ЧАСТОТА СУДИТСЯ НАБОРОМ ИЗ ТРЁХ ФОРМ, и он прогнан на карте.** Порог = ХУДШАЯ форма
    (`sdc_fma --transient` первой — шум напряжения живёт в переходах, затем `sdc_fma --sustain` и
    `branchy --sustain`), решившая форма называется вслух. Живой прогон на **2842 МГц, шаг +15 МГц** —
    три PASS из трёх, аренда сторожа 150 с, откат чист. **И один блок покраснел ПРАВИЛЬНО:** подъём
    ОДНОЙ записи таблицы не удешевил частоту вовсе — **андервольт покупает ВСЯ кривая**. ⚠️ **ЭТОТ ХРАПОВИК (фазы 5) ключуется индексом записи таблицы и потому
    зависел от температуры. В эпике 02 это отменено:** улика ключуется ЧАСТОТОЙ, а «частота →
    напряжение» от температуры замера не зависит.

38. **🔴 У ПОТОЛКА ЧАСТОТЫ ЕСТЬ ПОЛ — 2157 МГц, — И `Silent Cold` СПРОЕКТИРОВАН НИЖЕ НЕГО.** Потолок
    держится тем, что точки НАД ним придавливаются вниз, а придавливание — такой же сдвиг, и оно
    упирается в железные ±1000 МГц. Значит **кривой не удержать никакой потолок ниже `верх − 1000`**;
    на живой карте 2026-08-14: верх 3157 → **пол 2157 МГц**. Отсюда, по режимам:

    | потолок | кто способен удержать | что это значит |
    |---|---|---|
    | 2842 · 2400 | кривая | отгружаемая форма доступна |
    | **2100 — `Silent Cold` с тепловой лестницы (факт 34+36)** | **не кривая, утечка 57 МГц** | профиль сам по себе оставляет карте ход до 2157 |
    | 1700 · 2000 (низ диапазона §4.5) | только закрепление | замер законен, но это НЕ отгружаемая форма, и прогон говорит это по каждой ступени |

    🆕 **ЛЕКАРСТВО ИЗМЕРЕНО 2026-09-01 И БОЛЬШЕ НЕ «ПРОЧИТАНО»** (решение владельца `interviews/025`
    Q1 = A · Q2 = A): форма `-lgc <пол, потолок>` как ДИАПАЗОН прогнана на живой карте в отгружаемом
    профиле. Граница 180…1500 МГц под прожигом дала **1492…1500 МГц, ни одной пробы выше**, и карта
    спускалась до **180 МГц** — то есть это граница, а НЕ закрепление, и она держит. Без границы тот
    же прожиг сидит на ~2887 МГц. Замок-граница 180…3090 стоит теперь во всех трёх рабочих режимах;
    разбор — `bugs/99`, план — `plans/83`.

    Утечка НЕ зависит от подъёма (одинакова при Δ = 0 / 45 / 180 / 300 / 540) — кончается
    придавливание, а не подъём. **Что при этом НЕ меняется:** точка, обслуживающая любую частоту
    ≤ потолка, и её напряжение — одни и те же при равномерном подъёме и при отгружаемом векторе
    (9 из 9 на потолках 2842 / 2400 / 2172 × Δ 45 / 180 / 300). То есть смена формы не двигает ни
    одного уже измеренного числа. **Кандидат в лекарство, ПРОЧИТАННЫЙ, а не измеренный:**
    `nvidia-smi -h` says `-lgc <minGpuClock,maxGpuClock>` — это ДИАПАЗОН, а `1500,1500` лишь его
    вырожденный случай. Проект отверг `-lgc` за то, что `candidateProfile()` ставит `min = max`, то
    есть ПИН; диапазон «пол простоя … потолок» — это потолок, и карта остаётся свободна вниз, как и
    требовал владелец. **Одного живого прогона при владельце это ещё не проходило** — вопрос фазы 6.

39. **🎯 КРАЙ ЭТОЙ КАРТЫ НАЙДЕН ВПЕРВЫЕ — 2842 МГц, между 845 и 850 мВ.** Заказ владельца: *«я хочу
    увидеть наш оракул в работе»*. Прогон `--sweep --from 2842 --to 2842 --max-depth 245` прошёл
    **20 ступеней от стока 1045 мВ до 850 мВ, каждая PASS**, а на 845 мВ машина упала в BSOD
    `DRIVER_IRQL_NOT_LESS_OR_EQUAL (0xD1)`, `nvlddmkm.sys` (журнал Windows: `Kernel-Power/41`,
    23:32:51). **По правилу владельца напряжение отгрузки = последняя стабильная 850 + один шаг сетки
    = 855 мВ** (в этом месте сетка 5 мВ, поэтому прежняя форма «отказ 845 + 10» даёт то же число), и 850 доказано
    независимо. До этого самое глубокое измеренное на этой частоте было 885 мВ — **карта уходит на
    40 мВ глубже, чем проект знал.**

    **Три следствия, и второе важнее самого числа:**

    **(а) Шаг был 5 мВ — минимальный на сетке карты.** Это не «перепрыгнули грубым шагом»: к обрыву
    подходили самым мелким шагом, какой существует, и всё равно получили падение вместо вердикта.

    **(б) 🔴 НА ЭТОЙ ЧАСТОТЕ КРАЙ НЕ ПРОЯВЛЯЕТСЯ ПОРЧЕЙ ДАННЫХ — ОН РОНЯЕТ ДРАЙВЕР.** Оракул не
    увидел НИЧЕГО и не мог: контрольная сумма считается процессом, который умирает вместе с ОС.
    **За всю жизнь проекта оракул не выдал ни одного отказа — 584 прожига, 0 SDC, 0 CRASH**
    (87 в храповике фазы 5, 497 в журнале эпика 02). Знание спас **журнал упреждающей записи**
    (R15) — ступень записана и `fsync`-нута ДО обращения к карте, поэтому названа поимённо. Это
    первый раз, когда механизм отработал на НАСТОЯЩЕМ зависании.

    **(в) 🟡 НАХОДКА В ДОКУМЕНТ ЕЩЁ НЕ ПОПАЛА, НО ПУТЬ ОТКРЫТ — `bugs/23` ПОЧИНЕН 2026-08-17.**
    Было: `ЗАВИС` писался в журнал и частоту не закрывал, а возобновлённый прогон снова пошёл бы на
    845 мВ. Стало: зависание — третья стена спуска и КРАЙ частоты (**правило R18**), спуск на убившую
    ступень не возвращается, отгрузка 855 мВ. **Строка документа по 2842 МГц по-прежнему держит
    1000 мВ**, потому что живого прогона после починки не было: один прогон
    `--sweep --from 2842 --to 2842 --dashboard` переносит знание из журнала в документ.

**Сколько андервольта ДОСТУПНО по диапазону** — арифметика на живой кривой, без единой записи:

| частота | сток | предел рычага | доступно снять | ступеней | сторож глубины |
|---|---|---|---|---|---|
| 500 | 745 мВ | 450 мВ | 295 мВ | 3 | ❌ отклоняет: обрыв −5 → −230 мВ |
| 1100 | 770 | 450 | 320 мВ | 7 | ❌ отклоняет: тот же обрыв |
| 1700 | 795 | 750 | **45 мВ** | 7 | ✅ |
| 2000 | 815 | 765 | **50 мВ** | 8 | ✅ |
| 2400 | 910 | 785 | 125 мВ | 20 | ✅ |
| **2842** | **1045** | **800** | **245 мВ** | **39** | ✅ |

**Эта таблица уже сделала вывод за нас: профиль обязан быть ВЕКТОРОМ.** В середине диапазона рычаг
отдаёт 45–50 мВ и упирается в аппаратную стену ±1000 МГц, а внизу и наверху доступно 250–320.
Единый Δ на всю кривую физически не может быть оптимумом — это арифметика, а не гипотеза.

## 🔴 ГЛАВНОЕ ДЛЯ ВЛАДЕЛЬЦА, СКАЗАННОЕ ИМ ДВАЖДЫ — ЧИТАТЬ ДО ЛЮБОГО ПЛАНИРОВАНИЯ

**Владелец 2026-09-09 задал один и тот же вопрос второй раз НАМЕРЕННО**, объяснив зачем: *«Не для
того, чтобы ещё на сутки отложить прогон, а для того, чтобы дать тебе понять, что для меня важно.
И чтобы проверить тебя, что для тебя эта важность понятна и ты в этом направлении работаешь и
документируешь это»*. Вопрос: *«Ты уверен, что предохранитель-оракул спасёт машину от зависания на
краю напряжения?»*

И отдельно: *«не замером мне ответить, а твоей субъективной уверенностью на основе работ, которые
ты сделал»*.

### Что это значит для агента — три правила, выведенные из этих слов

1. **МАШИНА НЕ ДОЛЖНА УМИРАТЬ. Это ось работы, а не одна из задач.** Всякий план сверяется с
   вопросом «что этот шаг делает с вероятностью зависания». Первый живой вечер, где предохранитель
   поймает НАСТОЯЩЕЕ зависание, — веха проекта; её ждём и к ней ведём.
2. **ЧЕСТНОЕ «НЕ УВЕРЕН» — НЕ ПОВОД ОТКЛАДЫВАТЬ ПРОГОН.** Первый раз этот вопрос стоил суток
   простоя, потому что я ответил «не уверен» и остановился. Владелец на это: вопрос задан не ради
   отсрочки. Отвечать — оценкой и направлением работ, а не паузой.
3. **ОТ АГЕНТА ЖДУТ СУЖДЕНИЯ, А НЕ ТОЛЬКО ЗАМЕРА.** «Не нужны мне твои замеры» сказано не против
   измерений — они остаются основанием. Сказано против подмены: владелец спрашивает мою позицию,
   и таблица вместо позиции — уклонение. Числа держат ответ снизу; ответ формулируется словами.

### Моя позиция на 2026-09-09, чтобы следующая сессия не начинала с нуля

**Уверен, что предохранитель ловит МЕДЛЕННУЮ смерть — а именно она у нас записана.** Основание не
«прибор хороший», а конкретное: в единственной записанной смерти у него было БОЛЬШЕ ОДИННАДЦАТИ
СЕКУНД между перешагиванием уставки и гибелью машины — тысячи тактов при такте 2 мс, когда рукам
нужно полсекунды. Он не сработал не потому, что не заметил бы, а потому, что БЫЛ СЛЕП: его
собственный такт развалился всемеро (`bugs/128`). Дыра закрыта и доказана под нагрузкой 09.09.
Плюс с него снято взведение там, где он не видит (`bugs/131`): вход 3 не отличал смерть от
здоровья и мог оборвать работу, ничего не обнаруживая.

**НЕ уверен и не обещаю: мгновенную смерть без окна.** Против неё не поможет никакой сторож внутри
той же машины — он умирает вместе с ней. Это физика, а не недоработка; знание в этом случае спасает
журнал упреждающей записи, а не предохранитель.

**«Дыр не осталось» не скажу, пока он не поймает настоящее зависание живьём.** Такого случая у
проекта нет ни одного — и это довод ПРОВЕСТИ прогон и посмотреть, а не отложить его.

## Решено владельцем — не пересматривать

- **ЧЕТЫРЕ РЕЖИМА, не два профиля:** 🚀 **Max Perfomance** (весь выигрыш в производительность) ·
  ⚖️ **Optimised** (*«почти то же самое, что Max Perfomance»*, но тихо и относительно холодно) ·
  ❄️ **Silent Cold** (**МАКСИМУМ производительности под 59 °C** — задача оптимизации, а НЕ бюджет
  «до 10 %»: критерий перевёрнут поправкой владельца 2026-08-10 22:xx) · 🔄 **Stock Default**.
  **Механизм один, и он уточнён каноном 2026-08-15** (`GOAL.md` → «⭐ ЧТО ТАКОЕ ТЮНИНГ VF-КРИВОЙ»):
  вектор напряжений у всех режимов ОДИН, различает их **лимит мощности** — 300 · 250 · 250 Вт, — а
  потолок частоты остался только у `Silent Cold` (~2800 МГц). Прежняя формулировка «различает только
  МЕСТО потолка частоты» ОТМЕНЕНА. Фиксации частоты нет ни в одном режиме.
- 🆕 **ПОТОЛОК `SILENT COLD` СТАЛ ВЫВОДИМОЙ ВЕЛИЧИНОЙ — слово владельца 2026-09-01:** *«для silent
  cold режима нужно будет max частоту так подобрать, чтобы вертушки работали в районе 40...50% -
  больше не очень хорошо»*. То есть потолок больше не «условных 2800», а ТОТ, при котором обороты
  садятся в 40…50 % на ПЛАТО (транзиент занижает обороты на 10–18 пунктов, факт 35). Подбирается
  тепловой лестницей поверх тюнинг-кривой; прежний замер лестницы дал 40 % на потолке 2100 МГц, но он
  снят БЕЗ андервольта, значит под 40…50 % влезет потолок ВЫШЕ. Полностью — `GOAL.md` → «🔊 ПОТОЛОК
  SILENT COLD ПОДБИРАЕТСЯ ПО ОБОРОТАМ».
  ⚠️ **И там же его правка, снимающая мою ошибку прочтения: у `OPTIMISED` требования ПРЕЖНИЕ, лимита
  частоты НЕТ** (*«для оптимайзд - всё по-старым требования, без лимита частоты»*). Первое сообщение
  он назвал случайным — оно относилось к `Silent Cold`.
- **OPTIMISED НАЗВАН ДО КОНЦА: FPS ≥ 95 % от Max Perfomance**, а покупаем на это СИЛЬНОЕ снижение
  ватт, температуры и шума. Точка отсчёта — **Max Perfomance, а не сток**.
  ✅ **КРАЙ ЭТОГО КРИТЕРИЯ ЗАКРЫТ ЗАМЕРОМ 2026-09-01** (чередование из четырёх замеров): просадка
  −1,71 % ТОНЬШЕ разброса прибора 2,28 %, то есть её нельзя измерить, а верхняя граница вдвое меньше
  бюджета. Купленное: −49,1 Вт · −7,5 °C · −17 пунктов оборотов. Числа — в `profiles/optimised.json`. Ручек три: **кривая V/F —
  главный рычаг и обязателен во ВСЕХ режимах** · **`-pl 250` ставится** · потолок частоты решается
  ЗАМЕРОМ размена. Следствие для кода: откат обязан снимать обе части (кривая — NVAPI, ватты —
  `nvidia-smi`). Граница рычага: `-pl` двигается только 300 → 250 Вт.
- **Формула проектирования профиля, дословно:** *«снижаем потребление насколько можем до тех пор,
  пока не платим больше, чем N»*. N назначает владелец **с кривой на руках**.
- **Сходящаяся петля вместо статического запаса:** напряжение частоты ставится на минимальный шаг
  выше отказа, вся кривая тестируется целиком, напряжение сбойнувшей частоты поднимается храповиком и
  НИКОГДА не опускается обратно;
  длинные прожиги — после каждой итерации всей кривой, не на каждом шаге.
- **Трей с меню по правому клику** (отменяет «пассивный трей без меню» от 2026-08-09): смена режима
  равнозначна ярлыку, есть пункт закрытия; закрытие убирает трей из автозагрузки, любое переключение
  режима возвращает. **Повторный запуск активного режима не пишет в GPU ничего.**
  🆕 **ПУНКТ `Exit` ОПРЕДЕЛЁН ВЛАДЕЛЬЦЕМ 2026-08-23 ~15:3x:** *«пункт Exit в контекстном меню KAGO
  должен сбрасывать карту до завода, и закрывать трей-приложение KAGO»*. Это ДВА действия одним
  пунктом и в этом порядке: сброс через ту же повышенную задачу `\KAGO\apply-factory`, что и ярлык
  `🔄 Stock Default` (R1 не нарушается — писатель прежний), затем снятие из автозагрузки и выход.
  **Отменяет прежнее «закрытие трея не сбрасывает карту» ТОЛЬКО для осознанного `Exit`:** убитый
  диспетчером задач трей по-прежнему НЕ сбрасывает ничего, потому что обработчик выхода при
  убийстве не отрабатывает, а сторож на это был бы вторым механизмом с отказом вида «сбросил карту,
  когда ничего не случилось». Разбор — `PROJECT_ARCHITECTURE_INTERNAL_MAP.md` §4 и `plans/10` §1.2.
- **Бренд:** русская расшифровка — **«Криника Автоматизированный ГПУ Оркестратор»** (правка
  владельца, коммит `34de49b`) · логотип — свой чип «лёд и пламя», мастер **1024×1024 с прозрачным
  фоном** · **иконки ниже 128 не делаем** · лицо Stock Default — **🔄, две стрелки, не квадрат**.
- **Лестница:** бэкенд `nvidia-smi` сейчас, свой мост к NVAPI потом. **Никаких сторонних GUI.**
- **Приёмка** по нашим числам + один ручной 3DMark на финише · FurMark не пускаем вовсе · бинарники
  едут в репозиторий · **`D:\Games\Quake2RTX` — «сюда, игры тут»**.
- **Числа из PDF сняты с роли целей:** *«цифре 97 не верим, она может плавать от экземпляра к
  экземпляру»*. Слово владельца в чате старше PDF.
- **Права агента:** пуш в GitHub не требует отдельного разрешения на каждый раз — достаточно просьбы
  в чате; разрушающее остаётся в `deny`. Владелец: *«я вообще не люблю вопросы от тебя на твои
  команды консольные, никаких вопросов. кроме продуктового видения, для чего есть KAIF»*.
- **ГРАНИЦА МОИХ ВОПРОСОВ — сказано ТРЕТИЙ раз (`interviews/028`, 07.09):** *«это технические
  вопросы, а не вопросы уровня заказчика. Смотри, как ценнее для продукта»*. Прежде то же самое:
  04.09 `interviews/026` Q3 — *«Ты формулируешь в терминах команды разработки»*; тогда же —
  *«не понимаю вопрос. Я понимаю в поведенческих сценариях»*.
  **Проверка перед любым новым интервью: изменится ли от ответа то, что владелец УВИДИТ, УСЛЫШИТ
  или получит в руки?** Нет — решаю сам. Да — формулирую СЦЕНАРИЕМ (ситуация → что получает → чем
  платит), без имён файлов, флагов и внутренних терминов.
- **Спасение карты — ШЕСТЬ попыток в двух раундах по три (`interviews/027` Q1 = D, 07.09):** *«не две
  попытки, а три, и после трёх попыток — пауза в секунду, и второй раунд частых трёх попыток. И потом
  ещё секунда таймаут — и только потом закрываем прогон. Как бы пытаемся спасти»*. Между раундами и
  после второго — по 1 с. Его сомнение (*«если GPU так намертво зависло, то, скорее всего, и ОС тоже
  зависнет»*) проверено 07.09 и на этом классе отказа НЕ подтвердилось: машина выживала и доказывала
  это замером; для класса 04.09/31.08 сомнение остаётся верным и в реализацию не закладывается.
- **Гашение фона перед прогоном — его слово 08.09:** *«туши защитой бродкаст и парсек, и снятием
  защиты — поднимай назад»*. Broadcast и Parsec снимаются перед полосой и поднимаются после
  (`runs/restore-card-background.ps1`). Просил вплести это в САМ предохранитель — **долг открыт**.
- 🆕 **ОКНО НАБЛЮДЕНИЯ НЕ ПОДНИМАЕТСЯ ВПУСТУЮ — его слово 09.09, поднято сюда из блока сессии 93
  перед стрижкой:** *«ну так закрой визуализатор, если нет прогона !!!!!!!!!!!»*. Сказано после
  того, как я на вопрос «а где визуализатор?» поднял окно, которое читает пульс, телеметрию, кривую
  и журнал ПОЛОСЫ, — ничего этого стенд `--loaded-floor` не пишет вовсе. **Порядок обязателен:
  сперва проверить, есть ли у окна источники, и если показывать нечего — сказать это, а не
  поднимать пустое.** Смежное его требование (`plans/94`): *«Есть прогон — есть окно
  визуализатора»* — то есть окно живёт ровно столько, сколько прогон.
- **Полностью и дословно всё вышеперечисленное — `GOAL.md` и `AGENT_GUIDE.md` → Notes from the human.**

## ❓ Ждёт решения владельца

✅ **`interviews/029` — ЗАКРЫТО ВЛАДЕЛЬЦЕМ 2026-09-13, вариант A:** *«сколько натюнено и края найдены -
столько частот и принимаем в боевой режим»*. Порога покрытия у ворот передачи нет. **И в том же
сообщении — слово о темпе, старше любой очерёдности машинерии:** *«А то я нахуй состарюсь, пока ты этот
проект доделаешь. Я думал мы за два вечера мне карту затюним, а ты это уже больше месяца делаешь.»*
Предложенный ответ — план двух вечеров при владельце (живой прогон 42 строк + 7 краёв, потолок Silent
Cold, Max Performance в отгрузку) и вывод краёв офлайн; новые сторожа отложены. Владелец ответил
*«можешь запускать прогон»*; первый же прогон кончился смертью машины (`bugs/138`), и вопрос о
методе поиска края снова открыт — эстафета сессии 98, п. 0.

✏️ **ИСПРАВЛЕНО 2026-09-13 (сессия 98) — ЗДЕСЬ СТОЯЛО «🔴 `interviews/026` — ОТКРЫТО», И ЭТО БЫЛО
НЕВЕРНО ДЕВЯТЬ ДНЕЙ.** Документ говорит: **✅ ЗАКРЫТО ВЛАДЕЛЬЦЕМ 2026-09-04 — Q1 = B (19:37) · Q3 = A и
Q4 = A (19:54)**, машинная запись `interviews/decisions/…026….decision.json` от 04.09 19:54 это
подтверждает. Четвёртый случай пары «сводка ↔ документ» (прежние — `interviews/008`, `interviews/013` и
зеркальный: развилка, которой сводка не знала вовсе, — все три разобраны ниже в этом же разделе).

🎯 **ПРИКАЗ ВЛАДЕЛЬЦА 2026-08-24 — ДЕЙСТВУЮЩАЯ РАМКА ВСЕЙ РАБОТЫ, дословно:**

> *1) Чини*
> *2) Развивай виртуальную видеокарту, отлаживайся на ней, пока не починишь*
> *3) Симулируй ОТКАЗЫ ВСЕХ ВОЗМОЖНЫХ СЦЕНАРИЕВ, и чтобы твой код их корректно обрабатывал.*
> *4) Вечером будем запускать прогон на реальной карте. Если он свалится - то ты лох, это
> зафиксируем оффициально.*

**Это не вопрос, а распоряжение, и оно старше любой очерёдности из прежних блоков.** Пункты 2 и 3
исполнены (эпик `plans/36`, фазы 1–3). Пункт 1 — фаза 4, следующая работа. Пункт 4 — вечерний
прогон, он же приёмка, и владелец назвал цену падения.
⚠️ **Отладка на живой карте до вечера ЗАПРЕЩЕНА его же словом** («отлаживайся на ней, пока не
починишь»); стенд к этому теперь готов — он умеет отказывать шестью классами.

✅ **Закрыто 2026-08-24 по его слову:** поправка ложного зависания `seq 695` («1 - снимай»);
безопасный режим дисков снят («2 - выключай, прогонов же не делаем сейчас»); широкие права агента
выданы (`node tools/grant-agent-rights.mjs`), после чего фоновый запуск прогона прошёл с первого раза.

✏️ **ИСПРАВЛЕНО 2026-08-24 16:1x — ЗДЕСЬ СТОЯЛО «🔴 ОТКРЫТО `interviews/013`, ТРИ ВОПРОСА», И ЭТО
БЫЛО НЕВЕРНО СУТКИ.** Сам документ говорит: **✅ ЗАКРЫТО ВЛАДЕЛЬЦЕМ 2026-08-23 22:3x, Q1 = A ·
Q2 = C · Q3 = D** — причём Q3 он закрыл вариантом, которого в вопросе не было (ступень 2 переписана
с офлайна на read-only, и это замер на его живом фильме, а не рассуждение). Сессия 46 ответы
исполнила, а эту строку сводки не сняла.
⚠️ **Это ТРЕТИЙ случай одного дефекта, и он назван здесь ради регламента, а не ради строки:**
сводка и документ разошлись, пару никто не сверил. Раньше было так же с `interviews/008` (числился
открытым семь дней после ответа владельца) и зеркально — с развилкой, которой сводка не знала вовсе
(«Что за мной? Я даже не в курсе»). **Механическая половина лекарства существует и работает лучше,
чем о ней думали:** `npm run questions`, ось G3 «устаревший статус» (`bugs/40` — прибор был списан
по неверному диагнозу и укладывается в собственную планку канона). Найдено при закрытии сессии 47 —
сверкой раздела с файлами, а не памятью.

✅ **Прежние два вопроса закрыты владельцем 2026-08-23 18:04.**
· **`interviews/012` — объём доработки виртуальной карты: «как рекомендуешь» = ВАРИАНТ C, ЗАТЕМ A.**
  Пункт (0) эстафеты РАЗБЛОКИРОВАН. Порядок задан ответом и не пересматривается: **сперва заход C —
  разведка по ВСЕМ двенадцати смертям машины за настоящими вероятностями** (сегодня сведён один день,
  2026-08-23), итог — документ в `researches/`, кода ноль; **потом заход A** — стенд с тремя
  поведениями (истинное срабатывание · ложное · молчание у края) и мерой качества, построенный на
  измеренных числах, а не назначенных. Офлайн, записей в карту ноль.
· **`interviews/011` — звук визуализатора: Q1 «90» · Q2 «А» · Q3 «хватает».** То есть базовая доля
  **0,67 с (90 ударов в минуту, вариант C — рекомендация агента 75 ОТКЛОНЕНА)** · **без предзадержки
  реверберации** · **одного набора из восьми ходов хватает** (рекомендация «решить после
  прослушивания» отклонена). ⚠️ Все длительности `researches/12` выведены ОТ базовой доли — смена
  0,8 → 0,67 с двигает их все, это пересчёт агента, а не новый вопрос вкуса.

✅ **`interviews/018` ЗАКРЫТО ВЛАДЕЛЬЦЕМ 2026-08-30 11:51:07 — ВАРИАНТ A**, одним кликом, после
того как документ переписали с рисунком, тремя графиками и блок-схемой. Промахнувшийся замер
(`origin:overshot`) не поднимает чужие строки; исполнено в `curve-store.closePoint` (`bugs/63`).
Первая редакция вернулась с `choice: null` и словами *«Не понимаю вопрос и физику процесса. Нужен
рисунок, блок схема, график»* — урок **EXP-0192**: для вопроса О ФИЗИКЕ содержание это КАРТИНКА.

🔴 **`interviews/020` — ПОДНЯТО 2026-08-30 14:0x И СНЯТО АГЕНТОМ В ТОТ ЖЕ ЧАС. ЗА ВЛАДЕЛЬЦЕМ НИЧЕГО
НЕ ЧИСЛИТСЯ.** Он ответил 14:15:22: *«Не понимаю проблемы и вопроса. Нужно проще пояснить»*,
`choice: null` — второй раз за день. **Причина не в изложении: вопрос был инженерный, переодетый в
развилку владельца.** Политику он решил 30.08; я вынес ему ДЕТЕКТОР (по ширине промаха или по
повторению заказов), а порог «> 10 мВ» нигде в его словах не стоит — его выбрал агент. Признак
оставлен включённым как ПРОЧТЕНИЕ его решения (`bugs/73`), отменяется одной строкой. Урок —
**EXP-0196**. ⚠️ **Повторно этот вопрос НЕ поднимать:** он снят, а не отложен.

🔴 **ЧТО ЗА ВЛАДЕЛЬЦЕМ ОСТАЛОСЬ, И ЭТО НЕ ИНТЕРВЬЮ, А ДВЕ КОРОТКИЕ ФРАЗЫ В ЧАТ:**
· **Была ли ошибка explorer при выключении** (`bugs/38`) — одна чистая перезагрузка засчитана,
  журнал на этот вопрос не отвечает в принципе (id 1000 по `explorer.exe` за 7 суток — ноль).
· **Q1 плана 10 §0** — значок трея: логотип KAGO всегда или лица режимов, как сейчас. Меню трея
  строится без этого ответа; он решает только, откуда берётся картинка значка.

🔴 **ИСПРАВЛЕНО 2026-08-23 17:2x — ЗДЕСЬ СТОЯЛО «ОТКРЫТ ОДИН ВОПРОС: `interviews/008`», И ЭТО БЫЛО
НЕВЕРНО СЕМЬ ДНЕЙ.** Сам документ говорит: **✅ ЗАКРЫТО ВЛАДЕЛЬЦЕМ 2026-08-16 18:2x, вариант A**,
дословно — *«A — чини что умеешь, RT не трогай»*. То есть владелец ответил через ДВАДЦАТЬ МИНУТ
после заведения вопроса, а сводка этого не заметила и продолжала числить его за ним.
**Отложено там не решение, а ИСПОЛНЕНИЕ,** и причина названа в самом документе: карта была занята
другим агентом, а собирать нагрузку и переснимать эталоны при постороннем фоне нельзя.
⚠️ **И это ровно та ошибка НАОБОРОТ, которую разбирает абзац ниже.** Тогда вопрос существовал, а
сводка говорила «открытых нет». Теперь вопроса не было, а сводка говорила «открыт». Один и тот же
дефект — **сводка и документ разошлись, и никто не сверил пару**, — и оба раза он стоил владельцу
недоумения либо агенту похода за уже принятым решением. Найдено при снятии описи беклога для
эстафеты; механическая половина — `npm run questions`, ось G3 «устаревший статус», и она за эту
пару НЕ отвечает: 008 стоит правильно в СВОЁМ файле.

**Что решено в 008 и не пересматривается:** скважность и VRAM чиним своими `.cu`; **RT-ядра не
трогаем вовсе** (ни OptiX, ни DXR — правило нуля сторонних зависимостей цело); и граница едет
вместе с числами: **«прожиг грузит вычислительные ядра и VRAM полностью, RT-ядра не грузит вовсе»**,
то есть все найденные им напряжения доказаны БЕЗ нагрузки на RT.

⚠️ **И вот как этот вопрос сюда попал — читать, это про регламент, а не про RT.** Развилка полтора дня
существовала ОДНОЙ СТРОКОЙ в хвосте этого файла (блок сессии 29) и больше нигде, а строка над ней
говорила «Открытых вопросов владельцу — НОЛЬ». Владелец нашёл расхождение сам: *«Что за мной? Я даже
не в курсе»*. `AGENT_GUIDE.md` запрещает это прямым правилом — *«everything the agent wants FROM the
owner … lives ONLY in `interviews/`, never in the tail of a plan, research, or bug file»* — и там же
объясняет, почему правило всё равно ломают: *«chat is cheaper in the moment»*. 🔴 **ЭТА ОЦЕНКА СТОРОЖА ОПРОВЕРГНУТА ЗАМЕРОМ 2026-08-23 17:1x, И ОПРОВЕРЖЕНИЕ ВАЖНЕЕ САМОГО
СТОРОЖА (`bugs/40`).** Здесь стояло: *«`npm run questions` считается нерабочим — краснеет всегда, все
находки ложные»*. **Прогон и ручной разбор всех девяти находок: ложных ВОСЕМЬ, а ОДНА НАСТОЯЩАЯ** —
`researches/12` §7 «Открытые вопросы к владельцу», где с 2026-08-22 висят ТРИ неотвеченных вопроса
про звук визуализатора, и владелец их не видел. Это ровно тот дефект, ради которого правило и
существует, и он ТАКТИЛЬНО важен: родитель того документа — `homeworks/04_dashboard_sound_taste.md`,
то есть класс вкуса, где ухо владельца не один свидетель из нескольких, а единственный.
**И соотношение даже не плохое:** `AGENT_GUIDE.md` сам называет ожидание для сторожа ТЕКСТОВОГО
правила — «≈10 ложных на одну настоящую», — а этот даёт **8 к 1, то есть УКЛАДЫВАЕТСЯ в собственную
планку канона** и был списан как сломанный.
`bugs/40` называет ЧЕТЫРЕ класса ложных находок с МЕХАНИЧЕСКИМИ различителями (цитата ВЛАДЕЛЬЦА
читается как обращение К нему · сканируется хроника, то есть закрытое прошлое · ссылка на вопрос
читается как вопрос · **заголовок сопоставляется без чтения того, что под ним** — под «Open forks for
the owner» в `researches/10` §5 написано ровно «None.») и пятый дефект — в печати: закрытое интервью
с дословным ответом владельца печатается как «2 из 2 без ответа».
⚠️ **И первый заход самого `bugs/40` ошибся в счёте ровно тем же способом** — засчитал заголовок
`researches/10` §5 за настоящую находку, не открыв секцию. Исправлено на месте и оставлено видимым:
документ про то, как неверное число списало рабочий прибор, не может прятать своё собственное. До этого правило держалось на внимательности агента, и 2026-08-16 она
подвела. **Задача в беклог, и её порядок теперь известен: СНАЧАЛА закрыть настоящую находку
(три вопроса про звук — в `interviews/`), потом чинить четыре класса — починка, которая заглушит
заодно и настоящую находку, сделает сторож хуже, чем снятым.**

`interviews/007` закрыто владельцем 2026-08-15 в два хода (вариант A), и оба хода МЕНЯЮТ КОД, а не
регламент:

1. *«зависание компа и перезагрузка — осознанный риск»* → вердикт **`ЗАВИС` первого класса** наравне
   с `SDC`/`CRASH` · журнал упреждающей записи обязателен · потолка «стоп после N перезагрузок» нет ·
   единственная аварийная остановка — **две перезагрузки подряд на ОДНОЙ ступени** (поломка, не край).
2. *«человек будет за компом во время тюнинга и поиска края. Человек будет комп из экранов смерти
   перезагружать»* → **задача автозапуска, поднимавшая бы развёртку на boot, УДАЛЕНА ИЗ ПЛАНА.**
   Подъём — команда, продолжение с обрыва — журнал. Безнадзорный режим не нужен вовсе.
   **Это ещё и безопаснее:** `bugs/11` случился ровно на пути «применить при входе в систему», и
   селить туда развёртку, пишущую непроверенные напряжения, было бы худшим из возможных мест.

**⏱ ДЛИТЕЛЬНЫЙ ПРОЖИГ СОКРАЩЁН ДО 1 МИНУТЫ** (слово владельца, отменяет «3 минуты» шага 15 его же
алгоритма). **Это возврат к его исходному числу, а не ослабление задним числом:** 2026-08-10 он
сказал *«например, минуту»*, и P5-AC6 с тех пор стоит ровно на ≥ 60 с — выбивались как раз три
минуты. **Чего минута НЕ покупает:** ни она, ни три не доводят карту до теплового равновесия (плато
приходит на 395–753 с, факт 34+36) — длительность покупает вероятность поймать ошибку, тепло меряет
тепловая лестница. **Куда идёт минута (решение агента):** 60 с той формой, которая РЕШИЛА край этой
точки на поиске; `--lowload` — отдельным проходом по всей кривой, потому что она грузит НИЗ
диапазона и на закреплённой высокой частоте бессмысленна.

**Плюс политика шага, данная владельцем без вопроса** (`GOAL.md` → «📐 ЛЕСТНИЦА ШАГОВ СПУСКА»):
**25 мВ на первых 100 мВ от стока · 10 мВ от 100 до 150 · 5 мВ глубже**, ключ — ГЛУБИНА ОТ СТОКА.
Сторожа `bugs/03` не ослаблены (там потолок первого шага ровно 25 мВ, разрыва — 35). **Отменяет
`FAST_DESCENT_FLOOR_MV = 900`:** абсолютный ключ не ездит по диапазону (на 1700 МГц сток уже 795 мВ),
а глубина ездит; на 2842 МГц оба дают одно и то же (1045 − 150 = 895 ≈ 900). **Развёртка подешевела с
13 часов до 5–7.** Два следствия в код: отказ, найденный грубым шагом, **уточняется шагом 5 мВ** до
применения запаса (иначе «последняя стабильная» — это произвольная ступень над отказом, а не его
соседка, и запас указывает на непрожжённое напряжение); шаги отмеряются **ПО ТОЧКАМ**,
потому что сетка неравномерна (5 мВ ×94, 10 мВ ×32), с округлением всегда в сторону осторожности.

Остальные интервью закрыты (002 тихие часы · 003 значение N · 004 не заводилось — отвечен до
вопроса · 005 иконки · 006 определение тюнинга), хоумворки 01 и 02 — DONE. Полная история закрытий —
в хронике.

**Один вопрос ждёт в плане, не в интервью:** **Q1 (план 10 §0)** — что показывает значок трея:
логотип KAGO всегда или лица режимов как сейчас. Ответ цитируется в план 10, дальше слабая модель
исполняет §4.2+.

**Открытые сигналы по KAIF наверху:** [#3](https://github.com/MikalaiKryvusha/KAIF/issues/3) ·
[#4](https://github.com/MikalaiKryvusha/KAIF/issues/4) · [#5](https://github.com/MikalaiKryvusha/KAIF/issues/5) ·
[#6](https://github.com/MikalaiKryvusha/KAIF/issues/6) · [#7](https://github.com/MikalaiKryvusha/KAIF/issues/7) ·
[#30](https://github.com/MikalaiKryvusha/KAIF/issues/30) — **защищённый цикл закрыт раньше названного
часа (сессия 59, найдено ВЛАДЕЛЬЦЕМ):** «ранний финиш ломает заказ» живёт прозой без механического
носителя; локально — `bugs/KAIF/13` + урок EXP-0167 («время церемоний тратится ПОСЛЕ границы, а не
резервируется до неё»).
Локальные `bugs/KAIF/*` держать открытыми, пока обновление их не закроет. **KAIF#6 несёт локальное
расхождение** — секция *Languages* в `AGENT_GUIDE.md` переписана целиком (`bugs/KAIF/03`).

---

## Эстафета — что делать следующей сессии, по шагам

Ты стартуешь с пустым контекстом. Ниже всё, чтобы стать полезным за десять минут.

> ✅ **РЕЦЕПТ ПРОВЕРКИ ПЕРЕЗАГРУЗКИ ИСПОЛНЕН И СНЯТ ОТСЮДА 2026-08-23 17:2x. ВЛАДЕЛЕЦ ОТВЕТИЛ, И ВСЁ
> ЧЕТЫРЕ СОШЛОСЬ.** Здесь стоял вопрос «пережил ли профиль перезагрузку» с тремя командами; он
> закрыт дважды — его глазом и приборами. Команды остаются рабочими и живут в блоке сессии 43 ниже.
>
> | что проверялось | ответ | чем подтверждено |
> |---|---|---|
> | режим пережил перезагрузку | ✅ | `boot-apply.jsonl` → `16:28:32 verdict: applied`; карта читается **250 Вт** против заводских 300 |
> | окна терминалов при входе | ✅ ушли | владелец: *«окон терминалов на запуске не было»* + `EnumWindows`: консольных окон 13, **ВИДИМЫХ 0** |
> | язык на экране входа | ✅ ENG | владелец видел; `HKU\.DEFAULT\…\Preload` = `1:00000409, 2:00000419`, правка пережила перезагрузку |
> | ошибка explorer при выключении | 🟡 **одна чистая из скольких-то нужных** | только глаз владельца — записи в журнале нет ВООБЩЕ (перепроверено: id 1000 по explorer за 7 суток — ноль) |
>
> ⚠️ **Строка `state: "correction"` от 14:31 в журнале загрузок — НЕ история загрузок.** Агент сессии
> 41 сам испортил боевой журнал (блоки писали `sb.journal` вместо `sb.jr`, путь получался
> `undefined`), изъял 12 своих строк и записал поправку. Оригинал — `boot-apply.jsonl.contaminated.bak`.

## 🟡 №2. `bugs/91` — снятие нагрузки, РАЗБОР, не починка

Первое срабатывание 31.08: `ok=false`, **6693 мс при таймауте 5000**. Нагрузка прожила 6,7 с после
того, как защита решила спасать. Разница 1,7 с сверх таймаута — сама по себе улика. Причина
неизвестна; чинить вслепую запрещено ещё до первой попытки.

### 🎯 И ГЛАВНОЕ ПРО ПЛАН СЛЕДУЮЩЕГО ПРОГОНА — накопление теперь РАБОТАЕТ

`bugs/94` починен: архив проб больше не затирает себя, повтор той же тройки уходит в
`runs/vmin/archive/` со штампом времени того прожига. Владелец выбрал этот путь словами
*«можем накопить случаи, да, это более научный метод»*.

**Значит следующим прогоном имеет смысл СОЗНАТЕЛЬНО повторить ту же тройку** — 2145 МГц с подъёмом
555, на которой замок сорвался (+382 МГц). Раньше повтор уничтожал предыдущий случай; теперь
каждый добавляется. Три-четыре повтора дадут `bugs/86` то, чего у него нет с самого начала, —
**больше одного наблюдения.** Сегодня: один положительный случай против двенадцати отрицательных,
и на таком основании причину не устанавливают.

---

**План починки: `plans/82` с блок-схемой измеренного вечера.** Порядок шагов выведен, а не назначен.

1. ✅ **`plans/82` Ш1 ИСПОЛНЕН (сессия 74) — `bugs/93`, свой журнал у прогона (`--log`).** Стоял
   первым потому, что пока отказ невидим машине, любой следующий результат пришлось бы проверять
   глазами. Теперь он видим: код возврата доходит, журнал пишется сам и несёт код последней строкой.
2. ✅ **Ш2 ЗАКРЫТ ВЛАДЕЛЬЦЕМ 17:33 (`interviews/024` = E) — И ОН ОТМЕНИЛ ПОСТАНОВКУ ВОПРОСА:**
   *«Сколько угодно спасений разрешаю. KAGO — это автоматизированный движок тюнинга видеокарты.
   В идеале он должен работать по принципу "Запустил и забыл — оно само все сделало"»*.
   🔴 **Это отменяет МОЙ Ш6 `plans/81`, сделанный в тот же день:** я провёл спасения в накопитель,
   который ОСТАНАВЛИВАЕТ полосу, — построил ровно то, что он запретил. Ш3 переписан: спасения
   ПОКАЗЫВАТЬ, но НЕ ОСТАНАВЛИВАТЬ. Отказ АПВ, тормоз «два зависания подряд» и почастотный счёт
   пульса/драйвера не трогаются.
   🎯 **Вторая половина ответа — ВИДЕНИЕ, и оно перенесено в `GOAL.md` → «🤖 ЗАПУСТИЛ И ЗАБЫЛ»**
   вместе с честно названным противоречием: рельс S1 требует владельца У МАШИНЫ, и куплен он
   зависанием на 5 ч 40 мин. Слово «в идеале» прочитано как ЦЕЛЬ, а не приказ снять S1 сегодня;
   путь к ней — устранение причин (`bugs/86`, `bugs/92`), а не ослабление рельса.
   **Порядок работ `plans/82` переставлен: с симптома на причину.**
3. **Ш4 и Ш5 — РАЗБОР, не починка** (`bugs/92`, `bugs/91`): обе причины неизвестны, слепая починка
   запрещена ещё до первой попытки.
4. **Ш6 — сторож словаря** `tools/glossary-guard.mjs` в ворота `npm run check`. Долг сессии 73.
5. **Вторая половина правила владельца (поиск шагом 5 мВ)** — Ш5 плана 81 её разблокировал, ветки
   поиска всё ещё нет. Текст отказа исправлен и ПРЯМО запрещает мёртвую причину.
6. 🟡 **Долг с адресом:** мигание батареи повторилось один раз (`watchdog` красный в батарее,
   зелёный в одиночку — 25 блоков, 0 провалов). Полный вывод сохранён.

---

## 🔴 СЕССИЯ 98 (13.09, вечер) — СНИМОК КАК ФОРМАТ ГОТОВ ОФЛАЙН · ЖИВОЙ ПЕРЕПРОЖИГ УБИЛ МАШИНУ (19-я АВАРИЙНАЯ ПЕРЕЗАГРУЗКА)

> **Читать первым.** Мерка не сдвинулась: **краёв 5/389 · режимов 1/4**. Прогон 21:08 умер через
> 80 с на 2955 МГц / 890 мВ; документ кривой он не изменил.
> ✅ Долг принудительного закрытия 13.09 оплачен закрытием сессии 99 (14.09 23:4x, `/end-chat-soft`): проход
> судьи по утверждениям сессии, стрижка (сессии 88, 89, 92 → хроника, 488 строк), README сверен (замысел
> режимов не менялся — эксперимент в Optimised временный), `npm run kaif:check` — только предупреждения
> о размерах документов.

### 🔴 СМЕРТЬ МАШИНЫ 13.09 21:09 — `bugs/138`, коммит `343947a`

- **Порядок вечера:** владелец ответил на `interviews/029` (A) и сказал о темпе («я думал мы за два
  вечера мне карту затюним»); агент предложил план двух вечеров; владелец: *«можешь запускать
  прогон»*. Карта сброшена в завод, безопасный режим дисков взведён, Broadcast и Parsec закрыл
  ВЛАДЕЛЕЦ сам (классификатор разрешений запретил агенту и это, и первый запуск окна), полоса
  `plans/90` `--sweep --from 3067 --to 2377 --dashboard --log reburn-42` запущена в 21:08:20.
- **Смерть:** 895 мВ на 2955 МГц прошло (seq 964), на **890 мВ (seq 965) карта встала внутри
  драйвера за ≈40 мс** (прогресс прожига и чтение мощности встали вместе). Предохранитель сработал
  через 1,2 с; **рука 1 (`taskkill`) висела 74 с синхронно в цикле судьи — судья 74 с не писал
  ничего**; драйвер семь раз записал ошибку (nvlddmkm 153/14), восстановления нет; рука 2 запущена
  через 75 с и расписки не оставила. Перезагрузка владельцем ≈21:14.
- **Вывод, сказанный владельцу:** этот класс смерти (карта отвечает, но не считает — как 08.09)
  защитой изнутри машины не лечится; предложенная агентом починка «рука напряжения не ждёт руку
  нагрузки» эту машину, скорее всего, не спасла бы. Слепота судьи — настоящий, но не решавший дефект.
- **Счёт владельца:** *«Уже 1800 человек пало смертью невинных от твоей некомпетентности в
  разработке KAGO»*. Счёт по журналу Windows: **19 аварийных перезагрузок с 11.08**.

### ✅ ЧТО СДЕЛАНО — `plans/99` Ш1–Ш4, улики в §8 плана

| шаг | итог |
|---|---|
| Ш1 формат снимка | `curve-store` §3c; тело судит `validateCurveDoc`, запись жёсткой ссылкой — неперезапись доказана на настоящей файловой системе |
| Ш2 поле `curveSnapshot` | ОБЯЗАТЕЛЬНЫЙ ключ (строка `FORK:` у `SETTING_KEYS`); миграция по описи потребителей — 8 файлов + 8 мест в коде; `npm run profiles` против живой карты: 8 из 8 приняты |
| Ш3 применитель | ссылка и снимок — ОДИН путь, отличается только источник строк; счётчик приёмки научен снимку до перевода режимов |
| Ш4 команда | `curves/battle/2026-09-13T20-25-42.json`: краёв 5 из 389, sha256 источника совпал с `Get-FileHash` |

**Главная улика — на настоящих данных, а не на фикстуре:** одно чтение таблицы и сдвигов карты,
настоящая опора → вектор режима Optimised через `curveRef` и через снимок **совпали побайтно** (69
ненулевых сдвигов, намерение 3090 МГц, монотонен). Это условие Ш5 «перевод не делается, пока не
совпали» — выполнено.

**Батарея 13.09 20:28:** 53 набора, красных 0, **2747 блоков** (2697 в сессии 96; +11 `bugs/133`,
+39 этой работы: `curve` 132 → 155 · `profiles` 53 → 64 · `profile` 83 → 88). Мутаций 20 (С1–С6,
П1–П4, Р1–Р5), каждая краснит названные блоки; у П1 сверх перечня покраснел один блок по понятной
причине — разобрано в плане. Коммит `31c613a`.

### 🔴 ЭСТАФЕТА ПОСЛЕ СЕССИИ 98 — ПЕРВОЕ ДЕЛО СЛЕДУЮЩЕГО ЧАТА

0. 🔴🔴 **ОТКРЫТЫЙ ВОПРОС ВЛАДЕЛЬЦУ, заданный в чате 13.09 и не отвеченный — оформить `interviews/030`
   сценариями, если он не ответит в чате:** «Выводим края от известных (напряжение не падает с
   частотой; вывод разрешён 24.08) с запасом, а прожигом — только там, где вывести не из чего, и
   каждое такое место заранее считаем вероятной перезагрузкой при вас?» **Живых прогонов до ответа
   НЕ запускать.** Пока ответа нет, из офлайна законно только то, что не зависит от метода.
0а. 🔴 **Край, купленный смертью, живёт только в журнале:** 2955 МГц — 895 мВ PASS (seq 964), 890 мВ
   убило (seq 965, намерение без вердикта). Следующий запуск развёртки закроет seq 965 как `ЗАВИС`
   → пол R18 890 мВ на 2955 МГц → рабочая точка по правилу владельца 900 мВ. Сверить ЧИСТЫМ
   читателем (`hangFloors`), `resumeState` при живом писателе не звать.
0б. 🟡 **Найдено вечером, тикетов нет:** (1) окно наблюдения показало пульс МЁРТВОГО прогона 09.09 как
   живую тревогу «ЗАМЕРЛО на 368524 с» — поднялось раньше первой записи прогона; (2) сторож глубины
   на 2962 МГц мерил шаг от ЗАКАЗАННОЙ частоты, когда карта выдала 2880 — ложный `cut-short`, близнец
   закрытого `bugs/34`; (3) `tools/grant-agent-rights.mjs --help` исполняет инструмент (класс
   `bugs/118`; изменений не внёс — «права уже выданы»); (4) оговорки судьи по `31c613a`: устаревшие
   места `curves/README.md` §«Почему отдельные файлы» и `profiles/README.md` строки 15–28 · хеш
   снимка разойдётся со свежим клоном (autocrlf, нет `.gitattributes`) · битый JSON снимка даёт
   `SyntaxError` вместо отказа по полю · `--snapshot --from` без значения молча берёт `measured`.
1. 🔴 **Владелец у машины → `plans/99` Ш5–Ш6** (НЕ сделаны; режимы по-прежнему на `curveRef`). Ш5: в трёх профилях `"curveRef": null`,
   `"curveSnapshot": "2026-09-13T20-25-42"`; откат — обратные три строки. Ш6: клик по ярлыку
   `⚖️ Optimised` → задача возвращает 0 → иконка в трее (глаз владельца) → запись в рабочий документ
   не меняет применяемый вектор. ⚠️ **Если рабочий документ изменится до Ш5 (живой прогон), снять
   новый снимок и ПОВТОРИТЬ побайтную сверку векторов** (как 13.09: одно чтение таблицы и сдвигов,
   профиль разрешён через `curveRef` и через снимок) — перевод на снимок, не прошедший сверку, не делается.
2. ✅ **`interviews/029` отвечен владельцем 13.09 — A.** Ворота передачи — минимальные: только уже
   написанные проверки, новых не строить (слово о темпе в разделе «Ждёт решения владельца»).
3. 🟡 **`plans/100` — ярлык перестаёт молчать при отказе.** Офлайн Ш1–Ш4, Ш5 при владельце.
4. 🟡 **Долги сессии 97 без изменений:** `bugs/136` (опора немонотонна) · `bugs/137` (внешняя карта
   врёт) · приёмка эпика 03 (`bugs/25` п. 1) · **живые при владельце:** перепрожиг 42 строк
   `bugs/115` AC4 (попытка 13.09 кончилась `bugs/138`) и 7 краёв «без сторожа на посту»
   (`interviews/026` Q4 = A) · фаза 3 эпика 95.

**Состояние машины на закрытии 13.09 21:4x:** карта заводская, для автозагрузки запомнен `factory`
(Optimised НЕ применён — владелец вернёт ярлыком) · сторож не взведён · безопасный режим дисков
снят при входе (J, F, E на запись) · **Broadcast и Parsec закрыты владельцем и НЕ подняты** ·
окно наблюдения умерло с перезагрузкой.

`DELIVERY: краёв 5/389 → 5/389 · режимов 1/4 → 1/4; blocker: живой перепрожиг убил машину (bugs/138), метод поиска края ждёт слова владельца`

## 🟣 СЕССИЯ 99 (14.09, вечер, `/resume`) — РЕДАКТОР КРИВОЙ ДЛЯ ВЛАДЕЛЬЦА · ЭСТАФЕТА СЕССИИ 98 В СИЛЕ

> **Слово владельца:** *«открой картинку VS кривой, которая у нас получается. Сделай её интерактивной,
> чтобы я мог покрутить точки, и внизу кнопка [Сохранить] — чтобы я мог записать значения, которые
> накрутил — ты их подставишь в профиль»*. Живых прогонов не было, в карту не писалось ничего.

- **Сделано — `tools/curve-editor.mjs`, коммит `f132c1a`.** Страница `http://127.0.0.1:17387/`: 19 углов
  эффективной линии (`curve-map.effectiveCurve` через `loadFacts`), привязка к сетке и лестнице карты,
  предпросмотр рисков ОТ ПРАВОК против сегодняшней кривой (пол зависания · глубже доказанного, доказанное
  наследуется вниз по частоте). **«Сохранить» пишет только `curves/edits/<момент>.json`.** Самопроверка
  11 блоков, 4 мутации краснят свои блоки; строка в таблице инструментов `AGENT_GUIDE.md`.
- **Найдено первым скриншотом и исправлено до показа:** нетронутая страница писала «изменено 183, глубже
  доказанного 156» — сравнение шло со строками документа, а не с линией, что ложится в карту.
- **Показано владельцу:** в СЕГОДНЯШНЕЙ кривой 9 частот стоят на записанном полу зависания (2115 и
  2197 МГц на 775 мВ · 2977…3052 МГц на 1045 мВ) — записи противоречат прожигам выше по частоте; это
  долг «физика стен» (`bugs/124`, 13 противоречий), редактор его только показывает.

- **21:24 владелец сохранил правку** (`curves/edits/2026-09-14T21-24-52.json`, коммит `3db77c0`) и спросил «будет
  работать?» — разбор в чате по записям журнала: в рабочей зоне запас над реальными краями +45…+75 мВ;
  «стены» 1090–1135 мВ на 2977–3052 МГц — девять срабатываний защиты 08.09 за две минуты при выдаче
  2812–2910 МГц. Советы: низ вернуть к текущей, угол у 2115 МГц поднять до 800–805.
- **22:06 по его заказу — кривая агента** `curves/proposals/2026-09-14T22-06-04.json` (`tools/curve-proposal.mjs`):
  14 краёв-опор по выданной частоте, рабочая точка «последняя стабильная + шаг», интерполяция 106 строк,
  экстраполяция вниз 261 и вверх 8; открыта ему в редакторе рядом с его правкой.
- **Владелец: «почему твоя кривая такая рваная»** — причина названа по строкам (склейка минимумов/максимумов
  ступенчатых правил); на развилке он выбрал **A — плавная глубина**. **22:40 — плавная кривая агента**
  `curves/proposals/2026-09-14T22-40-17.json`: сток минус нижняя выпуклая оболочка ограничений глубины
  (60 → 140 → 185 → 205 мВ), запас над рабочими точками краёв +0…+35 мВ.
- 🧪 **22:5x — ЭКСПЕРИМЕНТ ВЛАДЕЛЬЦА ЗАЛОЖЕН В OPTIMISED.** Его слово дословно: *«Под мою ответственность -
  заложи в Оптимайзд профиль»* · *«и запиши, что ЭТО ЖКСПЕРИМЕНТ, НИКОЛАЙ ЗАХОТЕЛ ПРОВЕРИТЬ КРИВУЮ
  ПРИДУМАННУЮ ПРОСТО ИЗ ВОЗДУХА»*. `profiles/optimised.json`: `curveRef: null`, `curveSnapshot:
  "2026-09-14T22-54-29"` → `curves/battle/2026-09-14T22-54-29.json` ← `curves/experiment-2026-09-14.json`
  (388 строк `origin:experiment` — новый тег закрытого словаря, краем не считается; краёв в снимке 0).
  Сухой прогон пути ярлыка (resolve → вектор → R11/R12/R13, без записи): отказа нет, 8 нижних точек
  упираются в рычаг +1000 МГц (в безопасную сторону). **В карту не писалось; применит клик владельца по
  ярлыку ⚖️ Optimised** — и режим станет состоянием автозагрузки. Откат профиля — две строки (`curveRef:
  "measured"`, `curveSnapshot: null`), откат карты — 🔄 Stock Default. Приёмка в профиле снята на ПРЕЖНЕЙ кривой.
- 🧪 **22:59 владелец применил ярлыком; 23:0x–23:10 замер Q2RTX** (ворота прибора +42,7 %). Карта сверена
  чтением: 250 Вт, 121 из 127 точек = вектор профиля, 6 точек 740–770 мВ подрезаны порядком при записи (в
  безопасную сторону), поднятые точки ≤ 3082 МГц. Захваты `exp0914_a/b` — FPS НЕ МЕРКА: процессор 75–80 %
  (монтаж соседнего проекта); `exp0914_c/d` при процессоре 10 %: **FPS 57,65 / 57,71** (01.09 на прежней
  кривой 57,62 / 57,08) · 250 Вт · **68–69 °C** (было 70–72) · вентилятор 53–58 % (было 58–63) · частота
  2842–2857 МГц (было 2797). Сбоев 0, `nvlddmkm` 0 во всех четырёх захватах. Гипотеза владельца «напряжения
  выше, греется сильнее» проверена по кривой 01.09 (`eff421b`): в полосе 2700…3090 эксперимент ниже в 46
  частотах, выше в 6 — **2850…2887 МГц на +5…+15 мВ**, ровно там, где карта стоит под нагрузкой.
- **23:25 — кривая «до улик» методом владельца** (*«опустить кривую до улик… с сохранением тренда… экстраполировать
  гладко с оглядкой на сток»*): `curves/proposals/2026-09-14T23-25-14.json` — ТОЧНО через рабочие точки 14 краёв,
  глубина под стоком между краями линейно. Против кривой в Optimised: середина −15 в среднем (до −30), рабочая
  зона −20 (до −35), верх −19 (до −30), низ без изменений. Коммит `26b2d44`. **В профиль не заложена.**
  Изъяны, названные владельцу: лестница 840 → 875 → 900 в рабочей зоне (6 краёв наследуют один прожиг
  2865/870, 3 — 2947/895, потому что прожиги до 29.08 не засчитаны) · 2947 МГц = +10 мВ над смертью 13.09 ·
  низ −60 мВ под стоком без прожигов.

### 🟣 ЭСТАФЕТА ПОСЛЕ СЕССИИ 99

1. 🧪🔴 **ЭКСПЕРИМЕНТ №2 — ПРОВЕРИТЬ ЗАВТРА В НОВОМ ЧАТЕ.** Слово владельца 14.09 ~23:3x: *«Оставишь мне
   текущую оптимайзд, которую мы проверили. В номер 2 - в статус - проверить завтра в новом чате»*.
   **В Optimised по-прежнему эксперимент №1** (снимок `2026-09-14T22-54-29`) — его и оставить до слова.
   Эксперимент №2 — `curves/proposals/2026-09-14T23-34-10.json` (`tools/curve-proposal.mjs`, метод «тренд»):
   сток минус прямая МНК глубины краёв (глубина = −295,8 + 0,1674·f), сдвиг безопасности 8,7 мВ, **разброс
   краёв вокруг тренда 4,9 мВ**; у 9 краёв с чужой «последней стабильной» (2137, 2767, 2782, 2797, 2805, 2820, 2842, 2880, 2887) граница = отказ + 2 шага (его «где
   срезать ещё милливольт»), запас над границами 0…+20 мВ. Против №1: рабочая зона −20 в среднем (−15…−25),
   верх −14, середина −2, низ +56 (ушёл к стоку — безопаснее). ⚠️ Изъяны (вторая названа судьёй при закрытии): полка 24 частоты на 810 мВ (2205–2377 МГц) между краями — длиннее, чем у 22:40 (11) и 23:25 (14); полка ~1740–2197 МГц (795…800 мВ),
   65 подъёмов монотонности (плато стока против наклона тренда 0,17 мВ/МГц). **Шаги завтра:** открыть в
   редакторе рядом с №1 (`/?start=curves/proposals/2026-09-14T23-34-10.json&compare=curves/proposals/2026-09-14T22-40-17.json`,
   окном браузера — НЕ explorer.exe) → по слову владельца: рабочий документ `curves/experiment-2-2026-09-14.json`
   (теги `origin:experiment`) → `npm run curve -- --snapshot --from experiment-2-2026-09-14` → сухой прогон пути
   ярлыка (resolve → вектор → R11/R12/R13) → `curveSnapshot` в `optimised.json` с его словом → его клик →
   сверка карты чтением → Q2RTX c/d-захваты при процессоре ≤ 15 % (монтаж соседнего проекта грузит CPU).
2. **Эстафета сессии 98 не тронута и в силе целиком** — п. 0 (вопрос о методе поиска края, живых
   прогонов до ответа не запускать), 0а, 0б, 1–4.

**Состояние машины 14.09 23:4x:** **в карте Optimised = эксперимент №1** (применён ярлыком 22:59, сверен
чтением; запомнен для автозагрузки) · сервер редактора `node tools/curve-editor.mjs --port 17387` поднят
скрытым процессом, закроется сам после 3 ч без обращений · порт 8787 держит ЧУЖОЙ `tools/web-smoke.mjs --serve`
(не трогался) · `measured.json` не менялся.

`DELIVERY: краёв 5/389 → 5/389 · режимов 1/4 → 1/4; moved by: — (редактор кривой по заказу владельца); blocker: метод поиска края ждёт слова владельца`

## 🟦 СЕССИЯ 100 (18.09, вечер, `/resume`) — KAIF ОБНОВЛЁН С 2.5 ДО 2.7 · ЭСТАФЕТЫ СЕССИЙ 98 И 99 В СИЛЕ
<!-- KAIF-VERSION-OK: блок ниже — летопись обновления, прежние версии названы как история -->

> **Слово владельца** (чат 18.09, две строки одного сообщения): *«выполни resume»* · *«затем обнови kaif до
> последней версии»*. В карту не писалось
> ничего, живых прогонов не было; код проекта не менялся (две строки-комментария в `tools/`).

- **Сделано.** KAIF 2.5 → **2.7 «Audited KAIF»** одним прыжком через 2.6, bootstrap-маршрутом (свежее ядро
  2.7), проход привязан к расписке репетиции в копии дерева. Счётчики ядра: **28 заменено · 31 модуль влит ·
  11 добавлено · 54 сохранено**; задание обновления живого прохода побайтно равно репетиционному. Документы
  владельца машинерия не тронула. Ворота: `kaif-core check` — код 0, зеркал разошлось 0 · `npm run check` —
  100 файлов, 0 упавших, молитва 12/12 · `npm run selftest:all` — **53 набора · красных 0 · 2747 блоков ·
  86,6 с**. Отчёт — `reports/KAIF_UPDATES/KAGO_KAIF_2.7_UPDATE_REPORT.md`, доставлен в исток: **#83**.
  `update-verify` пройден, установочные файлы убраны.
- **Расхождение `baton → handover` (09.09) снято:** исток принял то же слово; четыре файла теперь побайтно
  равны чистому развёртыванию 2.7 (запись — в «Состоянии машины» ниже).
- 🔴 **Строка `DELIVERY:` в открытии и закрытии сессий СНЯТА — и это решение агента `[AI]`, вето за
  владельцем.** KAIF 2.7 убрал её у себя; основание задание обновления приводит так (по-английски, дословно
  ли — задание не говорит): «remove the DELIVERY feature from KAIF — projects started writing it, but I do
  not use it and see no value in it» (12.09, `KAIF_UPDATE_TASK.md`, эпик DR). У нас она числилась «носителем Q1»
  интервью 017, и агент этой сессии сперва подписал её в шести файлах как решение `[OWNER]` — ложно: в
  выбранном владельцем варианте A стоит **мораторий с порогом**, строки там нет; её 28.08 придумал агент и
  записал критерием 4 своей же `ideas/14`. Исправлено во всех шести местах до коммита (урок **EXP-0284**).
  **Что осталось как было:** метрика «краёв X/389 · режимов Y/4» (`MASTER_PLAN.md` → «Метрика приёмки»),
  команда `npm run curve -- --progress`, мораторий «краёв ≥ 195/389».
- 🔴 **Независимый судья (отдельные агенты с чистым контекстом) прошёл ПЯТЬ раз: первые четыре вынесли
  REFUTED — ни разу не по механике обновления** (ворота зелёные при каждом перезапуске), всякий раз по МОИМ
  фразам об уликах и авторстве; **пятый — VERIFIED WITH CAVEATS**, его мелочи исправлены после и повторно не
  судились. Вердикты дословно — отчёт, §5. Первый проход: мой «поиск → 0
  совпадений», которым обосновано снятие строки, оказался ЛОЖНООТРИЦАТЕЛЬНЫМ: кириллица в квадратных
  скобках `grep` в локали этой оболочки не совпадает ни с чем (0 против 4 под `LC_ALL=C.UTF-8`), а
  `git grep -E` слеп к такой скобке даже с локалью. Вывод устоял (слова владельца о строке нет ни в `GOAL.md`,
  ни в интервью), улика — нет; исправлена в трёх документах. Урок **EXP-0285**: ноль совпадений ничего не
  доказывает, пока тот же шаблон не ПОПАЛ по заведомо содержащей строке; для кириллицы — инструмент Grep
  харнесса либо `LC_ALL=C.UTF-8`, без скобок.
- 🆕 **Дефект самого KAIF 2.7 найден и отправлен в исток тем же движением — `bugs/KAIF/19` → origin #74.**
  «Дверь археологии» печатает агенту `grep -rniE "<слова заголовка>"` и велит исполнить как есть; в Git Bash
  флаг `-i` кириллицу не сворачивает: на её же команде 1180 строк против 1241 под `LC_ALL=C.UTF-8` — 61
  строка, где искомое слово стоит ТОЛЬКО с заглавной буквы, невидима. Среди них — **копия заглавными**
  вашего решения о каденции молитвы (`GOAL.md:9-10`, интервью 017, Q2 = B); само решение тот же поиск всё
  равно находит — в выбранном вами варианте (`interviews/interview_017…:52`) и в `GOAL.md:12`. То есть ваш
  прежний ответ спрятан не был; неверным вышло только число в аттестации поиска. ✏️ **Здесь трижды стояло
  неверное, и это качание само по себе урок:** сперва «ваши решения перепредставлены среди пропусков» (доля
  не считалась), затем «решений среди пропусков нет» (взято у второго прохода судьи, строки не открыты),
  затем «ваше решение поиск не увидел» (взято у третьего прохода, не проверено по записи интервью). Все три
  отозваны в тикете и тремя комментариями-поправками в задаче #74; последнюю я перепроверил сам в обеих
  локалях. Сегодня в чате я сказал вам «дефект реальнее» — это тоже было неверно, отозвано в чате.
- **Что меняется в поведении агента с 2.7** (правила фреймворка; приняты `[AI]` по мандату — «обнови kaif
  до последней версии», чат 18.09; каждое пересматриваемо, вето открыто):
  решения подписываются — `[OWNER] «дословно» · дата` либо `[AI]`; «не пересматривать» только у `[OWNER]` ·
  вопрос владельцу задаётся ПОСЛЕ поиска по его прошлым ответам, с аттестацией поиска под вопросом ·
  «тест» = функциональный прогон путём пользователя, самопроверки и сторожа называются гигиеной ·
  утверждение не шире наблюдения («сервер ответил 200», а не «страница открыта у вас») · ложь правится там,
  где оставлена, и оба ритуала закрытия спрашивают «Стоячая ложь: нет | список» · текст, который владелец
  читает как свой, пишется по портрету голоса (`kaif-voice-lint load` → `check`) · слово `resume` первым
  словом сообщения — приказ исполнить `/resume` целиком · урок опыта несёт `class:`, второй провал класса
  без механизма краснит `kaif-experience-lint`.
- **Тикеты о KAIF:** у трёх доставленных строка доставки приведена к читаемой форме, адреса сверены по
  телам задач в истоке — `18 → #55` · `16 → #37` · `06 → #23`. Шесть (07–12) отдельными задачами в исток
  не уходили (сверено списком задач истока), а их содержание стоит в KAIF с версии 2.5 — проверено по
  распакованным шаблонам 2.5 (в лестнице тяжести даже наше число из аудита: «65 % of 68 bug documents»).
  **Как оно туда попало — записано только для тикета 10:** `[OWNER]` «Оба отчёта заберёт исток» (28.08,
  14:07, строка «Outbound» отчёта об обновлении до 2.4: «this report + `bugs/KAIF/10` → origin»). Тикеты
  11–12 едут на отчёте о развёртывании команды, закоммиченном на 18 минут позже, и ваше слово там не
  названо; что ваше «оба» включало его — моё прочтение, `[AI]`, не запись. Про 07–09 запись говорит
  обратное: аудит 28.08 кончается словами «готовы к отправке в origin — по вашему слову», и такого слова в
  записях нет. **Решение `[AI]`, пересматриваемое:** новыми задачами шесть тикетов не заводятся — исток их
  содержание уже несёт, а тикет 07 просит ровно то, что KAIF в 2.7 убрал; несёт их полевой отчёт 2.7 —
  **origin #83** (ссылкой, не полным текстом); у всех шести строка доставки теперь `#83` и раздел закрытия. ✏️ Здесь сперва стояло «исток забрал их с диска по слову владельца от
  28.08», затем «слово записано для 10–12» — оба раза шире цитаты; второй и третий проходы судьи это
  опровергли (класс EXP-0284).
- **`TEAM_CONSTITUTION.md`:** возвращены три обязательства шаблона 2.7 (§2 правило 6 «свободное место просит
  работу» · §4 «строка `free` несёт запрос той же записью» · §9 «конституция входит в ядро перечитывания»).
  Первое назвала проверка ядра, два других нашёл судья. Команда по-прежнему распущена; документ спит.

**Стоячая ложь: нет.** Последняя — черновик `ЗАКАЗ.md`, §9: «строка доставки открывает и закрывает
сессию (Q1)», в Q1 этого нет — вычеркнута вашим словом: `[OWNER]` «вычеркнуть выделенный кусок» (чат
18.09); остальной текст абзаца не тронут. Два других пересказа того же, которые агент править вправе,
судья нашёл вторым проходом, и они исправлены на месте: `TEAM_CONSTITUTION.md` (абзац «What this is…») и
`plans/53`, шаг (9), из которого строка черновика и была собрана.

### 🟦 ЭСТАФЕТА ПОСЛЕ СЕССИИ 100

1. **Эстафеты сессий 99 (п. 1 — эксперимент №2 кривой, проверить при владельце) и 98 (п. 0 — вопрос о
   методе поиска края; живых прогонов до ответа не запускать; 0а, 0б, 1–4) — в силе целиком, не тронуты.**
1а. 🟢 **Долги эстафет сессий 94–96, которых нет в эстафете 98, — подняты сюда при стрижке 18.09** (блоки
   94–97 унесены в `PROJECT_HISTORY.md` дословно, там и полный контекст): `bugs/129` AC3 — шесть стен
   (3030 · 3015 · 3007 · 3000 · 2985 · 2977 МГц), снимаются только перепрожигом (`bugs/115` AC4); AC1 закрыт
   09.09, а метка «AC1/AC4» в старых эстафетах была неверной · `bugs/127` Ш1–Ш3 — реестр уставок, исполнительного органа нет ·
   `bugs/132` AC5 — почему провод входа 2 молчал весь прогон 09.09 (свидетели стоят, ответит первая строка
   следующего живого прогона) · протокол записи 3 переписать в плане: постановочное удушение подделывает
   удары, а не мощность (эстафета 94 п. 2) · эпик 95: фаза 3 — развилка «канарейка на вставшей карте» с TDR
   (ключей `Tdr*` в реестре нет, действуют умолчания ~2 с), не решена; фаза 4 — `E95-AC4`/`E95-AC5` ·
   🔴 **безопасный режим дисков после зависания 09.09 стоит DISARMED, тикет на восстановление при входе так и
   не заведён** — сперва спросить прибор `node tools/safe-mode.mjs`, потом утверждать.
2. ✅ **Оба вопроса закрыты владельцем 18.09:** (а) хук `prompt-resume-word.mjs` подключён в
   `.claude/settings.json` — `[OWNER]` «хук - да, сделать»; проверен на скрипте в обеих оболочках
   (`resume` и «резюм» первым словом → приказ; обычное сообщение и слово в середине фразы → молчание).
   **В живой сессии не наблюдался:** срабатывание увидит первое сообщение, начатое словом `resume`, после
   перезапуска клиента или открытия `/hooks`. (б) Строка черновика `ЗАКАЗ.md` вычеркнута — «Стоячая ложь» выше.
3. 🟡 **Долги, названные, а не спрятанные:** (а) новые линтеры 2.7 НЕ подключены к воротам — на новые
   контуры машинерии действует мораторий владельца (интервью 017, Q1 = A): `kaif-attribution-lint` без базовой
   линии показывает 91 находку, `kaif-experience-lint` — 0 находок и 246 предупреждений о записях без
   `class:`; (б) свой контур согласований (`tools/review.mjs`) на отгружаемый генератор 2.6+ не переводился;
   (в) 🔴 **ворота бюджетов КРАСНЫЕ, и закрытие 18.09 закоммичено поверх них — решение `[AI]`, названное
   открыто:** `node .kaif/kaif-core.mjs check --gate-budgets` → код 1: `STATUS.md` 1625 из 200 · `GOAL.md` 1995
   из 300 · `MASTER_PLAN.md` 478 из 300. Лекарство, которое печатает проверка для `GOAL.md` («вынести в
   летопись»), противоречит решению владельца (интервью 017, Q3 = A: `GOAL.md` — дословный архив, оперативный
   слой — в `ЗАКАЗ.md`, который ещё не утверждён); а без коммита пропала бы эта передача дел. Тикет в исток —
   `bugs/KAIF/20` → **#84** (объявление архива + храповик «должно уменьшаться»). Что делать дальше: STATUS
   стричь по мере исполнения эстафет 98–99 (их блоки — следующие кандидаты); `MASTER_PLAN.md` — пересмотр
   `/revision`; `GOAL.md` — ждёт утверждения `ЗАКАЗ.md` владельцем; (г) внешняя карта проекта
   устарела (`bugs/137`) — не тронута; (д) `.gitignore` в рабочей копии со смешанными окончаниями строк
   (ядро дописывает LF в CRLF-файл; git нормализует при коммите, вреда нет — класс `bugs/KAIF/06`);
   (е) в спящей `TEAM_CONSTITUTION.md` нет ещё шести обязательств шаблона (отсутствуют с шаблона 2.5,
   перечислены в комментарии под §2 правилом 6) — вернуть при первом же слове о возрождении команды.

**Состояние машины 18.09:** этой сессией не менялось и не перечитывалось — по записи закрытия 14.09 в карте
Optimised = эксперимент №1. Каталог `.kaif/backup-2.4-2.5/` заменён ядром на `.kaif/backup-2.5-2.7/` (под
gitignore).

`ПРИЁМКА: краёв 5/389 · режимов 1/4` — без изменений: обновление фреймворка, офлайн.

## 🟥 СЕССИЯ 101 (25.09, `/resume` + аудит по заказу владельца) — ЧИТАТЬ ПЕРВЫМ

> **Слово владельца:** *«выполни аудит проекта… что кардинально менять в подходах. Я уже так отчаялся,
> что вручную просто заказал настройку кривой профиля»*. В карту не писалось ничего, прогонов не было.

- **Аудит 4 — `reports/KAIF_AUDIT/2026-09-25_audit_04_change_the_method.md`.** Вывод: три прошлых аудита
  переставляли работы ВНУТРИ метода «край каждой частоты через отказ машины»; метод арифметически не
  доходит до цели (край = перезагрузка; рабочей полосе нужно ~60). Предложено: кривая по тренду 14
  известных краёв (разброс 4,9 мВ) + запас, спуск запаса с проверкой ЦЕЛОГО режима 20 мин — ваш же
  метод 10.08; заморозка машинерии; канон ≤ 80 КБ ядра; план «четыре ярлыка за два вечера» (§7).
- 🔴 **Развилка владельцу — `interviews/interview_030…` (что считать концом работы: A — четыре
  проверенных режима, B/C — края по частотам).** Работы §6–§7 не начинать до ответа. Эстафеты 98–100
  в силе, но их живые пункты (перепрожиг 42 строк, 7 краёв без сторожа) ответом A снимаются.

## Состояние машины, о котором надо знать

> Повадки машины — в досье среды `AGENT_GUIDE.md`; здесь только то, чего в досье нет: состояние,
> которое кто-то менял.
>
> **Две ловушки оболочки, за которые заплачено:** `/tmp` у Node из bash — это `D:\tmp`, которого нет
> (запись молча падает); **heredoc без кавычек отдаёт текст оболочке — обратные кавычки ВЫПОЛНЯЮТСЯ**.
> Текст правится файловыми инструментами, а не оболочкой; после машинной правки — перечитать.

- **`.claude/settings.local.json`** — грант прав от владельца, под gitignore: `allow` 82, `ask` 0,
  **`deny` 18**. В `deny` в том числе `PowerShell(Remove-Item *)` — он поймал небрежность агента
  2026-08-10, и это правильный сторож. Записи в GPU (`-pl`, `-lgc`, `-rgc`) в `allow`; механического
  тормоза нет — тормозом остаётся правило машины владельца. **Расширять права — только владельцу,
  скриптом `tools/grant-agent-rights.mjs`.** Общие права проекта — в `.claude/settings.json` (в git).
- **`D:\Games\Quake2RTX`** — Q2RTX 1.8.1, установлен по слову владельца. **Откат:
  `D:\Games\Quake2RTX\Uninstall.exe`** (принимает `/S`).
- **Автозагрузка: LosslessScaling выключен, NVIDIA Broadcast оставлен включённым** (по слову
  владельца), флагом в `HKCU:\...\Explorer\StartupApproved\Run`; откат — один байт. Имя значения у
  Broadcast несёт **хвостовой пробел**. Приложения владельца запускать через `explorer.exe <путь>`:
  моя оболочка под администратором, иначе ребёнок унаследует чужие права. ⚠️ **Это про ПРИЛОЖЕНИЯ, не
  про веб-страницы:** страница открывается окном браузера, как окно наблюдения —
  `run-dashboard.windowArgs` (`--app=` в своём профиле). 14.09 агент открыл страницу редактора через
  `explorer.exe`, владелец: *«зачем в проводнике пытаться открыть веб страницу?»*.
- **Фон на карте — помеха замеру.** Chrome, VS Code, Docker, Parsec, NordVPN, PotPlayer, NVIDIA
  Overlay, LG Hub: в покое карта сидит около 825 МГц, а не 180. Владелец разрешил останавливать то,
  что создаёт фоновую нагрузку, **и всё остановленное поднимается обратно**.
- **Рабочий стол был 1280×720 в момент замеров**, а телевизор владельца 144 Гц — отсюда факт 17.
- 🔴 **КОМАНДА РАСПУЩЕНА 2026-09-09, РАБОТА ИДЁТ В ОДНОЙ ВЕТКЕ `main`.** Слово владельца:
  *«я не давал разрешения на ворктри и воркспейс»* · *«закрыть его, слить в main»* · *«работает в
  одной ветке main»*. Сняты рабочие места `KAGO-team-engineer` и `KAGO-team-verifier`, их ветки
  удалены, каталоги убраны с диска. **Сливать было нечего и ничего не потеряно** — проверено ДО
  удаления: в обеих ветках 0 коммитов вне `main`, рабочие копии чисты, обе отставали на 441 коммит
  (заброшенные копии от 28.08). Доска `TEAM_STATUS.md` помечена как история, не как живая команда.
  **Впредь: одна ветка, одно рабочее место; разворачивать команду — только по отдельному слову.**
- ✅ **РАСХОЖДЕНИЕ С ORIGIN KAIF `baton` → `handover` СНЯТО ОБНОВЛЕНИЕМ ДО 2.7, 2026-09-18.** Исток
  принял то же слово (KAIF #57 → эпик HO версии 2.7), и предсказание абзаца ниже сбылось буквально:
  «расхождение исчезнет само». Проверено побайтно против чистого развёртывания 2.7:
  `.kaif/KAIF_REFERENCE.md`, `end-chat-force`, шаблон конституции команды и хук
  `stop-status-guard.mjs` теперь РАВНЫ шаблону истока; задвоенных разделов в навыках закрытия нет
  (свежее ядро 2.7 нашло наш уже переименованный заголовок). Каталога `.kaif/backup-2.4-2.5/` больше
  нет — ядро заменило его на `.kaif/backup-2.5-2.7/`, как и обещает («replaced next interval»).
  Запись ниже оставлена как история решения 09.09.
  <!-- KAIF-VERSION-OK: абзац ниже — запись 09.09 о прошлом состоянии, версии в нём названы как история -->
- 🔶 *(история, 2026-09-09)* **ОСОЗНАННОЕ РАСХОЖДЕНИЕ С ORIGIN KAIF (KAIF #57): `baton` → `handover`.**
  Термин передачи дел переименован в развёрнутой машинерии: **90 вхождений в 39 файлах** — четыре
  зеркала навыков (`.claude` · `.agents` · `.cline` · `.grok`), `.roo/commands`,
  `.kaif/KAIF_REFERENCE.md`, `.kaif/hooks/stop-status-guard.mjs`, `TEAM_CONSTITUTION.md`,
  `TEAM_STATUS.md`, `GPU_TUNING_RAILS.md`, `EXPERIENCE.md`. Причина по слову владельца KAIF:
  «baton» — местный сленг, в индустрии это **handover**; и он не переживает перевод — по-русски
  выходит «батон», то есть хлеб. У нас русские документы этой беды не знали (`grep -i батон` = 0,
  STATUS всегда говорил «Эстафета»), а английская машинерия — знала.
  **НЕ ТРОНУТО НАМЕРЕННО, это записи прошлого, а не инструкции будущему:** <!-- KAIF-VERSION-OK: следующая строка — запись 09.09 о каталоге бэкапа того дня, история -->
  `.kaif/backup-2.4-2.5/**` (бэкап прежней версии), `bugs/KAIF/16` (тикет-запись),
  `tools/verify-review-contour.mjs` и `tools/lib/review-core.mjs` (строки цитируют ФАКТИЧЕСКИЙ
  текст эстафеты `bugs/70`), `.kaif/deploy-manifest.json` (**описывает, что было развёрнуто из
  origin; затереть его — спрятать расхождение**).
  🔴 **Адресовано следующему `/kaif-update`:** примут #57 наверху — расхождение исчезнет само;
  предложат другое слово — **сливать в пользу апстрима, а не затирать молча**. Две `signature`-метки
  манифеста (строки 1766 и 1813) указывают на заголовки, которых в файлах больше нет, — миграция по
  якорю их не найдёт и должна спросить, а не промолчать. Ворота после правки: `check` 92 файла,
  0 упавших, новых нарушений 0.

---

---

## Открытые баги

### 📊 СВОДКА НАСТОЯЩЕГО, СНЯТАЯ МАШИННО 2026-09-07 16:5x — 33 открытых, и они РАЗНОГО РОДА

> ✏️ **ПОПРАВКА 2026-09-07 19:0x (сессия 87): открытых стало 34, 🔴-семёрка стала восьмёркой.**
> Заведён **`bugs/113`** — игровой стенд упирается в ПРОЦЕССОР, поэтому мерка владельца «просадка
> FPS не более 5 %» им структурно не измеряется (улики — `researches/34`). Строки таблицы ниже
> оставлены как сняты машинно; эта поправка — их дополнение, а не переписывание.

> Список ниже — историческое наращивание с заметками по каждому тикету; он остаётся. Эта таблица —
> то, чего в нём не было: **счёт по состояниям, снятый чтением строк статуса ВСЕХ незакрытых
> файлов**, а не памятью агента. Строки честны потому, что за ними теперь следит `backlogtruth`.

| состояние | сколько | тикеты |
|---|---|---|
| 🔧 **ПОЧИНЕНО, ЖДЁТ ЖИВОГО СВИДЕТЕЛЯ** | **13** | 111 · 98 · 97 · 89 · 88 · 87 · 71 · 65 · 64 · 63 · 60 · 56 · 45 |
| 🔴 требуют РАБОТЫ | 7 | 101 · 92 · 91 · 86 · 76 · 75 · 47 |
| 🟡 частично | 12 | 110 · 99 · 96 · 62 · 53 · 43 · 38 · 26 · 11 · 3 · 2 · 1 |
| 🔬 разбор, действие намеренно не предпринято | 1 | 44 |

🎯 **ГЛАВНОЕ ЧИСЛО ДЛЯ ВЕЧЕРА ВЛАДЕЛЬЦА — ТРИНАДЦАТЬ.** Это тикеты, где починка сделана и доказана
офлайн, а не хватает ровно одного: чтобы она отработала на живой карте. **Их закрывает сам прогон, а
не строчка нового кода.** Читать после прогона именно их, а не искать новую работу.

✅ **И БЫЛО ШЕСТНАДЦАТЬ — ТРИ ЗАКРЫТЫ УЛИКАМИ, КОТОРЫЕ УЖЕ ЛЕЖАЛИ НА ДИСКЕ С СЕГОДНЯШНИХ ПРОГОНОВ
И НА КОТОРЫЕ НИКТО НЕ ПОСМОТРЕЛ.** Это, пожалуй, самая дешёвая работа дня: ноль строк кода.
- **`bugs/94`** — свидетель совпал с тем, что тикет назвал ЗАРАНЕЕ, дословно: «ПРОБЫ ПРОШЛОГО ТАКОГО
  ЖЕ ПРОЖИГА: **убраны** в `archive/cap-2700-75-2026-09-04T…`». Пробы 04.09 сохранены со штампом
  ТОГО прожига, а не затёрты сегодняшним, — то самое накопление, которое владелец назвал «более
  научным методом».
- **`bugs/90`** — ждал вечера, где защита срабатывает НЕСКОЛЬКО раз, а полоса идёт до конца. Получил
  ШЕСТЬ срабатываний за два прогона и ни одной остановки.
- **`bugs/93`** — свой журнал прогона без конвейера, последняя строка `КОД ВОЗВРАТА: 0`.
  ⚠️ Его вторая половина («код 2 при остановке спасением») НЕ проверена: полоса сегодня не вставала.

**Урок для следующего прогона:** после каждого живого вечера ПЕРВЫМ делом проходить по 🔧-списку с
журналами в руке. Улика, снятая и не прочитанная, не отличается от неснятой.

⚠️ Честно про 🔴-семёрку: `bugs/76` (зависла рабочая машина) и `bugs/75` несут наивысшую тяжесть в
проекте; `bugs/86` и `bugs/92` — разборы без установленной причины, где слепая починка запрещена ещё
до первой попытки; `bugs/101` разблокирован сегодня (его вторая половина упиралась в `bugs/108`, а
тот закрыт, и сама арифметика блокировки развалилась вместе со штормом).

🎉 **`bugs/25` ЗАКРЫТ 2026-09-07 — прожив семнадцать дней.** Все четыре пункта его плана починки
исполнены: 13 шапок приведены в согласие со свидетелями-коммитами · раздел «Открытые баги» обновлён
и получил машинную сводку · шапка карты архитектуры правлена раньше · **у класса появился сторож, и
база долга 25 → 0 — расхождений в дереве НЕ ОСТАЛОСЬ.** Класс перестал зависеть от внимания агента:
он возвращался трижды и каждый раз находился глазами; теперь его ловит машина.

🔴 **И эту таблицу первым делом поймала МЕНЯ:** `bugs/111` и `bugs/110` числились «🔴 требуют работы»,
хотя 111 починен сегодня, а у 110 опасность закрыта. Я обновил сводку выше и не тронул строки самих
тикетов — ровно та болезнь, которую чинил весь день. Исправлено.

> 🆕 **ЗАВЕДЕНЫ 07.09 (сессия 86) ПЕРВЫМИ ЖИВЫМИ ПРОГОНАМИ ПРИ ВЛАДЕЛЬЦЕ — оба найдены прогоном, ни один чтением:**
> - **`bugs/110`** 🟡 **S1, ОПАСНОСТЬ ЗАКРЫТА В ТОТ ЖЕ ДЕНЬ (Ш0), ТИКЕТ ОТКРЫТ** — режимы `--band`
>   **и `--search`** (третий вход нашёлся при планировании) пишут в живую карту без аварийной защиты
>   и без ворот окна: взведение и ворота лежат целиком в `mainSweep`. Поймано тем, что первый прогон
>   дня был `--band` и через 90 с в списке процессов не нашлось судьи; карта после остановки
>   проверена тремя чтениями, записей ноль. **Оба входа теперь ОТКАЗЫВАЮТ на живом пути** — это
>   заглушка, а не починка. Настоящая — `plans/89` Ш1–Ш4, и она СОЗНАТЕЛЬНО отложена за вечерний
>   живой прогон владельца (довод в самом плане: риск сломать проверенный `--sweep` ради функции,
>   которая сегодня не нужна).
> - **`bugs/111`** ✅ **ПОЧИНЕН 07.09** — защита срабатывала ЭХОМ собственного спасения: второе
>   срабатывание через **1 мс** после взведения, на тишине 6754 мс, накопленной, пока прожиг был
>   законно мёртв. Причина оказалась НЕ в файле сердцебиения (первая редакция тикета), а в ПОРЯДКЕ
>   внутри такта: вердикт снимался до сброса, ворота открывались после. Правка — сверка счёта
>   взведений на входе в такт; набор `fuse` 109 → 116. **Эта же починка убила шторм `bugs/108`:
>   94 срабатывания → 1.**
>
> ✅ **УБРАНЫ ОТСЮДА 2026-09-07 — ОНИ ЗАКРЫТЫ И ПОМЕЧЕНЫ `DONE` НА ДИСКЕ, А СПИСОК ИХ ДЕРЖАЛ.**
> `bugs/104` (репетиция ждала остановки полосы — переписана под Ш5) · `bugs/103` (полигон краснел по
> совпадению маркера — 30 из 30 на 30 семенах) · `bugs/102` (батарея выбрасывала вывод красного
> набора; мигание `fuse` диагностировано, причина смерти `dashboard` по-прежнему неизвестна) ·
> `bugs/95` (восемь приборов исполняли CLI на импорте). Разборы — в самих тикетах.
> ⚠️ **Это ровно та болезнь, против которой заведён `bugs/25`** («репозиторий и список говорят
> разное»), и она нашлась не сторожем, а глазами при сверке имён файлов. Сторожа у неё до сих пор
> нет — пункт 4 плана починки `bugs/25` не исполнен.
>
> 🆕 **ЗАВЕДЁН 04.09 (сессия 81), разбором прогона 10:50 — офлайн, по трём журналам:**
> - **`bugs/101`** 🔴 — рука спасения перечитывает НАШУ запись следующей ступени и зовёт это «драйвер
>   правит результат» (случай 2 `bugs/92` опровергнут; 31.08 и 04.09 — точные совпадения `deltaMhz`);
>   `stopWhen` раз на частоту → пять ступеней без защиты; вход 2 предохранителя на развёртке не проведён
>   (ядро висело 165 с). Разбор, не починка; порядок починки в тикете. **Сессия 81:** находка 2 починена (опрос перед каждой ступенью). **Сессия 82:** находка 3 ПРОВЕДЕНА на живой путь с поправкой плана — уставка от нижней частоты полосы (`bugs/101` → «AMENDED»), режим первого прогона — `interviews/026` Q3. Открыта находка 1 (гонка).

> 🆕 **ЗАВЕДЕНЫ 31.08 ВЕЧЕРОМ (сессия 73) — ВСЕ ШЕСТЬ НАЙДЕНЫ ПРОГОНОМ, НИ ОДИН ЧТЕНИЕМ:**
> - **`bugs/90`** 🔧 — **ПЕРЕОПРЕДЕЛЁН ВЛАДЕЛЬЦЕМ, ЗАТЕМ ПОЧИНЕН В НОВОЙ РЕДАКЦИИ 2026-08-31
>   (сессия 74), живого свидетеля ждёт.** Был «тормоз по числу спасений не тормозит»; стало «тормоза
>   быть не должно» (`interviews/024` = E). Спасения выведены из накопителя в `report.rescues` и в
>   свою строку сводки; четверо новых ворот взяты, пять мутаций красят своё. Свидетель — вечер, где
>   защита срабатывает несколько раз, а полоса идёт до конца.
> - **`bugs/91`** 🔴 — снятие нагрузки не отработало на первом срабатывании: `ok=false`, 6693 мс
>   при таймауте 5000. Разбор, не починка.
> - **`bugs/92`** 🔴 — класс отказа записи **C3** («драйвер правит результат») был ОДНИМ случаем за
>   всю историю журнала; 31.08 всплыл дважды, во второй раз **127 записей из 127 мимо вектора, и
>   это случилось в руке спасения.** Может оказаться дефектом драйвера или деградацией карты —
>   вывод без замера делать нельзя.
> - **`bugs/93`** 🔧 — **ПОЧИНЕН 2026-08-31 (сессия 74), живого свидетеля ждёт.** У прогона появился
>   свой журнал: `--log <смысл>` → `runs/logs/<смысл>/<дата-время машины>.log` (имя ставит машина —
>   слово владельца). Конвейер больше не нужен, код возврата доходит до вызывающего, последней
>   строкой журнала идёт сам код. Семь мутаций красят своё. Свидетель — вечерний прогон при владельце.
> - **`bugs/94`** 🔧 — **ПОЧИНЕН, живого свидетеля ждёт.** Архив проб затирал себя и уничтожил
>   сырую улику `bugs/86`. Обе половины закрыты, доказано пятью мутациями (три сперва прошли
>   зелёными и все три оказались дырами). Свидетель — строка «ПРОБЫ ПРОШЛОГО ТАКОГО ЖЕ ПРОЖИГА:
>   убраны в … — не затёрты» при первом повторе тройки.

> 🆕 **ЗАВЕДЕН 31.08 (сессия 72):**
> - **`bugs/89`** 🔧 — сэмплер двойника писал форму, которую ветка ЗАКРЕПЛЕНИЯ прочитать не может:
>   закреплённая ступень на стенде не проходила НИКОГДА (`unknown` всегда). **ЗАВЕДЁН И ПОЧИНЕН**,
>   мутации TA8/TA9 краснят своё, TA8b зелёная и названа находкой. Ждёт живого свидетеля.

> 🆕 **ЗАВЕДЕНЫ 31.08 (сессия 71):**
> - **`bugs/88`** 🔧 — **РЕПРО ИСПОЛНЕНО И ПОЧИНЕНО 31.08 (сессия 72) по решению владельца
>   `interviews/023`.** Было: спасение обнуляет кривую под живой ступенью, ступень доходит до
>   прожига на ЗАВОДСКОЙ кривой **и закрывается ложным `PASS`**. Стало: спасение закрывает частоту
>   КРАЕМ (последняя прошедшая + шаг сетки), пять мутаций краснят своё. **Открытым уходит одно:**
>   вторая половина правила владельца (поиск шагом 5 мВ, когда прошедших нет) отказывает вслух и
>   ждёт `plans/81` Ш5. Половина механизма (срыв закрепления) на модели опровергнута, но двойник
>   его выразить не умеет — вопрос к живой карте открыт.

> - **`bugs/87`** 🔧 — журнал писал МЕДИАНУ в поле МАКСИМУМА (`vf-step.mjs`), поэтому на ветке
>   закрепления разброс частоты не мог быть записан НИКОГДА: 7 ступеней из 7 несли нуль, включая
>   ту, где прибор напечатал 158 МГц. **ЗАВЕДЁН И ПОЧИНЕН 31.08, доказан СЕМЬЮ мутациями**
>   (разбор — `PROJECT_HISTORY.md` → перенесённый блок сессии 71). Ждёт живого свидетеля:
>   `deliveredMinMhz` в строке вердикта.
>   Цена дефекта была — слепота архива, не вердикт.

> 🆕 **ЗАВЕДЕНЫ 31.08 (сессия 70), в порядке важности для следующей сессии:**
> - **`bugs/86`** 🔴 — срыв закрепления частоты: заказано 2145 МГц, карта под нагрузкой ушла на
>   2677…2835. **Шаг 1 эстафеты ИСПОЛНЕН в сессии 71** (замер по архиву внутри тикета):
>   гипотеза «дело в размере подъёма» как причина ОПРОВЕРГНУТА — подъёмы 847 и 922 дали превышение
>   15 и 8 МГц. Открыто: держатель как разделяющая переменная (10 прожигов против 381).
> - **`bugs/85`** 🔴 — подстой у стока стал вечной стеной частоты. Не починен НАМЕРЕННО: починка
>   ослабляет защиту, это развилка владельца (`ideas/16`).
> - **`bugs/84`** ✅ ЗАКРЫТ — накопитель предупреждений читал поле, которого развёртка не
>   выставляет, и за всю жизнь не получил ни одной записи.
> - **`bugs/79`** ✅ и **`bugs/77`** ✅ ЗАКРЫТЫ — это они снимали запрет владельца на живой прогон.


> ⚠️ **Список ниже отстал от реальности — это само по себе `bugs/25`** (аудит 2026-08-21): шапки
> и теги части багов противоречат коммитам. До прохода `/check-backlog` сверяйся с блоком
> сессии 33 и с `bugs/25`, а не только с этим списком.
>
> ✅ **ПРОХОД `/check-backlog` ИСПОЛНЕН 2026-08-30 18:1x (сессия 68) — по ПЛАНАМ. Багов он не
> сдвинул: из 19 открытых тикетов закрывать нечего, каждый несёт названный хвост.** По планам
> поставлено **8 тегов `DONE`** (`plans/12` · `17` · `18` · `34` · `35` · `48` · `49` · `50`),
> каждый с ДВУМЯ независимыми свидетелями — строкой родителя, воротами следующей фазы в
> уже помеченном документе, коммитом или замером в прогоне. Исправлены две шапки эпиков, врущие в
> сторону недооценки (`plans/13` «ни строки кода не написано», `plans/16` «кода не написано»);
> оба эпика оставлены ОТКРЫТЫМИ с названной причиной. 🔴 **Новая находка: `plans/15` — фаза 2
> эпика 02 исполнена, а расписки судьи нет нигде**, и времена спорят (STATUS: судья в 00:5x;
> ворота в `plans/13`: «ЖДЁТ СУДЬИ» в 01:1x, ПОЗЖЕ). Разбор — `bugs/25` § «Ревизия сессии 68».
> **Список открытых багов ниже сверен с именами файлов и шапками 2026-08-30 17:3x и на эту минуту
> им соответствует;** новый тикет сессии — `bugs/74` (сторож краснеет на закрытом документе).

- **`bugs/71_…`** 🔧 — **ВЕСЬ ПЛАН ИСПОЛНЕН 2026-08-30 17:4x (сессия 68): СТОРОЖ + ОТКАЗ ПОДЪЁМА.
  Ждёт живого подтверждения — им станет первый же вечерний подъём страницы при владельце.**
  Сторож вопросов несёт ось «владельцу нечего нажать»: **G12a** — вопрос есть, вариантов ноль, а в
  теле ячейки таблицы; **G12b** — слот ответа есть, а разобранных вопросов НОЛЬ (второй свидетель —
  собственная первая редакция `interviews/020`). Область сужена до документов, ЖДУЩИХ владельца:
  первый прогон дал 23 настоящие находки в закрытых интервью, а по ним действовать нельзя.
  🆕 **Шаг 3 закрыт, и половина его оказалась не нужна — это выяснил замер, а не чтение плана:**
  ветку «ноль ВОПРОСОВ» ворота `bugs/41` несут с 23 августа (прогон: отказов 1). Настоящей дырой
  была одна ветка — G12a: `interviews/018` @ `666e17c` давал **0 отказов подъёма**, ровно так
  владелец страницу и увидел. Теперь отказов 1, с буквами `A, B, C, D` и починкой в тексте.
  Улика вынесена в ядро одной функцией (`review-core.unreachableVariantLetters`) с ДВУМЯ
  потребителями — сторожем и воротами. `verify:contour` 24 → 28 → **30**; мутации **MA** (общая
  улика сломана) красит ОБЕ стороны, **MB** и **MC** — ровно свой блок. Контроль: ждущие документы
  проекта поднимаются по-прежнему. Урок — **EXP-0198**.
- **`bugs/52_DONE_…`** ✅ — **ЗАКРЫТ 2026-08-30 14:1x ПЕРЕИЗМЕРЕНИЕМ, а не починкой.** Премиса
  умерла вместе с набором из трёх форм: ступень была **36,0 с** и решалась `branchy/sustained`
  (23–25.08), стала **15,0 с** с ОДНОЙ формой `furnace/sustained@0` (21 из 21 после `plans/50`).
  Три прожига по 24 с в 15 с не помещаются — окно, показывающее один прожиг, показывает правду.
  Оставшиеся ~5 с объяснены `bugs/53` (голова 3 с + хвост 3 с, зазор между ступенями 0,0 с).
  Наблюдение владельца было верным про 24 августа; изменился предмет, а не его правота.
- **`bugs/12_DONE_…`** ✅ — **ЗАКРЫТ 2026-08-30 13:5x РЕПРО НА ДВОЙНИКЕ.** Наверху диапазона частоту
  держат ДВОЕ, и прогон говорит это по каждой из 16 ступеней 2887 МГц: «потолок держит **кривая +
  замок**». Пункт 4 его починки (потолок КОНВЕРТА вместо потолка частоты) был исполнен вопреки метке
  ОТКРЫТО. Оба хвоста закрылись раньше миграцией облака тегов. Уточнение важнее закрытия: замок —
  это `-lgc` ДИАПАЗОНОМ, а не пин, и буква шага 7 наверху недостижима по свойству карты
  (`researches/11`: `-lgc 3082,3082` выдал 2887), исполняется её СМЫСЛ.
- **`bugs/73_DONE_…`** ✅ — **ЗАВЕДЁН И ЗАКРЫТ 2026-08-30 13:2x.** Пол карты узнаётся ПОВТОРЕНИЕМ
  заказов, а метка мерила ШИРИНУ промаха: плато выдачи в журнале 35, из них **21 уходило чистым
  замером**, включая ступень, отобравшую 45 мВ у пятнадцати частот. Ширина не работает
  принципиально — рычаг в МЕГАГЕРЦАХ, достижимые напряжения частоты это подмножество сетки.
  `engine` 387 → 391, мутации EL · EM · EN. Правило владельца 16.08 не тронуто, и это стережёт
  блок, который краснит мутация EM. Урок — EXP-0195. **Вынесено на подтверждение: `interviews/020`.**
- **`bugs/72_DONE_…`** ✅ — **ЗАВЕДЁН И ЗАКРЫТ 2026-08-30 13:1x.** Ворота `bugs/63` закрыли одно
  направление храповика из двух, а на втором делали документ невалидным — полоса встала бы
  (`stoppedBy: 'document'`). Лечение одним предикатом: `demandsVoltage` не признаёт `origin:overshot`
  утверждением о потребности, тем же доводом, каким уже не признаёт `origin:ratcheted`. `curve`
  85 → 89, мутации EE · EJ · EK. Урок — EXP-0194.
- **`bugs/69_DONE_…`** ✅ — **ЗАВЕДЁН И ЗАКРЫТ 2026-08-29 (21:2x → 22:0x).** Сторож конверта И4 не
  краснел ни от какой карты двойника: зонд с перелётом буста ×10 дал максимум выдачи 3082 МГц при
  конверте 3090 и ноль строк документа выше конверта. Зажимов оказалось ТРИ (сетка частот кончается
  конвертом · `Math.min` по R13 · граница частоты эпика 43), и третий нашёлся только прогоном ПОСЛЕ
  снятия первых двух. Закрыт решением владельца (`interviews/019` = A): свойство
  `deliverStepsAboveEnvelope`, выдача 3098 МГц при объявленных 3090, И4 покраснел. В тикете осталась
  ОПИСЬ всех шести сторожей против того, что способна произвести карта — она объясняет, зачем
  понадобилась новая строка закрытого словаря физики. Уроки — EXP-0183, EXP-0184, EXP-0185.
- **`bugs/67_DONE_…`** ✅ — **ЗАКРЫТ 2026-08-29 22:1x, обе половины.** Трип предохранителя
  останавливал полосу, а прогон возвращал КОД 0 — отказ, невидимый машине. Стало: код 1, класс
  `fuse-rescue`, причина ведёт в песочницу прогона. Различитель — поле, а не разбор собственной
  прозы. Блоки 375 → 380, мутации ×4, стоп оператора проверен отдельным блоком. **Сторож полигона
  И2 получил первого живого свидетеля на настоящем выводе.** Урок — EXP-0187.
- **`bugs/59_DONE_…`** ✅ — **ЗАКРЫТ 2026-08-26 11:0x, найден ВЛАДЕЛЬЦЕМ глазом за десять секунд
  внимания.** Ступень 37 с там, где заказано 10. Сетка форм против ватт опровергла ДВЕ посылки
  самого бага: медиана 181 Вт — средняя по трём разным формам плюс 34 % простоя, а «не хватает
  105 Вт» относилось к `sdc_fma`, которым развёртка не пользуется с 2026-08-22. **`furnace` в предел
  упирается: 307,8 Вт при лимите 300.** Прожиг стал одной формой на 10 с (`plans/50`); нагрузки на
  ступень 25 → 10 с. Уроки — EXP-0157, EXP-0158.
- **`bugs/56_the_watch_window_reports_connected_without_a_witness_the_owner_can_see.md`** 🔧 —
  **ПОЧИНЕН 2026-08-25 19:1x, ЖИВОГО ПОДТВЕРЖДЕНИЯ ЖДЁТ.** Найден ВЛАДЕЛЬЦЕМ: окно докладывало
  «подключилось», опираясь на поток событий, который может держать ЧУЖОЙ фоновый процесс прошлой
  сессии. **Механизм подтверждён замером на живой машине: 52 процесса браузера, окон верхнего
  уровня 1, окон KAGO 0** — 51 из 52 безоконные. Появился второй свидетель
  (`countVisibleWindows`), доклад различает ТРИ состояния и в двух СПРАШИВАЕТ «ВИДИТЕ ЛИ ВЫ ОКНО?».
  Воротами новый свидетель НЕ сделан намеренно (он видит только Edge и Chrome) — развилка за
  владельцем. Блоки 72 → 75, мутации AQ · AS. Уроки — EXP-0145. **В вечернем прогоне оба свидетеля
  сошлись** (поток 1, окон 1), но это ещё не проверка отрицательной ветки.
- **`bugs/55_DONE_…`** ✅ — **ЗАКРЫТ 2026-08-25 01:1x, найден ЖИВЫМ ПРОГОНОМ.** Прерванный спуск
  писал менее глубокое поверх более глубокого, и храповик разносил это вверх: **22 измеренные
  строки подняты с 800–845 до 850 мВ.** Документ возвращён откатом, дефект вылечен в истоке
  (вариант B), проверено на тех же данных: 22 → 0. Мутации BC · BD. Урок — EXP-0144.
- **`bugs/54_DONE_…`** ✅ — **ЗАКРЫТ 2026-08-24 23:1x.** Журнал молча ронял выданную частоту: 678
  строк `passed`, поле в НУЛЕ из них. Лечение — не сторож над перечислением, а отсутствие
  перечисления (слово владельца о простоте). Урок — EXP-0143.
- **`bugs/51_safe_mode_cannot_be_fully_disarmed_because_it_reads_conditions_not_its_own_receipt.md`**
  🔴 — **БЛОКИРУЕТ ВЕЧЕРНИЙ ПРОГОН, ЕСЛИ НЕ ЗНАТЬ ОБХОДА.** Безопасный режим нельзя снять до конца:
  признак `torrent` читает УСЛОВИЕ («клиент не запущен»), а не расписку («мы его закрыли»), поэтому
  `--off` оставляет `HALF-ARMED`, а гейт на половине отказывает прогону. Совет тулзы после `--off` —
  та же команда, которая только что отработала. **Обход до починки: взводить ПОЛНОСТЬЮ**
  (`node tools/safe-mode.mjs --on`) — для живого прогона это и есть правильное состояние.
- **`bugs/57_a_cut_short_close_feeds_the_upward_ratchet_and_erases_measured_rows.md`** 🔧 —
  **ЗАВЕДЁН И ПОЧИНЕН 2026-08-25, ПОДТВЕРЖДЁН ЖИВОЙ КАРТОЙ.** Близнец `bugs/55`: тот сторож
  защищает строку, которую ПЕРЕЗАПИСЫВАЮТ, а урон нанесло третье направление — храповик ВВЕРХ от
  прерванного закрытия. Те же 22 строки, вторые сутки подряд. Шапка сторожа `bugs/55` при этом
  ПИСЬМЕННО объявляла это направление безопасным («без записи мельче ему нечего разносить»), и
  живой прогон это опроверг: закрываемая строка была ЗАВОДСКОЙ, запись законна, разнеслась она.
  Лечится тем же предикатом `demandsVoltage` — второго определения не заведено. Документ
  отремонтирован точечно (22 подняты → 0, 13 углублённых сохранены), следующий прогон подтвердил
  картой: **поднято 0, сторож сработал трижды**. Мутации BE · BF. Уроки — EXP-0147, EXP-0148.
  ⚠️ Не DONE: остался невосстановимый след из семи строк, см. эстафету.
- **`bugs/50_the_card_leaves_the_frequency_cap_and_the_rung_gets_no_verdict.md`** 🟡 — **ГЛАВНЫЙ,
  И 2026-08-25 ОН РАЗДЕЛИЛСЯ НА ДВА.** Прибор фазы 4 эпика 36 отработал на двух живых полосах, и
  его показания прочитаны из журнала — без единой записи в карту. **Ведущая гипотеза документа
  («таблица не устоялась») мертва:** у всех шести ступеней последней полосы `writeSettled: true`,
  поточечная сверка зелёная, а `offeredAfterMhz` РАВЕН потолку — форма записи вставала. Под одним
  сообщением стояли ДВА отказа: **`ЗАПИСЬ`** — кривая после записи сама предлагает выше потолка
  (`seq 702`, класс C3, **один случай за всю историю журнала**) и **`КАРТА`** — форма встала, а
  карта под нагрузкой ушла выше неё (`seq 727 · 728 · 731`, +15, +23, +23 МГц, **три случая в одной
  полосе**). Остановка теперь называет держателя, поле `ceilingBreachHolder` едет до журнала и до
  сводки (`a8bcb56`, мутации CB · CC · CD · CE).
  🆕 **ПОЛОВИНА `КАРТА` ПОЛУЧИЛА ЛЕКАРСТВО 2026-08-25 (эпик 43, фазы 1–2, коммит `58260e6`):** к
  поднятой кривой добавлена ГРАНИЦА частоты сверху, и на стенде полоса T8 пошла с «пропущено 3 из 3,
  закрыто 0» на «пропущено 0, закрыто 3». ⚠️ **Это доказывает ДВИЖОК, а не кремний** — держит ли
  граница на живой карте, меряет только фаза 3, при владельце (E43-AC5). **Открыто по-прежнему:**
  ПОЧЕМУ карта работает на частоте, которой перечитанная кривая не предлагает (два кандидата, ни
  один не наблюдался), и годится ли такой прожиг в урожай.
- **`bugs/47_the_offset_is_computed_from_one_table_read_and_applied_to_another.md`** 🔴 —
  **ПЕРЕОТКРЫТ 2026-08-24: причина опровергнута замером `tableDriftMhz` = 0.** Симптом
  воспроизвёлся слово в слово при СОВПАВШИХ таблицах, значит дрейф между чтениями его не вызывал.
  Починка НЕ откатана (схлопывание пары верно по своим основаниям), но документ перестал
  утверждать, что лечит этот отказ. Закроет его живой прогон, где `tableDriftMhz` ≠ 0 И ступень прошла.
- **`bugs/48_a_deliberate_stop_is_recorded_as_a_machine_hang.md`** 🟡 — **ОСТАЛСЯ ОДИН ПУНКТ ИЗ
  ТРЁХ.** Шаги 1–2 (класс исхода `operator-stop`, обработчики сигналов) **уже стояли с 2026-08-16**;
  дефект был в ЖЁСТКОЙ остановке (`TaskStop`), которая до обработчика не доходит и не должна.
  Ложная запись `seq 695` **ПОПРАВЛЕНА по слову владельца** — пол снят, 2355 МГц вернулась и дала
  PASS. Открыт шаг 3: возобновление после жёсткого убийства обязано СПРАШИВАТЬ, а не решать молча.
- **`bugs/49_DONE_the_journal_records_the_engines_offset_not_the_one_actually_written.md`** ✅ —
  **ЗАКРЫТ 2026-08-24 09:5x офлайн.** Журнал писал сдвиг ДВИЖКА, а атом его пересчитывал. Форма:
  **намерение ЗАЯВЛЯЕТ, вердикт ОТЧИТЫВАЕТСЯ** (перенести журналирование за пересчёт нельзя — R15).
  Проверка двойников нашла ВТОРОЕ переопределяемое число — частоту закрепления. Мутации CV · CW · CX · CY.
- **`bugs/46_DONE_…`** ✅ — **ЗАКРЫТ 2026-08-24 00:5x, третьим предъявлением владельца.** Печатался
  не тот шаг: у затравки не назывался вовсе, у следующей ступени печатался шаг ЗОНЫ (25) вместо
  сделанного (5). Теперь ШАГ — ведущая величина, законный прыжок читается как законный, расхождение с
  зоной объяснено. **Гейт**, а не формулировка: мутации DM · DN · DP. Подтверждено на живой карте.
- **`bugs/37_DONE_…`** ✅ — **ЗАКРЫТ 2026-08-23 13:1x.** Сэмплер писал без `fsync`, и у ступени,
  убившей машину, пульса не было вовсе: 768 нулевых байт в хвосте. Долговечная запись извлечена в
  `hardware-mon.appendSampleLine` — та же форма, что `sweep-journal.appendLine` (R15). **Цена
  ЗАМЕРЕНА и сняла вопрос: 1,01 мс медиана = 0,1 % такта**, тогда как порождение `nvidia-smi`
  стоит ~60 мс и платится КАЖДЫЙ такт. Запасной вариант «`fsync` раз в N» НЕ взят — выдуманное
  число в код не вошло. Блоки 18–18в, мутации PO · PP · PQ · PR. Живой дым: 12 проб, нулей в
  хвосте 0, медиана интервала 1001 мс — такт не сбился.
- **`bugs/24_DONE_…`** ✅ — **ЗАКРЫТ 2026-08-21 00:4x.** `curveCapMhz: null` дописан в оба
  собираемых кодом профиля (`ladder-descent.candidateProfile`, фикстура `virtual-pl250`); три набора
  зелёные (39 · 46 · 71), красный блок оказался ровно сторожем регресса `bugs/05`. Мутации в обе
  стороны, каждая красит ТОЛЬКО свои блоки. Путь замка доказан офлайн; ниже пола потолка держатель —
  закрепление, форма `uniform` (замерено на таблице стендовой карты).
- **`bugs/32_DONE_…`** ✅ — **ЗАКРЫТ 2026-08-23 10:0x.** Собственная улика частоты не доезжала до
  прогона (план 870 мВ, заказ 990), а без соседки развёртка падала `TypeError`-ом на первой же
  частоте полосы. Три сайта одного класса, найдены обходом близнецов. Блоки + мутации CQ · CR · CS.
- **`bugs/33_DONE_…`** ✅ — **ЗАКРЫТ 2026-08-23 10:0x.** Сухой прогон обещал оператору лестницу
  интенсивности из 4 ступеней и переигрывание, которых не происходит. Пара схлопнута в
  `sweepBurnLadder()`. Блоки + мутации CT · CU.
- **`bugs/36_DONE_…`** ✅ — **ЗАКРЫТ 2026-08-23 11:3x, найден ЖИВЫМ ПРОГОНОМ.** Землёй считалась
  последняя прошедшая ступень, а не самая глубокая: прогретая карта уводила её вверх, и попадание в
  цель объявлялось прыжком. Блоки + мутации DK · DL. Фикстура строилась дважды — оба раза правку
  диктовал прогон.
- **`bugs/35_DONE_…`** ✅ — **ЗАКРЫТ 2026-08-23 11:1x, найден ЖИВЫМ ПРОГОНОМ.** Сторож противоречия
  принимал стену рычага за утверждение и блокировал ЛЮБУЮ запись в документ. Лечится предикатом
  `demandsVoltage`, которым храповик пользуется с 22-го. Блоки + мутации DH · DJ.
- **`bugs/34_DONE_…`** ✅ — **ЗАКРЫТ 2026-08-23 10:4x.** Сторож глубины сравнивал доказанное на
  заказанной частоте с напряжением выданной и останавливал прогон на просадке. Блоки + мутации
  DB · DC · DD.
- **`bugs/26_the_rehearsal_stand_ignores_the_write_shape_so_the_lower_half_of_the_band_is_never_rehearsed.md`**
  🟡 — **пп. 1 и 4 ЗАКРЫТЫ 2026-08-23** (стенд слушается формы записи; заголовок больше не обещает
  закрепление на каждой ступени). Открыты п. 2 (пин мимо `profile-manager`), п. 3 (полоса ниже пола)
  и п. 5 — развилка `demandPin` владельцу. Прежняя редакция: `trap-suite.makeSweepStepFn` не читает
  `writeShape`: всегда пишет потолок на испытуемой частоте и всегда закрепляет — поэтому
  `npm run bench -- --from 2100 --to 2100` встаёт на отказе R11, которого живой путь не встречает,
  а `candidateProfile` на стенде не вызывается вовсе (потому стенд и не покраснел за `bugs/24`).
  Второй симптом того же корня: `bench-run.mjs:258` и строка стенда в этом файле обещали
  «частота ЗАКРЕПЛЯЕТСЯ на каждом прожиге», а выше пола потолка держит кривая и замка нет.
- **`bugs/25_bug_headers_and_backlog_tags_contradict_the_repository.md`** 🔴 — две правды о
  беклоге: шапки `bugs/12·14·17·18·20` «ОТКРЫТ» при починенном коде, `13`/`15` без DONE-тега,
  этот список и шапка внутренней карты отстали. Лечение — `/check-backlog` по таблице бага.
- **`bugs/20_writers_own_crash_is_blamed_on_the_card_as_a_hang.md`** 🔧 — **ПОЧИНЕН ДВАЖДЫ, ЖДЁТ
  ТОЛЬКО ПЕРЕИМЕНОВАНИЯ.** Писатель починен `901a123` (`writer-crash` — свой класс исхода, в пол и в
  тормоз не идёт). **Оба выдуманных отказа сняты 2026-08-22 (сессия 37):** 2542 МГц перемерен ещё
  16-го (845 мВ), а 2775 МГц перемерить было НЕЛЬЗЯ — R18 сделал записанное зависание стеной и тем
  отменил лечение, прописанное этим же багом. Лекарство — строка-поправка журнала
  (`sweep-journal.writeCorrection`, `f7b7739`), применена: полов было 5, стало 3, и живой прогон
  загрузил ровно три настоящих. **Читать шапку `writeCorrection` целиком: там разбор того, как два
  верных правила отменили друг друга** (EXP-0104).
- **`bugs/21_tidy_hook_killed_the_live_sweep_and_left_the_card_held.md`** 🔧 — починено и доказано
  (13 блоков, 4 мутации, проводка проверена на живом прогоне), но **живую границу хода при прогоне
  правка пережила ровно один раз** — оставлено «ждёт проверки», а не DONE.
- **`bugs/18_curveref_profile_can_never_be_applied.md`** 🔧 — применение ссылки на документ кривой
  починено и доказано офлайн; **на живой карте не прогонялось ни разу** (нечего применять, пока
  документ измерен на 8 %).
- **`bugs/19_watchdog_heartbeat_killed_the_writer_it_guards.md`** 🔧 — починено и доказано; живого
  подтверждения «прогон пережил сотни ударов пульса» ещё нет, его даст первый прогон, дошедший до
  конца полосы.
- **`bugs/07_search_walked_seven_rungs_into_the_dark_and_hung_the_machine.md`** ✅ — **ЗАКРЫТ
  2026-08-14 23:3x живым перепрогоном при владельце:** пять ступеней, все PASS, движок остановился
  сам на −30 мВ, откат доказан чтением (0 ненулевых смещений из 128). Границы работают на карте, а
  не только в мутациях.
- **`bugs/11_uncapped_raise_applied_beyond_the_proven_envelope_and_bsod.md`** 🟡 — **BSOD на машине
  владельца 2026-08-15 09:59** (`SYSTEM_SERVICE_EXCEPTION 0x3B`, `nvlddmkm.sys`, на простом столе).
  Машинный гейт **R13 поставлен и доказан** (6 блоков, 4 мутации, живая проверка чтением: файл как
  есть отвергается с превышением 82 МГц). Открыто: огибающая как поле формата, та же проверка в
  `profile-store`, пересборка режимов от определения владельца.
- **`bugs/KAIF/05_contour_voice_is_a_timbre_parameter_never_bound_to_intelligibility.md`** 🔴 — тикет
  наверх, наверх ЕЩЁ НЕ ОТПРАВЛЕН (ждёт слова владельца). Локально уже починено: контур переведён на
  Silero `eugene`, фолбэк SAPI выбирает голос по имени.
- **`bugs/12_step7_frequency_lock_not_applied_at_top_of_range.md`** 🟡 — **шаг 7 алгоритма владельца
  не исполнялся наверху диапазона: частота держалась потолком, а не закреплением.** Починено и
  доказано шестью мутациями (`02c4bcd`); открыты два хвоста — слово статуса `lever-limited` врёт,
  когда остановил наш потолок глубины, и закрытая нашим потолком частота не даёт затравку соседке.
- **`bugs/02_edge_search_writes_one_point_so_the_cap_is_never_undervolted.md`** 🟡 — шаг 1 закрыт
  (поиск пишет отгружаемую форму), шаг 3 открыт: перемерить край и заменить число факта 28. Живая
  запись, значит при владельце.
- **`bugs/03_band_sweep_started_the_ascent_at_a_deep_rung_and_hung_the_machine.md`** 🟡 — ПЕРВОЕ
  зависание, 5 ч 40 мин. Починено офлайном; правило R10 родилось здесь и подтвердилось второй раз.
- **`bugs/04_review_contour_orphaned_window_swallowed_answers.md`** 🟡 — владелец отвечал в окне
  убитого сервера контура. Ничего не потеряно. Правило: поднятый контур не перезапускается под
  открытым окном.
- **`bugs/01_owner_review_contour_adversarial_findings.md`** 🟡 — 54 дефекта контура согласований,
  30 ПРИПАРКОВАНО владельцем. **Не брать в работу.** ⚠️ Приписка 2026-08-14 «`npm run questions`
  краснеет всегда, все три его находки ложные» **ОПРОВЕРГНУТА 2026-08-23 замером — см. `bugs/40`:
  находок девять, ложных восемь, ОДНА НАСТОЯЩАЯ.** Файла долга у него по-прежнему нет.

**Закрыты в сессии 17:** `bugs/05` (гейт отвергал приборный пин) · `bugs/06` (ключ храповика грубее
эксперимента — **читать оговорку в его шапке: починка была верной и всё равно поучаствовала в
зависании**) · `bugs/08` (агент удалил хранилище улик).

**Закрыты в сессии 18:** `bugs/07` (границы доказаны живым прогоном при владельце) · `bugs/09` (сухой
прогон печатал лестницу вчетверо глубже реальной; урок EXP-0052 — **граница, добавленная в ПРОГОН, не
добавлена, пока её не печатает ПЛАН**) · `bugs/10` (улики ключевались индексом точки, а точка едет с
температурой; урок EXP-0053 — **ключуй по оси, которая не движется, и докажи, какая это ось, прежде
чем выбирать**).
