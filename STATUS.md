# KAGO — текущее состояние

> Этот файл ИИ-агент читает перед каждой задачей. Обновляй его при каждом значимом изменении
> состояния. Это **главная эстафета между сессиями**: новая сессия агента стартует с пустым
> контекстом и должна суметь стать продуктивной из одного этого файла. Пиши соответственно —
> конкретно, с путями и командами.
> 🧠 Ведущий принцип мышления — `PHILOSOPHY.md` (ПРОСТОТА: KISS + Оккам). Рабочий регламент —
> `AGENT_GUIDE.md`.
>
> ⚠️ **STATUS — это СВОДКА НАСТОЯЩЕГО, а не хроника.** Закрытая работа ПЕРЕЕЗЖАЕТ в
> `PROJECT_HISTORY.md`. Мягкий ориентир ~200 строк.

---

## Где мы сейчас

Фаза 0 закрыта. **Эпик распланирован целиком, фаза 1 идёт: закрыты шаги 3.1–3.6 из девяти.**
Контур согласований построен и работает — владелец уже ответил через него на три вопроса.

Записи о закрытом — в `PROJECT_HISTORY.md`, сессия 2 (2026-08-09).

| Фаза | Состояние | План |
|------|-----------|------|
| 0 — Развёртывание KAIF + исследования | ✅ закрыта | `PROJECT_HISTORY.md` |
| 1 — Стенд и базовая линия | 🟡 **идёт: 6 шагов из 9** | `plans/02_epic01_phase1_harness_and_baseline.md` |
| 2 — Silent Cold на `nvidia-smi` | 🔲 скелет | `plans/01_EPIC_kago_orchestrator.md` §3 |
| 3 — Три ярлыка + трей | 🔲 скелет | там же |
| 4 — Свой мост к NVAPI | 🔲 скелет | там же |
| 5 — Движок поиска Vmin | 🔲 скелет | там же |
| 6 — Два профиля + приёмка | 🔲 скелет | там же |

**Что уже работает на диске** (проверено прогоном, не заявлено):

| Команда | Что делает |
|---|---|
| `npm run check` | гейт сборки — парсит все `.mjs`. Сейчас 12 файлов, 0 провалов |
| `npm run gpu:info` | read-only проба карты (`-- --json` для машины) |
| `npm run workloads:build` | собирает нагрузки, доказывает детерминизм 5 прогонами, пишет манифест |
| `npm run workloads:verify` | сверяет манифест, ничего не собирая |
| `npm run questions` | сторож места вопросов: 4 оси + храповик долга |
| `npm run ask <документ.md>` | поднять контур согласований на документе и позвать владельца |
| `npm run ask:batch` | одна страница «накопилось N» по всем ждущим интервью |
| `npm run verify:contour` | QA-прогон контура: 18 блоков по враждебным фикстурам, ~7 с. Гонять после ЛЮБОЙ правки в `review*.mjs` / `send-upstream.mjs` |
| `npm run mon -- --once` | один сэмпл телеметрии в stdout |
| `npm run mon -- --seconds 30 --out runs/x.jsonl` | писать JSONL: монотонный индекс, сортированные ключи, ноль `Date.now()` |
| `npm run mon -- --check-decode` | сторож таблицы троттлинга: сверяет расшифровку маски с именами, которые печатает сама карта |
| `npm run events -- --last 24h` | четыре провайдера журнала Windows за окно; у каждого свой статус `ok`/`no-events`/`error` |
| `npm run events -- --fixtures` | суита фикстур (P1-AC3): 5 шт., две захвачены с этой машины, три сконструированы |
| `npm run stress -- --workload sdc_fma --seconds 30` | вердикт PASS/SDC/CRASH: сумма против эталона + журнал за то же окно |
| `npm run stress -- --workload branchy --transient` | та же проверка переходной нагрузкой 5/5 с |
| `npm run stress -- --selftest` | 10 блоков вердикта, доказаны красным мутацией |

### Восемь фактов, определяющих все решения

1. **Потолок мощности двигается только 250 → 300 Вт** — 50 Вт запаса. Цели PDF (−60…−120 Вт) одним
   ограничением мощности недостижимы; правка кривой обязательна.
2. **`nvidia-smi -pl` здесь пишет — проверено** (2026-08-09, с согласия владельца): 300 → 290 Вт,
   перечитано, возвращено. **Ловушка:** сообщение утилиты печатает в поле «from» значение по
   умолчанию, а не прежнее. Состояние только перечитывать (`researches/01` §5).
3. **`nvidia-smi` вообще не умеет читать напряжение** — такого поля нет. Поэтому фаза 5 *зависит*
   от моста к NVAPI из фазы 4 (`researches/03` §2).
4. **Vmin зависит от программы на ~100 мВ**, отказ у края — обрыв, а не склон. Один бенчмарк не
   доказывает ничего; запас не опционален.
5. **Больше половины отказов от андервольта — молчаливые.** «Не упало» не является приёмкой.
6. **KAGO собирает свои нагрузки — доказано.** `sdc_fma` даёт `e27ec24a82d509d7`, `branchy` —
   `67e95c85bb6299a2`, по 5 прогонов каждая. Эталон пережил смену компилятора (три конфигурации
   тулчейна — одна сумма).
7. **HWiNFO64 выведен из архитектуры** — стороннее GUI (запрет `GOAL.md`), а датчик горячей точки на
   RTX 50 отключён драйвером; эта карта возвращает `N/A`.
8. **Собранные бинарники едут в репозиторий** — решение владельца (интервью 001, Q3 = B). Отсутствие
   CUDA Toolkit больше не лишает KAGO поиска Vmin; тулчейн нужен только для ПЕРЕСБОРКИ.

### Решено владельцем — не пересматривать

- **Три ярлыка, пассивный трей.** 🚀 Max Optimal · ❄️ Silent Cold · ⏹ Сброс к заводским. Любой из
  трёх становится запомненным состоянием. Трей только показывает. Отменяет строку `GOAL.md` про
  сброс при убийстве иконки — почему, разобрано в `PROJECT_ARCHITECTURE_INTERNAL_MAP.md` §4.
- **Лестница:** бэкенд `nvidia-smi` сейчас, свой мост к NVAPI потом. Никаких сторонних GUI.
- **`profile-manager.mjs` — интерфейс со сменными бэкендами.**
- **Интервью 001 (2026-08-09 23:23):** приёмка по нашим числам + один ручной 3DMark на финише ·
  FurMark не пускаем вовсе · бинарники едут в репозиторий.

---

## 🟡 Баг контура — блокеры закрыты, остальное припарковано владельцем

> **Решение владельца 2026-08-10:** *«чини явные блокеры, и переходим к KAGO, срать на леса»*.
> Блокеров больше нет. Оставшиеся 30 находок ждут в списке и в работу НЕ берутся: приоритет — KAGO.

**`bugs/01_owner_review_contour_adversarial_findings.md` — 54 дефекта, 23 закрыто, 1 опровергнут,
30 припарковано, блокеров 0.**

Сессия 3 сделала то, что бумага требовала первым: **перепроверила, а потом чинила.** Написан
`tools/verify-review-contour.mjs` (`npm run verify:contour`) — 18 блоков, каждый утверждает
ПРАВИЛЬНОЕ поведение, фикстуры враждебные, браузер не нужен, ~7 с. Каждый доказан **красным против
копии `HEAD`** до правки; единственный, кто и там зелёный, — `I4b`, им находка и опровергнута.

**Два блокера, которые стоит помнить, потому что оба были ложным зелёным:**

- **Гейт отправки не мог одобрить ничего.** `decision.artifacts` не писал никто, а страница
  переписывала файл решения ЦЕЛИКОМ — то есть даже одобрение, вписанное руками, стиралось следующим
  ответом владельца. Теперь `writeDecision` **сливает**, а не перезаписывает, и отказ гейта прямо
  печатает, какой блок вписать. Одобрение отправки осталось ручным намеренно: одобрить ОТПРАВКУ —
  не то же самое, что ответить на вопрос.
- **Сторож вопросов был слеп к русскому языку.** `\w` в Node ASCII-only даже под `/u`, поэтому
  «Вопросы владельцу», «Нужно ваше решение», «Владелец, подтвердите» и все формы «развилки» шли <!-- owner-review:allow because=цитата шаблонов сторожа как доказательство находки, а не вопрос владельцу -->
  мимо, а сторож печатал ЧИСТО. Замерено до правки: **13 настоящих обращений из 18 невидимы.**

Прочее закрытое: страница показывает документ ЦЕЛИКОМ · код-блок внутри списка рендерится кодом, а
не словом `FENCE` · сводка считает то, что ЛЕГЛО в документ · очередь очищается сама · замок держит
документ, а не прогон · три сторожа отправщика научились отказывать · вариант в две строки не тащит
блочную вёрстку внутрь метки.

**Поправка к прежней редакции:** документ говорил «23 находки, 8 серьёзных». Журнал рецензентов
уцелел на диске — в нём **71 сырая находка, 54 после дедупликации, 6 блокеров**, и **14 находок по
`tools/questions-guard.mjs`**, которого прежняя редакция не разбирала вовсе. Список перенесён в
баг-документ целиком: фикс судится по списку, а списка не было.

**Что знать, пока остальное припарковано:** `send-upstream.mjs` ничего никогда не отправлял и на
счастливом пути не стоит. **Тикеты наверх подаём руками**, как подавался KAIF#7.

---

## 🤖 Пул автономных задач — что делать дальше

> **Вся фаза 1 автономно безопасна:** по устройству она не делает ни одной записи в GPU.
> Работать строго по `plans/02_epic01_phase1_harness_and_baseline.md`, цитируя якорь шага.

**Закрыто:** 3.1 `config.mjs` · 3.2 `toolchain.mjs` · 3.3 `workloads/` + `tools/build-workloads.mjs` ·
3.4 `hardware-mon.mjs` (00:27) · 3.5 `event-logger.mjs` (00:36) · 3.6 `stress-tester.mjs`
(00:52, 2026-08-10).

**Следующее, по порядку:**

- [x] ~~**3.4 `hardware-mon.mjs`**~~ — закрыт. Два прогона по 30 с: 60 записей, **ноль `null` на
      120 записей × 13 полей**, заголовки байт в байт совпадают, расходятся только `t` и значения,
      которые сообщает карта. Список полей не продублирован — модуль читает `config.TELEMETRY_FIELDS`,
      поэтому пары «истина↔зеркало» здесь нет, есть одна истина. Таблица причин троттлинга
      доказана красным мутацией: `npm run mon -- --check-decode`.
- [x] ~~**3.5 `event-logger.mjs`**~~ — закрыт. Нашёл все ТРИ настоящих `Kernel-Power` 41 (29.07,
      05.08, 06.08) и выдал `CRASH` — это P1-AC2. Фикстур пять, две захвачены с этой машины
      (в т.ч. НАСТОЯЩИЙ отрицательный случай `Display` 4107), три сконструированы и так и подписаны
      в имени файла. Ключевое свойство: **«ничего не нашёл» и «не смог посмотреть» — разные ответы**,
      у каждого провайдера свой статус, и при ошибке вердикт НЕИЗВЕСТНО, а не «чисто».
- [x] ~~**3.6 `stress-tester.mjs`**~~ — закрыт. Guard плана выполнен на НАСТОЯЩЕМ файле: одна
      шестнадцатеричная цифра в `runs/baseline/sdc_fma.json` — и `SDC`, выход 1. Сама самопроверка
      доказана красным мутацией. Вердиктов четыре, а не три: **НЕИЗВЕСТНО** — честный четвёртый.
      **Найдено настоящим прогоном:** эталон действителен только для тех аргументов, с которыми снят
      — иначе выходил ЛОЖНЫЙ SDC (58 из 58), а ложный SDC хуже пропущенного: он объявил бы здоровую
      карту нестабильной в каждой точке будущей развёртки.
- [ ] **3.7 Снять базовую линию** — прогнать обе нагрузки на стоке, сложить в `runs/baseline/` с
      отметкой драйвера `610.88` и VBIOS `98.03.58.40.8b`. `runs/` уже под gitignore.
- [ ] **3.8 Расширить `tools/gpu-info.mjs`** — выгрузить лестницу поддерживаемых частот
      (`nvidia-smi -q -d SUPPORTED_CLOCKS`) в отсортированный JSON. Это пространство поиска фазы 5.
- [ ] **3.9 Хозяйственное** — дописать новые команды в таблицу стенда в `AGENT_GUIDE.md`; завести
      пару «истина↔зеркало»: список полей `researches/03` §2 ↔ поля, которые сэмплит `hardware-mon`.

**Отдельной строкой, не из плана фазы 1:**

- [x] ~~**QA-прогон контура**~~ — сделано: `tools/verify-review-contour.mjs`, 18 блоков,
      `npm run verify:contour`. Каждый доказан красным против `HEAD` до правок.
- [x] ~~**Блокеры `bugs/01`**~~ — все пять закрыты. Остальные 30 находок ПРИПАРКОВАНЫ решением
      владельца: в работу не брать, приоритет — KAGO.

---

## ❓ Ждёт решения владельца

- **`interviews/interview_002_quiet_hours.md`** 🔴 — окно тихих часов контура. Владелец работал в
  23:20, зов приглушился, и он спросил почему. Вопрос оформлен интервью, а не оставлен здесь:
  место вопросов — только `interviews/` (это же нарушение сторож поймал на предыдущей редакции
  этого файла). Поднять страницу: `npm run ask interviews/interview_002_quiet_hours.md`.
- **`-lgc` (фиксация частоты) ни разу не прогонялась.** Согласие владельца покрывало только `-pl`.
  Спросить до первой записи через `-lgc` — это вторая половина бэкенда пути A.
- **Открытые сигналы по KAIF наверху** — ничего не блокировано, но локальные `bugs/KAIF/*` держать
  открытыми, пока обновление их не закроет:
  [#3](https://github.com/MikalaiKryvusha/KAIF/issues/3) ·
  [#4](https://github.com/MikalaiKryvusha/KAIF/issues/4) ·
  [#5](https://github.com/MikalaiKryvusha/KAIF/issues/5) ·
  [#6](https://github.com/MikalaiKryvusha/KAIF/issues/6) ·
  [#7](https://github.com/MikalaiKryvusha/KAIF/issues/7).

  > **KAIF#6 несёт локальное расхождение.** Секция *Languages* в `AGENT_GUIDE.md` переписана
  > целиком (маршрутизация языка **по аудитории**). Апстримная правка той же секции даст текстовый
  > конфликт при `/kaif-update` — смысл совпадает, спорить придётся о формулировке. См. `bugs/KAIF/03`.

---

## Эстафета — что делать следующей сессии, по шагам

Ты стартуешь с пустым контекстом. Ниже всё, что нужно, чтобы стать полезным за десять минут.

0. **НЕ начинать с бага контура.** Решение владельца 2026-08-10: блокеры закрыты, остальное
   припарковано, приоритет — сам KAGO. Открытый баг обгоняет план ТОЛЬКО когда он в продукте;
   `bugs/01` весь в обвязке. Начинай с пункта 3 ниже — с шага фазы 1.
   Если всё же трогаешь контур: **каждая правка получает блок в
   `tools/verify-review-contour.mjs`, и блок сперва доказывается КРАСНЫМ** — против копии
   предыдущей версии: `git archive HEAD tools | tar -x -C <врем.папка>`, туда же положить новый
   файл проверок и прогнать.
1. **Прочитать по порядку:** этот файл → `plans/01_EPIC_kago_orchestrator.md` (вся форма разом, по-русски)
   → `plans/02_epic01_phase1_harness_and_baseline.md` (что делать сейчас) → `researches/03` (чем
   стенду можно пользоваться). `researches/01` и `02` — доказательная база: **читать, не выводить
   заново.**
2. **Проверить среду тремя командами** и сравнить с фактами выше:
   ```
   npm run check              # ждём: checked 12 .mjs file(s), 0 failed
   npm run gpu:info           # ждём: драйвер 610.88, VBIOS 98.03.58.40.8b, 250–300 Вт
   npm run workloads:verify   # ждём: манифест сходится
   ```
   **Если драйвер изменился с 610.88 — каждый сохранённый профиль и каждая контрольная сумма
   недействительны до перепроверки.** Это правило R6, и оно не обсуждается.
3. **Взять шаг 3.4** из пула выше. Перед кодом процитировать якорную строку плана — шаг, который не
   к чему привязать, это уползание области работ, пойманное до появления дифа.
4. **Не писать в GPU.** Вся фаза 1 обходится без записи. Файл прав
   (`.claude/settings.local.json`, под gitignore) заставляет каждый вызов `-pl`/`-lgc`/`-rgc`
   спрашивать, а `winget upgrade/install` запрещает глухо — это сторож, рождённый инцидентом
   2026-08-09 (`EXPERIENCE.md` → EXP-0005).
5. **Нужно слово владельца — не спрашивай в чате.** Заведи интервью и подними контур:
   `npm run ask interviews/interview_NNN_<тема>.md`. Он сам откроет окно, позовёт (кроме тихих
   часов) и погаснет, когда владелец ответит. Закрытие интервью — это РАСПРОСТРАНЕНИЕ ответов по
   объявленным целям, и только потом смена статуса; сторож проверяет оба плеча:
   `npm run questions`.
6. **Редактор светит красным в `.cu`** — это его C++-анализатор не знает про CUDA. Не дефект,
   пояснение в `workloads/README.md`. Проверять тем, что собирает: `npm run workloads:build`.
7. **Пишешь текст, который подписывает владелец** (README, заметки к релизу) — открыть
   `AUTHOR_STYLOMETRY.md` (корень, под gitignore) и пройти его чек-лист. После свежего клона его
   нет: `cp d:\work\krinik_voice\AUTHOR_STYLOMETRY.md .`
8. **Язык документов — по аудитории, а не по папке** (`AGENT_GUIDE.md` → Languages). Читает
   владелец — по-русски: мета-планы эпиков, интервью, `GOAL`, `MASTER_PLAN`, `STATUS`, README.
   Читает только агент — по-английски: этот регламент, фреймворки, `EXPERIENCE`, карты,
   операционные планы, `researches/`, `bugs/`.

---

## Состояние машины, о котором надо знать

- **`nvcc` требует загруженной среды MSVC** — сам он хост-компилятор не находит. `toolchain.mjs`
  ищет его через `vswhere`; кросс-компилятор с x86-хостом проверен как запасной путь.
- **Visual Studio отремонтирована 2026-08-09** после того, как команда агента запустила
  незапрошенное обновление, а её обрыв оставил MSVC установленным наполовину. Урок: EXP-0005.
- **Grep локализует, Read проверяет** — вывод поиска не побайтно точен по слэшам и дважды за сессию
  показывал исправный код сломанным. Урок: EXP-0007.
- **`.claude/settings.local.json`** — грант прав агенту от владельца, под gitignore.

---

## Открытые баги

- **`bugs/01_owner_review_contour_adversarial_findings.md`** 🟡 — 54 дефекта контура согласований:
  23 закрыто и под сторожем, 1 опровергнут, **блокеров 0**, 30 припарковано владельцем. Не приоритет.

Пять сигналов по самому фреймворку открыты наверху — см. «Ждёт решения владельца».
