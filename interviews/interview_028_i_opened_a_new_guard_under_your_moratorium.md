# Интервью 028 — я завёл новый сторож под вашим мораторием: оставить или снять?

**Topic:** одна развилка, и я в ней провинившаяся сторона. 2026-09-05 я завёл новый прибор
(`tools/assert-dialect-lint.mjs`) с новыми воротами сборки и новым набором батареи — то есть ровно
«новый контур машинерии», который вы запретили открывать до порога.
**Status:** **🟡 ждёт ваших ответов**
**Создано:** 2026-09-05 (сессия 85, автономный цикл), при разборе `bugs/25` — там мораторий назван,
и я узнал в нём собственную утреннюю работу ·
**Родители:** `interviews/017` Q1 = A · `bugs/106` · `bugs/25` пункт 4
**Куда уйдёт ответ:** `tools/assert-dialect-lint.mjs` (жить или быть снятым) → `bugs/106` §сторож →
`bugs/25` пункт 4

<!-- owner-review:target Q1 -> bugs/106 + bugs/25 пункт 4 + tools/assert-dialect-lint.mjs -->

---

## Ваше правило, дословно

`interviews/017` Q1, ваш ответ **A** от 2026-08-28 01:23:

> **Мораторий с порогом:** до «краёв ≥ 195/389 (50 %)» новые контуры машинерии (сторожа, стенды,
> наборы, окна, каноны) не открываются. **Исключение одно: блокер БЛИЖАЙШЕГО живого прогона.**

**Счёт на сегодня: краёв 4 из 389.** До порога далеко.

## Что я сделал и почему это подпадает

Утром нашлось, что две строки в батарее предохранителя написаны чужим диалектом помощника `ok` и
**горят зелёным структурно** — чем бы ни кончился прогон. Одна из них называла себя главной строкой
шага Ш5. Пять дней они стояли украшением.

| что сделано | подпадает ли под мораторий |
|---|---|
| починены обе строки (стали настоящими условиями) | **нет** — это починка дефекта |
| просканирован весь проект: 1373 вызова, диалектов два, других таких строк нет | **нет** — это разбор |
| **заведён `tools/assert-dialect-lint.mjs` + седьмые ворота `npm run check` + набор `okdialect`** | **ДА. Прибор, ворота и набор — три из пяти категорий, названных в вашем правиле** |

**Я не заметил границы в момент работы** и увидел её через три часа, читая `bugs/25`, где ваш
мораторий назван по имени. Это моя ошибка, а не спорный случай: правило написано ясно.

## Аргумент, который у меня есть, — и почему я не считаю его достаточным

Исключение в вашем правиле одно: блокер ближайшего живого прогона. В понедельник вы стоите у машины,
и защита, которая будет её беречь, — это набор `fuse`. Две неспособные краснеть строки жили именно
там.

**Но чинит это ПОЧИНКА строк, а не сторож.** Сторож не даёт классу вернуться — польза будущая, а не
понедельничная. Поэтому под исключение он, на мой взгляд, не попадает, и решать не мне.

## QUESTIONS

### Q1. Сторож диалекта — оставить или снять до порога?

**Answer target:** `tools/assert-dialect-lint.mjs` и седьмые ворота `tools/check.mjs`; следом —
`bugs/106` §сторож и пункт 4 `bugs/25` (там же ждёт второй такой сторож, и он не заводился именно
из-за моратория).

**Origin:** вопрос поставлен агентом на себя. Возник 2026-09-05 в автономном цикле: разбирая
`bugs/25`, я прочёл в нём ссылку на ваш мораторий и узнал в запрете собственную утреннюю работу,
уже закоммиченную и отправленную. Спрашиваю, потому что мораторий — ваше решение, а не инженерная
деталь: смысл правила ровно в том, чтобы агент не решал такие случаи сам. От ответа зависит и
соседний предмет: пункт 4 `bugs/25` предлагает второй сторож того же рода (ловить `✅|DONE` в шапках
не-DONE-файлов), и он не заводился именно из-за моратория — ваш ответ здесь закроет и его.

- **A) (рекомендую) Оставить, и мораторий не смягчать.** Прибор уже написан, доказан красным трижды
  и стоит в воротах; снять его — потратить работу дважды и вернуть класс, который за сутки дал ДВА
  ложных зелёных (`bugs/106` и `bugs/109`). Но это разовое исключение вашим словом, а не новое
  правило: **счёт «сколько контуров открыто под мораторием» остаётся 1, и следующий требует нового
  вашего слова.** Почему рекомендую именно так: цена снятия — реальная (класс вернётся), цена
  оставления — только в том, что правило один раз согнулось, и согнулось задним числом при мне.
- **B) Снять до порога.** Правило важнее прибора: мораторий держится на том, что исключений нет, и
  первое же прощённое нарушение делает его советом, а не правилом — ровно тем, что за неделю не
  удержалось до вашего ответа A. Я убираю ворота, прибор и набор; починенные строки остаются, разбор
  остаётся в `bugs/106`, сторож возвращается при 195/389.
- **C) Смягчить мораторий до варианта B из `interviews/017`** — новый контур можно, но каждый вашим
  явным словом. Тогда этот сторож остаётся как первый такой случай. Цена названа вами же тогда:
  фильтр переезжает с правила на ваше внимание.
- **D) ваш ответ** — напишите здесь.

**Answer:**

---

## Что я сделаю после ответа

- **A** → оставляю как есть, дописываю в `bugs/106` строку «открыт под мораторием вашим словом
  от <дата>, счёт 1»; пункт 4 `bugs/25` остаётся закрытым для меня до порога.
- **B** → снимаю ворота, прибор и набор одним коммитом; в `bugs/106` остаётся разбор и починенные
  строки, в `bugs/25` пункт 4 — прежняя пометка «под мораторием».
- **C** → правлю формулировку в `AGENT_GUIDE.md`, где мораторий записан, и дальше спрашиваю вас
  по каждому контуру отдельно.
- Любой ответ → в `STATUS.md` появляется строка о том, что граница моратория была перейдена и как
  это решено; молча это не останется.
