# Эпик 01 — KAGO: от нуля строк кода до двух доказанных профилей на ярлыке

> **Создан:** 2026-08-09 22:41 +03:00 (агент, по слову владельца *«следующий чат начнём с эпик
> планирования и нарезания эпика на фазы и операционные планы»*)
> **Родитель:** `MASTER_PLAN.md`, фазы 1–6 — этот документ уточняет их форму, а не заменяет их.
> Доказательная база: `researches/01` (пути управления) · `researches/02` (Vmin и запас) ·
> `researches/03` (стенд без графического интерфейса).
> **Статус:** 🟡 фазы 1 и 4 закрыты (2026-08-10 01:00 и 18:0x +03:00) · фаза 2 приостановлена,
> её результаты переопределены как пол и калибровка · **фаза 5 в исполнении:** §4.1–§4.4 и §4.5a
> закрыты, §4.6 решён словом владельца; открыты §4.5, §4.7, §4.8 · **фаза 3 не начата — парето-срез
> эпика и самый дорогой долг; операционный план написан 2026-08-14: `plans/06`** · фаза 6 намеренно
> скелет (план фазы N+1 пишется на закрытии фазы N) ·
> **🗺 Маршрут до конца эпика — §9 этого документа (операционная карта, добавлена 2026-08-14)**
> **Вовне:** три развилки → `interviews/interview_001_harness_boundaries.md` (закрыто владельцем
> 2026-08-09 23:23) · доказательство порядка фаз → журнал решений `MASTER_PLAN.md` · вердикт по
> цели −100 Вт → владельцу на закрытии фазы 2
> **Дети:** `plans/02_epic01_phase1_harness_and_baseline.md` ✅ ·
> `plans/03_epic01_phase2_silent_cold.md` 🟡 ·
> `plans/04_epic01_phase4_nvapi_bridge.md` ✅ ·
> `plans/05_epic01_phase5_vmin_engine.md` 🟡 ·
> `plans/06_epic01_phase3_shell.md` 🔲 (создан 2026-08-14)

---

## 1. Вектор цели

**Боль.** Видеокарта греется и шумит сильнее, чем должна, а любой обычный путь это починить несёт
дефект, который вы назвали сами: он требует поставить стороннее GUI-приложение для разгона, и он
судит результат по принципу «не упало». `researches/02` измерил, что этот вердикт пропускает: из
57 программ **37 портят данные молча**, ещё до всякого падения, а отказ у края — обрыв, а не
пологий склон (вероятность ошибки 3 % → 90 % на двух процентах напряжения).

**Куда идём.** Два профиля, измеренных на **этой** карте, каждый проверен и на молчаливую порчу
данных, и на падения, переключаются ярлыком с рабочего стола, помнятся через перезагрузку. А
заводское состояние всегда в одном ярлыке или одном выключении питания — без единого действия с
вашей стороны и без стороннего GUI где бы то ни было в зависимостях.

**Типы целей:** *Достичь* — два профиля и оболочка управления. *Удерживать* — заводское состояние
по умолчанию и ноль сторонних GUI. *Избежать* — профиля, который молча портит данные.

## 2. Критерии приёмки эпика

У каждого — шкала, чем меряем и цель (`REQUIREMENTS_FRAMEWORK.md` → критерий соответствия). Где
цифра пришла из вашего PDF, это сказано. Ничего здесь не выдумано.

| № | Критерий | Шкала · Чем меряем · Цель |
|---|---|---|
| **AC1** | КОГДА нажат ярлык, оркестратор применяет профиль и **подтверждает это перечитыванием состояния**. | Шкала: ярлыки, у которых запись↔перечитывание сошлись · Меряем: журнал проверки `profile-manager.mjs` · **Цель: 4 из 4** (было «3 из 3»; ярлыков четыре по слову владельца от 2026-08-10 — четыре режима) |
| **AC2** | ПОКА идёт загрузка ПК, карта приходит либо к запомненному профилю, либо к заводскому состоянию — и никогда к непроверенному промежуточному. | Шкала: загрузки, закончившиеся в проверенном состоянии · Меряем: `npm run gpu:info -- --json` против записи о запомненном состоянии · **Цель: 5 из 5 подряд** |
| **AC3** | Приёмочный прогон не даёт ни падения, ни молчаливой порчи данных. | Шкала: число вердиктов CRASH + SDC · Меряем: `event-logger.mjs` + `stress-tester.mjs` на наборе фазы 6 · **Цель: 0** |
| **AC4** | Оба профиля построены по одной формуле владельца: **снижаем потребление, насколько можем, пока цена не превысила N; больше N платить не хотим.** Профиль — это одно значение N. | Шкала: ватты снижения при цене ≤ N · Меряем: спуск по измеренной лестнице, на каждой ступени — потребление, цена и разброс прибора рядом с ней (`hardware-mon.mjs`, сток против профиля, под одинаковым фоном) · **Цель: назвать найденную точку и её цену числом.** −100…−120 Вт из PDF — справка, а не порог: величина плавает от экземпляра к экземпляру (слово владельца, 2026-08-10) |
| **AC5** | **N назначает владелец, и вопрос задаётся с КРИВОЙ на руках**, а не до неё: спросить «какую потерю вы примете?» до того, как кривая этой карты снята, — значит просить его гадать. | Шкала: снятая кривая мощность↔производительность этого экземпляра + разброс прибора между двумя одинаковыми стоковыми прогонами · Меряем: **автоматический гейт — наши собственные числа** (пропускная способность и стабильность кадрового времени на нагрузках KAGO против стокового эталона); **итоговый процент — один ручной прогон 3DMark владельцем на закрытии фазы 6**, идёт в приёмку как его наблюдение (интервью 001, Q1 = A, 2026-08-09) · **Цель: показать кривую, на которой видно, чем платим на каждой ступени, и получить от владельца два N.** ≥ 97 % из PDF — чужая мерка и порогом не является; на части карт потери нет вовсе при существенном снижении потребления (слово владельца, 2026-08-10) |
| **AC6** | В списке зависимостей нет ни одного стороннего GUI-приложения. | Шкала: число таких зависимостей · Меряем: чтение `package.json` и дерева · **Цель: 0** (источник: `GOAL.md`, ваши слова) |
| **AC7** | ЕСЛИ сменились драйвер или VBIOS, ТО каждый сохранённый профиль помечается недействительным до перепроверки. | Шкала: профили, применённые с устаревшей отметкой · Меряем: проверка отметки в `profile-manager.mjs` · **Цель: 0** |

Критерии можно править по ходу — изменение критерия это правка, а не провал. **AC4 и AC5 переписаны
2026-08-10 по вашему слову в чате.** И сверка 2026-08-14: везде, где этот документ говорит «два
профиля», читать «три рабочих режима + Stock Default» — расщепление 2026-08-10 записано в
`MASTER_PLAN.md` → «Четыре режима» и в `GOAL.md`; формула AC4 «одно значение N» действует для каждого
режима со своим N (у `Optimised` N = 5 % FPS от Max Perfomance, у `Silent Cold` критерий стал
оптимизацией под T(40 %)). Что именно изменилось в 2026-08-10:

- Унаследованные из PDF пороги (≥97 % от стока, −100…−120 Вт) сняты с роли целей и оставлены
  справкой: величина плавает от экземпляра к экземпляру, а на части карт существенное снижение
  потребления вообще не стоит производительности.
- **Форма поиска задана вами дословно:** *«хотим снизить потребление видеокарты, и смотрим, чем за
  это платим. Снижаем потребление насколько можем до тех пор, пока не платим больше, чем N. Больше N
  платить не хотим.»* То есть максимизируем снижение мощности при цене ≤ N; управляемая величина —
  потребление, производительность — валюта, а не цель; **профиль это одно значение N.**
- **N — ваше, и спрашивается с кривой на руках.** Поэтому N нет в этой таблице: он появится в
  интервью на закрытии фазы 5, вместе со снятой кривой, где видно, чем платим на каждой ступени.
- Рядом с любой ценой стоит разброс прибора, иначе «потери нет» означает только «прибор тупой».
- Прежняя пометка о пустой графе «чем меряем» у AC5 снята: интервью 001 её закрыло.

## 3. Фазы, их порядок и почему именно такой

```
  1 Стенд и база ──► 2 Silent Cold ──► 3 Оболочка ──►  [законченный полезный продукт]
                                            │
                                            ▼
                                  4 Мост к NVAPI ──► 5 Движок Vmin ──► 6 Четыре режима
```

**Фаза 1 — Стенд и базовая линия.** *Нельзя судить о том, чего не видишь.* Телеметрия, сторож
неисправностей, собственные нагрузки KAGO и золотые эталоны, снятые на стоке. **Ни одной записи в
GPU за всю фазу** — именно поэтому она первая: это единственная фаза без риска для вашего железа, а
всё дальнейшее опирается на то, что её вердиктам можно верить.

**Фаза 2 — Silent Cold на одном `nvidia-smi`.** Первый настоящий профиль, на бэкенде, который уже
доказал, что пишет (`researches/01` §5: 300 → 290 Вт, перечитано, возвращено). Фиксация частоты
ядра гонит карту вниз по её же стоковой кривой; это даёт *холодно и тихо* и принципиально не даёт
*быстро и холодно*.

**Фаза 3 — Оболочка.** Три ярлыка, пассивный трей, автозапуск и повышение прав через задачу
планировщика (`researches/03` §3.6). Поставлена **раньше** дорогой работы с NVAPI намеренно: после
фазы 3 у вас на руках законченный продукт, которым можно пользоваться каждый день — профиль на
ярлыке, переживающий перезагрузку. Это парето-срез эпика; всё, что дальше, покупает второй профиль.

**Фаза 4 — Собственный мост к NVAPI.** FFI к `nvapi64.dll` ради V/F-кривой. Самая дорогая и
наименее предсказуемая фаза — и единственная с названным запасным путём (`green-curve` как бэкенд,
что интерфейс из фазы 2 уже допускает).

**Фаза 5 — Движок поиска Vmin.** *Формально зависит от фазы 4, и это доказательство, а не вкус:*
у `nvidia-smi` **вообще нет поля напряжения** (`researches/03` §2), так что до появления моста
поиску нечем наблюдать собственную управляющую переменную.

**Фаза 6 — Два профиля и приёмка.** Сборка, длинные прогоны, ваш вердикт.

## 4. Ворота фаз

Фаза не закрывается ощущением законченности. Вход и выход проверяемы.

| Фаза | Ворота входа | Ворота выхода |
|---|---|---|
| **1** | `researches/03` существует (✅) | `npm run check` зелёный · каждая нагрузка даёт бит в бит одинаковую сумму на повторных прогонах · сторож неисправностей **краснеет** на настоящем историческом событии · золотые эталоны сохранены с отметкой драйвера и VBIOS · у каждого нового модуля честный маркер `[NOT-TESTED]`/`[TESTED: …]` |
| **2** | ворота выхода фазы 1 зелёные | профиль применяется, перечитывается и совпадает · его откат **прогнан**, а не просто написан · маркеры `[TESTED]` несут наблюдение |
| **3** | ворота выхода фазы 2 зелёные | AC1 и AC2 взяты · ярлык сброса проверен из не-заводского состояния · трей переживает убийство, не меняя состояния GPU |
| **4** | ворота выхода фазы 3 зелёные · **ваше согласие на запись в кривую** (новый класс записи — список рисков `researches/01`) | чтение-после-записи на кривой совпадает, иначе профиль отвергается · шаг сетки напряжения **измерен на живой кривой**, а не принят на веру |
| **5** | ворота выхода фазы 4 зелёные | по каждой точке: трёхзначный вердикт, край взят в вилку, запас по именованной политике — **слово владельца 2026-08-10 21:xx: рабочая точка = отказ + 2 шага сетки (+10 мВ), режим `ratchet` по умолчанию**; ~~≥ 4 шага и ≥ 25 мВ~~ из `researches/02` §3 остаётся альтернативой `guardband` · порог точки — худший по всему набору нагрузок, никогда не первый найденный |
| **6** | ворота выхода фазы 5 зелёные | AC3, AC4, AC5 взяты · проход `/fable-judge` · ваша приёмка |

Каждая фаза дополнительно закрывается проходом `/fable-judge` до того, как будет написан
операционный план следующей (`AGENT_GUIDE.md` → дисциплина исполнения задач).

## 5. Развилки — закрыты владельцем

Все три закрыты **2026-08-09 23:23 +03:00** через контур согласований
(`interviews/interview_001_harness_boundaries.md`, решение записано в `interviews/decisions/`):

| Вопрос | Ответ | Что это значит для эпика |
|---|---|---|
| Чем мерить приёмку по производительности | **A** | Автоматический гейт считает наши числа; итоговый процент от стока владелец прогоняет руками в 3DMark на закрытии фазы 6. Вписано в AC5 |
| Пускать ли FurMark на стенд | **A** | **Не пускать вовсе.** Ни в зависимостях, ни на стенде. Графическую нагрузку KAGO пишет сам |
| Едет ли собранный бинарник в репозиторий | **B** | **Едет.** Против рекомендации агента — и это решение владельца. KAGO работает на машине без CUDA Toolkit; сборка из исходника остаётся, но перестаёт быть условием работы |

Открытых развилок нет.

## 6. Риски по ярусам

**(а) Высшие — с построенной защитой, а не просто перечисленные.**

- *Профиль, который проходит наши тесты и портит данные в вашей настоящей работе.* Защита: оракул
  на золотых эталонах (уже доказано, что он строится — `researches/03` §2.1), разнородный набор
  нагрузок и запас, рассчитанный от того, чего поиск увидеть не мог.
- *Запись в GPU без дороги назад.* Защита: правило R5 внутренней карты — у каждой записи откат в
  том же модуле — плюс то, что профили живут в энергозависимой памяти, так что выключение питания
  всегда остаётся последним средством.
- *Обновление драйвера, молча меняющее поведение напряжений* (так уже было: `595.71`). Защита:
  AC7 — проверка отметки.

**(б) Вероятные — с названным планом на случай.**

- *Blackwell не поддастся записи в кривую через NVAPI.* → откат на `green-curve` как бэкенд;
  интерфейс фазы 2 спроектирован ровно под это.
- *Тулчейн, собирающий нашу нагрузку, отсутствует или сломан.* → уже случилось в поле в этот же
  вечер; фаза 1 ищет его через `vswhere`, а не полагается на PATH.

**(в) Наименее вероятные — записаны, чтобы не забылись.**

- Политика планировщика задач меняется и ломает схему повышения прав без запросов UAC.
- Меняется API иконки в трее на очередном обновлении Windows.

## 7. Трассировка

Дети называются `NN_epic01_<фаза>_<имя>.md`, и каждый операционный шаг цитирует строку этого
документа, которую он исполняет. Шаг, который не к чему привязать, — это уползание области работ,
пойманное до того, как появился диф.

## 8. Решения, принятые без владельца

*Заполняется на закрытии эпика. Текущий список:*

- **HWiNFO64 убран из архитектуры** — это стороннее GUI-приложение (`GOAL.md` запрещает), а датчик,
  ради которого он там стоял, на RTX 50 отключён на уровне драйвера; эта карта возвращает `N/A`.
  Записано в `researches/03` §3.5.
- **Фаза 3 поставлена раньше фазы 4** — чтобы законченный полезный продукт оказался у вас на руках
  до дорогой и непредсказуемой работы с мостом.
- **Фаза 1 расширена** с «телеметрии» до «стенда», потому что `researches/03` показал: нагрузки
  купить нельзя, их надо писать здесь.

---

## 9. Операционная карта — маршрут до конца эпика

> Добавлена 2026-08-14 00:3x +03:00 по слову владельца, дословно: *«Мне не нравилось, что предыдущий
> агент Opus много наботал по наитию, без эпик и операционного планирования. … Может напишешь это в
> проекте, операционные карты по шагам, куда в этом проекте двигаться и как для достижения успеха.»*
> Карта не заменяет операционные планы фаз — она называет ПОРЯДОК шагов между ними и ворота каждого;
> каждый шаг цитирует свой якорь (§7 — шаг без якоря это дрейф). Пересматривается на закрытии каждого
> шага: устаревшая карта хуже отсутствующей.

Маршрут идёт **двумя треками**, потому что у них разные ворота. Трек Б в карту не пишет вовсе — он
исполняется автономно, в том числе когда владельца нет за машиной. Трек А пишет в кривую V/F, и после
инцидента `bugs/03` (ночная развёртка повесила машину на 5 ч 40 мин) действует правило процесса:
**живые прогоны записи — только при владельце за машиной**, пока механизм не прошёл хотя бы один
полный прогон при человеке. Между треками зависимостей нет до самой фазы 6 — она собирает результаты
обоих.

### Трек Б — оболочка (фаза 3): парето-срез, исполняется автономно

Ценность трека: всё уже измеренное владелец впервые сможет **применять** — сейчас у него нет ни
одного ярлыка. Операционный план — **`plans/06_epic01_phase3_shell.md`** (создан вместе с этой
картой). Фаза 6 потом только наполняет профили измеренными числами: оболочка строится не «на потом»,
а как готовое гнездо под них.

| Шаг | Что делаем | Якорь | Чем закрывается |
|---|---|---|---|
| **Б1** ✅ | Разведка трея без стороннего GUI → `researches/07` | `plans/06` §4.1 | ✅ 2026-08-14: документ разведки есть, путь без стороннего GUI найден — развилка владельцу не поднималась |
| **Б2** ✅ | Формат профилей четырёх режимов с отметкой пригодности: Stock Default настоящий, три рабочих — черновики до фазы 6 | `plans/06` §4.2 | ✅ 2026-08-14: отказ черновику доказан тестом И живьём путём задач (код 1, ноль записей) |
| **Б3** ✅ | Четыре ярлыка `.lnk`, применение с перечитыванием состояния | `plans/06` §4.3 | механизм 2/2 путём задач; **AC1 «4 из 4» закрывает фаза 6**, когда три режима квалифицируются |
| **Б4** 🟡 | Запомненное состояние + задача планировщика на входе в систему | `plans/06` §4.4 | механизм доказан живьём 2026-08-14; **AC2: 5 из 5 естественных входов — журнал копится сам (0 из 5 на 12:4x)** |
| **Б5** ✅ | Пассивный трей — только показывает активный режим | `plans/06` §4.5 | ✅ 2026-08-14: P3-AC4 — kill трея = 0 настраиваемых диффов из 29 полей |
| **Б6** ✅ | Приёмка фазы 3: `/fable-judge`, обновление канона | `plans/06` §4.6 | ✅ 2026-08-14 12:4x: вердикт СВЕРЕНО С ОГОВОРКАМИ · `MASTER_PLAN.md` фаза 3 закрыта · единственный открытый прибор — серия AC2 (Б4) |

### Трек А — физика (закрытие фазы 5): живые записи только при владельце

| Шаг | Что делаем | Якорь | Ворота | Чем закрывается |
|---|---|---|---|---|
| **А0** | Проверка среды одной командой | `STATUS.md` эстафета, шаг 1 | — | `check` зелёный · драйвер 610.88 на месте · сторож не взведён |
| **А1** | Доделка `bugs/02`: поиск ищет в ОТГРУЖАЕМОЙ форме (`buildRaiseAndCapVector`), офлайн | `plans/05` §4.1 · `STATUS.md` шаг 5а | без карты, автономно | самопроверки и мутации зелёные |
| **А2** | **Первая живая ступень после инцидента:** `npm run engine -- --band 2842 --dry-run`, затем живьём | `STATUS.md` эстафета, шаг 5 | **ВЛАДЕЛЕЦ ЗА МАШИНОЙ** | одна ступень пройдена набором форм, откат чист |
| **А3** | Развёртка по диапазону: ступени 1700 · 2000 · 2400 · 2842 | `plans/05` §4.5 · `STATUS.md` шаг 5б | владелец за машиной | потабличный безопасный сдвиг по точкам — профиль-ВЕКТОР |
| **А4** | Политика запаса: рабочая точка = отказ + 10 мВ (`config.marginAboveFailureMv()`) | `plans/05` §4.6 · `STATUS.md` шаг 5в | — | кандидат-вектор на всю кривую, записан в храповик с политикой в записи |
| **А5** | Длинный прожиг всей кривой: три формы + `--lowload` + игровой свидетель живьём; две независимые серии ватт | `plans/05` §4.7 | владелец за машиной | **P5-AC6, P5-AC8** |
| **А6** | Тепловая лестница ПОВЕРХ поднятой кривой; заодно сузить вилку 2100–2400 | `STATUS.md` эстафета, шаг 4 · `plans/05` §4.5a п. 4 | владелец за машиной | потолок `Silent Cold` назван по равновесию на поднятой кривой |
| **А7** | Перепрогон акустической лестницы — пол 30 % владелец честно не слышал | `STATUS.md` факт 30 | владелец рядом (слушает) | достоверный ряд 30…100 % |
| **А8** | Закрытие фазы 5: `/fable-judge` → ТОЛЬКО тогда пишется план фазы 6 (`plans/07`) | `plans/05` §4.8 | — | вердикт судьи · план фазы 6 существует |

### Финал — фаза 6: сборка и приёмка

Собрать четыре режима из ИЗМЕРЕННОГО (А3–А6) в профили оболочки (Б2), длинные приёмочные прогоны,
ручной 3DMark владельца, приёмка. Операционный план фазы 6 пишется на шаге А8 и не раньше — правило
лестницы планирования (`AGENT_GUIDE.md`): план фазы N+1 пишется на закрытии фазы N, иначе к моменту
исполнения он фикция.

### Что нужно от владельца — всё остальное работа агента

1. **Присутствие за машиной** для живых шагов А2, А3, А5, А6 (и ухо для А7). После `bugs/03` это
   правило, а не вежливость.
2. ~~`interviews/interview_002_quiet_hours.md`~~ — **закрыт 2026-08-14: тихих часов нет, звать
   владельца можно круглосуточно.** Открытых вопросов владельцу — ноль.
3. **Приёмки:** Б6 (оболочка на руках) и финальная приёмка фазы 6 (ручной 3DMark — интервью 001, Q1 = A).

Без владельца агент работает трек Б целиком и офлайн-части трека А (А0, А1). Порядок внутри трека —
сверху вниз; первый шаг при владельце — всегда А2, он самый короткий и он открывает всё остальное.
