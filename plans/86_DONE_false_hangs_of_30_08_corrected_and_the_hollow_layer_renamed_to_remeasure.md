# План 86 — поправки к ложным зависаниям 30.08 и слой «перемерить» вместо «снято движком»

> **Создан:** 2026-09-04 09:2x +03:00 (сессия 80; слово владельца у окна наблюдения)
> **Родитель:** `homeworks/06_curve_widget_taste.md` (вопрос владельца про точку «зависание снято») ·
> `bugs/85` (подстой у стока стал стеной; починен правилом B, ждал перемера) · `bugs/75` (тревога
> «ЗАМЕРЛО» и подстои 30.08) · `interviews/022` Q1 = B «и перемерить частоты»
> **Статус:** ✅ **ИСПОЛНЕН 2026-09-04 09:3x, все шесть шагов, критерии сняты прогоном** (карта 28 блоков, окно 87, `npm run check`; AC3 — единственная оставшаяся строка со старым словом — комментарий шапки рендерера, который называет, ПОЧЕМУ слово ушло). План принят владельцем 09:1x (*«оба пункта принял, план, делаем»*)
> **Вовне:** журнал прогонов `runs/sweep/journal.jsonl` (четыре строки-поправки, дописью) · рендерер
> `automation-engine/lib/curve-map.mjs` · страница `tools/build-curve-map.mjs` · облик окна
> `tools/build-dashboard-page.mjs` + `assets/dashboard/_wiring.js` · `AGENT_GUIDE.md` (строка карты) ·
> `bugs/85` → DONE

---

## Слово владельца, дословно

*«и всё равно смысл "зависание снято" для меня неясен, непонимаю, какую оно мне несёт ценность. Тут не
про твой график дело, а про терминологию в целом, что есть какие-то такие "ошибочные точки, и мы это
наследение ошибки несём»* → предложены два пункта → *«оба пункта принял, план, делаем»*.

## Вектор цели и критерии приёмки

**Боль.** Карта показывает две полые точки «снято движком» (2872 и 2835 МГц). Это записи ложных
зависаний вечера 30.08, которые перемер 31.08 опроверг. Поправок к ним в журнале нет: правило B
(`interviews/022`) их обошло, а не исправило, и карта тянет наследие ошибки. Слово «снято движком»
не называет ни причины, ни действия.

**Достичь:** движок, статическая карта и виджет читают одну правду — ложные зависания 30.08 названы
поправкой с уликой; полый круг означает одно: «противоречие, перемерить», и исчезает, когда
противоречий нет. **Сохранить:** журнал дописываемый — ни одна строка не стёрта; пол 2857 МГц / 885 мВ
(seq 808) стоит, потому что перемера у него нет. **Избежать:** поправки без улики; переименования,
после которого движок и карта расходятся.

| # | Критерий | Мера |
|---|---|---|
| P86-AC1 | Четыре поправки дописаны | `grep -c '"state":"correction"' runs/sweep/journal.jsonl` = 9 + 4 = 13; строки ЗАВИС 804/805/809/810 на месте |
| P86-AC2 | Полых точек нет; полы не ушли, а ПРИБАВИЛИСЬ ровно одним настоящим | `node automation-engine/lib/curve-map.mjs` печатает `полов зависания 14 · перемерить 0` (было `13 · снято движком 2`). ✏️ Цель писалась как «13»: поправка вскрыла, что `hangFloors` берёт самое высокое зависание частоты, и ложная 1035 заслоняла настоящий край 2872 МГц на 865 мВ (seq 838) — он встал полом. Диф полов до/после снят скриптом (EXP-0231) |
| P86-AC3 | Слово «снято движком» ушло из кода и легенд | `grep -rn "снято движком\|зависание снято" automation-engine tools assets/dashboard/_wiring.js` = 0 строк |
| P86-AC4 | Легенда виджета и статической карты показывают строку «перемерить» только при непустом слое | selftest карты: SVG несёт `data-remeasure="N"`; блок на фикстуре с противоречием → N=1 и подпись «ПЕРЕМЕРИТЬ»; на боевом документе строка легенды скрыта |
| P86-AC5 | Все ворота зелёные | `curve-map --selftest` (27 → ≥28), `run-dashboard --selftest` 87, `npm run check` |

## Улика для поправок (из журнала, не из головы)

| seq | частота / мВ | 30.08 записано | 31.08 перемер |
|---|---|---|---|
| 804 | 2872 / 895 | ЗАВИС, пульс сэмплера, зазор 2481 мс | ПРОШЛО на 895, 890, 885, 875, 870; настоящий край 865 (seq 31.08 19:33) |
| 805 | 2872 / 1035 | ЗАВИС, пульс сэмплера, зазор 6043 мс | то же |
| 809 | 2835 / 895 | ЗАВИС, пульс сэмплера, зазор 2472 мс | ПРОШЛО на 895 дважды и на 890 дважды |
| 810 | 2835 / 1015 | ЗАВИС, пульс сэмплера, зазор 5887 мс | то же |
| 808 | 2857 / 885 | ЗАВИС, пульс сэмплера, зазор 2370 мс | **перемера нет — НЕ поправляется**, пол стоит до перемера |

Пять зависаний за три минуты (21:43–21:46) на напряжениях у стока и на самом стоке — это вечер
`bugs/75`, а не край карты. Словарь поправки: `writer-crash` (наша смерть, не карты), как у поправок
сессии 37.

## Механизм

```mermaid
flowchart LR
  J[(journal.jsonl<br/>804 805 809 810 = hung)] -->|дописать 4 строки<br/>state=correction, why=улика| J2[(journal.jsonl<br/>+4 correction)]
  J2 --> HF[hangFloors<br/>fixed.has(seq) → стена снята]
  J2 --> RH[rawHangs<br/>fixed.has(seq) → точки нет]
  HF & RH --> CF[curveFacts<br/>floors 14 · remeasure 0]
  CF --> SVG[renderCurveSvg<br/>svg data-remeasure=N<br/>title «ПЕРЕМЕРИТЬ …»]
  SVG --> W[виджет: wiring читает<br/>data-remeasure, прячет строку легенды при 0]
  SVG --> S[статическая карта: строка легенды<br/>и таблица только при N>0]
```

## Шаги

- [x] Ш1. Резервная копия журнала в `runs/sweep/journal.before-plan86.jsonl` (runs/ вне git), затем
  четыре `writeCorrection` через `openJournal({ dir: SWEEP_DIR })` с `why` из таблицы улик,
  `correctedBy: 'сессия 80, по слову владельца 2026-09-04 «оба пункта принял»'`. Проверка P86-AC1.
- [x] Ш2. Рендерер: `refuted` → `remeasure` в именах и текстах (`<title>` «ПЕРЕМЕРИТЬ: …», строка
  счёта `перемерить N`, комментарий шапки), корневой `<svg data-remeasure="N">`; selftest: regex
  блоков переписан, +1 блок на атрибут. P86-AC2, P86-AC4.
- [x] Ш3. Статическая карта: легенда и абзац про полый круг — только при N>0, слова «перемерить»;
  таблица переименована. Пересборка `assets/curve-map.html`.
- [x] Ш4. Виджет: легенда `id="legend-remeasure"` с текстом «перемерить», по умолчанию `hidden`;
  wiring после вставки SVG читает `data-remeasure` и снимает `hidden` при N>0. Пересборка страницы.
- [x] Ш5. `AGENT_GUIDE.md` строка карты · `homeworks/06` таблица · `bugs/85` → DONE (перемер состоялся
  31.08, поправки дописаны) · блок сессии 80 в STATUS.
- [x] Ш6. Ворота: P86-AC3, P86-AC5; окно перезапущено отдельным процессом, владелец смотрит.

## Проверка — какими артефактами

- `automation-engine/lib/curve-map.mjs --selftest`: блоки «полый круг = перемерить» (фикстура с
  противоречием, N=1) и «на документе без противоречий атрибут 0» — там же, где живут остальные 27.
- `node automation-engine/lib/curve-map.mjs` без флагов — счёт по боевым файлам (P86-AC2).
- `run-dashboard.mjs --selftest` — 87 блоков не худеют; wiring-блок про `data-remeasure` при
  необходимости добавляется в тот же набор.

## Риски

1. **Поправка не туда** (seq перепутан) — `writeCorrection` отказывает, если seq не ЗАВИС; сверка
   после: `grep` по четырём seq в строках correction. Реакция: журнал восстанавливается из копии Ш1.
2. **Слово ушло из кода, но осталось в собранных HTML** — сборка обеих поверхностей входит в шаги,
   P86-AC3 гоняется по исходникам, P86-AC2 по счёту.
3. **808 всплывёт как «ошибочная точка» позже** — названо здесь заранее: это пол до перемера, а не
   наследие; перемер 2857 — дело следующего живого прогона.
