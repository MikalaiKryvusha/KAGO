# Эпик 02 — Поиск края по ВСЕМУ диапазону: одна измеренная кривая, три режима поверх неё

> **Создан:** 2026-08-15 15:4x +03:00 (агент, по заданию владельца в `ideas/03`: *«Запланировать эпик и
> операционные планы — переписать движок KAGO под написанный выше алгоритм»*)
> **Родитель:** `ideas/03_edge_finder_algorithm.md` (алгоритм) · `GOAL.md` → «⭐ ЧТО ТАКОЕ ТЮНИНГ
> VF-КРИВОЙ» (определение) · `researches/09` (доказательная база: отраслевая разведка + измеренное
> пространство поиска). **Отношение к эпику 01:** фазы 0–4 эпика 01 — фундамент этого эпика и
> остаются в силе; **этот эпик ОТМЕНЯЕТ метод фазы 5** (один потолок, одна дельта) и **переопределяет
> фазу 6** (три режима над ОДНОЙ найденной кривой).
> **Статус:** 🔲 распланирован 2026-08-15, ни строки кода не написано · вход фазы 1 открыт ·
> **посылка `interviews/007` закрыта владельцем в тот же день: зависание — осознанный риск**, отсюда
> вердикт `ЗАВИС` первого класса и самовозобновление после перезагрузки (§5)
> **Вовне:** остаток развилки (присутствие) → `interviews/007` · закрытие → `MASTER_PLAN.md`
> (фазы 5 и 6 переписываются от этого эпика) · формат кривой → `profiles/README.md`

---

## 0. Одна страница для владельца — что мы строим и во что это обойдётся

**Ваш алгоритм принят целиком.** Ни один шаг из восемнадцати не выброшен и не смягчён. Что даёт эпик:
движок сам проходит **весь диапазон частот карты**, для каждой частоты находит минимальное напряжение,
на котором расчёты ещё верны, поднимает его на 10 мВ и записывает в один документ — **тюнинг-кривую**.
Три режима становятся тремя ссылками на этот документ, отличаясь только лимитом мощности и (у
`Холодный Тихий`) потолком частоты.

**Три числа, которые надо знать до старта — все измерены на вашей карте сегодня, без единой записи:**

| Что | Число | Почему это важно |
|---|---|---|
| Сколько на самом деле «ручек» у карты | **74 точки** в рабочей полосе (не 389 частот) | 389 частот лестницы обслуживаются всего **75 различными точками** кривой — в среднем **5,19 частоты на точку**. Идти по всем 389 значит пять раз крутить одну и ту же ручку |
| Сколько времени займёт развёртка | **≈ 1,5 часа** прожига + перезагрузки | Буквальное исполнение — **67 часов**. Свёртка к точкам — 13 часов. Плюс монотонность напряжения по частоте — **1,5 часа**. Ни один прогон при этом не пропущен |
| Длительная проверка (ваш шаг 15) | **≈ 5 часов** | 75 точек × 3 минуты. Делится по полосам, можно за несколько заходов |

**Одна честная граница, которую нельзя обойти.** В середине диапазона (примерно 1700–2400 МГц) наш
рычаг упирается в аппаратный предел ±1000 МГц **раньше, чем карта дойдёт до отказа**: там доступно
всего 45–125 мВ. Для этих частот движок обязан сказать **«предел рычага»**, а не «край найден» — это
разные вещи, и выдать одно за другое было бы враньём. На краях диапазона доступно 245–350 мВ, там край
достижим.

**Риск зависания принят вами 2026-08-15** (`GOAL.md` → «⚠️ ЗАВИСАНИЕ — ОСОЗНАННЫЙ РИСК», дословно:
*«зависание компа и перезагрузка — осознанный риск»*). Значит падение — **штатный путь вердикта**, а
не авария: движок получает вердикт `ЗАВИС` первого класса, **возобновляется после перезагрузки сам**
и не теряет ни одного рунга. Единственная аварийная остановка — две перезагрузки подряд на одном и
том же рунге.

**Открыта одна узкая половина** (`interviews/007`): идти ли развёртке, когда вас нет в комнате.
Ваш ответ снял вопрос «страшно ли, что упадёт»; остаётся «кто заметит, если она встанет в 03:00, и
что при этом открыто несохранённого». Фазы 1 и 2 этого не ждут.

---

## 1. Вектор цели

**Боль.** Проект умеет доказать одну точку кривой на одном потолке. Владелец просит кривую целиком, и
дважды сказал это прямо: *«КАЖДУЮ точку этого диапазона мы протестировали»* и *«ты дрочишь целую
неделю одну точку»* (EXP-0056). Три препятствия, каждое измерено:

1. **Движок физически не достаёт до нижней половины диапазона.** Область под тестом он держит
   ПОТОЛКОМ кривой, а потолок ниже `верх − 1000` = **2157 МГц** удержать нечем (факт 38). Всё, что
   ниже, сегодня недоступно вовсе.
2. **Результат негде хранить.** Профиль несёт голый массив целых чисел без статуса и без даты; у
   точки нет истории, а ваш алгоритм требует «статус и дата последнего редактирования».
3. **Зависание стирает то, ради чего оно случилось.** Вердикт пишется ПОСЛЕ шага, значит рунг, который
   повесил машину, не записан нигде — а именно он и есть ответ.

**Куда хотим прийти.** Одна команда проходит весь диапазон сверху вниз, фиксируя карту на каждой
частоте, спускаясь по напряжению шагом сетки под прожигом и оракулом, переживая перезагрузку без
потери знания, и оставляет **один документ**: частота → напряжение → статус → дата, по каждой точке,
со штампом драйвера и VBIOS. Три режима собираются из него ссылкой.

**Типы цели.** *Достичь* — измеренная кривая всего диапазона и движок, который её произвёл.
*Удержать* — заводское состояние по умолчанию на каждом пути, храповик (сбойнувшая точка не
опускается никогда), потолок карты 3090 МГц. *Избежать* — профиля, который тихо портит расчёты, и
числа, полученного интерполяцией вместо прожига.

---

## 2. Критерии приёмки эпика

По `REQUIREMENTS_FRAMEWORK.md` — **Шкала · Прибор · Порог**, чтобы будущая сессия их ПРОГНАЛА, а не
оценила на глаз.

| # | Критерий | Шкала · Прибор · Порог |
|---|---|---|
| **E2-AC1** | Тюнинг-кривая существует как отдельный документ, и у каждой точки есть частота, напряжение, статус и дата правки. | Шкала: точек с полным набором полей · Прибор: валидатор формата (`npm run curve -- --show`) · **Порог: 127 из 127, ноль точек без статуса, ноль без даты, штамп драйвера/VBIOS обязателен** |
| **E2-AC2** | Развёртка покрывает ВЕСЬ диапазон, и покрытие СЧИТАЕТСЯ, а не заявляется. | Шкала: точки рабочей полосы с закрытым вердиктом · Прибор: отчёт развёртки · **Порог: 74 из 74, каждая с одним из трёх вердиктов — «край», «предел рычага», «пол частоты»; непосещённых ноль** |
| **E2-AC3** | Ни одно число не получено интерполяцией — то, чем вендорский сканер отличается от нас. | Шкала: точки, значение которых выведено, а не прожжено · Прибор: журнал развёртки (у каждой точки свой прожиг со своим временем) · **Порог: 0** |
| **E2-AC4** | Рабочая точка = напряжение отказа + 10 мВ, и политика названа В ЗАПИСИ. | Шкала: мВ между измеренным отказом и записанным значением · Прибор: документ кривой · **Порог: ровно два шага сетки, у каждой точки назван режим, которым получено** |
| **E2-AC5** | Зависание НИЧЕГО не теряет и приписывается верному рунгу. | Шкала: вердиктов, потерянных при обрыве · Прибор: учебная тревога — процесс убивается посреди рунга, журнал перечитывается · **Порог: 0 потерянных; журнал называет ТОЧНЫЙ рунг, на котором оборвались** |
| **E2-AC6** | Итоговая кривая никогда не предлагает частоту выше максимума экземпляра. | Шкала: точек с частотой > 3090 МГц · Прибор: сторож R13 в валидаторе И в последней строке перед записью · **Порог: 0, отказ в ДВУХ местах, каждый называет свою точку** |
| **E2-AC7** | Край квалифицирован длительным прожигом, а не только коротким. | Шкала: секунд под нагрузкой на точку, с вердиктом · Прибор: проход квалификации · **Порог: ≥ 180 с на каждую точку края, все PASS на закрытии; сбойнувшая поднята храповиком и перепрожжена** |
| **E2-AC8** | Три режима собраны из ОДНОЙ кривой и отличаются только названным. | Шкала: полей, которыми режимы различаются · Прибор: чтение трёх файлов профилей · **Порог: ровно два — лимит мощности (300/250/250 Вт) и потолок частоты (нет/нет/−10 % от 3090 = 2781 МГц); вектор напряжений побайтово один и тот же** |
| **E2-AC9** | Фазы 1–2 не трогают карту вовсе. | Шкала: записей в GPU · Прибор: все команды фаз 1–2 — только чтение или офлайн · **Порог: 0** |
| **E2-AC10** | Каждая новая проверка доказана КРАСНЫМ до того, как ей поверили. | Шкала: мутаций, покрасневших в СВОЁМ блоке · Прибор: самопроверки модулей · **Порог: 100 %, адресаты названы ДО прогона (EXP-0016)** |

---

## 3. Фазы, порядок и почему именно такой

Порядок продиктован одним правилом: **всё, что можно сделать без карты, делается до того, как карта
включается**. Живое время владельца — самый дорогой ресурс эпика, и тратить его на отладку формата
недопустимо.

| Фаза | Что делает | Карта | Владелец |
|---|---|---|---|
| **1 — Словари и документ кривой** | Ваши шаги 1–5: две сетки как артефакты, тюнинг-кривая как документ, профиль ссылается на неё | 🔍 только чтение | не нужен |
| **2 — Движок развёртки** | Ваши шаги 6–14 как код: пин, спуск, оракул, храповик, **журнал упреждающей записи и восстановление после перезагрузки**, сухой прогон | ⛔ ноль записей | не нужен |
| **3 — Живая развёртка** | Тот же движок, но на карте: весь диапазон сверху вниз | ✍️ пишет | **нужен** (S1) |
| **4 — Длительная квалификация** | Ваш шаг 15: 3 минуты на точку края, храповик поднимает сбойнувшие | ✍️ пишет | **нужен** |
| **5 — Три режима и приёмка** | Ваши шаги 16–18 + замер выгоды и цены | ✍️ пишет | **нужен** |

**Зависимости, а не пожелания:**

- **2 после 1** — движку некуда писать, пока документа кривой нет. Это ровно та ошибка, которую
  `plans/12` уже исправляла один раз (EXP-0056: цикл без места под результат схлопывается в скаляр).
- **3 после 2** — сухой прогон обязан напечатать ту же лестницу, которую пройдёт живой (`bugs/09`,
  EXP-0052). Пока плана нет на экране, живой прогон запускать нельзя.
- **4 после 3** — квалифицировать нечего, пока края не найдены.
- **5 после 4** — режим, собранный из неквалифицированного края, это `bugs/11` во второй раз.

**Фазы 3 и 4 делятся по полосам частот** и могут идти в несколько заходов: точки независимы, а журнал
переживает разрыв. Это прямое следствие требования E2-AC5 и единственный способ уложить пять часов
прожига в жизнь человека.

### Скелеты фаз — детализация ТОЛЬКО ближайшей

Операционный план пишется на одну фазу вперёд: план фазы N+1 рождается на закрытии фазы N, со всем,
чему фаза N научила. План всех пяти фаз сразу был бы вымыслом к третьей — это правило проекта, и оно
оплачено (`AGENT_GUIDE.md` → Planning discipline).

**Фаза 1 — детализирована:** `plans/14_epic02_phase1_grids_and_curve_document.md`.

**Фаза 2 — скелет.** Внешняя петля по точкам сверху вниз · пин частоты через `profile-manager`
(R1) · спуск по сетке напряжений от стока · прожиг 10 с с оракулом · край = отказ + 10 мВ · вердикт
`ПРЕДЕЛ РЫЧАГА`, когда упёрлись в ±1000 МГц · **журнал упреждающей записи: намерение на диск ДО
записи в карту; на загрузке намерение без вердикта = отказ на этом рунге** · затравка спуска ответом
предыдущей частоты (монотонность Vmin, `researches/09` §2.3) · `--dry-run` печатает всю лестницу,
включая глубину первого шага · самопроверки на инъекциях, мутации с названными адресатами.

**Фаза 3 — скелет.** Ворота стенда (`gpu:info`, `nvapi`, `watchdog --status`, эталоны) · развёртка по
полосам, сверху вниз · после каждой полосы — коммит документа кривой · отчёт: покрытие, вердикты,
сколько перезагрузок, где упёрлись в рычаг.

**Фаза 4 — скелет.** По каждой точке края: пин частоты и напряжения, 180 с прожига набором форм,
включая `--lowload` (единственная форма, ни разу не прогнанная на кривой) · отказ → +10 мВ храповиком
→ перепрожиг · закрытие: 0 отказов на всех точках.

**Фаза 5 — скелет.** Три файла профиля, ссылающихся на один документ кривой · различие ровно в двух
полях · замер выгоды в игре владельца (плато) и цены на Q2RTX (пол прибора 0,90 %) · ручной 3DMark
владельца · приёмка.

---

## 4. Ворота фаз

Ворота — это то, что должно быть ИСТИННО, а не сделано.

| Фаза | Вход | Выход |
|---|---|---|
| **1** | `npm run check` зелёный · драйвер 610.88 (иначе R6 отменяет всё) | Документ кривой валиден на 127 точках · обе сетки прочитаны с карты и записаны · самопроверки с мутациями, каждая краснит свой блок · **ноль записей в GPU** |
| **2** | Фаза 1 закрыта · `/fable-judge` по фазе 1 | Сухой прогон печатает ту же лестницу, что пройдёт живой (сверено блоком самопроверки) · учебная тревога по E2-AC5 пройдена (убитый посреди рунга процесс не потерял ничего) · **ноль записей в GPU** |
| **3** | Фаза 2 закрыта · **владелец за машиной (S1)** · ответ на `interviews/007` · сторож не взведён · эталоны сходятся со штампом карты | 74 из 74 точек с закрытым вердиктом · откат доказан чтением (0 ненулевых смещений из 128) · документ кривой закоммичен |
| **4** | Фаза 3 закрыта · владелец за машиной | Все точки края прожжены ≥ 180 с, 0 отказов на закрытии · поднятые храповиком точки названы |
| **5** | Фаза 4 закрыта | Три профиля применяются и откатываются · выгода и цена названы с собственным разбросом прибора рядом · приёмка владельца |

**Каждая фаза дополнительно закрывается проходом `/fable-judge`** — до того, как написан операционный
план следующей.

---

## 5. Развилки

**Закрыта посылка, 2026-08-15:** зависание и перезагрузка — **осознанный риск владельца**, дословно в
`GOAL.md` → «⚠️ ЗАВИСАНИЕ — ОСОЗНАННЫЙ РИСК». Следствия для кода, а не для регламента: вердикт
`ЗАВИС` первого класса · развёртка **возобновляется после перезагрузки сама** · потолка «стоп после N
перезагрузок» по умолчанию нет · единственная аварийная остановка — **две перезагрузки подряд на
одном и том же рунге** (это поломка, а не край).

**Открыта одна узкая половина.** Тот же `interviews/007` — **присутствие**: может ли развёртка идти,
когда владельца нет в комнате. Его ответ снял вопрос «страшно ли, что упадёт»; остаётся «кто заметит,
если она встанет в 03:00, и что при этом открыто несохранённого». Фазы 1 и 2 не ждут: они не трогают
карту вовсе.

**Закрыто владельцем ранее и не пересматривается:** запас +10 мВ над отказом (два шага сетки) ·
никогда не гнать выше максимума экземпляра · режимы различаются лимитом мощности и одним потолком
частоты · сходящаяся петля с храповиком вместо статического запаса · порог шума 60 % / 45 % ·
`Silent Cold` = максимум производительности под T(40 %).

---

## 6. Риски по ярусам

**(а) Высшие — с построенной защитой, а не с названной.**

| # | Риск | Защита |
|---|---|---|
| R1 | Развёртка паркует карту в области ТИХОЙ порчи данных — худшее, что может случиться | Трёхзначный оракул на каждом рунге · `prove:gradient` уже показал, что градуированная половина видит переворот одного бита при СОВПАВШЕЙ контрольной сумме · запас +10 мВ · длительная квалификация (фаза 4) |
| R2 | Зависание стирает рунг, который его вызвал, и развёртка идёт по кругу | Журнал упреждающей записи (E2-AC5): намерение на диск ДО записи в карту. Отраслевая форма та же (`researches/09` §2.4). **Владелец принял сам риск зависания, значит этот сторож — единственное, что стоит между «край найден» и «крутим один и тот же рунг вечно»** |
| R2a | Развёртка бесконечно перезапускает рунг, который ломает не карту, а что-то ещё | Аварийная остановка: **две перезагрузки подряд на ОДНОМ рунге** — единственный потолок, который остаётся после принятия риска владельцем. Отчёт называет рунг и останавливается, а не продолжает |
| R3 | Записан профиль, предлагающий частоту выше максимума карты — ровно `bugs/11` | Сторож R13 в двух местах, каждый называет свою точку (E2-AC6) · 9 точек выше 3090 МГц не поднимаются никогда |
| R4 | Развёртка идёт без владельца и вешает машину на часы — ровно `bugs/03` | S1 как STOP-линия + `interviews/007`. Машинного гейта на присутствие человека не существует, и это сказано вслух |
| R5 | Сухой прогон обещает не ту лестницу, которую пройдёт живой — ровно `bugs/09` | Одна вычислительная функция на план и на прогон, и блок самопроверки, который их сравнивает |

**(б) Вероятные — с названным запасным ходом.**

| # | Риск | Запасной ход |
|---|---|---|
| R6 | Перезагрузок оказывается не десятки, а сотни, и развёртка не умещается в жизнь | Полосы независимы: сузить полосу, поднять затравку на шаг, сообщить владельцу число вместо тихого продолжения |
| R7 | Пин частоты ведёт себя не так, как ожидается, на низких ступенях | Механизм уже прогонялся живьём (`thermal`, 4 ступени, пин держался в каждой пробе); при расхождении — прогон говорит это по ступени, как уже умеет |
| R8 | Монотонность Vmin по частоте нарушается на живом кремнии, и затравка проскакивает край | Затравка ставит только НАЧАЛО спуска, и ставится на шаг ВЫШЕ ответа соседа; каждый отчитанный рунг всё равно прожжён. При отказе на первом же рунге затравка отменяется и спуск идёт от стока |
| R9 | Обновление драйвера посреди эпика обнуляет эталоны, id и записи (R6) | Штамп в каждой записи; `verify-baseline` и валидатор кривой сообщают, а не поглощают |

**(в) Наименее вероятные — записаны, чтобы их не открывали заново.**

- Тепловой дрейф за время развёртки сдвигает кривую по оси частот настолько, что частоту начинает
  обслуживать другая точка (≈ −1,7 МГц на градус). Обнаружимо: у каждой записи своя температура, а
  ключ — индекс точки, ось которого не движется (`bugs/10`, EXP-0053).
- Спуск сходится на напряжении, которого у железа нет (между рунгами сетки) — сетка неравномерна
  (5 мВ ×94 и 10 мВ ×32), поэтому шаг делается ПО ТОЧКАМ, а милливольты отчитываются.

---

## 7. Трассировка

| Шаг владельца (`ideas/03`) | Куда лёг |
|---|---|
| 1, 2, 5 — профиль ссылается на документ кривой; точка несёт статус и дату | Фаза 1 |
| 3, 4 — две сетки как JSON-словари | Фаза 1 |
| 6 — обход массива частот сверху вниз | Фаза 2 (петля), фаза 3 (живьём) |
| 7 — частота ФИКСИРУЕТСЯ в карте | Фаза 2 (пин через `profile-manager`, R1) |
| 8–11 — спуск по напряжению под прожигом и оракулом | Фаза 2 |
| 12 — перезагрузка как событие цикла; край = отказ + 10 мВ | Фаза 2 (журнал), E2-AC4, E2-AC5 |
| 13, 14 — следующая частота, пока не пройден весь диапазон | Фаза 2 (петля), E2-AC2 |
| 15 — длительная верификация 3 минуты на точку | Фаза 4, E2-AC7 |
| 16, 17, 18 — три профиля | Фаза 5, E2-AC8 |

---

## 8. Решения, принятые без владельца

*(метод — авторство агента, EXP-0026; каждое дёшево откатить и каждое названо здесь, а не в чате)*

1. **Развёртка идёт по ТОЧКАМ кривой, а отчитывается по ЧАСТОТАМ.** 389 частот лестницы обслуживаются
   75 различными точками — в среднем 5,19 частоты на точку. Идти по всем 389 значит пять раз крутить
   одну ручку и получить ту же таблицу. **Ответ не меняется, цена делится на пять.** Арифметика —
   `researches/09` §3.2, снята с живой карты.
2. **Спуск каждой следующей (более низкой) частоты начинается с ответа предыдущей, а не со стока.**
   Vmin не убывает с ростом частоты — это физика setup-нарушений, а не догадка (`researches/09` §2.3).
   Затравка ставит ТОЛЬКО начало спуска; каждый отчитанный рунг прожигается. Без этого 13 часов, с
   этим — 1,5.
3. **Поиск держит область пином частоты, а не потолком кривой.** Потолок ниже 2157 МГц удержать нечем,
   пин держит любую ступень лестницы. Пин — ПРИБОР и в отгружаемый профиль не идёт никогда: `min = max`
   запрещает карте сбрасываться на простое, чего владелец прямо не хочет.
4. **Короткий прожиг судит ОДНОЙ формой, край — НАБОРОМ, квалификация — набором на 3 минутах.** Ваш
   шаг 9 говорит «10 секунд», и на спуске это правильная цена. Но проект дважды заплатил за то, что
   вердикт одной формы не вердикт (факт 37): поэтому найденный край доказывается набором из трёх форм
   до того, как его запишут в документ. Первой на спуске идёт `sdc_fma --transient` — шум напряжения
   живёт в переходах.
5. **9 точек кривой выше 3090 МГц не поднимаются никогда.** Карта туда не пойдёт, а зазор между верхом
   таблицы (3172) и максимумом экземпляра (3090) — это ровно та щель, через которую 2026-08-15 уехало
   3180 МГц и упало в BSOD.
6. **44 точки на полу 180 МГц не являются целями поиска, но остаются рычагами.** Своей частоты для
   поиска у них нет; именно они обслуживают низкие частоты дёшево, когда их поднимают.
7. **Вердиктов у точки три, а не два: «край», «предел рычага», «пол частоты».** Второй обязателен —
   в 1700–2400 МГц спуск кончается на нашем рычаге, а не на кремнии, и назвать это краем значило бы
   выдать ложный `[TESTED]`.
8. **Ключ записи — индекс точки.** Ось напряжений неподвижна, ось частот едет с температурой
   (`bugs/10`). Ключевать по частоте — значит хранить улику, которая означает разное в 41 °C и 63 °C.

---

## 9. Что нужно от владельца — и это весь список

1. **Ответ на `interviews/007`** — может ли развёртка идти без него в комнате (фазы 3–4).
2. **Присутствие за машиной** на фазах 3, 4, 5 в том объёме, который следует из ответа на п. 1.
3. **Один ручной прогон 3DMark** на приёмке фазы 5 — это его решение от интервью 001.
4. **Значение N** — сколько кадров он готов отдать — спрашивается **с кривой на руках**, то есть в
   фазе 5, а не сейчас. Спросить раньше значит просить гадать.

Всё остальное — работа агента: шаги, порядок, формы нагрузки, форма журнала, устройство документа.
